# 04 Verification

## 测试分层总览
- **单测**：单组件、mock 依赖——coordinator 故障恢复分支（W-04）、准入/运行时 service（W-05）、N6 触发器（W-07）、v1c recheck 发射（W-08）、provider-diverse profile 解析（W-09）、标定分析（W-13）。
- **集成**：组件 + 共享 service（harness）——N6 debate 准入路径、人审 N2/N5 回归。
- **全链 e2e（mocked acceptance 隔离）**：N6 debate（生成→triage escalation→debate→synthesizer→gate 准入）、coordinator 故障注入。
- **不变量回归 / replay 守卫**：legacy 404、mocked product 拒绝、replay 幂等、N1 golden replay-identity（W-12 每 slice）、T-108 前向唯一（W-08）、debate byte-identity（W-09）、debate execution_plan 结构守卫（W-09 pre-provider_llm 加固：runtime 拒 plan+legacy 混供、coordinator〔removeAdditional off〕拒外来/未知 key〔直连调用者权威〕、route 仅 enum/type 拒〔Fastify removeAdditional on，外来/未知 key 被 strip 非拒〕）。
- **执行模式隔离**：`mocked_llm`（test DB / fixture）vs `provider_llm` product（live，门控）vs `codex_assisted`（operator）边界承 T-088 D-04。

## Phase 验证矩阵
### Phase 0（夯实）
- W-01：提交后 backend 套件 + tsc 双绿；`git status` 核对仅剩并行 session 文件（排除清单逐项确认）。
- W-02：coordinator N11 穿越单测（deterministic 自动穿越 / model-like 正确组装 input）。
- W-03：grep 确认无生成方法测试引用（删则连带删测）；卫生改动零行为、套件无回归。

### Phase 1（鲁棒性）
- W-04：三类负例——feedback 工件缺失 → 结构化报错（非 500）；upstream blocked → `halt('upstream_blocked')`；node_timeout → halt 附 retry 指引；nonce 守卫拒陈旧重跑。
- W-05：~12 service 各 ≥1 `legacy_unverified` 拒绝 + 1 happy path；N6 dedup warning 码、N8 阈值 + provisional tripwire 交互断言。
- W-06：provisional 门禁文档化；可选覆盖入口隔离 A/B 测试；确认未翻 `provisional:false`、tripwire 仍发射。

### Phase 2（选项 A：harness 一次拆透）
- W-12：每 slice 前后 N1 golden replay-identity 守卫 + v1b 全套件 + `topic-selection:v1a-harness-replay-smoke` + replay 幂等对比（同输入同 replay key 同结果）；`pnpm typecheck` 0；harness 类对外契约不变；**一次拆透**——壳仅余生命周期 + 持久化、纯函数簇出文件、行数显著下降；D-T127-01 登记。

### Phase 3（选项 B）
- W-07：触发器单测全分支（原语对应的候选质量 gate）；mocked debate e2e 绿 + provenance `debate_extension` 完整 + **触发后正常继续**（escalation→debate→synthesizer→gate 准入→下一节点）；不触发时既有 N6 路径回归不变（对比）；**harness 改动 replay byte-identity 保持**（差分探针 prompt_packet_hash / runtime_invocation_context_hash / source_hashes 跨角色 byte-identical）；D-T127-02 登记 + 矩阵 N6 行 reserved→implemented + T-089 留痕。
- W-08：recheck 发射单测（建议性记录 + 排序）；**T-108 前向唯一回归绿**（无新回环路由、downstream-recheck 仍 record-only）。
- W-09：provider-diverse execution plan 解析单测（slot_overrides 映射）；debate byte-identity 不变；既有 profile 解析回归。

### Phase 4（选项 C：工作台收口，核心段终点）
- W-10：`topic-workbench-ui-vs-flow-gaps` 审计条目逐条结论；只读节点（N7/N9/N10）注释；gate 拒绝 UX（可选 trace 细化）；desktop typecheck + UI gate 0/0。
- W-11：迁移脚本对样本旧 option-set backfill `n4_handoff_hash`，验证人审 N5 选择不再 409；脚本幂等可重跑；人审 N2/N5 e2e 回归绿。**→ 核心段 sign-off。**

### Phase 5（选项 D：延期尾巴，record-and-defer）
- W-13（**done record-and-defer 2026-06-24**）：标定姿态登记 record-and-defer；N8（+ 镜像 N6）维持 provisional + tripwire；mock/合成 dry-run **仅自检工具链不产出阈值**（机器强制：合成 run 即便判 `separates` 也不翻 deployed `provisional:true`）；真实语料就绪后标定分析单测、阈值更新经 contracts 数据层、达标证据（样本量 ≥100 多 provider、precision/recall 误报 <5%、记录 sign-off）记本文件；不达标维持 provisional（零产品风险）。

## 回归清单（每个 Phase 收口必跑）
- `apps/backend` v1b 全套件（当前基线 1332/0 fail / 35 skip）。
- `pnpm --filter @paper-engineering-assistant/shared test`（当前 255/0）。
- `cd apps/backend && npx tsc --noEmit -p tsconfig.json`（0 error）。
- `topic-selection:v1b-harness-e2e` / `v1b-runtime-stress`。
- F-11 replay-identity 守卫（N1 golden 哈希，`topic-selection-v1b-workflow-harness-service.unit.test.ts`）。
- desktop typecheck + UI gate 0/0（涉 Phase 4 选项 C）。
- 不变量：v1b legacy write 404、mocked product 拒绝、replay 幂等、T-108 前向唯一。

## 运行记录
> 各 Phase 收口验证在此追加（日期 + 套件计数 + commit hash + 结论）。

- **2026-06-16 · Phase 0 / W-01 预检 + 落地**：backend 全套件 `apps/backend pnpm test` = **1367 tests / 1332 pass / 0 fail / 35 skip**（基线吻合）；`pnpm --filter @paper-engineering-assistant/shared test` = **255/0**；backend & shared `tsc --noEmit` = **0**；W-01 文件单测 32/0、coordinator 单测 47/0。结论：W-01 残留落地、双绿，工作树仅余并行 session 文件。重定标：W-02 N11 recipe 条目已存在→仅缺终端穿越测试；W-03 ① 已 moot。
- **2026-06-16 · Phase 0 收口 / W-02 + W-03**：W-02 补 N11 终端穿越单测（coordinator 16/0）；W-03 ② 丰富 6 处 admission `legacy_unverified` 消息（码不变）+ ③ decision-memory SSOT 注记入 contracts。证据：affected admission+projection 单测 32/0；backend 全套件 **1367 tests / 1333 pass / 0 fail / 35 skip**；shared **255/0**；backend & shared `tsc --noEmit` **0**。结论：**Phase 0（M0）收口** —— W-01/02/03 全 done，核心段进入 Phase 1。
- **2026-06-16 · Phase 1 收口 / W-04 + W-05 + W-06**：W-04 coordinator 故障恢复（upstream_blocked / feedback_artifact_missing 结构化 halt + 人审 nonce 守卫；timeout 指引核验已存在）+ 3 负例（coordinator 19/0）；W-05 为 12 个 admission/runtime service 各补单测（66/0）；W-06 形式化 N8 provisional 产品门禁常量 + 守卫单测。证据：backend 全套件 **1402 pass / 0 fail / 35 skip**；shared **256/0**；backend & shared `tsc --noEmit` **0**。结论：**Phase 1（M1）收口** —— 进入 Phase 2（选项 A：harness 一次拆透，先登记 D-T127-01）。
- **2026-06-17 · Phase 2 / W-12 slice — N8/N9/N10 authority-hash 簇**：D-T127-01 已登记；5 个 `hashN8/N9/N10*Authority` 纯函数逐字搬迁到 `harness-authority-hash.ts`（`this.hash`→`canonicalHash`），harness 16 调用点改模块调用、移除 5 私有方法。证据：harness **12,898→12,829 行**；harness 单测 **97/0**（`GUARD_GOLDEN_N1` + `OPTION_AUTHORITY_GOLDEN` 绿，N1 byte-identical）；backend 全套件 **1402/0/35**（与拆分前**完全一致** → N8/N9/N10 authority hash byte-identical）；`tsc` 0。结论：W-12 首个 slice 落地、replay byte-identity 保持；Phase 2 进行中（续抽 parse-and-resolve / 剩余 hash-authority / ref-issue builder 至壳）。
- **2026-06-17 · Phase 2 / W-12 slice 2 — N7 + N5 authority-hash**：续抽 5 纯函数（`hashN7TopicQuestionAuthority`/`hashN7ContractAuthority`/`hashN7AnswerabilityPlanAuthority`/`hashN5DecisionAuthority`/`hashN5ResearchSliceAuthority`）到 `harness-authority-hash.ts`，11 调用点改模块调用。证据：harness **12,829→12,753 行**；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（抽取前后一致 → byte-identical；基线 1402→1403 的 +1 为 nits 的并发 nonce 测试）；`tsc` 0。另：本 session 已就质量做对抗式评审（4 维 0 个确认缺陷，3 个 nit 已修：W-04 并发 nonce 竞态测试、W-05 n8 正则收紧、W-06 注释精确化，commit `d2df3f7a`）。
- **2026-06-17 · Phase 2 / W-12 slice 3 — pure-utils 工具簇**：新建 `harness-pure-utils.ts`，抽 `buildRef`（原 `ref`，别名避遮蔽）/`isRecord`/`hasOnlyKeys`/`isHash`/`recordString` + `HASH_PATTERN`，~270 调用点改模块调用。证据：harness **12,753→12,729 行**；`tsc` 0（裸名遮蔽核验通过）；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（byte-identical）。意义：解耦 parse-and-resolve 簇与 `hashN6CandidateAuthority`，为后续主体拆分铺路。
- **2026-06-17 · Phase 2 / W-12 slice 4 — hashN6CandidateAuthority（hash-authority 簇收口）**：抽最后一个 `hashN*Authority` 到 `harness-authority-hash.ts`（用 slice 3 的 `buildRef`）。证据：harness **12,714 行**、`private hashN*Authority` 计数归 0（N5–N10 全出壳）；`tsc` 0；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（byte-identical）。另：slice 2+3 对抗式评审 **0 确认缺陷**。**hash-authority 簇完成**；W-12 续推 parse-and-resolve 主体。
- **2026-06-17 · Phase 2 / W-12 slice 5 — predicate 类型守卫簇**：新建 `harness-predicates.ts`，抽 12 个纯守卫（`isFunctionalRefValue`/`isStringArray`/`isFunctionalRefArray`/… 连续块），161 调用点改模块调用；簇内互调模块内解析，`isRecord`/`isHash`/`hasOnlyKeys` 由 pure-utils import。证据：harness **12,714→12,648 行**；`tsc` 0（无遮蔽/孤立 import）；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（byte-identical）。意义：解锁 `parseN1..parseN11` 搬迁。
- **2026-06-17 · Phase 2 / W-12 slice 6 — parser batch 1（N1/N3/N4/N6）**：新建 `harness-parsers.ts`，抽 4 个纯 frozen-input 解析器（依赖核查：9/11 parser 已 PURE，仅 N2/N5 待验证器先抽）。证据：harness **12,648→12,526 行**；`tsc` 0（清理了 1 个孤立类型 import）；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（byte-identical）。
- **2026-06-17 · Phase 2 / W-12 slice 7 — parser batch 2（N7–N11）**：续抽 5 个纯解析器到 `harness-parsers.ts`（模块补 4 个 predicate import + N7–N11 类型）。证据：harness **12,526→12,328 行**；`tsc` 0；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（byte-identical）。parser 簇仅余 N2/N5（依赖验证器，待下一刀）。
- **2026-06-17 · Phase 2 / W-12 slice 8 — parser 簇收口（N2/N5 + 2 validators）**：抽 2 个 accepted-payload 验证器 + parseN2/parseN5 到 `harness-parsers.ts`（6 调用点改模块调用；清理 4 个孤立 predicate import）。证据：harness **12,328→12,140 行**、`private parseN*` 计数归 0（**parser 簇 N1–N11 全出壳**）；`tsc` 0；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（byte-identical）。仅余 resolver 簇收壳。
- **2026-06-17 · Phase 2 / W-12 核查 + slice 9**：核查发现 `resolve*` 均 async/stateful → 留壳（无纯 resolver 簇）；到字面壳尚余 ~107 个纯节点助手，用户定 grind 至壳。slice 9 抽 5 个 N7 support-payload guards 并入 `harness-predicates.ts`（清理 3 个孤立 import）。证据：harness **12,140→12,030 行**；`tsc` 0；harness 单测 **97/0**（golden 守卫绿）；backend 全套件 **1403/0/35**（byte-identical）。
- **2026-06-17 · slices 4–9 对抗式评审 + 覆盖补强**：评审 byte-identity **0 缺陷** + DAG 无环/无孤立 import；确认既有覆盖盲点（parser/guard 负分支无测试，mutation 可证）→ 新增 `harness-parsers.unit.test.ts`（41）+ `harness-predicates.unit.test.ts`（25），独立 mutation 复验闭合（削 parseN9 isHash → 测试转红）。backend 全套件 **1469/0/35**；`tsc` 0。
- **2026-06-18 · Phase 2 / W-12 收壳完成（slices 10–13 + batches 0–6c）**：slice 10 gate-utils（`a83decbe`）、slice 11 守卫收口（`015fdc6c`）、slice 12 ref-builders（`15e72aec`）、slice 13 asserts（`922f84c3`）；清单工作流 `wf_2dd916ca` 裁定 132 候选 = 70 可抽/62 留壳后，batches **b0 折叠**（`a809904b` hash→canonicalHash / hashResearchSliceOptionAuthority→shared，删 2 委托壳）→ **b1** route-handoff（`2ab7b3b7`）→ **b2** intake-readiness（`db78d775`）→ **b3** n5（`50143b88`）→ **b4** n6（`9ec3d1c8`）→ **b5** n4+leaf-trio（`3ab3ea8c`）→ **b6a** n7（`78220492`）→ **b6b** n8（`dec4f51e`）→ **b6c** node-misc（`21fc36db`）逐批逐字搬迁。证据：harness **12,898→9,933 行（-2,965，-23%）**、`private hash`/`hashResearchSliceOptionAuthority` 归 0；**16 个兄弟模块严格 DAG 无环、无模块反向 import harness service**；每批 harness 单测 **97/0**（`GUARD_GOLDEN_N1`+`OPTION_AUTHORITY_GOLDEN` 绿）、`tsc` 0、backend 全套件 **1469/0/35**（11 次 commit 全程 byte-identical）；完整性核查确认余下 sync 方法皆 `this.<stateful>` 或 runner-local 类型绑定。**独立逐字对抗评审**（`wf_063839cb`，10 模块 / **90 函数** vs 基线 `015fdc6c`）：**CLEAN，0 确认缺陷**（每函数 token-for-token 一致，差异仅 `private`→`export`/`this.`→bare·别名/缩进）。结论：**W-12（M2 选项 A）收口** —— harness 即字面壳（生命周期 + 持久化 + stateful async runner + runner-local 类型助手），后续 W-07 N6 debate 准入/gate 落于模块化结构。
- **2026-06-21 · W-07 item (a) 后续 / triage 供给半 — coordinator `support_payloads` 通道**：补 N6 escalation 真实可达的另一半（与 projection-threading 合一即 coordinator 驱动真实 run 端到端可达）。coordinator per-node input 加 `support_payloads` 通道，把 caller 的 `n6_loopback_triage` 经 `recordSupportSemanticArtifacts`（复用 `recordDraftSemanticArtifact` 的 targetSlotId 路径）记为 fixture_replay/support_only 工件、additive 附到失败 N6 attempt，harness 据此路由 `n6_debate_escalation`（否则默认 `n6_regenerate_candidates`）。守卫：slot 必 support_only / 不与 debate·execution_spec 同供 / 不重 record draft 已占 slot。**对抗式复核** `wns5xk7rr`（4 lens〔production-path-proof / coordinator-logic / test-fidelity / consistency〕+ 完整性批判；21 raised / 每条独立复核 / 1 false-positive）：**0 critical / 0 high，生产路径逐 check 确认 sound**（真 harness round-trip 由 harness e2e `:5946` 独立覆盖）；finding 集中**测保真 + caller 合同文档**，已修（field doc 补 caller 合同〔bare payload / triage 需 candidate-level loopback draft / affected_refs lineage / product 拒〕、execution_spec+duplicate-slot 守卫、provenance label 参数化、测名诚实化 + 补 product-propagation 与守卫负例测）。证据：coordinator **41/0**、backend tsc 0、shared **258/0**、**full backend 套件 1521/0/35skip**。

- **2026-06-21 · W-07 收尾 / N6 `schema_version` 专用 blocker code 落地 + `9671be11` 残缺提交补全**：承 holistic-review item (3)。`9671be11` 仅提交 admission 的 `block('N6_DIVERGENT_DEBATE_ROLE_OUTPUT_SCHEMA_VERSION')` 发射、漏提交契约 union 成员 + 测，致 **HEAD 单独 `tsc` 不过**（TS2345）。补全：契约 union 加该码（N6 blocker **14→15**）、admission 单测 **13→17**（role_slot↔schema_version 分离 + drifted/missing/empty-string 三例，均重哈希三身份字段直达分支）、`N6_DEBATE_OUTPUT_CONTRACT` 单源化（alias 导入常量，镜像 runtime:93）。质检：多 agent 对抗式复核 `wf_8caad487`（5 lens，**29/29 confirmed / 0 refuted**）裁定 "correct, well-tested, genuine hardening"。证据：admission **17/0**、shared **258/0**、backend & shared `tsc --noEmit` **0**、**full backend 套件 1523/0/35skip**。跟进：N8/v1c-N2 同款 schema_version 守卫 levelling-up（另起任务，additive-safe）。

- **2026-06-21 · W-07 收尾 II / N8 + v1c-N2 admission schema_version 守卫 levelling-up（风险台账 8 RESOLVED）**：把 N6 的 model self-declared schema_version 强校验回填 N8 + v1c-N2，关闭同源 free-string 缺口。N8 复用既有常量 + 新码 `N8_BOUNDED_DEBATE_ROLE_OUTPUT_SCHEMA_VERSION`（+3 测）。**v1c-N2 关键发现**：role-output schema_version **非单值**——`RoleOrFinal` union 按 `…-role.v1`（role）/`…-final.v1`（final）判别，正交于 artifact output_contract；首版单常量误设致 v1c-N2 runtime 5 红，改为**按角色类**钉值（两常量 + `N2_FINAL_SLOT_ID` + 新码 `N2_BOUNDED_DEBATE_ROLE_OUTPUT_SCHEMA_VERSION`，+4 测含 class-swap）。dotted 值在 provider-canary（**acceptance 诊断,非 production runtime**）+ runtime test + canary test 全部单源化 import（唯一源 = admission service 常量）。纯 gate、replay byte-identity 保持、契约成员与发射同提交。**对抗式复核** `wf_9d702c03-ba9`（5 lens，**14/14 confirmed / 0 refuted**）裁定 **"SHIP WITH FOLLOW-UP"**：fail-closed gate、无 live 回归（v1c-N2 admit() 无 production caller）；**v1c-N2 生产可达性未决**——真实 runtime 未在 emission/prompt 层产出 dotted 值，接入 production 前须抓真 provider 样本 + emission↔admission 对齐（已 spawn 跟进，见风险台账 8）；复核后补 reverse class-swap 测。证据：N8 admission **16/0**、v1c-N2 admission **21/0**、v1c-N2 runtime **7/0**、provider-canary **36/0**、backend & shared `tsc` **0**、shared **258/0**、**full backend 套件 1531/0/35skip**。

- **2026-06-23 · W-09 / provider-diverse 命名 debate execution plan（全 S0–S4 收口）**：DP-3.5 加法——把 v1a 的单 `model_option_id` 提升为可命名 role-keyed plan，逐字复用 `TopicSelectionAgentExecutionSpec`（无第二 spec/runtime/LLM 路径/hash）。**S0** 共享 leaf（命名/`debateLevelToExecutionPlanName` 两 enum 族/`resolveDebateExecutionModelOptionId` precedence/mixing guard/schema factory，`0126653f`）→ **S1 N8**（`9f8fc6d9`，`handoff.executionPlan`+`input.execution_plan`+`invocationEnvelope` resolver）→ **S2 N6**（`7934e456`，per-ROLE-FAMILY：`ctx.slotId` 即族、fan-out 实例共族 spec；arbiter→draft gate bridge 独立 single-agent 不动）→ **S3 v1c-N2 Option A**（`e8408a83`，caller 逐 slot resolve 进既有 `input.model_option_id`，**零 runtime/admission 改**；新 helper `resolveCallerSideDebateModelOptionId`+mixing guard；runtime 无 production caller=tested-ready seam）→ **S4**（`15d317b7`，新 registry service：三族各 3 plan，option_id 由 model-profile registry 同源常量派生；provider_diverse=openai-vs-dashscope，deepseek 结构性缺席 debate profile 不扩；`selectN6/N8/V1cN2DebateExecutionPlan`；coordinator `execution_plan` caller-injectable→verbatim 转发 N6/N8 runtime；复用既有 anti-fallback 不加字段；level→plan 自动决策=deferred harness seam）。**★ DORMANCY 诚实边界**：三 debate 限 codex\|mocked，`resolveProfile` 仅对 provider_llm 解析 option（`:1298-1300`）→ 命名 plan 对 codex\|mocked **完全休眠**（prompt-packet/transcript/已解析 artifact model_option_id 全与无 plan 逐字同——经诊断实测发现并纠正 study 的"identity-bearing"误判，三族各证 N8 codex/N6 mocked/v1c-N2 codex），plumbing 就绪、活效待 provider_llm（OOS）。三族 admission-identity 形状各异（N8 role-keyed maps / N6 ordered array+stage_arities / v1c-N2 final_* hashes，且 v1c-N2 `runtime_audit_hash` 为不可复现遥测）——byte-identity 测各比其可复现分量。每刀 study workflow grounding（`wf_30c262a5`/`wf_4d1f02ee`/`wf_0340bd7d`）。**证据**：registry **4/0**、coordinator **42/0**、N8 runtime **6/0**、N6 runtime **8/0**、v1c-N2 runtime **11/0**、shared **264/0**、双 tsc **0**、**full backend 套件 1542/0/35skip**（S0→S4 全程 byte-identity 保持）。**接 production 前提醒**：命名 plan 当前对 codex\|mocked 休眠；真 per-role provider 多路执行须先启用 provider_llm debate 执行路径（W-09 范围外）。

- **2026-06-23 · W-09 后续 / pre-provider_llm 契约加固（3 LOW/INERT gap 闭合）**：W-09 holistic 复核（CLEAN/SHIP、0 defect）标记的「在启用任何 live provider_llm debate 路径**之前**须闭合」的 3 个 LOW、INERT 契约紧致缺口——今提前闭合（仍 INERT：三 debate 限 codex\|mocked、plan 对其休眠，故纯契约加固、零行为改、byte-identity 不变）。**Gap 1（coordinator）**：`assertDebateExecutionPlanValid` 用**共享 factory** `topicSelectionNamedDebateExecutionPlanSchema(roleSlotIds)` 按 debate kind〔n6_divergent/n8_bounded〕的 role slot ids 结构校 `node_inputs.debate.execution_plan`，转发 runtime 前 `AppError(400)` 拒；自有 Ajv〔`removeAdditional` off〕→ 对**直连调用者**为权威结构拒（外来 role key / 未知 field / 非法 enum）。**Gap 2（advance route）**：`debateNodeInputSchema`（`if/then` on `debate.kind`）把 per-kind execution_plan sub-schema 挂上 HTTP body schema。**关键诚实边界（经实测探针确认）**：Fastify 默认 Ajv `removeAdditional:true` → route **仅** 400 enum/type/required 违例〔未命名 `name`、非法 role `execution_mode`〕，`additionalProperties:false` 违例〔外来 role key / 未知 key〕被**静默 strip 非拒**；故 route = 早期 enum/type 400 + 净化，coordinator〔removeAdditional off〕= 直连调用者的权威结构拒〔HTTP 路径上 controller 转交的是已被 route strip 过的 body〕——两层互补、注释已据实校正（原注释过度宣称「route 拒」）。**Gap 3（N8+N6 runtime）**：加可选 legacy per-loop `model_option_id` 通道 + 入口调 `debateExecutionPlanMixingError(plan, legacy)`〔plan+legacy 混供 `AppError(400)` 拒〕+ 把 `input.model_option_id ?? null` 织入 base ctx〔替换硬编码 null〕；缺省 undefined→null 故 byte-identical。该 legacy 通道**无 production producer**〔coordinator debate 输入 `Pick<>` 结构性排除 `model_option_id`〕——守的是未来直连调用者，非 provider_llm 落地自动激活（注释已据实校正）。v1c-N2 经既有 `resolveCallerSideDebateModelOptionId` 已含同 guard（不在本轮）。**多 agent 对抗式复核**（`wf_c8c402c7`，6 lens〔schema 正确性/mixing-guard byte-identity/测充分性/死码陈旧/doc-sync/边界一致性〕+ 逐 finding 复核；24 raised / 16 confirmed / 8 refuted）+ 修复：confirmed 聚为 5 簇——(a) route/coordinator reject-vs-strip 注释过度宣称〔实测探针证 Fastify strip〕**已据实改** + route 测补「bad execution_mode→400」「外来 key→非 400〔被 strip〕」；(b) coordinator n8_bounded validator 分支原**无测覆盖**〔仅 n6_divergent〕**已补** N8 foreign-role-key 拒测〔经 `driveToN8DebateLoopback`〕；(c) legacy 通道注释过度宣称**已软化**；(d) 共享 factory docstring 称 schema 强制 `model_option_id↔provider_llm` 耦合而实际不强制〔resolution-time 由 model-profile registry 强制〕**已据实改 docstring**〔不加 if/then——免与 v1a mirror 背离、免引入未测约束〕；(e) doc-sync〔本条 + line-7 守卫枚举〕。**2 个 refute-as-false**：复核误称「DORMANT 测变红」〔实测 N6/N8 dormant 测**全绿**，反驳〕、「给 orchestrator no-spec 路径加 `model_option_id↔provider_llm` 拒」〔**会破坏 dormancy**——plan 按设计给 mocked debate 喂非 null option 由 resolveProfile 静默落 null，加拒即令 dormant 测变红，故弃〕。**验证**：coordinator 单测 **45/0**〔+N6 两拒测 + N8 拒测〕、N8 runtime **7/0**、N6 runtime **9/0**、route 集成除 env-gated Prisma smoke 外全绿、双 tsc **0**、shared **265/0**、**full backend 套件 1548/0/35skip**。**剩余**：真 live provider_llm debate 执行路径仍 OOS（其落地时本 3 guard 即生效）。

- **2026-06-23 · W-09 后续修订 / N8 runtime mixing-guard 拒测补全（probe→真测，验证诚实化）**：上条「W-09 后续」记 N8 runtime **7/0**（含「N8 拒测」），但工作树实况 = 6 真测 + 1 残留**调试探针**（`PROBE stability`，唯一断言 `assert.equal('MK', …)`——在 a/b 两 run 均 `completed`〔常态 mocked 路径〕时**恒抛** `ERR_ASSERTION`，两 run 未全 completed 时早返静默过 → **零有效覆盖**）；故官方 runner 下实为 **6/1**、「7/0」**未坐实**。**本轮修订**：① 删探针；② 补**真** N8 mixing-guard 拒测（镜像 N6 line-542：同供命名 N8 plan〔`selectN8DebateExecutionPlan('provider_compact')`〕+ legacy `model_option_id` → `assert.rejects` 配 `/named debate execution_plan cannot be combined with a legacy per-loop model_option_id/` **且** `listArtifactRefsByWorkflowRunId(...).length===0`〔守拒发生在 debate loop 前、零工件录入，与 N6 同样 load-bearing〕）；③ `makeSubject()` additive 加返 `controlPlane`（既有 `{runtime,stub}`/`{runtime}` 解构不受扰）。**load-bearing 经变异实测**：把入口 guard 第二实参 mute→`null`（不再侦测混供）⇒ **仅** #7 拒测变红（`Missing expected rejection.`）、余 6 全绿——证该断言确钉 guard 行为；guard 旋即复原（runtime 净 diff = Gap-3 本体、无变异残留）。**验证**：N8 runtime 现真 **7/0**（官方 runner `node scripts/run-node-tests.mjs` 同源 env-scrub〔provider secret keys 剥除〕单文件复跑确认）。**注**：runtime Gap-3 本体（guard + legacy `model_option_id` 通道）系上条既存的未提交工作树实现，本轮仅补测 + 校文档，未改 runtime 行为。

- **2026-06-23 · W-08 / v1c 反馈触发 recheck 建议性发射（record-only + ranking，Phase 3 收口）**：study（`wf_508e1e6e`）证大半已建（record-only + advisory 工件发射 + 100/85 priority 原语），真实 delta 仅 ① binary→确定性 ranked score、② 加 scoped 排序只读读面。**S1**（`6560b7c3`）纯函数 `computeDownstreamRecheckAdvisoryPriority`（severity 权重 span 50 > impact 30 > cause 10，0–100，单源 contract）+ `impact_summary` 2 可选字段 + sink `priority?`（缺省回落 binary）。**S2**（`b72d5c31`）Seam A：仅 `requires_recheck` 时算 advisory → sink priority + 镜像 `impact_summary`，record-only。**S3**（`2dc605dd`）Seam B：`listDownstreamRecheckAdvisoriesByBridge`（scoped、priority-desc/created_at-asc、filter requires_recheck）+ controller + `GET …/paper-project-bridges/:bridgeId/recheck-advisories`。**T-108 前向唯一回归绿**（advisory 仅 record+排序信号、永不路由 loopback / 改前向态）——`topic-selection-v1c-cross-node-acceptance` **4/0**（T-108 L4 forward-only chain + N6 recheck 不自动 N1–N5 环）、`…-n6-feedback-normalization-admission-service` **4/0**（forbidden-authority + record_only_feedback_candidate）、harness forward-only filtered **3/0**、recheck-service zero-mutation 测保持绿。**新增覆盖**：排序确定性端到端（critical+invalidated+need_invalidated=99 > warning+stale+stale_evidence=36，score 流入 queue item）、no-recheck 反馈零 advisory/零 queue emission、scoped 排序读面（[99,69,36] + no-recheck 排除 + 空 bridge→[]）、route GET advisories 探针。**证据**：shared **267/0**、recheck-service **12/0**、risk-memory **15/0**、route 集成（v1c）绿、双 tsc **0**、**full backend 套件 1551/0/35skip**。**Phase 3（M3 / 选项 B）收口** —— W-07 + W-09 + W-08 全 done。**剩余**：Phase 4（选项 C：W-10 工作台收口审计 + W-11 `n4_handoff_hash` 迁移 = 核心段终点）、Phase 5（选项 D：W-13 N8 阈值标定，语料门控）。

- **2026-06-23 · W-11 / `n4_handoff_hash` 数据迁移（Phase 4 第一刀）**:study（`wf_37512359`）定**重算非重跑**。**问题**:人审 N5 缺 `comparison_payload.n4_handoff_hash` → 409 `GATE_CONSTRAINT_FAILED`;旧（T-115 Phase 2 前）option-set 缺该字段。**重算源**:完整 N4→N5 handoff 已作 control-plane artifact 持久化（挂 option-set `artifact_refs`）,backfill = `getArtifactRef` → `canonicalHash(payload)` → 与 N4 当初写入逐字节同（同纯函数同对象;handoff 折 run-specific `node_attempt_id`/`gate_result_hash` 故标量不可重建）。**Seam**:仅 service + script、**无 HTTP 路由**（`60295d10` repo seam + service、`f14bc33a` script、`3e5b9a46` 回归）。repo 加 `updateOptionSetComparisonPayload`（caller 端 merge 无竞态）+ `listOptionSetsMissingN4HandoffHash` + 单源 `optionSetLacksN4HandoffHash`。service `run({optionSetIds?,titleCardIds?,dryRun})`:枚举→skip-if-present→双守卫（`handoff_kind`+`target_node_id`）定位 artifact→canonicalHash→merge;`unrecoverable`（无可取 artifact）上报不伪造。幂等(已有即 skip、二次 run 枚举返 0)。**回归(load-bearing)**:旧记录 backfill 前 409 →（共享 store）backfill → `selectSlice` 解析 + 重算 hash 流入 N5 frozen_input。**证据**:backfill-service 单测 **6/0**（重算===canonicalHash(持久 handoff)+sibling 保留、幂等、dry-run 零写、unrecoverable、explicit-ids skip、title-card scoping）、human-selection **6/0**（+回归）、双 tsc **0**、**full backend 套件 1559/0/35skip**。**注**:Prisma 实现（JSON read-merge-write + findMany filter）随既有 env-gated Prisma smoke;in-memory + service 逻辑由单测覆盖。**剩余 Phase 4**:W-10（工作台收口审计 + 只读节点文档化）→ 核心段 sign-off。

- **2026-06-23 · W-10 / 工作台收口审计 + 只读节点文档化（Phase 4 收口 = 核心段 sign-off）**:study（`wf_5ad50583`）证**真实 delta 几乎只是文档**——工作台已与真实流程对齐（v1a/v1b/v1c 人审写面齐全、HumanOverride/Trace 按 D5 正确延期、QueuePanel 非 gap、无陈旧 UI）。**核心交付 = N7/N9/N10 只读语义注释（防误改、零行为改）**:N9/N10 确认 `deterministic`+`['none']`+`delegated_payload_allowed:false` → 读only by design;**N7 纠偏**——policy `delegated` 允许 `human_delegated`,但初始 materialization 机械（`chooseN7Candidate`),human/codex 仅供 N8-failure loopback 可选 support、非必需人审 gate（allowance≠needs-review）→ 注释落 policy（contracts node 7/9/10）+ runtime（`chooseN7Candidate`）。**gate 拒绝 UX 打磨**:用户定**不纳入**（保持收口;现状已适度可见,打磨是 desktop 加法 + contract-dynamic gate 风险,归后续可选）。**边界**:零行为改、不建 HumanOverride/Trace（D5 延期）、不动 desktop。**证据**:shared **267/0**、双 tsc **0**、harness golden replay-identity 守卫绿（node policy 数据未变）、**full backend 套件 1559/0/35skip**（与改前一致=零回归）。**→ Phase 4（选项 C）收口 = 核心段（Phase 0–4）sign-off**;延期尾巴 Phase 5 / W-13（N8 标定,语料门控）不阻塞核心。

- **2026-06-24 · W-13 / DP-3.3 N8 阈值标定（record-and-defer，Phase 5 延期尾巴登记）**:grounding study `wf_7ed7fe42` 证标定 scaffold（analysis/materializer/runner）早已 W-01 建全 + 26 单测绿、仅未接算子入口;provisional gate 系刻意 hold（D8）。**交付 = 接线 + 登记、非伪造标定**:① report-only 算子入口 `v1b:n8-calibration-dry-run`（`--self-test` 合成 4-band 语料经真实临时文件端到端跑通 loader→materialize→真 N8 gate preflight→mock assessor→analysis、对 deployed provisional 阈值评估、跑后删临时文件不留伪语料、绝不 mutate policy/翻门、尾置 backstop;`--corpus` 校验真语料 gate 就绪）② record-and-defer 不变量守卫单测（合成 run 判 `separates` 也不翻 deployed `provisional:true`、runner 结果无 adopt/flip 面）。**实跑证据**:self-test 链路绿（4 preflight 全 passed、`separates`、`provisional_after_run:true`、exit 0）、`--corpus` 对 placeholder 模板正确拒绝（exit 1）、临时目录零泄漏。**既有守卫复绿（引为 W-13 验证、非新主张）**:placeholder-拒绝守卫、W-06 N8 + W-07 N6 早翻 tripwire、26 标定单测（W-01 落）。**回归门**:标定套件 **19/0**（runner 5→6）、双 tsc **0**、**full backend 套件 1560/0/35skip**（1559 + S2 守卫 1），provisional 全程未动 = 零产品风险。N8 + N6 双 gate 同登记 hold（共享 `released_by:'W-13 calibration'`）。**真标定仍阻塞于两人工输入**（人工标注语料 + 独立 content-grounded assessor）+ 释放门（≥100 多 provider、FP<5%、sign-off），不阻塞已 sign-off 核心段。

### （待开工）
