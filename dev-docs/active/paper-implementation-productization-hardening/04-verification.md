# 04 Verification

## 2026-07-15 — T-132 formal PI scope → Pack A product evidence

- Active PaperProject `P313` traversed the normal PI bootstrap/motive/trace-board/ValidationCycle routes and admitted exact WorkOrder revision `pi_experiment_revision_v2_fed4f563-4717-4bb3-89d6-1295a1b751db` with two ordered cells.
- T1-T4 converged to Run `ef_run_v2_c4ab7919-2d7b-415c-ab53-201b11464aca`, exact PI branch head and sole EF acknowledgement `ef_integration_inbox_v2_366831a8-fe25-4361-a701-e83a85004ce8`.
- Final read-only run `formal-pi-scope-packa-product-20260715-verify-r5` passed with cutover `true`, admission/simulation `false`, legacy 257-row digest unchanged, selected scientific 4-row digest unchanged and all six Pack B tables empty.
- The evidence closes the formal product-bound Pack A prerequisite only. Product Pack B E1-E5, PI event-derived projection, D-16/D-17/D-18 and full bootstrap-to-dossier acceptance remain open.
- Canonical evidence: T-132 `artifacts/product-pack-a-local-20260715/05-product-landing-closure.md`; final JSON SHA-256 `341eba9ae1e38ce282947d9d9554102f04ff5c93fa7cb5564a7152b0c245ee48`.

## 2026-07-14 — T-132 Pack B named-local landing evidence

- The named loopback local-development target applied the reviewed Pack B migration; 59/59 migrations are up to date and the source/database checksum is `c0c49f0f7a268f09619f6c693a1f41955ce6cb1b36b656dd9c8e1d51abe0f70e`.
- The gitignored local simulation override is true after `env-localctl` compile/doctor; source default and all non-local environments remain false.
- Disabled/enabled app-composition probes returned the exact capability/prerequisite errors and continued to reject legacy mutation. No network/provider transport was reachable.
- Final run `packb-local-closure-20260714-r1` passed with Pack A 34/34 plus Pack B 6/6 tables, unchanged Pack A 208-row digest and legacy 257-row digest, zero cross-domain PI foreign keys and zero rows in every Pack B table.
- Post-landing regression passed Pack B gate units 17/17, shared execution contracts 5/5, backend execution/cutover suites 43/43 with zero skips, shared/backend typechecks, Prisma validate and environment/database central suites.
- At that historical checkpoint the target had no formal PI branch/revision/admission/head or EF Run/RunCell/TaskSpec/final acknowledgement. The 2026-07-15 formal-product evidence supersedes the missing-head state; E1-E5 and `workflow_simulation_passed` remain unclaimed, so no T-124 scientific/closure/UI acceptance receives credit.
- Canonical evidence: T-132 `artifacts/implementation/03-pack-b-local-landing-closure.md`.

## 2026-07-13 — T-132 Pack B same-payload simulation evidence

- Gate run: `packb-20260713-final4`; PB01-PB16 all passed with `blockers=[]` on isolated disposable PostgreSQL.
- Shared targeted contracts/hash tests passed 5/5; backend targeted service/worker/route/gate tests passed 43/43; the forced relational Prisma suite passed 4/4 with 0 failed and 0 skipped.
- Final full regression passed shared typecheck and 323/323 shared tests, backend typecheck and Prisma schema validation, plus 1,982 backend tests with 1,939 passed, 0 failed and 43 expected DB/live-provider opt-in skips. No existing database was contacted.
- Exact write census: 2 ProviderPayloads, 2 simulation ExecutionAttempts, 12 append-only AttemptEvents, 8 ProviderCommands, 2 CollectionAttempts and 2 diagnostic ProvisionalOutputs. All 231 non-Pack-B table digests remained unchanged.
- Real provider/network/CreateJob, legacy writer, scientific result/validation/evidence/closure and PI persistence writes were zero. Scientific status remained `not_started` and evidence eligibility remained false.
- Migration digest: `c0c49f0f7a268f09619f6c693a1f41955ce6cb1b36b656dd9c8e1d51abe0f70e`; summary digest: `456ed62ac01de9055c8720d0dfdbdb3b5c43a4979b228c1c2cc240b866f553da`.
- OpenAPI strict verification and context strict verification passed; the regenerated API index contains all five Pack B routes and 192 total endpoints. DB/env context was refreshed through the owning SSOT workflows without applying a migration.
- At the disposable technical-closure checkpoint no existing database had received the Pack B migration, no product capability was enabled and no real provider was called. The section `2026-07-14 — T-132 Pack B named-local landing evidence` records the later deployment. Canonical disposable evidence: `.ai/.tmp/experiment-foundation-productization/packb-20260713-final4/summary.json`.

## 2026-07-13 — T-132 Pack A named-local landing evidence

- PostgreSQL 17 recovery point completed before apply; dump SHA-256 `021be380ee58580d33905135838500faa86b2200b81ba09407447d1c2771c4e0`, and `pg_restore --list` passed.
- `prisma migrate deploy` applied the only pending migration; migration history is 58/58 up to date, Pack A table census is 34/34 and PI↔EF cross-domain FK count is 0.
- The same deterministic digest query proved all five legacy tables identical before/after migration. The later authorized fake-capability-job cancellation is recorded separately and left `running=0`; the submitted HarnessRun has one exact trusted terminal monitor/evidence lineage and is non-blocking.
- Typed fixture import created exactly 23 identities/revisions, 48 lifecycle events and 23 readiness attestations; replay exact-reused all rows with no duplicate. Reviewed source-policy digest remained `sha256:48dd3546bcf314c478f80a5bc6ba5bcc0ecc57b848ac0d83fd7d5c9b8ac3bb6e`.
- Cutover tests close 8 PI and 8 EF overlapping legacy mutations before controller/service/repository, preserve GET diagnostics, reject `admission=true/cutover=false`, allow drain-only rollback and leave relay independent.
- App-composition smoke under the local enabled config returned 409/`LEGACY_RECORD_NOT_ELIGIBLE` for a legacy mutation, 200 for legacy diagnostics and 404/`BRANCH_SCOPE_CONFLICT` for a full v2 request against a deliberately absent scope. The latter proves capability routing without fabricating a Project/Cycle or writing partial v2 state.
- Final read-only run `packa-local-landing-20260713-final-r2` passed with `blockers=[]`, exact source-backed fixture hashes, valid Prisma composition, resolved legacy blockers and zero DB mutation/provider/fetch/scientific effects during the gate.
- Post-review runs `packa-local-landing-20260713-post-review-r3` and `packa-d19-post-review-hardening-20260713-r4` passed after strict boolean parsing, exact endpoint/cluster fingerprint enforcement and full fixture authority checks. The disposable run also proves two concurrent real-PostgreSQL imports plus exact replay converge without reopening any PI legacy writer.
- Final hardening backend suite: 1,942 total, 1,903 passed, 39 expected skipped and 0 failed; focused cutover, target fingerprint, importer and local gate suites also passed.
- The source defaults remain false and only gitignored local override values are true. No dev/staging/prod/cloud environment was changed.

## 2026-07-13 — T-132 Pack A source-backed joint-spine evidence

- Gate run: `packa-d19-source-policy-20260713-r2`
- Technical result: A01-A04 and B01-B10 all passed on isolated disposable PostgreSQL
- Source-policy result: passed; both exact Dataset/DataPolicy revisions bound the reviewed source-policy attestation
- Final authority: 1 admitted revision, 2 exact cells, 2 TaskSpecs, 1 VersionLock, 1 RunRecipe, 1 Run/manifest, 1 PI branch head and 1 EF final inbox acknowledgement
- Four local Units of Work and three events converged under replay; two concurrent identical admissions produced one commit and one exact replay
- Five legacy sentinels and all 197 non-v2 application-table digests were unchanged; excluded writes were zero
- Shared suite: 318/318 passed; backend suite: 1,926 total, 1,887 passed, 0 failed, 39 expected skipped; targeted Prisma, EF/spine, harness and relational PostgreSQL suites passed
- At the time of the disposable run, admission remained default-off and existing-environment apply/cutover had not occurred. The named-local Pack A landing section records the later deployment; neither run made a provider call, created scientific evidence or edited protected T-124 runtime/result/REU implementation.
- Overall source-backed status: `passed`; the PI↔EF seam is closed only within the Pack A control-plane scope
- Open follow-up scope: D-16/D-17/D-18, full bootstrap-to-dossier golden scenarios and scientific execution
- Canonical run evidence: `.ai/.tmp/experiment-foundation-productization/packa-d19-source-policy-20260713-r2/summary.json`

## 验证梯度（继承 T-114 L1-L6，新增 L7 usage-fit）

| Layer | Purpose | Expected proof |
|---|---|---|
| L1 contract/unit | schema、identity、admission、forbidden fields、裸参数负例、policy/assessment 契约 | shared schema tests + backend unit tests |
| L2 service integration | runtime slot / coordinator / 压缩分支 / 记忆注入经 admission 与 Domain Gate | in-memory backend service tests |
| L3 Prisma smoke | queryable identity、replay、coordinator run/step 行、无重复写 | local/dev DB smoke runner |
| L4 provider canary | 真实 provider 路径：压缩分支 live、升档 debate live、长上下文 | env-gated 金丝雀 |
| L5 adversarial/stress | 必检用例机器解析：双分支压缩、档位漂移、记忆负例、coordinator 三件套、manifest 四向对账 | runtime-stress runner（先注册后实现） |
| L6 near-prod runtime | 同路由 + live provider + Prisma + replay/幂等 + coordinator 推进证据段 | near-prod gate summary，`passed | blocked | failed` |
| L7 usage-fit（新增） | 语义产出是否满足实际科研工作需求 | golden scenario 全链推进 + 人审 rubric 评分留档（候选质量/批判有效性/证据可追溯/约束遵守） |

约定：
- 新增必检用例必须先注册进 `.ai/scripts/paper-implementation-runtime-stress.mjs` 对应 `required_*` 组（缺失即红）再写实现，防止"实现了但没进闭环门"。
- 每 Phase 收口与里程碑的 run id 一律记入下方 Log。

## Log

### 2026-07-15 S3 闭合（gs-001 v3 三评 + run 006 全链 + 全门复跑）
- **gs-001 v3**：素材修订（自包含探针判据+5 warning 吸收+§GT-8）后 live 两跑——run 005 暴露 S3 硬化×真实 LLM 输出三处相互作用（可选键 `legacy_ref:null` 形状等值误判×2 + gaps-only 空集误伤×1，根因与修法详 03）；修后 **run 006：lane A 首次全链 completed（四槽全 passed）+ 受理桥双物化**（TechnicalRouteCandidate + FeasibilityProbe created），curation/evolution 转诚实语义 blocked，failed_run_review 技术死路清零。
- **v3 代评审**（run 006，AI 代评审人工可覆盖，留档 run 目录 `rubric-scored.md`）：四维 **4.9/4.9/4.9/5.0**（vs v2 4.8/5.0/4.8/5.0，候选↑ 追溯↑ 约束= 批判微降非放行错误）。连续第三评不劣化。
- **收口证据**：ref 语义等值修复后全量 runtime-stress `t114-paper-implementation-runtime-stress-1784078240326` **passed**；prisma smoke **37/0/15**；**near-prod gate（L6）`t114-paper-implementation-near-prod-runtime-gate-1784074155740` passed**（P1/result-analysis wire 编码真 provider 路径首验）；backend tsc 本包面零错。
- S3 全部验收项闭合；移交项见 03（evolution 槽内内容注入 / disposition 语义形式化 / blocked 队列分类 / 素材 v4）。

### 2026-07-15 S3 落地 + 复审修复轮收口（gs-001 v3 与 near-prod 另记）
- **S3-α**（D9 resume/契约加深/admission 真实现/echo 扩面）+ **S3-β**（evolution wire 根因/N7 dossier 项目级对账）合流后，8 角度 code review（finder→verify 两段）产出 ~20 条去重候选：**10 项 CONFIRMED 级修复**（F1 resume 共享引擎+run 归属精确化+model option 继承+幂等版本语义+断链 409+仓储 run 过滤；F2 runtime 全 section 扫描+disposition 谓词单源+admission 复核补齐+prompt 措辞；F3 `__proto__` 双污染点+prompt 按模式分支+blocked 漂移门三服务补齐+echo 收敛；F4 dossier 豁免实校验+ref_type 过滤；F5 P1/result-analysis wire 编码+gateway strict 降解 fail-closed 护栏——根治 OpenAI 模式三链空心 final 物化率恒 0 的 P0），4 项 REFUTED 留档（03 号文档）。
- **收口证据（代码态）**：全量 runtime-stress `t114-paper-implementation-runtime-stress-1784073185367` **passed**；prisma smoke **37/0/15**；backend+shared tsc 零错（工作树中并行 T-132 会话的 experiment-foundation 测试文件有其在途报错，非本包面）。L5 必检新增：resume×2（前缀复用零重发/漂移拒续）、evolution wire×2（全链/legacy 复现 run 004 签名）、P1 wire 全链、degenerate schema gateway fail-closed。
- **过程教训**：手动直跑集成测试文件时若不显式设 `PAPER_IMPLEMENTATION_REPOSITORY`，.env.local 的 TITLE_CARD/RESEARCH_LIFECYCLE fallback 会把策略推成 prisma（持久库跨测试累积），而测试内 in-memory 分支断言只看 PAPER_IMPLEMENTATION_REPOSITORY——出现"queue resolve 系列 2!==1"假红签名时先查调用环境；正规入口（stress runner / `pnpm run paper-implementation:runtime-prisma-smoke`）不受影响。
- gs-001 v3 修订+live 重跑+代评审与 near-prod gate 见后续条目。

### 2026-07-13 T-132 Pack A source-backed control-plane closure handoff
- Final run `packa-d19-source-policy-20260713-r2` passed the source-backed D-19 gate and A01-A04/B01-B10, closing the PI↔EF seam through the Pack A durable EF acknowledgement only.
- Existing-environment DB apply and product enable/cutover were neither authorized nor executed. D-16/D-17/D-18, full bootstrap-to-dossier golden scenarios and scientific execution remain open and receive no acceptance credit from the Pack A run.
- `node .ai/scripts/lint-docs.mjs --path dev-docs/active/paper-implementation-productization-hardening --strict`: passed 12/12 Markdown files with 0 errors and 0 warnings.

### 2026-07-13 T-132 Implementation Pack A readiness closure adoption（docs-only）
- T-132 `07-implementation-readiness-review.md` froze the exact five-kind asset census, PI/EF Prisma-family matrix, stable error reasons, A01-B10 evidence, `PAPER_IMPLEMENTATION_EXPERIMENT_V2_ADMISSION_ENABLED=false`, source edit population and legacy database digests. The result is `ready_for_implementation_authorization`, not authorization to edit code/config/schema or apply a migration.
- PI adoption accepts only new dedicated v2 contracts/routes/controllers/services/repositories plus clean Prisma/env/app integration points. Dirty T-124 result/dossier/runtime/REU files and existing singular WorkOrder/HarnessRun repositories are excluded; any required edit to those surfaces reopens readiness review.
- Baseline shared/backend typechecks, Prisma validate and targeted EF/PI WorkOrder tests passed. Existing green legacy tests receive no Pack A acceptance credit; implementation must still pass A01-B10 on disposable real Postgres and preserve all legacy digests.
- The submitted legacy HarnessRun and running legacy ExternalTrainingJob are recorded as future cutover blockers. Read-only population inspection changed no row; no migration, DB apply, product code/config/schema edit, cloud action, staging or commit occurred.

### 2026-07-13 T-132 D-22 PI schema-pack adoption（docs-only）
- The planned schema/invariant matrix and first-migration census must contain only the minimal Phase 1 identity/readiness logical families plus the D-19 branch/revision/admission/head, typed materialization/Run-manifest-cell and two-domain inbox/outbox spine through the durable EF acknowledgement. Exact Prisma names and DDL remain review outputs rather than claims in the D-22 decision record.
- Disposable-Postgres/Prisma tests must prove same-domain relational identity, unique, CAS, cell-binding and event constraints; duplicate revision sequence, second Run, duplicate/substituted cell binding, stale head CAS and duplicate/conflicting event identities must fail or converge exactly as specified. Repository/schema scans must report zero cross-domain ORM relation/FK/cascade.
- Canonicalization tests must recompute server hashes for every explicitly named schema-versioned typed snapshot and event payload, reject caller hashes and semantic tampering, and prove JSON does not hide identity/unique/CAS/cell/event invariants. Schema scans must report zero generic domain `kind/payload` table, EAV table, untyped event-authority blob, persisted legacy/v2 eligibility annotation or `dispatch_eligible` mirror.
- Migration and write-census tests must report zero bootstrap/promotion, ExecutionAttempt, provider, result, validation, evidence, Cycle closure, UI/search, legacy migration/backfill and global-cutover model/write. Capability-off remains a zero-write admission guard; committed sagas still drain. Legacy before/after digests remain identical.
- Documentation synchronization only; no product code, config, Prisma schema, database, cloud behavior or historical verification result was changed or claimed implemented. Next is the joint schema/invariant matrix followed by implementation-readiness and explicit implementation-authorization review; no D-23 decision was introduced.
- Post-sync checks passed: strict docs lint checked 12/12 Markdown files with 0 errors/0 warnings, and scoped `git diff --check` passed for the seven canonical files. The first strict-lint pass found that one newly added vague reference raised the existing word count to the warning threshold; naming the D-22 decision record explicitly removed the warning without changing semantics. Current-decision scans found no non-historical pending-D-22 wording.

### 2026-07-13 T-132 D-21 PI storage/cutover adoption（docs-only）
- D-21 acceptance must prove independent domain-owned additive PI/EF v2 canonical table families and repository ownership. PI writers may reach only PI branch/revision/admission/head/inbox/outbox tables; EF writers may reach only EF identity/readiness/materialization/Run-manifest/cell-binding/inbox/outbox tables. Same-Postgres deployment cannot introduce a shared mutable authority table, cross-domain writer or runtime union view.
- Capability-off tests must reject a new PI v2 admission with a stable disabled outcome and zero PI v2, EF v2 and legacy writes. A committed PI admission/outbox must still drain through `RunManifestFrozen`, PI head CAS/`BranchHeadAdvanced` and the durable EF inbox acknowledgement after new intake is disabled.
- D-19 acceptance-scope enablement must produce the planned one-revision/two-cell/one-Run/one-head/one-ack v2 census while before/after legacy row digests remain identical. Scans must prove no backfill, persisted legacy annotation, dual write, fallback, legacy trust upgrade or mixed runtime read model.
- Product-cutover tests must prove that, only after D-19 passes, every new paper-bound admission uses v2 and every overlapping singular/generic legacy writer is disabled at the same boundary. Rollback must stop new v2 intake, preserve v2 canonical/inbox/outbox audit and replay state, drain committed sagas and never restore a legacy writer.
- Documentation synchronization only; no product code, config, Prisma schema, database, cloud behavior or historical verification result was changed or claimed implemented. The earlier D-20 log correctly recorded D-21 as pending at that time; D-21 is now confirmed and D-22 is the next schema-pack decision.
- Post-sync checks passed: strict docs lint checked 12/12 Markdown files with 0 errors/0 warnings, and scoped `git diff --check` passed for the seven canonical files. The first strict-lint pass found one vague `below` reference in the new architecture transition; replacing it with the exact D-21 section title removed the warning without changing the decision.

### 2026-07-13 T-132 D-20 PI transaction-boundary adoption（docs-only）
- The planned D-19 implementation must prove four separately committed domain-owned authoritative Unit-of-Work stages: PI admission/`WorkOrderRevisionAdmitted` outbox; EF event inbox/materialization/Run/`RunManifestFrozen` outbox; PI `RunManifestFrozen` inbox/head CAS/`BranchHeadAdvanced` outbox; EF `BranchHeadAdvanced` inbox receipt, which itself is the exact durable acknowledgement and not a second acknowledgement row.
- Transaction rollback tests must inject failure before each commit and prove zero partial inbox/domain/outbox rows. Recovery tests must inject crash after commit-before-publish, publish-before-relay-marker and consumer-commit-before-transport-ack, then prove outbox replay plus inbox dedup converges without duplicate authority or events.
- Conflict tests must prove exact event/payload replay returns the stored outcome, event-id payload drift is terminal conflict, lower sequence is stale-consumed with zero head/outbox mutation, same sequence with a different Run/manifest is invariant conflict with zero head/outbox mutation, and missing prerequisites remain retryable with zero domain/outbox mutation.
- Ownership scans must reject any transaction callback/repository method that writes both PI and EF domains even under one Postgres. Relay lease/delivery markers are infrastructure bookkeeping only; only the EF `BranchHeadAdvanced` inbox receipt is acknowledgement authority, and no `dispatch_eligible` mirror may exist.
- Documentation synchronization only; no historical verification result, product code, config, schema, database or cloud behavior was changed or claimed implemented. D-21 additive-v2-storage-versus-legacy-extension decision remains pending.
- Post-sync checks passed: strict docs lint checked 12/12 Markdown files with 0 errors/0 warnings; scoped `git diff --check` passed for the seven canonical files. The first strict-lint pass exposed one vague pronoun introduced in the new D-20 architecture paragraph; replacing the pronoun with explicit `D-20` restored the strict gate without changing semantics.

### 2026-07-13 T-132 D-19 PI first-slice contract adoption（docs-only）
- Acceptance starts from an already bound PaperProject/ValidationCycle and typed v2 assets persisted through the real identity/readiness path. The fixture is a joint seam precondition only; the D-19 slice neither implements bootstrap nor replaces T-124 D10's final real-bootstrap golden scenario.
- The positive fixture is fixed at one admitted WorkOrder revision with exactly two required cells, one-to-one EF TaskSpec bindings, one immutable batch Run/manifest, atomic `RunManifestFrozen`, exact-scope PI head CAS plus atomic `BranchHeadAdvanced`, and one EF exact inbox receipt/acknowledgement through typed HTTP/service/repository/Prisma and inbox/outbox replay.
- Required replay/conflict cases are same input convergence, duplicate/lost-ack replay, lower-sequence no rollback and same-sequence/different-manifest fail-closed. Required row/side-effect census at the endpoint is one revision/one Run/one head/one acknowledgement and zero ExecutionAttempt/provider/result/validation/evidence/Cycle closure/UI/search/legacy-migration write.
- Existing ResearchWorkOrder/HarnessRun/coordinator fields and historical S3 tests are not D-19 evidence. D-19 is capability-gated with no dual write/fallback and leaves the D-16/D-17/D-18 atomic cutover pending. The D-18 `CYCLE_ACTIVE_REAL_ATTEMPT` blocker remains unchanged for later phases and includes active real Attempts on non-head Runs.
- Documentation synchronization only; no historical verification result was edited and no product code, config, schema, database or cloud behavior was changed or claimed implemented.
- Post-sync checks passed: T-124 strict docs lint checked 12/12 Markdown files with 0 errors/0 warnings, and scoped `git diff --check` passed for the seven canonical current-target files.

### 2026-07-12 T-132 PI architecture adoption through D-17 (docs-only)
- Synchronized PI ownership, exact PI→EF iteration scope, deterministic `revise | fork`, existing-Run non-rebinding and retrieval authority into T-124 roadmap/overview/plan/architecture/decision notes/pitfalls.
- Added terminology guards separating `PaperImplementationCoordinatorRun` from EF experiment Run, PI runtime `node_attempt_id` from EF ExecutionAttempt and coordinator candidate path from WorkOrder branch.
- `node .ai/scripts/lint-docs.mjs --path dev-docs/active/paper-implementation-productization-hardening --strict`: passed, 12/12 Markdown files, 0 errors, 0 warnings.
- Cross-package check: T-132 strict docs lint passed 8/8; governance sync/query/lint passed with T-124 remaining `in-progress` and T-132 remaining `planned`; diff check, trailing-whitespace and Markdown-fence scans passed.
- D-12 follow-up: AuthorityGate/CoordinatorStop two-layer semantics, one-action auto-resume and zero in-bound per-Run/Attempt confirmation were synchronized into both bundles. Action acceptance now classifies Initiation/Authority/Recovery/Plumbing; the fixed T-132 zero-write flow is `1/2/0/0`, and the T-124 one-Cycle/one-admitted-revision/one-strong-claim/one-export reference flow is `1/4/0/0`. Each fixture must prove actual AuthorityActions equal its named-gate formula and retryable/Stop-only paths add zero actions.
- D-12 action-target refinement: T-124 now treats the four coordinator Stops as conditions rather than four mandatory confirmations, adopts the fixed reference `1/4/0/0` target and requires every golden fixture to emit the four-class action budget. Cross-package strict docs lint passed T-132 8/8 and T-124 12/12 with 0 errors/0 warnings; governance sync/lint and scoped whitespace/fence/diff checks passed. Documentation only; the D-12 pass did not modify current S3 code/config/schema/database/cloud work.
- Post-D-12 sync checks passed: T-124 strict docs 12/12, T-132 strict docs 8/8, both with 0 errors/0 warnings; governance sync/lint, diff check, trailing-whitespace and Markdown-fence scans passed.
- D-13a adoption: PI-side verification must prove admitted revision `0..1` Run uniqueness, exact revision→Run-manifest→required-cell/TaskSpec→Attempt scope, same-manifest idempotency, conflicting second-Run rejection and no per-cell AuthorityAction.
- Post-D-13a documentation checks passed: T-132 strict docs 8/8 and T-124 strict docs 12/12, both with 0 errors/0 warnings; governance sync/lint, scoped diff, trailing-whitespace and Markdown-fence checks passed. Existing T-124 S3 work remains untouched by the D-13a docs-only adoption.
- D-13b adoption: PI tests must prove branch-local sequence issuance, exact `RunManifestFrozen` scope validation, atomic head+`BranchHeadAdvanced`, duplicate/stale/conflicting sequence behavior, crash/lost-ack replay, no EF Attempt before exact ack, failed/cancelled-head retention, no old-Run auto-cancel and zero user actions. Existing HarnessRun/coordinator/status fields must not satisfy these assertions.
- Post-D-13b documentation checks passed: T-132 strict docs 8/8 and T-124 strict docs 12/12, both with 0 errors/0 warnings; governance sync/lint and scoped diff/whitespace/fence checks passed. Existing T-124 S3 work remains untouched by the D-13b docs-only adoption.
- D-14 adoption: joint PI tests must prove terminal simulation leaves Run/cells scientifically `not_started`; workflow status rebuilds from Attempts; no-evidence closure writes D-18 refs with null disposition/selected exit and `evidence_eligibility=false`; EF digests match and zero scientific evidence exists. Same-Run real reuse is allowed only while Cycle is open; closed-Cycle Attempt creation fails and follow-up uses a successor Cycle/new Run. No extra action occurs.
- D-15 adoption: joint PI tests must reject range-only/generator-only/empty/forged exact plans, runtime autotune and changed scientific cells; prove a complete exact plan without TaskSpec refs admits once, EF resolves/materializes one-to-one cells, extra/missing/drift/substitution fails before Run/head/Attempt, same revision replay preserves the scientific manifest and N cells add zero user actions. Existing coordinator/HarnessRun/status/autotune fields cannot satisfy or override the exact-cell contract.
- Post-D-15 documentation checks passed: T-132 strict docs 8/8 and T-124 strict docs 12/12, both with 0 errors/0 warnings; governance sync/query/lint and scoped terminology/Markdown-fence/trailing-whitespace/diff checks passed. T-132 remains `planned`, T-124 remains `in-progress`, and the docs-only adoption left concurrent S3 implementation work unstaged and unchanged.
- D-16 confirmed verification contract: failed/cancelled/incomplete execution must create zero REU and appear by exact Run/Attempt ref in the immutable Cycle closure snapshot/hash; results later assigned positive/negative/inconclusive may create REU only through the same gateway while retaining completed execution, and REU carries no disposition; dossier must consume only explicit closed-Cycle snapshot refs/hashes and reject open/tampered/incomplete/wrong-project scope. Ownership scans must prove no project-wide failed-like REU scan, Sidecar authority, FailureEvidenceUnit, second gateway, compatibility alias or dual-read remains. Action budgets stay T-132 `1/2/0/0` and T-124 `1/4/0/0`.
- D-16 implementation status: target semantics are confirmed but the current mixed status contract, bridge/live failed-REU writers, Cycle closure storage gap, `assertProjectRunEvidenceAccounting` reader and related tests remain one blocking migration slice. The shared evidence seam cannot be claimed closed from S3 evidence.
- Post-D-16 documentation checks passed: T-132 strict docs 8/8 and T-124 strict docs 12/12 with 0 errors/0 warnings; all 55 changed Markdown files passed individual strict lint; governance sync/query/lint, scoped diff, whitespace, fence and semantic-supersession scans passed. No code/config/schema/database/cloud state changed, and current S3 implementation remains historical/migration-debt evidence rather than a second accepted path.
- D-17 confirmed verification contract: malformed/unknown/unsupported required rules must return stable `UNSUPPORTED_RULE` before Run freeze/head/Attempt and again at final-profile recheck, producing zero passed report/Candidate/REU; a passed report must cover the exact immutable batch Run/manifest, canonically ordered required-cell result refs/hashes, exact protocol revision/hash, validator-profile hash and ordered rule results through the sole ScientificValidationService writer. Results later assigned positive/negative/inconclusive remain execution-completed and evidence-eligible, with no disposition on EF/REU.
- D-17 PI acceptance must prove the D-18 watermark idempotently derives readiness; eligible evidence produces one proposal, while no-evidence/control-only closes with null disposition/selected exit. Model/runtime/caller assessment/exit/Packet writes fail; the existing closure action is the sole writer, Packet follows `ValidationCycleClosed` and is excluded from the closure hash, and the action budget stays `1/4/0/0`.
- D-17 downstream and ownership acceptance must reject open proposal, independently materialized packet, open/tampered/wrong-project Cycle or hash drift; ResultInterpretationPacket/Claim/Dossier/retrieval/motive/next-step consumers must resolve the exact closed Cycle. Scans must prove no generic validation/evidence writer, model/caller conclusion writer, negative/inconclusive REU status, ScientificConclusion aggregate, open-proposal consumer, dual-read/fallback or extra AuthorityAction remains.
- D-17 implementation status: the target responsibility chain is confirmed but not implemented. Opaque protocol JSON, heuristic per-job validation, generic record writers, caller-authored Cycle assessment/exit, four-scenario Result Analysis authority and direct/open ResultInterpretationPacket consumption remain blocking migration inventory; current S3 evidence cannot close D-17.
- Post-D-17 T-124 documentation checks: `node .ai/scripts/lint-docs.mjs --path dev-docs/active/paper-implementation-productization-hardening --strict` passed 12/12 Markdown files with 0 errors/0 warnings; scoped `git diff --check` passed for the seven canonical files. Documentation only; no product code/config/schema/database/cloud behavior was changed or claimed implemented.
- 2026-07-13 D-16 closure-scope acceptance refinement (supersedes broad target wording, not earlier test records): joint tests must freeze one Cycle/branch-set/current-revision/per-branch-head watermark; include every admitted branch's durable head Run and all head-Run Attempts; reject a no-head branch as `BRANCH_HEAD_NOT_FROZEN`; exclude non-head history unless named by an exact `comparison_input_ref`, while proving comparison does not expand execution scope; reject any active real-provider Attempt anywhere in the Cycle as `CYCLE_ACTIVE_REAL_ATTEMPT`; and inject Cycle/branch/revision/head drift to prove `CYCLE_CLOSURE_SCOPE_DRIFT`, zero partial closure write and deterministic rebuild/idempotent replay. Historical S3 failed-REU/project-wide tests remain unchanged audit evidence and cannot satisfy the refined contract.
- Post-2026-07-13 closure-scope documentation checks: `node .ai/scripts/lint-docs.mjs --path dev-docs/active/paper-implementation-productization-hardening --strict` passed 12/12 Markdown files with 0 errors/0 warnings; scoped `git diff --check -- dev-docs/active/paper-implementation-productization-hardening` passed. Documentation only; no historical verification record, product code, config, schema, database or cloud state was changed.
- Post-D-14 documentation checks passed: T-132 strict docs 8/8 and T-124 strict docs 12/12, both with 0 errors/0 warnings; governance sync/query/lint, terminology, Markdown-fence, scoped whitespace and diff checks passed. T-132 remains `planned`, T-124 remains `in-progress`, and existing S3 implementation changes were untouched by the D-14 docs-only adoption.
- Documentation only. Existing T-124 dirty implementation/docs changes and S3 scope were preserved; no code/config/schema/database/cloud state changed.

### 2026-07-12 S2 收口（A/B/C/D 全落地 + gs-001 v2 复评）
- **S2-A**：token 双计修复覆盖全部 11 slot（估算降至 ~52-55%，可用预算实翻倍，7 个 over-budget 必检零改动仍触发）；压缩 attempt-builder（L1 摘录/L2 剔除，refs 骨架永不裁）六槽穿透，L5 恢复分支必检 +6（`*_over_budget_compression_applied_completes`）；实测抓到并修掉 compressed_context 复门双计洞（改 manifest hash）。**S2-B**：evolution InvalidRequestError 根因=schema 名 65>OpenAI 64 上限（全仓唯二超限）；run_mode 映射统一（evolution 独有 dry_run→test 分叉）；lane A 正文穿引（hash 围栏 + 只读 reader 注入），runner 手工补喂删除。**S2-C**：echo 失配/blocked 无码改可重试技术失败（不再 HTTP 400/孤儿）；UpstreamError 从 slot 重试集移除（gateway 终裁不再全价重打）；runtimeIdentity 显式含 run_id + DB unique（同执行重放 409）；preflight 终态七槽统一 blocked final admitted；清理簇收敛（校验器核心合一/直查方法消 3×全表扫描/normalizedRefType 8 副本→单源/echo 对齐下沉 slot 层恢复漂移门可测）。**S2-D**：SlotParameterManifest@v1（运行时导出+提交式快照+四向完备性互查含负例+materialization class+P-07 裸参数负例×11 schema）；纠正 P-04"18 路由"笔误（实 14）。
- **gs-001 v2 复评**（run `gs001-lora-live-004`，7 provider 调用；AI 代评审，评分 `rubric-scored.md`）：四维 **4.8/5.0/4.8/5.0**（对比 003 基线不劣化且候选质量↑）。skeptic 从 5 blocker→1 blocking+5 分级 warning/info——B3 穿引生效进入实质审查；唯一 blocking `BASELINE_GATE_ORDER_AMBIGUOUS` 指出 v2 包 Stage 0/1 顺序矛盾（**系统连续第二次正确拦下有真实缺陷的输入**）。行动项：gs-001 v3（自包含探针判据+吸收 5 warning）；evolution SCHEMA_VALIDATION_FAILED → S3 role-output 契约实证靶。
- **收口证据**：全量 runtime-stress `t114-paper-implementation-runtime-stress-1783789410626` passed（含 6 条新恢复分支必检）；迁移 `20260712090000`（identity unique）已 apply、pg_indexes 确认；迁移后 prisma smoke **37/0/15**；near-prod gate `t114-paper-implementation-near-prod-runtime-gate-1783814902610` **passed**；双包 tsc 零错。

### 2026-07-11 S5 首评（gs-001-lora run 003，L7 usage-fit 基线；AI 代评审，人工复核可覆盖）
- 四维：候选质量 **4.7**、批判有效性 **5.0**、证据可追溯 **4.8**、约束遵守 **5.0**（评分详情与逐节点表：run 目录 `rubric-scored.md`）。
- 核心判断：语义产出显著高于 v1 验收线；**链未走完是治理在工作**——skeptic 对薄选题包的 revise 处置四条批判逐条命中真实缺口（预算矩阵/指标预承诺/基线控制/基线选择依赖），RF-001 在提案正文不可见时拒绝装懂是模范 fail-closed。route 三候选与 ground truth 路线空间几乎完全对齐（含主动尊重早期检查义务的分阶段路线与对应论文 §7 的机制诊断路线）。
- 行动项：gs-001 **v2 选题包**按 skeptic 修订后重跑（预期 lane A 完链+probe 物化）；evolution InvalidRequestError/run_mode 映射/提案正文穿引 → **S2**；skeptic blocked 态 repair_suggestions 完备性 → S3。
- 首跑证据：run `gs001-lora-live-003`（8 provider 调用），W4 回流+waiting_review 停驻+受理桥 live 物化全部真实路径成立。

### 2026-07-11 修复轮二审收尾（R1-R10，coordinator 语义定稿）
- 对修复轮 diff（41db6dcc..HEAD）的 code-review 发现 10 项全部收掉：**R1** budget_exhausted 无 raise 的 re_advance 改 409 于 resolve 之前（+no-op 带 overrides→400，不再静默丢）；**R2** raise 应用改取锁后 max（交错不可缩预算）+ `budget_raise_events` 审计事件（含被吸收的 raise）；**R3** dedup_key 只收 trusted 码/outcome 哨兵（LLM 措辞不再铸新队列项，retry_budget 真正可触发）；**R4** 可信性规则补零调用 blocked final（确定性预检产出入 trusted——真 tier-budget 恢复 budget_exhausted+loop_budget_review 语义，D2 前保留码注释登记）；**R5** persistStep 前 holder fence + 持久化失败 best-effort 释放自身 lease（残余双执行窗口关死、输家不再困 run）；**R6** 崩溃测试包装器转发 options（fence 真被测）；**R7** CAS null 二义消除（终态竞态报 terminal 而非 CONCURRENT_ADVANCE）；**R8** 双实现 404/409 对齐 + 一致性测试；**R9** step 投影 runtime_artifact_id 与 hash 同门（admitted 才置值）+ prisma round-trip/GET 读回断言；**R10** 终态集合入契约单源（四处字面量删除）、白名单派生基表、withBumpedLease 统一时钟、consumed 单径化（steps 唯一事实源）、集成测试 PROJECT_ID 进程唯一 + in-memory 全表无泄漏断言恢复。
- 验证：coordinator unit 25/25、prisma coordinator repo 4/4、集成 36/0/16、l5 52/52、双包 tsc 零错；全量 runtime-stress run `t114-paper-implementation-runtime-stress-1783765586663` **passed**；prisma smoke 复跑 **37/0/15**。

### 2026-07-11 S1 复审修复 + 迁移 + 全门收口（S1 闭合）
- code-review（506a6073..HEAD，8 角度 + 逐条验证）10 发现全处置：**F1** 无候选步改落 blocked（回流可达，run 级正反测试）；**F2** 终态收窄 completed|failed + advance/resolve 支持 `raise_budget_envelope` 只增提额（兑现 D1"提额后 re-advance"）+ resolve 后 advance 失败降级为 `coordinator_advance_error` 响应字段（不再掩盖已生效 resolve）；**F3** lease TTL 600s + slot 前心跳 + updateCoordinatorRun 带 holder fence（双执行/lost update 封死）；**F4** step 契约加 `runtime_artifact_id`，投影→受理桥缝合封口测试（真实 createFeasibilityProbe 走通+血缘回填，hash 一致性核实 admitted_artifact_hash===final_artifact_hash）；**F5** blocker 分类/终态只信任 coordinator 自产 code（LLM 输出不能再驱动 budget_exhausted/trace_repair）；**F6** advance 开头以 steps 重建 consumed（崩溃欠计封死）；**F7** portfolio 混合决策激活项无条件入 target 集；**F8** lease CAS 拒终态 + payload 写带 holder 条件。发现 #9/#10（echo 高度/复制簇）按既定判断移交 S2。
- 修复轮测试：coordinator unit 21/21（+6 新恢复路径用例）、prisma coordinator repo unit 2/2（新）、motive-board 16/16、integration 52（36 pass/16 env-skip）、必检 +2（F1 回流可达 / F4 缝合封口）。
- **迁移 `20260711100000` 已 apply**（coordinator 两表 + 全部 S1 列，pg_tables 确认）；prisma smoke 首跑 4 失败为**测试隔离问题**（持久化下队列行跨 run 累积、fixture 假设全表恰 1 条）——修为按 source_coordinator_run_ref 过滤（断言强度不变），复跑 **37 pass / 0 fail / 15 env-skip**。
- 全量 runtime-stress：run `t114-paper-implementation-runtime-stress-1783750367028` **passed**；**near-prod gate（L6，live provider + Prisma）：run `t114-paper-implementation-near-prod-runtime-gate-1783751668927` passed**。双包 tsc 零错。S1 全部验收项闭合。

### 2026-07-11 S1 收口（W1-W5 全落地）
- W1 受理桥：45/45 + L5 `acceptance_bridge_lineage_drift_rejected`；W2 链内回查：route/cycle/feasibility 三 slot 全量回查 + blocked 冒充封死，L5 +4；W3 coordinator：contracts 6/6 + unit 12/12（故障注入三件套/选择复算/零权威依赖面）+ routes integration，L5 +5；W5 确认消费：consume-before-write 语义 + target 覆盖 + 5 处校验副本收敛，13 文件绿，L5 +2（另修 S0 遗留 T-101 夹具红，pristine HEAD 验证非回归）；W4 队列回流：coordinator blocked 入队（枚举分类表）+ resolve re_advance + retry/cooldown 真语义（Prisma reopen retryCount 覆盖 bug 根治），14/14+13/13+2/2+集成 49（33 pass/16 env-skip），L5 +2。
- 合并态验证：双包 tsc 零错；全量 runtime-stress ×3 全 passed（W1+W2 后 `t114-...-1783727795027`、W3+W5 后 `t114-...-1783731130335`、W4 收口 `t114-...-1783733835126`），本切片新增必检 14 条全部命中。
- **待办**：统一迁移 `20260711100000_add_paper_implementation_s1_coordinator_and_lineage`（coordinator 两表 + 6 模型血缘列 + 确认消费列 + 队列关联列，纯加性）**已写未 apply，待用户审批**；apply 后补 prisma smoke（L3）与 near-prod gate（L6，需 provider keys）；S5 第一条 golden scenario 素材构造随后。

### 2026-07-10 S0 复审修复收口（code-review 10 发现 → 6 修复 + 3 移交 + 1 随迁移关闭）
- 修复项：①WO admit / dossier ready 的 gate result 增加 **trace_manifest_id 绑定断言**（挪用他 manifest 的 passed gate 被拒）；②motive evolution 门改为 `请求声明 || (structural_evolution && 源 motive control.human_confirmation_required_for_major_change)` 强制（不再自愿制），落库的 human_confirmation_required 反映有效值；③admitCoreMotiveVersion 增加 primary 晋升（非自举）确认门（与 apply 路径对齐）；④两处新 prisma create 改 catch P2002 → 409（消 TOCTOU 500 + 对齐 mapDuplicate 惯例）；⑤可空 Json 列改写 Prisma.DbNull；⑥`paper-implementation-v1-runnable-replay.mjs` 修复（15 处构造依赖 + 两处硬编码 gate id 改走真实 evaluate 流程，新步骤 15a/21a）——**replay 全绿**（46 步 ok + BP0-01..10 passed，artifact `t109-s0-fix-check`）。
- 新增负例测试 3 个（WO gate 异 manifest 拒、dossier gate 异 manifest 拒、primary 变更无 ref + control 标志强制确认）；受影响套件全绿（bridge 15/15、motive-board 12/12、result-claim 12/12）。
- **迁移已 apply**：`20260710120000_add_paper_implementation_s0_governance_tables`（两表落 dev 库，pg_tables 确认）；**L3 prisma smoke 补齐**：30 pass / 0 fail / 15 skipped（live 金丝雀 env 门控），exit 0。
- 复跑全量 runtime-stress：run id `t114-paper-implementation-runtime-stress-1783696518823`，**status=passed** 9/9 步。`tsc --noEmit` 0 错误。
- 移交项已登记 `01-plan.md` §S0 复审移交项：确认记录 target 绑定+消费语义与校验器收敛（S1）、桌面确认入口与 blocker 推导（S4）、13×测试夹具 helper（S1 顺手）。

### 2026-07-10 S0 收口（治理与正确性补洞）
- `tsc --noEmit`：0 错误。
- 逐文件套件（node --test，全绿）：human-confirmation schema 4/4、human-confirmation service 3/3、motive-board 11/11（含 primary 替换/evolution 确认解析负例）、result-claim-dossier 11/11（含 strong claim 伪造 ref/错 scope/ready dossier 悬空 gate 负例）、workorder-bridge 14/14（含 admit gate 解析负例）、trace-kernel 11/11（含 gate result 落库可解析）、live-adapter 11/11、routes integration 4/4（改走真流程：trace-gates/evaluate 产出真实 gate id 再 admit/ready）、11 个 runtime unit + l5 52/52 + domain-gate 6/6（S0-3 子代理逐文件验证）。
- 全量 runtime-stress：run id `t114-paper-implementation-runtime-stress-1783691982563`，**status=passed**，9/9 步全绿；本次新注册 14 条必检用例（11×`*_inactive_project_rejected_before_orchestrator`、1×`trace_integrity_profile_and_model_option_drift_rejected_before_gateway`、2×deterministic lane：`work_order_admission_gate_result_must_resolve`、`trace_gate_result_persisted_and_resolvable`）全部命中匹配。
- governance sync：ok。`git diff --check`：干净。
- **未收项**：Prisma 迁移（两张新表 `PaperImplementationHumanConfirmationRecord`/`PaperImplementationTraceGateResult`）待用户审批后 apply（`prisma generate` 与 db-ssot sync-to-context 已跑，L3 Prisma smoke 在迁移后补）；S0-5 待 N8 裁定；near-prod gate（L6）按 D7 属里程碑级，建议与 S1 收口一起跑。

### 2026-07-12 S3-β 收口（evolution wire 编码 + dossier 项目级失败对账）
- **Historical/superseded notice（T-132 D-16）**：本节测试结果仍准确证明当时 S3-β 实现，但 trusted failed/cancelled REU 与项目级 REU 对账不再是目标契约，也不能作为 D-16 或共享 evidence seam 的验收证据。后续迁移必须用 closed-Cycle snapshot/hash fixtures 替换旧用例，而非让两组测试并存。
- `tsc --noEmit`：shared 与 backend 均 0 错误。
- 逐文件套件（node --test，全绿）：shared `paper-implementation-runtime-contracts.schema.test` 47/47（含 run-004 失配复现 + wire 正反例 + strict-representable 走查）；shared `topic-selection-llm-runtime-contracts.schema.test` 17/17；motive-evolution unit 23/23（含 wire→canonical 往返、重复 option_key 重试耗尽、B1 名长测改钉 wire 名 ≤64）；result-claim-dossier unit 16/16（含 β2 三条新测：漏 REU→409 列缺失清单、显式豁免/段内披露/packet source 三路覆盖通过、非 ready 不检 + untrusted 不进对账集）；contract-evaluation-suite 5/5；run-coordinator unit 26/26；slot-parameter-manifest 9/9；routes integration（paper-implementation）6/6。
- L5（真 orchestrator+ajv）：`--test-name-pattern "L5 motive evolution"` 10/10（含 2 条新必检：wire 全链通过 + legacy map 形态 fail-closed 复现，均已先注册进 runtime-stress `REQUIRED_L5_CASES`）。runtime-routes integration 的 motive evolution 两测绿。
- **已知非本切片红**（并行 S3-α 工作流在飞改动面）：L5 全文件 59/60（`L5 trace transient provider failure ...` 1 红）与 runtime-routes integration 的 trace-integrity / claim-boundary(P1) / Domain-Gate 矩阵 trace 段 3 红——均落在 α 侧 trace/P1 契约与服务在编辑中的文件，β 未触碰；全量 runtime-stress 按 D7 于 α/β 合流后统一跑。
- YAML 版本纪律：`prompt_templates.yaml` motive-evolution 条目 version 1→2 + wire 编码注记；shared 常量 `PAPER_IMPLEMENTATION_MOTIVE_EVOLUTION_PROMPT_TEMPLATE_VERSION` 'v1'→'v2'（全仓引用均走常量，硬编码 grep 零命中）。

| Date | Command | Status | Summary |
|---|---|---|---|
| 2026-06-11 | `pnpm run paper-implementation:runtime-stress` | passed | 包创建基线：T-114 闭环复跑，run id `t114-paper-implementation-runtime-stress-1781132291471`，290 tests / 226 passed / 64 env-gated skips / 0 failed，9/9 steps，95/95 必检用例通过。 |
| 2026-06-11 | `pnpm run paper-implementation:near-prod-runtime-gate` | passed | 包创建基线：near-prod gate run id `t114-paper-implementation-near-prod-runtime-gate-1781132560502`，live openai 13 次 provider 调用与 debate 拓扑一致，Prisma 证据、exactly-once 并发 materialization、replay 幂等、drift `VERSION_CONFLICT`、no-dual-track 与 redaction 护栏全 true。 |
| 2026-07-03 | backend tsc + research-lifecycle unit + full backend suite | passed | research-lifecycle id 生成 max+1 修复（外部会话留痕，9fb04a26 同类）：unit 10/10；full backend 1639 tests / 1604 passed / 0 failed / 35 skipped（基线 1632/1597/0/35 + 本修复 3 测 + 41ac51b3 W-11 4 测，只增不减）；backend tsc 干净。注意：全量须单会话独占运行——双会话并发跑套件会因 ts-node 子进程资源互踩产生文件级崩溃假红（本次取证 43/4 个文件级 not ok 均为此因，静置复跑全绿）。 |
| 2026-07-03 | 并发双起 `pnpm test`（apps/backend，错开 8s）+ backend tsc | passed | 套件跨进程互斥锁落地验证（run-node-tests.mjs，实现见 03 同日工具链留痕）：B 检测到 A（pid 50340）持锁即打印 "Another backend suite run is in progress — waiting..." 并 16s 心跳等待 286s，A 全绿 1639/1604/0/35 释放后 B 自动接锁复跑同样全绿 1639/1604/0/35；两份日志 0 个 `not ok`——上行取证的并发假红（43/4 文件级崩溃、总数骤降）在互斥下不再复现，"单会话独占"自此由 runner 强制而非人记。stale 锁（死 pid）启动时/等待中两路接管、SIGTERM 释放且 `child.kill` 不留孤儿舰队、逃生口三项单独手验通过；backend tsc 干净。 |
| 2026-07-04 | shared typecheck+test + backend tsc + desktop typecheck + full backend suite | passed | snapshot id 校验三处 `\d{4}`→`\d{4,}` 纯放宽（27ad677b）：shared 269/269（含新 paper-project-contracts.schema.test 两测——isSnapshotId 4/5 位接受+3 位拒绝、writing-package schema SP-10000 过/SP-999 400）；backend tsc 与 desktop typecheck 干净；full backend 1639/1604/0/35。 |
| 2026-07-04 | runtime-stress（预植活锁全流程）+ full backend suite（经重构 runner） | passed | 套件锁共享模块化验证：预植他人活锁下 runtime-stress 单文件 L5 步照跑（不取锁）、13 文件步正确打印等待，撤锁后接锁恰 1 次并全程绿 290 tests / 226 passed / 64 env-gated skips / 0 failed（与 T-114 基线一致，后续多文件步直取无等待）；full backend 经 lib 重构 runner 1639/1604/0/35、锁正常释放；v1c-production-depth 同形集成以 node --check + 同构行为证明收口（全跑含 provider 门控）。四脚本 node --check 全过。 |
| 2026-07-04 | 锁加固实测 5 场景 + full backend suite（经加固 runner） | passed | 复审修复验证（修复形态见 03 同日条目）：①stale(死 pid)接管 + 心跳 mtime 推进（15s tick 实测 +15s）；②组信号不变量——TERM 后 12 个舰队进程 200ms 内全灭且"锁消失时刻无任何舰队进程存活"逐 200ms 采样零违例（此场景实测揪出协调器瞬死孤儿化 worker，随即升级 detached+组信号）；③年龄兜底接管（pid=1 存活 + mtime 超 5min → 正确判 stale）；④release 外来内容锁不删（foreign payload 存活）；⑤双 runner 排队/增强等待消息（ps -p 提示）/TERM 交接（B 接锁 pid 正确）/锁+claim 零泄漏。full backend 经加固 runner 全绿 1639 tests / 1604 passed / 0 failed / 35 skipped，锁正常释放。claim 串行化接管的微秒级双 waiter 竞争无法确定性摆拍，以构造证明收口（见 03）。flow-runner backend-test 预算 300s→900s 同轮生效。 |
