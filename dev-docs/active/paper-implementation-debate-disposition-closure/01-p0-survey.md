# 01 P0 勘察记录（2026-07-18，只读勘察，无代码改动）

支撑 `00-overview.md` §决策 的事实基础。三路并行深读：skeptic 三态语义 / coordinator waiting_review 机器 / evolution 确认停驻。文件简称：**coordinator** = `apps/backend/src/services/paper-implementation-run-coordinator-service.ts`；**route-planning** = `apps/backend/src/services/paper-implementation-route-planning-runtime-service.ts`；**curation** = `apps/backend/src/services/paper-implementation-evidence-board-curation-runtime-service.ts`；**evolution** = `apps/backend/src/services/paper-implementation-motive-evolution-runtime-service.ts`；**contracts** = `packages/shared/src/research-lifecycle/paper-implementation-runtime-contracts.ts`；**board-service** = `apps/backend/src/services/paper-implementation-motive-evidence-board-service.ts`。

## 五条 load-bearing 事实

1. **gs-003 skeptic artifact 同时带 `status=blocked` + `recommended_disposition=revise`**（`.ai/.tmp/paper-implementation-golden-scenario/gs003-bitfit-live-003/21-fetch-final-runtime-artifacts-main.json` items[5]；blockers 三码含 `MNLI_GRADIENT_SUBSAMPLING_PROTOCOL_UNSPECIFIED`；6 findings 中 3 条 `severity=blocking`+`blocks_route_progression=true`；role_summary 自陈 "revision-level risks, not reasons to abandon"）。裁决已是 revise，是推导层收敛成了终态。
2. **blocked 的机械成因是双扳机**：route-planning:330-332 `finalStatus = blocker_codes 非空 || role_status==='blocked' ? 'blocked' : 'passed'`。即使 prompt 教会答 `passed+revise`，只要同时报 blocker_codes（prompt 要求 blocking finding 带 code，必然报）仍落 blocked——**纯 prompt 修不成立**。
3. **方案 A 的 coordinator 前提已在**：coordinator `stepOutcome`（:1524-1566）三分支——① curation 特例 `disposition==='revise' && admitted → waiting_review`（:1549-1555，显式排在 blocked 判定前，D2-pre2 产物）；② 通用 `status==='blocked' → blocked`（:1556-1558，gs-003 skeptic 在此被截）；③ skeptic 槽 `passed && disposition!=='proceed' → waiting_review`（:1559-1564，**设计好但从未触发**——gs-001 时代 review packet 已定性"waiting_review 停驻点从未触发"）。
4. **不存在可修 vs 硬阻断的 blocker-code taxonomy**：gs-003 三码与 N6 停驻码全是 LLM 现场自造自由字符串（TS 代码零命中）。唯一确定性判定先例是 curation `deriveFinalStatusAndDisposition`（curation:924-944）：按**结构事实**（`role_status==='passed' && gap findings 非空 → revise`）推导，disposition 为 server-derived，LLM 回显漂移时服务端赢、只打 warning `EVIDENCE_BOARD_CURATION_DISPOSITION_ECHO_DRIFT`（:952-960）。
5. **N6 确认机器 100% 现成、缺的全是接线**：HumanConfirmationRecord（human actor 强制、scope 化、目标绑定、consume-before-write 单次燃烧，`paper-implementation-human-confirmation-service.ts:36-79` + `paper-implementation-governance-gate-refs.ts:55-155`）；scope 枚举已含 `motive_evolution_decision`/`motive_portfolio_decision` 且已被权威写 API 消费（board-service:800-897 决策创建强制 confirmation_ref；:677-791 portfolio apply majorStructuralChange 判定 :1245-1258 + 消费后写 :772-784）。缺口=① coordinator 对 evolution 无 waiting_review 分支（blocked 直落队列）；② override 仅"改载荷重跑"一种动作、advance 契约无任何确认 ref 字段；③ runner 从不调权威链（`.ai/scripts/paper-implementation-golden-scenario.mjs:1132-1137` blocked 如实终止）。

## N2 侧勘察细节

- **契约形状**（contracts）：route skeptic `role_status: 'passed'|'blocked'`（:1374，schema :3846）、`recommended_disposition?: 'proceed'|'revise'|'park'|'abandon'|null`（:1326-1333、final artifact :1407）、`blocker_codes: string[]`（:1377 自由串）、finding 级 `severity`（:1357）与 `blocks_route_progression`（:1362）。**四轴共存、无组合一致性约束**（blocked+revise schema 合法，gs-003 实证 admitted）。正交性正式表述只存在于 curation 侧契约注释（contracts:2018-2035，D2-pre2："blocker codes = 输出不可用/技术性；disposition = 对可用输出的裁决"，并点名与 route-skeptic disposition 同构命名）；route skeptic 契约本体零注释——S3 收尾已把"skeptic/curator blocker-vs-disposition 语义形式化"列为移交项（`../paper-implementation-productization-hardening/03-implementation-notes.md:54`）。
- **prompt 现状**：roleMessages builder（route-planning:945-954）四句 system，处置指引仅一句（"may recommend proceed, revise, park, or abandon…"）；**对 role_status 零指引、对"输入可修缺口"场景零指引**——是空白而非误导。对照：result-analysis prompt 有明确 role_status 指引（`paper-implementation-result-analysis-runtime-service.ts:914`）。
- **运行时处理**：blocked 时 disposition 仍被读取入 final artifact（route-planning:831）；语义完备检查（六维度全查/findings 非空/disposition 必给，:1415-1427）**只对 passed 执行**，blocked 只做回显对账；admission 唯一 blocked 硬约束="blocked 且 envelope codes 空"拒收（`paper-implementation-runtime-admission-service.ts:198`）；blocked 无修复 refs 只打非阻断 warning `ROUTE_SKEPTIC_REPAIR_SUGGESTIONS_MISSING`（:1438-1449）。
- **blocked 后果链**：coordinator 通用 blocked → 入 DecisionWorkQueue（:1013-1015；LLM 自由码不过 R4 信任白名单 :943-949，兜底 `human_review` 桶 :376-388）→ run `blocked`（非终态，可 re-advance 整槽重跑，:869-874 只认 passed step）。waiting_review 则"semantic stop 不入队"（:1011-1012）。"无复审续路"的准确含义=无语义复审出口（blocked=缺陷态+队列项，重跑是同输入重试），非机械不可推进。
- **gs-003 缺口埋法**：GT-9 GAP-1（`.ai/golden-scenarios/paper-implementation/gs-003-bitfit/ground-truth.md:93-98`）——stage-2 梯度矩阵预承诺网格与阈值但从不预承诺 MNLI 子采样协议；"skeptic 命中它=系统在工作"。fixtures 未捕获原始 role_status（telemetry `outcome` 是技术调用成败），无法断定 LLM 答的是 blocked 还是 passed+codes——**双扳机下两者产生完全相同的终态,这本身就是推导层缺陷的证据**。
- **范围澄清**：lane A 的 skeptic 是 `route_skeptic_review.route_risk_critique` 槽（lane `validation-planning` step-1，coordinator:96-101）；trace-integrity debate 未挂任何 coordinator lane（:114-115 前瞻守卫注释），其 skeptic 无 disposition 概念（处置是 finding 级 reconcile，contracts:634-656），**不在 N2 范围**。

## N6 侧勘察细节

- **停驻链**：park/split 是 option_kind 枚举值（contracts:2456-2464 七种；`portfolio_impact_class` 含 `lineage_change`，:2468-2477）；slot 零副作用（八个 `no_*_side_effect` 守卫 evolution:2063-2072 + FORBIDDEN_AUTHORITY_FIELDS :292-323）。确定性护栏只"强制打旗"：PORTFOLIO_CHANGING 选项必须 `human_confirmation_required=true` + 合法 next_gate，违者 `MOTIVE_EVOLUTION_HUMAN_CONFIRMATION_GATE_MISSING`（可重试技术失败；判定集 :333-339，检查 :2018-2038）。停驻本体=LLM 在 park/split 选项 challenge_check 写 `human_confirmation_status='blocked'`+自由码 → `outputBlockerCodes` 把**所有选项**（含未选中）codes 并入 final blockers（:2164-2173）→ final blocked（:455-459）→ coordinator 无 evolution 分支直落 blocked。gs-003 live-002 实证：keep_current 选项干净、split 选项三码，final 仍 blocked。
- **触发非确定**：gs001-lora-live-014 blocked（park 变体码）、gs003 live-002 blocked（split 三码）、gs002-live-002 与 gs003-live-003 completed（challenger 只提 keep_current/repair，无停驻）。live 复现 park/split 形态不可控——P2 验证以测试面为主、live 证据机会主义拿。
- **confirm 后续链（现有权威链，与 coordinator 脱钩）**：确认（scope=`motive_evolution_decision`）→ POST `/motive-evolution-decisions`（structural_evolution + 默认 `human_confirmation_required_for_major_change=true` → 强制 confirmation_ref + requireActive + approved/applied 需完整 trace_manifest + 消费后写）→ 语义版本变更需 approved 决策（board-service:128-141）→ park/split 组合执行 POST `/motive-portfolio-decisions/apply`（majorStructuralChange → `human_confirmed` + scope=`motive_portfolio_decision` ref + target 全覆盖 + 消费后写；写 lifecycle `'split'/'merged'`、`parked_motive_ids`、`role_decision_ref`）。
- **能力缺口（登记，非本包全修）**：① admitted evolution artifact → 决策请求之间无机器接线（纯人工转录）；② `CoreMotiveIdentity.lineage` 图字段（split_into/merged_into/parent/child/superseded_by）创建时初始化为空后**全仓无写入者**——"血缘变更"实际只落 lifecycle_status+role+set 成员；③ 20-check 血缘枚举与 evolution 停驻相关的 check=零（回溯链 dossier→claim→packet→REU→WO→cycle→probe/route 不经 motive lane），confirm-and-continue 做成后在现有 20-check 上无可见性，若要审计面覆盖需新增 check。
- evolution 是 motive lane 末槽（coordinator:102-105）——N6 的"续链"实质=lane 能诚实 completed（也消解 N7 的一个构成因子）。

## coordinator/override 机器细节（P1/P2 共用）

- Run 状态：`created/advancing/waiting_review/blocked/budget_exhausted/completed/failed`，终态仅 completed/failed（`packages/shared/src/research-lifecycle/paper-implementation-coordinator-contracts.ts:34-56`）。step outcome：`passed/blocked/failed_runtime/waiting_review`（:60-65）。
- waiting_review 唯一出口=re-advance（routes POST `/coordinator-runs/:id/advance`）；advance 请求面仅 `holder_id` + `slot_request_payload_overrides` + `raise_budget_envelope`（coordinator-contracts:239-258）。re-advance 找第一个非 passed step **整槽重跑**（新 attempt、真跑 LLM、再计预算；coordinator 从不注入 `resume_from_run_id`——它在 coordinator-owned 禁带字段 :117-123，槽内 D9 resume 引擎只服务 trace-integrity/P1-review 两 runtime，不认识 coordinator run）。
- **override 无动作枚举、无 confirm 语义、无专门审计事件**（budget raise 有 `budget_raise_events`；payload override 的痕迹仅 run 行覆盖后的 payloads + 新 step 行；holder_id 只活在 lease、finish 清空，产品侧不落盘 actor）。HumanConfirmationRecord 机器与 coordinator override 完全不接——无字段把确认关联到一次 advance。此即 D-133-3 要补的面。
- golden runner 停驻纪律（`.ai/scripts/paper-implementation-golden-scenario.mjs:1132-1207`）：waiting_review → 诚实记 stop → 至多一次载荷不变 override re-advance（holder_id=`${SCEN}_human_override_reviewer`）→ 再停则 `waiting_review_terminal` 诚实终止；blocked → 如实终止；非四点停驻一律不 override。

## 更正与备查

1. `HUMAN_CONFIRMATION_REQUIRED_FOR_LINEAGE_CHANGE`（T-124 尾巴 tracker N6 原文）非后端常量，是 LLM 自产 blocking code（同族变体：`PORTFOLIO_DECISION_REQUIRED_FOR_LINEAGE_CHANGE`、`NO_RESULT_OR_HUMAN_REQUEST_TRIGGER_FOR_SPLIT`、gs-001 run 014 小写变体 `human_confirmation_missing_for_parking_execution`）。本包文档一律按"LLM 自产码"表述。
2. `dossier_export` 确认 scope 在契约存在（`paper-implementation-human-confirmation-contracts.ts:8-15`）但全后端无消费方——四点集#3 目前纯靠 runner 自律停驻。跨包事实，备查，不在本包修。
3. gs003-live-003 `rubric-scored.md:86` 把 evolution 行记为"n/a（停驻）"与该 run summary 的 passed 不一致——评分文档措辞残留，不影响结论。
