# Topic Selection — Observed Problems and Desired Improvements

## Purpose and authority

This register preserves recurring product and research-process problems observed while operating the real topic-selection chain, together with the desired behavior and a concrete success signal. It is evidence and requirements input, not a second status, roadmap, architecture, or verification authority. Current scope and sequencing remain in `00-roadmap.md`; settled contracts remain in `02-architecture.md`; decisive checks remain in `verification.md`.

## Current problem and improvement register

| ID | Observed problem from actual work | Desired improvement | Success signal | Route |
|---|---|---|---|---|
| TS-I01 | Researcher-facing conversation was dominated by N-node names, record types, UUIDs, hashes, and backend actions. The evolving research object was difficult to see and control. | Generate one concise human Markdown result per semantic stage, backed by a manifest and a much larger retrievable LLM working plane. Keep technical lineage in a secondary trace. | A researcher can explain the current evidence, alternatives, decision, risks, and requested action without reading internal identifiers; regeneration from canonical owners is deterministic. | Phase 7 contract, Phase 9 integration |
| TS-I02 | Authorization was requested for backend restarts, deterministic nodes, and short or multi-minute local work, producing slow cadence without adding meaningful control. | Bind authorization to effect and human authority. One instruction advances bounded local, non-provider, non-destructive work to the next semantic decision while emitting progress. | Routine work does not pause; research-meaning, provider/cost, destructive/control, environment, scope-expansion, and ambiguous-recovery actions still stop exactly. | Phase 7 contract, Phase 9 integration |
| TS-I03 | The live chain produced a coherent, gate-ready but only moderate-value topic and spent substantial effort defending value inside the initial scope. | Separate topic search from topic defense and make cheap `park`, `drop`, `reframe`, or `expand` outcomes first-class before expensive N8/package work. | A clearly low-value or dominated topic stops successfully with evidence and reopening conditions; gate readiness is not treated as comparative value. | Phases 8-10 |
| TS-I04 | The highest-value novelty correction was triggered by the researcher questioning the retrieval-method relation, not by an autonomous product mechanism. | Add an independent prior-art/topic-killer role that actively searches for near-isomorphic work and argues for stopping before synthesis. | Shadow runs recover known nearest-work collisions and can preserve a fatal minority report even when other roles favor continuation. | Phase 8 |
| TS-I05 | Existing v1a/v1b/v1c debate mechanisms did not run in the current lineage; one Codex subagent supplied useful research but was not a recorded debate. | Make independent role prompts, evidence scopes, outputs, disagreements, termination, failures, and provenance product-visible and recoverable when divergence is claimed. | A debate claim can be replayed from durable pre-exposure role artifacts; a subagent conversation alone never counts as product debate. | Phases 7-8 |
| TS-I06 | Candidate and debate prompts structurally preferred continuation: non-empty outputs were required, N6 lacked a successful no-topic exit, and repair was favored over rejection. | Change prompts together with output schemas, gates, loopbacks, and terminal-status semantics so zero viable, park, drop, and scope switch are successful outcomes. | Clearly low-value fixtures are not forced into candidates; no-topic returns evidence, reasons, confidence, and reopening conditions without a runtime failure. | Phases 8-9 |
| TS-I07 | Literature processing and hybrid retrieval existed, but the live chain relied on basket snapshots/manual imports and later passed refs, hashes, and summaries rather than repeatedly retrieving claim-bearing text. | Product orchestration executes role-specific RAG and resolves cited refs into bounded EvidencePackets with excerpts, locators, query intent, role, and support/challenge relation. | Every material model claim is traceable to content that role actually received; UUID-only and inherited-summary inputs fail admission. | Phases 7-8 |
| TS-I08 | The scoped corpus contained 410 readable records, yet most downstream convergence operated on the already-selected EvidenceBasket, reinforcing path dependence. | At semantic arena entry, permit bounded retrieval outside the current basket according to role intent while preserving topic-scope, authorization, and provenance limits. | Opportunity and topic-killer roles can discover relevant semantic groups or direct neighbors absent from the current basket without unrestricted browsing. | Phase 8 |
| TS-I09 | N8 retained material originality/reviewer risks and triggered critic review, while the current promotion gate exposed zero warnings, risk refs, or actions. | Carry objections, critic findings, and minority reports by stable ref until each has an explicit repair, accepted-risk, loopback, park, or drop disposition. | A final green gate cannot coexist with unresolved material dissent; handoff rematerialization preserves exact dispositions. | Phase 9 |
| TS-I10 | Existing checkpoint currentness assumes one current lineage per TitleCard/stage, so a genuine two-path fork may cause sibling supersession or competing authority. | Default to one active path plus bounded parked alternatives; add a second active path only after exact human approval and a proven child-TitleCard or branch-identity representation. | No sibling silently supersedes another, and research status has one unambiguous current path unless an explicit supported fork exists. | Phase 7 decision, Phase 9 if needed |
| TS-I11 | Existing success signals reward reaching a ready gate; they do not measure whether a bad topic was stopped or a good topic was wrongly killed. | Calibrate with labeled worthwhile, low-value, and ambiguous cases and count justified stopping as success. | False continuation, false drop, human override, evidence delta, cost/latency, and downstream work avoided are reviewed before activation. | Phase 10 |
| TS-I12 | Re-running debate or loopback on unchanged context can create activity without new information. | Require a recorded evidence, candidate, constraint, or human-objective delta before another arena version. | No-delta retry is rejected or terminates; accepted retries show the exact new information and its decision effect. | Phases 7-9 |

## Improvement discipline

- Solve these issues through the smallest semantic contract that makes the correct behavior unsurprising; do not build a generic agent platform first.
- Full divergence belongs only at research-semantic choices. Deterministic snapshot, publication, package, and gate mechanics remain deterministic.
- Debate creates useful pressure only when participants are genuinely independent; literature/RAG enables correct convergence only when roles inspect claim-bearing evidence. They are one combined quality mechanism.
- Human authority remains exact and snapshot-bound. Faster cadence means fewer meaningless pauses, not weaker control over research meaning, cost, destructive effects, or environment changes.
- An improvement is not complete because a new record or agent exists. It must change stop/continue quality, evidence visibility, risk survival, or researcher control under verification.
