# Verification

## Selected local development schema synchronization — 2026-09-08

- T-148 Phase 4 recovered the normal local backend and reproduced `P2022` on `TopicSelectionEvidenceMap.predecessorEvidenceMapId` through the canonical evidence-map HTTP read. Prisma status identified exactly the convergence-contract and Arena-state migrations as pending.
- After SQL review, existing `pnpm db:dev:migrate` applied `20260903090000_add_topic_selection_evidence_convergence_contracts` and `20260907100843_align_arena_execution_state_constraints` to PostgreSQL `127.0.0.1:5432`, database `postgres`, schema `my_researcher_dev`. Migration status now reports all **82 migrations current**. No migration file, table reset or runtime workaround was introduced.
- The original HTTP read returned 200 after synchronization, nine source units were readable, and two historical checkpoint packets plus the promotion support, dossier and decision bundle remained equal after JSON parsing. The bounded T-148 fresh evidence materialization also passed. Evidence is linked in [the Phase 4 observation bundle](../topic-selection-real-flow-findings/artifacts/phase4-local-reread-2026-09-08.json), committed with `c339fc17`.
- This records deployment to that local schema only. It adds no live provider, new scenario adoption, normal-database concurrency experiment, or stronger crash-recovery claim. The prior disposable PostgreSQL verification remains the evidence for those tested relational seams. T-150 remains done at its existing bounded scope.

## Supplemental disposable PostgreSQL verification — 2026-09-07

- The user approved direct bounded verification without a separate task package. The uncommitted T-151 draft and its registry/generated projections were withdrawn through governance prune; no new task or scenario adoption is required for this check.
- Baseline: replaying 81 migrations on PostgreSQL 17.7 / pgvector 0.8.1 passed migration status, but the real Arena test failed at `tsras_current_chk`. The persisted CHECK constraints still recognized only `open`, `synthesized`, and `superseded`, while the existing runtime also uses `executing` and `blocked`.
- Repair: additive migration `20260907100843_align_arena_execution_state_constraints` aligns the three state/currentness/synthesis CHECK constraints with the existing lifecycle. Current-key and supersession requirements remain intact; executing sessions cannot contain completion data; blocked sessions require a termination reason and paired transcript ref/hash without a synthesis timestamp. A blocked record may omit both transcript fields, but cannot persist only one. Historical migration files and product database data were not changed.
- Real verification: a fresh nonce-named local database replayed **82 migrations with zero unfinished migrations**. The existing successor/request and Arena integration tests passed **three consecutive rounds, 6 passed / 0 failed / 0 skipped**, using explicit `DATABASE_URL`, `TOPIC_SELECTION_EVIDENCE_CONVERGENCE_PRISMA=1`, and `TOPIC_SELECTION_RESEARCH_ARENA_PRISMA=1`.
- Covered: durable request deduplication and competing Human/claim transitions; stale successor CAS; Arena supersession and active-session fence; concurrent single-winner execution claim; audited blocked-result persistence and rejection of malformed completion data; concurrent gap-projection recovery. The Arena fixture exercises shared repository invariants, not adoption of another scenario into the convergence kernel. Its stale error-message matcher was aligned with the existing `not current and executable` rejection.
- Migration status was up to date before and after the final tests. Migration replay versus `prisma/schema.prisma` returned an empty diff with exit code 0. The checked Arena/request/EvidenceMap fixture tables had zero remaining rows, and all three session-owned disposable databases were removed after their respective attempts.
- Additional verification: focused coordinator/round/pilot/Arena/search-resource **48/48**, root `pnpm typecheck` (including Prisma generation and DB-context constraints), independent review **Clear**, and `git diff --check` passed.
- Limits: this verifies the named relational seams, not full coordinator crash recovery across processes or a live provider. The incremental migration was applied only to disposable databases; the normal product database has not been migrated. No build or new Human research decision ran. The bounded T-150 outcome remains unchanged.

## Evidence

| Claim / reference | Check / procedure | Latest result | Evidence / limitation |
|---|---|---|---|
| Full managed-library retrieval already exists | Inspect `LiteratureRetrieveRequest`, its route schema, and `LiteratureRetrievalService.resolveCandidateVersions` | observed | `query` is the only required request field. Without `topic_id` or `paper_id`, the service lists all active embedding versions and keeps evidence-ready records; rebuilding the retriever is not part of this task. |
| Snapshot-bound evidence admission is distinct from retrieval scope | Inspect SearchPlan creation, SearchRun authority blockers, and EvidenceMap allowed-ref validation | observed | SearchPlans require a literature snapshot; SearchRun bindings outside it are blocked; EvidenceMap accepts only SearchRun input/binding refs. The pilot must onboard new hits without weakening provenance. |
| The research-question candidate stage freezes evidence lineage | Inspect the downstream input bundle, intake snapshot, current-EvidenceMap guard, and evidence-expansion route | observed | The bundle freezes `evidence_map_ref`, SearchRun, SearchPlan, and literature snapshot. Evidence expansion returns to intake with no successor-map handoff, so this stage cannot host an in-place evidence refresh without reopening upstream lineage and Human decisions. |
| Evidence-landscape convergence owns the natural pilot loop | Inspect search recheck, child SearchPlan, SearchRun, EvidenceMap materialization, checkpoint creation, and checkpoint loopback | observed | These canonical owners already compose the search and evidence lineage before the downstream frozen handoff. The pilot can add role-issued requests and successor links without changing downstream question authority. |
| Arena is a partial reusable pilot | Inspect `TopicSelectionResearchArenaRetrievalService.prepare` and SearchRun recording | observed | Non-local Arena mode invokes the unscoped retriever and records a SearchRun, but outside-map hits remain unresolved and the service is Arena-specific. Phase 2 must reuse its retrieval/provenance seam without importing participant authority. |
| Debate cannot currently request evidence | Search production and shared contracts for RetrievalRequest and inspect Debate role outputs | observed | RetrievalRequest exists only in T-148 planning text; current role contracts consume frozen context and cannot trigger retrieval. |
| Current Debate core is one frozen role walk | Inspect `TopicSelectionBoundedDebateCoreService.runLoop` and need-discovery `round_index` validation | observed | The core hashes one fixed role-order transcript. `max_rounds` limits an index but supplies no parent-transcript or evidence-delta link. |
| Pre-Phase 1 EvidenceMap lacked successor lifecycle | Inspect the pre-Phase 1 shared record, Prisma model, freshness update route, and repository update | repaired | Phase 1 added predecessor/successor/delta/revision state, a single compare-and-swap successor transition, and runtime/type guards that prevent direct creation or ordinary freshness updates from claiming supersession. |
| T-148 scope split | Compare T-148 findings, `dde23f76`, and the approved split with this bundle | passed for the exchanged contract | FIND-029 through FIND-031 supply discovery evidence; T-148 retains presentation and strict-Human acceptance while `REQUIRED_COVERAGE_MISSING` with exact row refs is now available for this pilot. T-150 remains limited to evidence-landscape convergence. |
| Managed-library manifest and exact candidate universe (ECK-01) | Compare unscoped retrieval and manifest eligibility through the same resolver; create full and Human-constrained manifests without a title basket | passed | Focused retrieval/search-resource tests prove evidence readiness, active-profile compatibility, current-index filtering, deterministic source/member order, captured candidate-window identity, full-corpus default, and strict Human-only subset authority. Existing non-pilot snapshot defaults remain unchanged. |
| Coordinator request identity and SearchRun lineage (ECK-02) | Canonicalize equivalent inputs, reject malformed authority refs/role-authored keys/empty strategy, reuse concurrent durable requests, atomically claim/resolve/complete, and recover or fail closed from persisted execution state | passed | HTTP ingress, shared persisted schemas, service validation, synchronous in-memory key indexing, the Prisma unique-key race, and expected-status compare-and-set prove closed input and replay behavior. `executing` is rejected as a public outcome; Human resolution cannot overwrite a claim, automatic completion cannot overwrite a Human result, and ambiguous provider work is never repeated automatically. Manual materialization closes and preflights nested input before persisting its exact hash and planned child refs; focused restart and concurrent-service tests recover final-write failures into those same targets, reject changed input, terminalize deterministic post-claim conflicts, and prevent coordinator-keyed work from entering the manual path. |
| Saturation and execution boundaries (ECK-03) | Exercise no-delta unchanged strategy and each named standing-policy boundary, then carry measured retrieval accounting into the linked round | passed | The only terminal results are `saturated_unresolved` and `boundary_exhausted_unresolved`; neither exposes a pass result. The coordinator increments actual new orchestration work and elapsed time, converts query-embedding telemetry to micro-USD, does not recharge durable retrieval reuse, and lets a reached post-execution boundary override a ready-hit result. The linked round carries and advances the same accounting. |
| Linked frozen rounds (ECK-04) | Validate the closed round-link schema, run a successor round, and revalidate the exact parent transcript, EvidenceDelta, successor snapshot, evidence packets, role audits, transcript identities, and replay inputs | passed | The runtime creates a new Arena session rather than mutating the parent, derives workspace and predecessor target scope from durable records, regenerates every packet from current EvidenceUnit/source authority, verifies round-link and synthesis hashes on execution and replay, preserves an unresolved arbiter disposition, and counts historical linked rounds per durable issue rather than globally. |
| EvidenceMap successor compare-and-swap (ECK-05) | Exercise direct-create/supersession bypasses, child-identity failure, malicious successor revision, and stale competing writers in memory and on disposable PostgreSQL | passed | Initial creation and ordinary freshness updates cannot claim successor state; successor revision is coordinator-owned zero; child conflicts leave the predecessor current; Prisma updates the predecessor and creates the successor records in one transaction. |
| Typed immutable resolution support (ECK-07) | Persist and replay content-addressed EvidenceDelta and ResolutionRoute artifacts within and across title/lineage boundaries, then follow the route through the local pilot | passed | Identical artifacts deduplicate only inside the same title/workspace/workflow/input-snapshot authority; the route declares evidence-landscape ownership and deterministic-gate/strict-Human authority. The pilot returns to the same deterministic gate without authoring a Human decision. |
| Phase 2 role request → durable retrieval/distribution | Submit equivalent intents from multiple roles, execute two canonical queries, inspect call order and stored request/plan/run lineage, replay, exercise zero hits and the standing boundaries, and validate strict HTTP ingress | passed | One coordinator execution is persisted before every role distribution; replay performs no retrieval and incurs no new retrieval cost; child coverage rows preserve the parent matrix; predecessor EvidenceMap authorities are carried into the SearchRun; zero hits persist and stop unresolved; exhausted work creates no new request. |
| Phase 2 claim admission → successor | Admit an exact quote-bearing persisted hit, reject a tampered quote and workspace mismatch, inject a coverage-assessment persistence failure, submit an already-present claim, inspect predecessor/successor lineage and refreshed coverage, and validate strict HTTP ingress | passed for the second Phase 2 slice | Claim admission is bound to the exact recheck request, SearchRun, query, literature, chunk, hash, issue role, title, and workspace. A duplicate records `material: false` and leaves the head current; a material claim records the route/delta, persists its deterministic coverage assessment before the head compare-and-swap, then atomically publishes the successor while preserving predecessor content. Assessment failure leaves the predecessor current. It does not publish a checkpoint before Debate and the successor result no longer exposes a misleading placeholder checkpoint field. |
| Phase 2 successor → linked round → same gate | Run two independent support-only first passes and one synthesis arbiter over exact successor packets, persist audited role executions and a linked transcript, then invoke the real evidence-landscape checkpoint | passed for the third Phase 2 slice | The runtime rejects exhausted round accounting before side effects, requires the requested synthesized parent to remain current, verifies artifact versions and inline hashes, supersedes the parent with a new frozen Arena session, and materializes the checkpoint only after synthesis. The real checkpoint returns `eligible_for_human_review` with no policy issue in the bounded success fixture; no role output can author that result or a Human decision. |
| Composed bounded local pilot (ECK-06) | Run retrieval → exact admission → material successor → linked round → fresh checkpoint over real in-memory repositories and canonical services | passed | Three equivalent role intents share one durable full-managed-library execution over two queries; the exact persisted paragraph hit becomes one admitted challenge unit and material EvidenceDelta; compare-and-swap supersedes the predecessor; three regenerated EvidencePackets drive a linked audited round; the same `evidence_landscape` checkpoint key moves from `loopback_required` to `eligible_for_human_review`. Accounting carries 1 retrieval step plus 1 linked-round step, 1 linked round, measured elapsed time, and 250 micro-USD of retrieval cost. Only the retriever response and role-model outputs are mocked system boundaries. |
| ECK-08 | Run the real-flow replay, observability, concurrency, blocked-terminal, historical-checkpoint, and Human-reviewed failure proof | passed | The composed pilot and focused matrix prove single-flight retrieval, persisted SearchRun recovery, durable stale/provider-failure replay, atomic Arena execution claim, open-session recovery, active-session supersession fencing, blocked terminal replay, exact completed replay after live coverage drift and real successor-map supersession, distinct provenance identities, and preservation plus idempotent replay of the original strict-Human loopback decision. No replay repeats provider/role work or creates a second authority. |

## Mainline integration — 2026-09-07

- Merge `9a2ed867` integrates the verified `23156cb6` worktree into `main`, preserving the mainline advisory `model_hint` and pricing changes. No manual conflict resolution or new kernel behavior was needed.
- Integration checks passed: 86 focused backend tests (coordinator, round, pilot, Arena, search resources, agent orchestrator, v1a HTTP, convergence HTTP, and checkpoint contract parity), 453 shared tests, root `pnpm typecheck`, staged `git diff --check`, and T-150 governance lint.
- This checkpoint adds no live provider, target database migration, or Phase 3 PostgreSQL concurrency claim. T-150 remains complete at the same bounded local scope.

## Phase 3 quality and completion checkpoint — 2026-09-07

- The post-review repair adds atomic retrieval and Arena claims, expected-status claim/Human-resolution/automatic-completion transitions, durable failed SearchRuns, restart-safe manual materialization with predeclared child targets, open/interrupted/blocked round recovery semantics, active-session supersession fences, and transcript-frozen checkpoint inputs. Focused coordinator/round/pilot/Arena/search-resource tests passed 48/48; the v1a HTTP suite passed 8/8, including rejection of public `executing` and malformed manual materialization payloads; two Prisma concurrency cases were explicitly environment-gated.
- Manual materialization now preflights deterministic constraints before claim, then keeps an interrupted infrastructure attempt `executing` with a canonical input hash and planned SearchPlan/SearchRun refs. SearchPlan-final-write and SearchRun-final-write failure tests recreate the service and resume into those exact targets; a changed retry is rejected, and two concurrent service instances converge on one plan and one run. Deterministic post-claim conflicts close as traced `materialization_failed` work, while corrected preflight input can proceed on the still-open request. In-memory title/version and coverage-key uniqueness mirrors Prisma, including a two-request explicit-version race whose loser settles as `materialization_failed`; coordinator-keyed requests cannot enter the manual path or expose planned refs as completed retrieval output.
- Concurrent exact retrieval makes one provider execution and charges only its owner; an already-persisted SearchRun recovers without retrieval, and both stale output and provider exceptions persist one failed SearchRun whose exact replay makes no provider call. Concurrent exact linked rounds share one execution, a pre-opened session resumes through its durable claim, a competing session cannot supersede an active claim, a schema-blocked role persists one terminal session transcript, and exact blocked replay returns the same accounting without role records or a checkpoint.
- Completed round replay continues to return the original checkpoint after a newer coverage assessment appears and after the successor EvidenceMap and Arena are both superseded. Replay reads the conflict/coverage input frozen in the transcript rather than live `latest` state.
- The Human-reviewed packet records one strict-Human `loopback` decision against the original `loopback_required` checkpoint with the exact snapshot, review payload, required actions, and loopback refs. Successor publication preserves that decision as history; the fresh `eligible_for_human_review` checkpoint has no automatic decision, and exact replay of the historical Human decision is idempotent.
- The provenance audit confirms distinct RetrievalRequest, SearchRun, EvidenceDelta, round-link, transcript, and checkpoint identities. Each SearchRun query record carries the exact coordinator request/strategy keys, query-local ranked hits, provider telemetry, and a recoverable raw retrieval-log artifact.
- Shared tests passed 453/453. Backend tests passed 2,885 with 0 failures and 81 explicit external database/provider skips. Root TypeScript checks passed for shared, backend, DB-context, and desktop; `git diff --check` passed. No database/provider result is inferred from a skipped test, and build was not run because project instructions reserve it for explicit requests.
- The completion design reuses the existing SearchRun, Arena session/transcript/role executions, content-addressed round link, and checkpoint. It adds no completion receipt, aggregate pilot authority, temporary dual route, or second Debate scenario.

## Phase 2 quality and residue checkpoint — 2026-09-03

- Review-driven focused regression suite: 39 passed, 0 failed. It covers coordinator/successor workspace scope, closed HTTP actor authority, assessment-before-head failure ordering, successor packet regeneration, exact parent-map lineage, round-link hash/replay validation, unresolved synthesis disposition, per-issue linked-round accounting, and Arena retrieval workspace binding.
- Shared suite: 453 passed, 0 failed. Full backend suite: 2,875 passed, 81 conditionally skipped for absent external database/provider environments, 0 failed.
- Shared, backend, DB-context, and desktop TypeScript checks plus `git diff --check`: passed. Repository build was not run because project instructions reserve build commands for explicit requests.
- The bounded cleanup scan found no disposable T-150 files, temporary instrumentation, ignored `.ai/.tmp` content, task-added dependency residue, or unanchored duplicate implementation. The misleading successor `checkpoint: null` response field and public `created_by` ingress were removed during the quality repair.
- Global exact-file matches were retained because they are distinct environment/template entry points (`prod`/`staging`, directory-local Claude pointers, and Codex environment aliases). Older desktop scaffold dependency candidates were observed outside T-150 and left untouched. Directories outside the T-150 touched paths received only the required shallow/global checks, not a deep cleanup claim.

## Phase 2 bounded local pilot — 2026-09-03

- The integration pilot uses real in-memory repositories and the canonical control-plane, search-resource, EvidenceMap, EvidencePacket, Arena, Debate-core, and checkpoint services. Only the external retriever response and role-model outputs are deterministic fixtures.
- Full managed-library retrieval produced one durable execution shared across three roles and two persisted query provenances. Exact claim admission published one material successor; the linked round bound the predecessor transcript and EvidenceDelta before the original deterministic checkpoint was materialized again.
- The initial checkpoint reported `loopback_required`; the fresh checkpoint retained the same `evidence_landscape` current key and reported `eligible_for_human_review`. The prior checkpoint and EvidenceMap were preserved as superseded history.
- Coordinator accounting was repaired during pilot composition: actual new work increments the orchestration step, wall-clock time is accumulated, query-embedding USD is converted to micro-USD, replay does not recharge retrieval cost, and post-execution exhaustion remains unresolved. The linked round consumes the returned accounting rather than caller-invented counters.
- The affected coordinator/EvidenceMap/EvidencePacket/linked-round/pilot suite passed 16/16. The full backend suite passed 2,876 tests with 81 explicit environment-gated skips and zero failures; shared/backend/DB-context/desktop TypeScript checks and `git diff --check` passed.
- The post-pilot residue scan found no temporary instrumentation, ignored `.ai/.tmp` content, undiscovered test, exact duplicate of the new pilot, or new dependency residue. The pilot is discovered by the full `src/**/*.test.ts` runner. The older desktop `concurrently`/`wait-on` candidates remain outside T-150 with their original scaffold ownership. Build was not run because project instructions reserve it for explicit requests.

## Phase 1 repaired checkpoint — 2026-09-03

- Shared contract suite: 451 passed.
- Focused backend retrieval, search-resource, control-plane, EvidenceMap, Arena retrieval/retry, and downstream handoff suites: 70 passed; the single gated Prisma case was skipped in the no-database run and executed separately.
- Full v1a HTTP integration suite after final ingress wiring: 8 passed.
- Disposable PostgreSQL verification: all 81 migrations applied and the gated successor compare-and-swap suite passed 1/1, including coordinator-owned successor revision; the disposable database was deleted and zero matching databases remained.
- Shared and backend TypeScript checks, Prisma generation, DB context constraint check, and `git diff --check`: passed.
- A repository-wide backend run before the final HTTP schema wiring reported 2,856 passed, 79 skipped, and 6 failed out of 2,941. Two failures are database smoke checks that explicitly require `DATABASE_URL`; the other four v1b/matrix failures were reproduced unchanged at clean Phase 1 HEAD `430b0bcc`. The final focused suites cover every changed Phase 1 boundary, but this checkpoint does not claim the unrelated global baseline is green.
- Repository build was not run because the project instructions reserve build commands for explicit requests.

## Phase 2 linked-round checkpoint — 2026-09-03

- Shared contract suite: 453 passed, 0 failed.
- Full backend suite after repairing the two unconfigured Prisma HTTP smoke gates: 2,870 passed, 81 conditionally skipped, 0 failed.
- Linked-round/Arena focused suite: 9 passed; strict evidence-convergence HTTP suite: 3 passed.
- The bounded successor-round test executes the real evidence-landscape checkpoint after Arena synthesis and observes `eligible_for_human_review` with no policy issues.
- Shared, backend, DB-context, and desktop TypeScript checks plus project-governance lint and `git diff --check`: passed.
- No database/provider result is inferred from a skip. T-054 and T-067 now follow the repository's established conditional-smoke convention when `DATABASE_URL` is absent.
- Repository build was not run because the project instructions reserve build commands for explicit requests.
- This checkpoint closes the linked-round implementation unit, not ECK-06: the complete retrieval-to-checkpoint flow still needs one composed bounded local pilot.

## Source-to-acceptance traceability

| Source | Observed problem or accepted adjustment | T-150 response | Acceptance refs |
|---|---|---|---|
| T-148 FIND-029 | The real run used a topic-scoped frozen snapshot even though the retriever already supports the full managed library; relevant outside-snapshot hits have no provenance-preserving admission path. | Keep the broad managed library as retrieval fuel, bind each execution to a canonical corpus manifest and retrieval-stack identity, admit material claim-level evidence through durable SearchRun lineage, and publish a successor result rather than weakening snapshot provenance. | ECK-01, ECK-05, ECK-06 |
| T-148 FIND-030 | Recorded Debates performed zero retriever operations, and role contracts cannot request evidence after identifying a literature gap. | Add typed, durable, reusable RetrievalRequests; measure evidence delta; continue material changes as linked frozen Debate rounds. | ECK-02, ECK-03, ECK-04, ECK-06, ECK-08 |
| T-148 FIND-031 | Gate failures, Debate findings, and loopbacks are separate mechanisms, so an issue may have no owned repair delta or same-gate return route. | Add one typed ResolutionRoute for the pilot and prove issue → repair → declared delta → same-gate recheck without creating a second decision authority. | ECK-06, ECK-07, ECK-08 |
| Accepted process adjustment | EvidenceMap is a selected, replayable result; the managed literature library is the process fuel. | Keep retrieval process state in RetrievalRequest/SearchRun/round artifacts and preserve EvidenceMap history through explicit successor lineage. | ECK-01, ECK-02, ECK-05 |
| Accepted process adjustment | Debate retrieval must not stop at a fixed request count, while repeated work still needs a convergence boundary. | Let the coordinator derive request/strategy identity, reuse equivalent requests, saturate only an unchanged issue/strategy with no material delta, and enforce explicit step/round, time, and cost boundaries as unresolved stops rather than pass conditions. | ECK-02, ECK-03, ECK-08 |

FIND-029 through FIND-031 are reproduced defects. RetrievalRequest, linked rounds, successor lineage, strategy-level saturation, and ResolutionRoute are approved design responses; they are not independently claimed as pre-existing failures. Completion means the single evidence-landscape pilot closes the mapped behavior end to end. Adoption by downstream question, value, or promotion Debate scenarios requires separately accepted follow-up work.

## Follow-up boundary

- T-150 has no outstanding acceptance verification at its bounded local pilot scope.
- The named relational seams now have the supplemental disposable PostgreSQL evidence above. Live external provider verification remains unexecuted and requires a concrete input, route, and budget; skips do not imply success.
- Keep downstream question/value/promotion adoption out of scope until a separately accepted outcome and owner satisfy the recorded adoption criteria.
