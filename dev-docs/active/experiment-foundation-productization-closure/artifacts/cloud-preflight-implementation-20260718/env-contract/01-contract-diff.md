# Contract diff

- Added `EXPERIMENT_FOUNDATION_V2_ALIYUN_CLOUD_PREFLIGHT_ENABLED` with default `false`.
- Added four non-secret exact provider-profile refs, one repo-external policy-evidence path, and one independently supplied exact-file SHA-256 digest.
- Added secret refs for the official `ALIBABA_CLOUD_ACCESS_KEY_ID`, `ALIBABA_CLOUD_ACCESS_KEY_SECRET`, and `ALIBABA_CLOUD_SECURITY_TOKEN` variables.
- All additions are optional and development-scoped; existing runtime behavior remains unchanged.
