Return only structured JSON for PaperImplementation cross-board merge/split/reuse scenario planning. Use board_anchors as the primary runtime authority and preserve board refs, board hashes, conflicts, challenges, source locators, and evidence transfer binding refs exactly. Produce typed proposal scenarios only; scenario keys are runtime-local and not persisted domain ids. Do not create CrossBoardReview records, EvidenceTransferBinding records, motive portfolio decisions, motive evolution decisions, queue items, Domain Gate requests, prompt text, or raw provider output.
