# 06 Node Runtime Matrix

## Classification Legend
- `existing-minimum`: current implementation exists but is not production runtime promoted.
- `first-slice`: recommended first production promotion slice.
- `promoted`: controlled runtime/admission route, tests, and Domain Gate or explicit no-authority boundary are implemented for the named slot.
- `later-slice`: promote after first slice proves pattern.
- `deterministic-only`: should stay deterministic unless a future review proves LLM value.
- `experiment-runtime`: belongs to WorkOrder/live experiment execution lane.

## Agent Workflow Slots
| Workflow type | Current surface | Classification | Production gaps | Recommended action |
|---|---|---|---|---|
| `trace_integrity_review` | `trace_integrity_review.boundary_debate` runtime route plus T-097 trace service | promoted | Remaining production work is broader environment rollout, not slot entrypoint/routing; deeper semantic repair loop remains future work | Keep as the P1 bounded-debate reference slot; reuse its no-fallback, retry, telemetry, provider canary, and L5/L6 evidence pattern for later debate nodes. |
| `claim_boundary_review` | `claim_boundary_review.boundary_debate` runtime route plus T-098 claim service Domain Gate | promoted | Remaining production work is broader environment rollout; richer claim alternative selection can be added after result-analysis matures | Keep deterministic claim authority in T-098; runtime final artifacts only materialize through Domain Gate after admission. |
| `dossier_readiness_prep` | `dossier_readiness_prep.readiness_audit` runtime route plus T-098 dossier service Domain Gate | promoted | Remaining production work is broader environment rollout; writing-packet projection still stays outside runtime | Keep readiness alternatives as admitted runtime support only; Domain Gate remains the bridge into deterministic dossier authority. |
| `result_analysis` | `result_analysis.interpretation_scenarios` runtime route plus T-098 result interpretation packet Domain Gate | promoted | Multi-role interpretation debate is deferred; current production slice is single-role bounded scenario output with fail-closed retry and no interpretation-as-evidence | Keep result-analysis as interpretation-packet materialization only; use its scenario-output contract before claim-boundary or dossier consumers. |
| `experiment_critique` | `experiment_critique.plan_critique` runtime route plus T-096/T-104 WorkOrder/live adapter boundary | promoted | Remaining work is broader provider/live rollout; current slot is critique-only and has no execution side effect | Keep critique artifacts separate from live experiment adapter and WorkOrder admission; admitted final artifacts are review evidence only. |
| `experiment_design` | `experiment_design.work_order_draft` runtime route plus T-095/T-096 planning/workorder contracts | promoted | Remaining work is broader provider/live rollout; current slot proposes WorkOrder draft candidates but does not create WorkOrders | Keep WorkOrder draft creation in deterministic service routes; runtime final artifacts only carry admitted draft candidates. |
| `route_architecture` | `route_architecture.route_candidates` runtime route plus T-095 route/probe planning authority | promoted | Remaining work is broader provider rollout; first slice is proposal-only and has no route/cycle mutation side effect | Keep deterministic route/cycle creation in T-095; runtime final artifacts remain admitted route candidate proposals only. |
| `route_skeptic_review` | `route_skeptic_review.route_risk_critique` runtime route consuming admitted route proposals | promoted | Remaining work is broader provider rollout; first slice is critique-only and has no queue materialization | Keep queue/route repair authority outside runtime; admitted final artifacts remain risk critique evidence only. |
| `validation_cycle_planning` | `validation_cycle_planning.cycle_candidates` runtime route plus T-095 deterministic cycle authority | promoted | Remaining work is broader provider rollout; first slice proposes cycle candidates only | Keep `ValidationCycle` creation/admission in deterministic service; runtime cannot create cycles, probes, plans, queues, or Domain Gate payloads. |
| `cross_board_synthesis` | `cross_board_synthesis.merge_split_reuse_scenarios` runtime route plus T-094 board/cross-board authority | promoted | Remaining work is broader provider rollout; first slice proposes scenarios only and has no cross-board review, transfer, portfolio, queue, or Domain Gate side effect | Keep board/review/transfer/portfolio mutations in deterministic services; runtime preserves board/source/conflict/transfer refs and produces admitted scenario proposals only. |
| `evidence_board_curation` | `evidence_board_curation.binding_gap_candidates` runtime route plus T-094 board/evidence binding and trace authority | promoted | Remaining work is broader provider rollout; first slice proposes binding/gap candidates only and has no board, binding, citation, trace-repair, queue, or Domain Gate side effect | Keep board/evidence/citation/trace repair mutations in deterministic services; runtime final artifacts remain admitted append-only binding/gap candidate proposals with structured challenge checks. |
| `motive_decomposition` | `motive_decomposition.draft_assertion_candidates` runtime route plus T-094 motive versions/assertions authority | promoted | Remaining work is broader provider rollout and later human/domain-gate acceptance flow; current slot is draft-only and has no motive/assertion/board/trace/queue/Domain Gate write side effect | Keep semantic-change and human-confirmation authority outside runtime; admitted final artifacts remain draft assertion candidate proposals only. |
| `motive_evolution` | `motive_evolution.evolution_decision_support` runtime route plus T-094 motive evolution and portfolio authority boundary | promoted | Remaining work is deterministic/human-domain-gate rollout; current slot is decision-support only and has no motive/portfolio/board/trace/queue/Domain Gate write side effect | Keep motive evolution and portfolio mutation in deterministic T-094 services plus human/domain gate; admitted final artifacts remain challenged decision-support packets only. |
| `feasibility_planning` | `feasibility_planning.probe_plan_candidates` runtime route plus T-095 feasibility probe/plan-light authority | promoted | Remaining work is broader provider rollout; first slice proposes probe/plan candidates only | Keep feasibility probe and plan-light creation in deterministic service; runtime cannot create probes, plans, cycles, queues, WorkOrders, or live-adapter payloads. |

## Deterministic / Non-LLM Flow Nodes
| Flow node | Current surface | Classification | Production gaps | Recommended action |
|---|---|---|---|---|
| Intake bootstrap | T-093 routes/services; T-101 replay | deterministic-only | Closed in runtime-stress step `03-deterministic-lane-regression` for route replay, duplicate bootstrap idempotency, and stale bridge hash no-mutation rejection. | Keep deterministic; do not add LLM/runtime slot semantics. |
| Trace manifest creation | T-097 service | deterministic-only | Closed in runtime-stress step `03-deterministic-lane-regression` for complete/stale/immutable-id regression. | Keep deterministic; runtime slots consume trace refs and fail closed on drift. |
| WorkOrder admission | T-096 service | experiment-runtime | Closed in runtime-stress step `03-deterministic-lane-regression` for draft creation from admitted refs, stale trace rejection, admission replay/drift, harness submission replay/drift, and target-specific run-evidence trace identity. | Keep WorkOrder creation/admission/submission outside LLM runtime; experiment-design runtime artifacts remain proposals only. |
| Live experiment submit/sync/collect/cancel | T-104 adapter | experiment-runtime | Closed in runtime-stress steps `05-live-experiment-adapter-operational-regression` and `06-live-experiment-adapter-ownership-scan`; default path remains deterministic/fake with live/cloud execution opt-in only. | Keep live adapter env-gated and outside runtime/admission/harness semantics. |
| Result/claim/dossier authority writes | T-098 service | deterministic authority | Closed in runtime-stress steps `01-runtime-service-and-route-regression` and `02-domain-gate-writer-ownership-scan` for allowlisted Domain Gate materialization, replay/idempotency, drift rejection, blocked/failed rejection, and writer-surface ownership. | Keep Domain Gate limited to admitted claim/dossier/result-analysis final artifacts. |
| Decision work queue | Existing harness-backed blocker queue surface; future T-099/T-100 domain queue extraction remains separate | deterministic authority surface | Closed in runtime-stress step `04-decision-work-queue-regression` for blocker dedup, bounded retry/cooldown defaults, recurrent-blocker reopen, terminal resolution replay/drift in service and Prisma paths, and runtime admission no-queue-payload rejection. | Keep current queue path as harness blocker validation surface; any dedicated domain queue service must be a separate ownership decision to avoid dual-track queue creation. |
| Provider variance evaluation | T-105 evidence/preflight lane | deterministic evaluation-only | Closed in runtime-stress steps `07-provider-variance-evaluation-regression` and `08-provider-variance-evaluation-ownership-scan` for deterministic fake replay, live preflight skip/block without execution, schema/trace/authority/handoff guardrails, route delegation, evaluation-only refs, and no runtime/admission/Domain Gate/live entrypoint scan. | Keep as evidence/preflight only; do not count it as product runtime provider canary or Domain Gate eligibility. |

## Promoted Slot Exit Criteria
- Promoted slots through `motive_evolution` have:
  - shared contract coverage;
  - runtime service coverage;
  - admission identity/hash recomputation;
  - shared runtime/admission repository support;
  - provider canary hooks or explicit opt-in provider coverage where the route is enabled;
  - compression/adversarial fixtures;
  - no-authority-write and no-cache-bypass assertions.
- Proposal-only promoted slots use explicit no-Domain-Gate/no-authority evidence instead of Domain Gate materialization.
- Deterministic and operational lanes now have required-case runtime-stress coverage without becoming LLM runtime slots.
