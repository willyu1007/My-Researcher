# 04 Verification

## 2026-05-17
- `node .ai/scripts/ctl-project-governance.mjs sync --apply --project main`
  - Passed; created/updated project derived registry views.
- `node .ai/scripts/ctl-project-governance.mjs lint --check --project main`
  - Passed; existing unrelated warning remains for T-073 done status without acceptance criteria checkboxes.
- `node .ai/scripts/ctl-project-governance.mjs map --project main --task T-079 --milestone M-001 --feature F-001 --requirement R-009 --apply`
  - Passed; registry now maps T-079 to `M-001 > F-001 > R-009`.
- `node .ai/scripts/ctl-project-governance.mjs sync --apply --project main && node .ai/scripts/ctl-project-governance.mjs lint --check --project main`
  - Passed after final task status/mapping update.
- `pnpm --filter @paper-engineering-assistant/backend run prisma:format`
  - Passed.
- `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/my_researcher pnpm --filter @paper-engineering-assistant/backend run prisma:validate`
  - Passed.
  - Note: first validate attempt without `DATABASE_URL` failed at Prisma config loading; rerun used a disposable validation URL and did not apply migrations.
- `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/my_researcher pnpm --filter @paper-engineering-assistant/backend run prisma:generate`
  - Passed.
- `pnpm --filter @paper-engineering-assistant/shared test`
  - Passed, 73 tests.
- `pnpm --filter @paper-engineering-assistant/shared run typecheck`
  - Passed.
- `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/my_researcher pnpm --filter @paper-engineering-assistant/backend run typecheck`
  - Passed.
- `node --test --loader ts-node/esm src/services/topic-selection-resource-sampling-service.unit.test.ts src/routes/topic-selection-resource-sampling-routes.integration.test.ts`
  - Passed, 7 tests.
- `pnpm --filter @paper-engineering-assistant/backend test`
  - Attempted.
  - T-079-related tests passed inside the full run.
  - Full run did not complete green because existing Prisma HTTP smoke tests require a real `DATABASE_URL`; the env-gated T-054 and T-067 Prisma HTTP smoke tests assert `DATABASE_URL is required`.
- `.ai/.tmp/topic-selection-real-flow.mjs`
  - Updated to call `POST /topic-selection/v1a/resource-samples` before creating the title card and attaching selected literature.
  - Added deterministic mock LLM mode via `TOPIC_SELECTION_REAL_FLOW_MOCK_LLM=1` for local E2E runs where the operator acts as the LLM provider.

## 2026-05-17 Live DB + E2E Closure
- `node --env-file=.env.local ... prisma migrate status --schema prisma/schema.prisma`
  - Pre-apply status showed exactly one pending migration: `20260517120000_add_topic_selection_resource_sampling`.
- `node --env-file=.env.local ... prisma migrate deploy --schema prisma/schema.prisma`
  - Passed; applied `20260517120000_add_topic_selection_resource_sampling` to local Postgres schema `my_researcher_dev`.
- `node --env-file=.env.local ... prisma migrate status --schema prisma/schema.prisma`
  - Passed; database schema is up to date.
- `node .ai/scripts/ctl-db-ssot.mjs sync-to-context`
  - Passed; refreshed `docs/context/db/schema.json`.
- `TOPIC_SELECTION_REAL_FLOW_MOCK_LLM=1 ... node --env-file=.env.local --loader ./apps/backend/node_modules/ts-node/esm.mjs .ai/.tmp/topic-selection-real-flow.mjs`
  - Passed.
  - Run id: `real-flow-1779013637735-7ff31f`.
  - Artifact dir: `.ai/.tmp/topic-selection-real-flow/real-flow-1779013637735-7ff31f/`.
  - Resource sample set: `resource_sample_set_0bbb6a8c-9074-4469-8d76-d40af0d3f799`.
  - Sample status: `ready_with_warning`; warnings: `CONTEXT_CAP_APPLIED`.
  - Selected 16 real literature records with role counts support=4, challenge=4, baseline=4, context=4.
  - Drove v1a -> v1b -> v1c through paper-project bridge: `paper_project_bridge_fe282d7c-a343-42a7-bb8d-f7d684e5d4c2`.
  - Verified `answerable_with_risk` path created accepted risk `accepted_risk_eb0709a5-2913-42fa-837c-097ea6cf66d1` and carried it through value assessment/package input.
- `DATABASE_URL=dummy node --test --loader ts-node/esm src/services/topic-selection-resource-sampling-service.unit.test.ts src/routes/topic-selection-resource-sampling-routes.integration.test.ts`
  - Passed, 7 tests.
- `node --env-file=.env.local ... pnpm --filter @paper-engineering-assistant/shared test`
  - Passed, 73 tests.
- `node --env-file=.env.local ... pnpm --filter @paper-engineering-assistant/shared run typecheck`
  - Passed.
- `node --env-file=.env.local ... pnpm --filter @paper-engineering-assistant/backend run typecheck`
  - Passed.

## Coverage Notes
- Service tests cover role-balanced sampling, mocked LLM classification, support/challenge/baseline/context/review/excluded outcomes, risk-heavy-not-support guardrail, topic drift exclusion, fine-tuning undercoverage warning, deterministic sample hash, and LLM failure blocked status.
- Route tests cover create/read behavior and stable `INVALID_PAYLOAD` malformed payload handling.
- Persistence tests cover Prisma repository round-trip through a fake Prisma client plus migration SQL declarations for the three new tables.
- Live local DB migration, schema-context refresh, and deterministic mock-LLM real-flow E2E are now complete.
- Remaining explicit external-provider gate: rerun the same harness with `TOPIC_SELECTION_REAL_FLOW_MOCK_LLM=0` when cost/latency/provider behavior itself needs acceptance.

## 2026-05-17 Deep Quality Test 1: Provider LLM Real-Flow
- Scope: `resource sample -> v1a -> v1b -> v1c -> paper-project bridge` with real provider LLM (`TOPIC_SELECTION_REAL_FLOW_MOCK_LLM=0`) and live Prisma repositories.
- First provider run: `real-llm-quality-20260517203320`
  - Failed at TopicQuestion formation.
  - Root cause: provider output copied an inherited `research_slice_boundary_*` ref into `trigger_source_refs` with the `research_slice_` prefix, causing source-ref drift.
  - Fix: normalize known inherited source refs in `falsification_conditions[*].trigger_source_refs` when ref_type and id suffix match a canonical upstream ref; unknown refs still fail.
  - Regression: `falsification source refs normalize copied boundary ids with missing boundary prefix`.
- Second provider run: `real-llm-quality-20260517203721`
  - Passed full chain.
  - Quality review found one weak context resource selected with `topic_relevance=0.55`.
  - Fix: target roles now require `topic_relevance >= 0.6`; lower relevance target-role candidates are moved to `review` with guardrail `TARGET_ROLE_RELEVANCE_TOO_LOW`.
  - Regression: `resource sampling keeps low-relevance target-role candidates out of the selected sample`.
- Third provider run: `real-llm-quality-20260517204207`
  - Failed at TopicQuestion formation.
  - Root cause: provider output copied a valid `evidence_unit_*` ref with a drifted `title_card_id`.
  - Fix: normalize known inherited evidence refs by canonical `ref_type + ref_id`; wrong ref_type remains blocking.
  - Regression: `known evidence refs with drifted title_card_id are normalized to inherited refs`.
- Final provider run: `real-llm-quality-20260517204717`
  - Passed full chain.
  - Artifact dir: `.ai/.tmp/topic-selection-real-flow/real-llm-quality-20260517204717/`.
  - Sample set: `resource_sample_set_d4697ca6-6168-4688-9fde-836785bb45cd`.
  - Sample status: `ready`; warnings: none.
  - Selected role counts: support=4, challenge=4, baseline=4, context=4.
  - Minimum selected topic relevance: 0.60.
  - Accepted risk carried forward: `accepted_risk_1fb3be4d-4d82-412e-a7ac-ced8123f6a70`.
  - Bridge created: `paper_project_bridge_ead2a490-2281-43bd-a930-7d8767b65818`.
- Verification after fixes:
  - `DATABASE_URL=dummy node --test --loader ts-node/esm src/services/topic-selection-v1b-topic-question-service.unit.test.ts`
    - Passed, 28 tests.
  - `DATABASE_URL=dummy node --test --loader ts-node/esm src/services/topic-selection-resource-sampling-service.unit.test.ts src/routes/topic-selection-resource-sampling-routes.integration.test.ts`
    - Passed, 8 tests.
  - `pnpm --filter @paper-engineering-assistant/backend run typecheck`
    - Passed.
  - `node .ai/scripts/ctl-project-governance.mjs lint --check --project main`
    - Passed.

## 2026-05-17 Deep Quality Test 2: Resource Sampling Stability
- Scope: repeated real-provider `POST /topic-selection/v1a/resource-samples` runs against the live topic `ai-rag-finetuning-2022-2026`, sample size 16, same seed/model/policy, live Prisma repositories.
- Harness: `.ai/.tmp/topic-selection-sampling-quality.mjs`.
- Initial run: `sampling-quality-20260517211533`
  - Failed 2/3 runs with `LLM request timed out`.
  - Root cause: one structured-output request classified 102 eligible candidates at once; successful run used ~46k input tokens and ~15k output tokens.
  - Fix: classify candidates in batches of 24 and aggregate telemetry/output.
- Post-batching run: `sampling-quality-batched-20260517212719`
  - Passed provider reliability: 3/3 runs succeeded, no blocked sample sets, 5 LLM requests per run, no timeout.
  - Failed semantic stability: sample hashes differed and selected/candidate role drift remained.
- Guardrail hardening runs:
  - `sampling-quality-stable-20260517213714`: context underfilled after over-aggressive canonicalization.
  - `sampling-quality-stable2-20260517214209`: context restored, baseline underfilled; risk heuristic too broad.
  - `sampling-quality-stable3-20260517214720`: role counts full; selected role drift remained around weak-topic agentic resources.
  - `sampling-quality-stable4-20260517215203`: role counts full; selected role drift reduced to context/support edge cases.
  - `sampling-quality-stable5-20260517215659`: selected role drift removed in some runs but sample hash still unstable due context candidate swaps.
  - `sampling-quality-stable6-20260517220133`: selected role drift removed; sample hash still unstable from context set replacement.
  - `sampling-quality-stable7-20260517220545`: latest strict run.
    - Chain reliability passed: 3/3 sample sets succeeded; no blocked sets.
    - Role balance passed: each run selected support=4, challenge=4, baseline=4, context=4.
    - Hard semantic flags passed: no risk-heavy support, no weak baseline/challenge, no selected item below 0.70 relevance.
    - Residual failure: `sample_hash_stable=false`; three different comparable hashes.
    - Selected role drift: none.
    - Remaining instability is selected-set drift in context candidates, not role-count or hard semantic failure.
- Verification after latest code changes:
  - `DATABASE_URL=dummy node --test --loader ts-node/esm src/services/topic-selection-resource-sampling-service.unit.test.ts src/routes/topic-selection-resource-sampling-routes.integration.test.ts`
    - Passed, 9 tests.
  - `pnpm --filter @paper-engineering-assistant/backend typecheck`
    - Passed.
- Acceptance status for this test point: not fully closed. Product behavior is much stronger, but strict real-provider deterministic sample hash remains open.

## 2026-05-17 Deep Quality Test 3: Deterministic Pre-Classification Closure
- Fix scope:
  - Source-only deterministic pre-classification now drives selected roles and selected-set identity.
  - Per-role deterministic relevance floors prevent LLM relevance jitter from moving otherwise deterministic source-role candidates between target roles and `review`.
  - Benchmark-first resources are preserved as `baseline` unless they carry strong adversarial/security/failure markers; this moved ASTRA-QA out of `challenge` and into `baseline`.
- Regression verification:
  - `DATABASE_URL=dummy node --test --loader ts-node/esm src/services/topic-selection-resource-sampling-service.unit.test.ts src/routes/topic-selection-resource-sampling-routes.integration.test.ts`
    - Passed, 11 tests.
  - `pnpm --filter @paper-engineering-assistant/backend typecheck`
    - Passed.
- Real provider stability run: `sampling-quality-deterministic-20260517225835`
  - Status: `passed_with_warnings`.
  - Sample hash stable across 3/3 comparable runs.
  - Hard failures: none.
  - Selected role drift: none.
  - Candidate role drift: none.
  - Role counts every run: support=4, challenge=4, baseline=4, context=4.
  - Minimum selected relevance every run: 0.84.
  - Only soft warning: `CONTEXT_CAP_APPLIED`, expected because more than four context candidates are eligible and the policy caps context at target.
- Real provider risk/baseline tightened run: `sampling-quality-risk-tightened-20260517230318`
  - Status: `passed_with_warnings`.
  - Sample hash stable across 3/3 comparable runs: `9f4240b15c7126219dd6770695af35367e68150777b52995880842c2b075dbd3`.
  - Hard failures: none.
  - Selected role drift: none.
  - Role counts every run: support=4, challenge=4, baseline=4, context=4.
  - Minimum selected relevance every run: 0.84.
  - ASTRA-QA is selected as `baseline`; challenge slot now selects explicit risk/failure resources including visual retrieval aggregation failure, adversarial SQL injection, knowledge-base poisoning, and persistent-memory attacks.
  - Remaining soft warning: `CONTEXT_CAP_APPLIED`, accepted as policy behavior rather than quality failure.
- Acceptance status for resource sampling stability and quality baseline: closed for selected-set determinism, role balance, relevance floor, and hard semantic flags. Candidate role drift remains observable for a non-selected weak-topic candidate (`LIT-0024`) when provider output marks it excluded, but it does not affect selected sample identity or hard quality gates.

## 2026-05-19 Cleanup Note
- Legacy temporary harnesses and run artifacts under `.ai/.tmp/topic-selection-real-flow*` and `.ai/.tmp/topic-selection-sampling-quality*` were removed during workspace cleanup.
- Historical commands above are retained as verification evidence; new real-flow execution should use the durable runner `.ai/scripts/topic-selection-real-e2e.mjs` or `pnpm topic-selection:real-e2e`.

## 2026-05-24 Underfilled Resource Pool Regression
- Update: added service coverage for partially usable resource pools where selected resources exist but role targets and sample size cannot be fully satisfied.
- Command: `pnpm --dir apps/backend exec node --test --loader ts-node/esm src/services/topic-selection-resource-sampling-service.unit.test.ts`
  - Result: passed; 13 tests passed.
- Command: `pnpm --dir apps/backend exec node --test --loader ts-node/esm src/services/topic-selection-resource-sampling-service.unit.test.ts src/routes/topic-selection-resource-sampling-routes.integration.test.ts src/services/topic-selection-need-discovery-debate-loop-service.unit.test.ts src/services/topic-selection-agent-orchestrator-service.unit.test.ts`
  - Result: passed; 31 tests passed.
- Command: `pnpm --filter @paper-engineering-assistant/backend typecheck`
  - Result: passed.
- Command: `git diff --check`
  - Result: passed.
- Coverage:
  - underfilled baseline/context role targets produce `ROLE_TARGET_UNDERFILLED_*` warnings;
  - underfilled overall sample size produces `SAMPLE_SIZE_UNDERFILLED`;
  - a partial but non-empty eligible pool remains `ready_with_warning`, not `blocked`;
  - no fallback role invention is required to satisfy the requested sample size.
