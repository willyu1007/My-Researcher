# Research Argument Graph And State V1 Roadmap

## Decision
- Child task: `T-025 research-argument-graph-and-state-v1`
- Mapping: `M-001 > F-001 > R-011`
- Primary goal: 建立 research-argument runtime V1 基座，包括扩展后的关键对象图、9 维抽象状态、state recompute、decision / lesson log、report projection 和基础 read models。

## Phases
1. Domain schema and persistence plan
2. Repository / service baseline
3. State synthesis and recompute
4. Read models and verification

## Explicit defaults
- V1 仅做人工 / 规则驱动 `StateSynthesizer`。
- V1 先不做 planner bundle ranking、CriticHub 或学习型 memory。
- V1 明确拥有 `Baseline / Protocol / ReproItem / Run / Artifact / Boundary / IssueFinding / ReportProjection`。
