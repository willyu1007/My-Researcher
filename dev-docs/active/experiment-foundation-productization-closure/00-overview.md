# T-132 Experiment Foundation Productization Closure

## Status
- State: in-progress
- Task ID: `T-132`
- Mapping: `M-001 > F-001 > R-012 > T-132`
- Next step: **M5-A3 closure preparation, available-actions enumeration, and closure body/path compat easing are implemented and locally verified**. Run the guarded A1/closure relational cases on an authorized disposable PostgreSQL host, then continue with M5-A4 convergence-gate work. OD-M5-2 remains revised to no projection/no schema in M5-A. M7-L1/L2 remain separate authorization boundaries; M5-A3 did not enable a capability, apply a migration, add persistence, or perform a write.

## M7 default-off implementation — 2026-07-23

- 新增六张 typed ExecutionBundle identity/draft/revision/lifecycle/readiness 表；draft 使用 expected-version CAS，freeze 为 server-hashed immutable revision，readiness 只能按 exact revision/hash 解析。
- PI WorkOrder revision v2 绑定一个 exact ExecutionBundle。EF T2 物化 executable RunRecipe v2 与两个 ordered TaskSpec v2，T3/T4 仍复用唯一 PI head CAS 与 EF final inbox acknowledgement，不产生第二 saga 或 manifest authority。
- 既有六张 Pack B provider-control 表以 exact discriminated tuple 同时接受 simulation/fake 与 `real_provider`，simulation history 不 backfill、不 trust-upgrade。simulation worker 与 real worker 分别按 execution mode claim，避免跨 lane 抢占。
- real-provider intake 与 control drain 使用两个独立 default-false capability；关闭 intake 不会截断已提交 command。App composition 只接受显式注入的 official-SDK transport/profile/bundle resolver，不构造 live client。
- official-SDK adapter 对 `CreateJob/GetJob/ListJobs/StopJob` 使用 bounded、no-autoretry 调用；accepted-response loss 只能 exact discovery，不能盲目再次 `CreateJob`。collect 校验 exact object name、canonical envelope、bundle/Run/cell/TaskSpec/parser binding，且产物仍为 diagnostic-only provisional output。
- M7 gate 只使用注入式 SDK fake 和 digest-pinned disposable PostgreSQL；现有/named database、真实 provider、OSS、科学结果与证据写入均保持 0。
- 最终 run `t132-m7-offline-20260723-v1` 通过 M7-01..M7-15：shared 10/10、backend 88/88、强制 disposable PostgreSQL 9/9 且 skip=0；summary SHA-256 为 `7bccf0b8bedd041f65374ce0e6ccff3cc26be662a008c1ff6951a57f71743679`。

## Aliyun public-resource preflight spine — 2026-07-22

- Shared/backend cloud-preflight contract 已升级为 v2 判别联合：`exact_quota` 必须携带 exact `resource_id`；`public_resource` 必须完全不携带该字段。历史 v1 schema 仅保留为既有 evidence compatibility，不再进入 runner。
- Public-resource `CreateJob` 离线 payload、canonical bytes 和官方 SDK wire map 均省略 `ResourceId`。Redacted manifest 显式记录 `resource_mode=public_resource`、`resource_id_hash=null`，且 redacted field census 中不存在虚假的 `ResourceId`。
- Read-only transport 仍严格限制为 `GetWorkspace → ListResources → ListEcsSpecs`。Public mode 只做一次 workspace resource enumeration 来证明 API/policy 可达，不把公开按量资源伪装成 quota，也不要求 `ListResources` 返回 quota row；CPU spec 可见性仍为必须条件。
- Env contract 新增 default=`exact_quota` 的 `EXPERIMENT_FOUNDATION_V2_ALIYUN_PREFLIGHT_RESOURCE_MODE`。未知值、public mode 携带 resource ID、profile/manifest/payload mode drift 均 fail closed；cloud capability 仍默认关闭。
- 已登录 PAI 控制台的只读检查确认 `cn-shanghai` / workspace `1450165` 的通用计算 quota 为 0；DLC 新建表单切换到“通用计算”后由产品界面选中“公共资源”。正式离线镜像引用冻结为控制台可选的 CPU 官方镜像 `torcheasyrec:1.3.0-pytorch2.12.1-cpu-py311-ubuntu22.04`，表单未提交、任务数仍为 0。
- 使用该正式镜像引用的本地 runner `cloud-preflight-public-resource-offline-20260722-r2` 在 capability=false 且零 provider operation 下按预期返回 `blocked`；CP01-05、CP10-12 passed，CP06-09 等待临时 STS 与 reviewed external policy evidence。两个 payload 均为 manifest v2、`resource_id_hash=null`，payload hashes 为 `sha256:d460d32eb8225141959b6d2744174f9576bef4d8898c988327bee28b2c052989` 与 `sha256:45e32261a38aec77142ebfe967c75e5126f28713f9e7071569379c8d363bf1d2`。88 张 protected tables unchanged，cloud/CreateJob/database/scientific write 均为 0；summary SHA-256 为 `41c32e433e0ab6f8b51eb31e8c429a85156b7bbf14966ffdb66c260db763f834`。该 run 不构成真实 image-pull 或 provider acceptance。
- 真实只读 r3 使用仅含两个 documented AIWorkspace actions 的 STS：`GetWorkspace`、`ListResources` succeeded，`ListEcsSpecs` failed；三次均为 read，CreateJob/provider/database/scientific writes 全为 0。官方 API 页面仍未披露该接口的 RAM action，因此 `paidlc:ListEcsSpecs` 是由该单变量实测支持的最小推定，不是官方文档声明。
- r4 代码与测试已把该推定 action 加入 exact session-policy/evidence allowlist，继续显式 deny `paidlc:CreateJob`，并将 SDK 异常稳定归类为 `ALIYUN_READ_ONLY_PROVIDER_CALL_FAILED` 且不保留 provider diagnostics。第一次 r4 harness 因 raw STS expiration 未转换为项目要求的毫秒 UTC 形式，以 `ALIYUN_IDENTITY_POLICY_EVIDENCE_INVALID` 在 provider transport 前 blocked；provider operations/writes、CreateJob、database/scientific writes 均为 0，临时凭证目录已删除。该本地 precondition failure 不构成真实 read-only acceptance。
- 授权后的 r5 在写 evidence 前将 STS expiration canonicalize 为 `.sssZ`，并用生产 parser 自检；identity-policy evidence 与 independent digest 均通过。真实 ledger 仍为 `GetWorkspace succeeded → ListResources succeeded → ListEcsSpecs failed`，稳定 reason 为 `ALIYUN_READ_ONLY_PROVIDER_CALL_FAILED`。这说明增加 session-policy action 并不足以取得第三个 read；由于 STS session policy 只能缩小、不能扩大被扮演角色的既有权限，下一诊断边界是 `cloud-0001` role identity policy/effective permission，而非继续扩张临时 policy。
- 2026-07-23 只读 RAM 控制台审查确认 `cloud-0001`（role ID `300275336508927114`，ARN `acs:ram::1183869713036194:role/cloud-0001`）绑定 10 个账号级系统策略，其中包括 `AdministratorAccess`；捕获的策略清单没有自定义策略或单独的 deny 策略。`AdministratorAccess` 已提供 `Action=*`、`Resource=*` 的 role identity Allow，所以“角色缺少 `paidlc:ListEcsSpecs` Allow”被证伪，最小 IAM diff 为零。该证据不把 r5 解释为成功：推定 action 仍未获官方文档支持，实际阻断点可能是 action 名称、接口鉴权模型或更高层服务边界。
- Gate-1 单变量诊断证明同一 STS 下最小 `ECS` request 和增加 `AcceleratorType=CPU` 均为 HTTP 200；仅增加 `SortBy=CPU` 后稳定返回 HTTP 400 `BadRequest`。控制台白屏是前端页面加载故障，刷新恢复，与该 API 参数拒绝没有因果证据。
- 修复后 DLC 分页独立使用 provider 已验证的 page size 10，不再发送 `SortBy`/`Order`；安全失败证据仅保留格式受限的 status/code/RequestId，原始 SDK diagnostics 仍不会进入 summary。r6 完成 1 次 `GetWorkspace`、1 次 `ListResources`、11 次 `ListEcsSpecs`，全部 succeeded；CP01-CP12 全绿，summary SHA-256 为 `ae524752ef64f658ddfb796e8c0834bf0903baadf1c8e79cfbc392887c516053`。

## Zero-write Aliyun cloud-preflight implementation — 2026-07-18

- 新增独立 shared v2 contracts、exact Aliyun PAI-DLC `CreateJob` payload materializer、same-payload fake lifecycle、official SDK read-only transport/service 和机器 gate。产品代码没有 `CreateJob` 调用；provider operation allowlist 精确为 `AIWorkspace.GetWorkspace`、`AIWorkspace.ListResources`、`PaiDlc.ListEcsSpecs`，任何 write operation 在 transport 前 fail closed。
- 全 payload 只在进程内 canonicalize/hash，最大 65,536 bytes；durable output 只允许 exact payload hash、byte size、execution-profile hash 与 hashed/redacted refs。caller expansion、TaskSpec/RunCell drift、payload oversize、policy drift/expiry、incomplete STS 和 unavailable workspace 均有负例。
- 新增 default-false `EXPERIMENT_FOUNDATION_V2_ALIYUN_CLOUD_PREFLIGHT_ENABLED` 及 development-scoped profile/temporary-STS/policy-evidence path + independent SHA-256 env contract。Evidence 必须通过 exact reviewer/24-hour lifetime、realpath/non-symlink/inode/permission/digest fences；没有写入 secret value、没有注册远程 cloud target、没有打开 capability。
- named-local 解析、exact Run prerequisite 和 88 张 protected table before/after digest 现在位于同一 `REPEATABLE READ` 且 server-enforced `transaction_read_only=on` 的事务。Official SDK adapter 以 no-network injected clients 验证 exact request/response mapping 和多页 resource/spec 遍历；Pack A/B/cloud 三条 runner 共用同一 evidence helper。
- 最终本地 runner `cloud-preflight-local-20260718-r9` 正确返回 `blocked`：CP01/04/05/11/12 passed；CP02/03/10 等待 exact profile，CP06-09 等待 capability、temporary STS、repo-external reviewed policy evidence 和 independent digest。88 张 protected tables `changed_tables=[]`，provider/CreateJob/database/scientific write 均为 0，scientific status 仍为 `not_started`、evidence eligibility=false。
- r9 summary SHA-256 为 `77f8f9973f2237e706216c894d55ff44657c6bede27fd32e42c0c6e09a3b07ea`。Backend full suite 2,247 tests：2,197 passed、0 failed、50 conditional skips；Fastify 5.10.0 升级后 production audit 为 `No known vulnerabilities found`。Durable handoff: `artifacts/cloud-preflight-implementation-20260718/00-implementation-closure.md`。该 checkpoint 证明 fail-closed implementation，不宣称 `cloud_preflight_passed`、真实签名成功、cloud workspace/resource 可用或训练运行时有效。

## Formal PI scope → Pack B product execution — 2026-07-15

- `run-experiment-foundation-packb-product-landing.ts` 在 E1 前锁定 named-local target fingerprint、Pack A 最终只读证据、exact Run/head acknowledgement、Prisma composition 和 live exact readiness；任何 foreign Run/business-key lineage 均 fail closed。
- simulation intake 只通过 `env-localctl` 打开一次。正常产品 POST route 为 exact two-cell Run 写入 2 ProviderPayload、2 ExecutionAttempt、12 AttemptEvent、8 ProviderCommand、2 CollectionAttempt 和 2 diagnostic-only ProvisionalOutput；生产 Prisma worker 以 deterministic fake adapter 完成 E1-E5，8 次 operation、0 fetch、0 real provider/`CreateJob`。
- 最终只读 verifier `formal-pi-scope-packb-product-20260715-verify-r2` 在 simulation 已关闭后通过：2/2 cells 均 `succeeded` + `collected`，`workflow_simulation_passed`，但 `scientific_execution_status=not_started`、`evidence_eligibility=false`。
- verifier 对所有 `PaperImplementation*` authority、产品 source、Pack A、legacy 与 scientific sentinels 共 88 张受保护表逐表 digest，`changed_tables=[]`；PI、Pack A、legacy、科学结果/验证/证据写入均为 0。
- 最终配置保持 cutover=`true`、Pack A admission=`false`、Pack B simulation=`false`、auto-pull scheduler=`false`。Durable evidence: `artifacts/product-pack-b-local-20260715/05-product-execution-closure.md`；最终 JSON SHA-256 `7cc6044bc3822e4197f99638b09b7a4f9e90640bb205cde929f98df2b998e9c7`。

## Formal PI scope → Pack A product landing — 2026-07-15

- 正式来源是 active PaperProject `P313` 及其 exact active bridge `paper_project_bridge_d956e811-7042-4f26-9f28-afba3d5258fa`。产品 route 依次建立 ImplementationProject、admitted CoreMotive、trace-ready minimal evidence board/binding、admitted ValidationCycle，再调用独立 v2 admission route；没有 fixture scope、直接 repository bootstrap 或 legacy fallback。
- PI scope guard 现在同时要求 ImplementationProject 为 `active`、ValidationCycle 为 `admitted`。不满足时以稳定 `BRANCH_SCOPE_CONFLICT` fail closed，且 admission/outbox/legacy writer 均零写。
- 四事务/三事件已在 named-local PostgreSQL 完整 drain：1 WorkOrder revision、2 ordered cells、1 VersionLock、1 RunRecipe、2 TrainingTaskSpecs、1 Run/2 RunCells、1 PI branch head，以及 final EF inbox 中唯一 durable acknowledgement。
- 产品终态配置为 `PAPER_IMPLEMENTATION_EXPERIMENT_V2_CUTOVER_COMMITTED=true`、`PAPER_IMPLEMENTATION_EXPERIMENT_V2_ADMISSION_ENABLED=false`、`EXPERIMENT_FOUNDATION_V2_WORKFLOW_SIMULATION_ENABLED=false`。cutover 保留，新的 Pack A intake 已关闭；已提交 saga 已完成，不存在待 drain 事件。
- 最终只读 verifier `formal-pi-scope-packa-product-20260715-verify-r5` 为 `passed`。legacy 257 行摘要、既有 4 行科学哨兵逐行 digest 不变，六张 Pack B 表合计 0 行；未创建 Attempt/provider/scientific result/evidence/closure，也没有网络或 provider 请求。
- Durable evidence: `artifacts/product-pack-a-local-20260715/05-product-landing-closure.md`；最终 JSON SHA-256 `341eba9ae1e38ce282947d9d9554102f04ff5c93fa7cb5564a7152b0c245ee48`。

## Pack A/Pack B deep-cleanup closure — 2026-07-15

- 在既有 placeholder cleanup 之后新增 migration `20260714210000_normalize_experiment_v2_event_payloads`，SHA-256 `37eed54494aa2dc246fe7d5a9b2de2a027474b416ada8a5f18ab4bc194a65f3a`。四张 PI/EF integration inbox/outbox 现在只保存 event payload JSON；typed envelope 由结构列重建，并分别验证 payload hash 与完整 envelope hash。migration 同时把 38 条 Pack A 同域 FK 固定为 `ON DELETE RESTRICT ON UPDATE RESTRICT`，为 9 个固定 v1 schema-version 字段增加 DB CHECK；repository read fence 对历史异常值继续 fail closed。
- T2 materialization 在同一 commit transaction、任何写入前，以 batched `FOR SHARE` 重新 fence exact attestation、ordered dependency manifest、23 个 lifecycle projections 和 Dataset location；typed family key 与 `logical_id` 独立，family key 不可改名且同 family 冲突 fail closed。VersionLock 只保留 relational exact dependencies 与 server-derived canonical hash，不再保存重复 snapshot JSON。
- E1 使用 batched conflict lookup、`createMany` 和 latest-Attempt `groupBy`，readiness graph 使用 transaction-local cache，in-memory latest Attempt 为 O(N)；48-cell relational query-shape 已进入强制 PostgreSQL acceptance。所有落到 PostgreSQL `Int` 的外部输入与内部增量路径都使用 `-2147483648..2147483647` fence：除 seed/repeat/run-policy 外，还覆盖 revision/lifecycle/state/head/relay/lease/attempt counters，达到上界时在 commit/dispatch 前 fail closed 且零 partial write。Persisted provider payload 的 `redacted_manifest` 每次 replay、scope resolution 与 dispatch 前均经 exact typed v1 parser，而不是把未知 JSON 重新类型断言为可信记录。
- 最终 source-backed disposable D-19 run `d19-deep-cleanup-final-20260715-r19` 为 `passed`、`blockers=[]`，source policy、A01-A04/B01-B10 全部通过；standalone real-PostgreSQL relational 6/6、skip=0，reset 前后 marker assertions 均通过，container cleanup=true。summary SHA-256 为 `9961eec956d216c65d1ac24be57214c05680dd7c1ae6d8ea510c8dbcef73a647`；exact attestation digest 仍为 `sha256:48dd3546bcf314c478f80a5bc6ba5bcc0ecc57b848ac0d83fd7d5c9b8ac3bb6e`。r13 因调用方传入已废弃的 attestation 路径而正确 `blocked`，但 A/B 检查自身通过且 container cleanup 成功；该 run 是 fail-closed invocation 负例，不是产品失败。
- disposable Pack B run `packb-deep-cleanup-final-20260715-r16` 为 `passed`、PB01-PB16 全绿；shared targeted 6/6、backend targeted 89/89、embedded Pack A PostgreSQL 6/6、Pack B PostgreSQL 7/7，全部 skip=0，reset marker 与 container cleanup 均通过。summary SHA-256 为 `207450f7104b24542574f883ea2e851425e11412c03f21e65413444d3c2bfd6d`。named-local read-only gate/app smoke 仍为已验证的 `packb-deep-cleanup-final-local-20260714-r18` source；本地 migration history 62/62，40 张 approved v2 tables exact，238 张 application tables before/after `changed=0`，external fetch=0，provider-command delta=0，六张 Pack B 表均为 0 行。
- authority read closure 现在要求 exact evidence keysets、显式 frozen event/command/provider-control hash profiles，以及 typed/canonical/hash/relational-mirror/order/enum allowlist 全量一致。ProviderCommand 必须在 read、claim、heartbeat、release、outcome 和 collection 全路径绑定 exact Attempt provider payload id/hash；cancel reason semantics 与 Attempt/command state 必须一致。任何 snapshot、payload identity 或 reason drift 都在写入/transport 前 fail closed，tamper negatives 证明零 write/transport。
- 深度代码清理删除 14 个经全仓消费扫描证明为 zero-consumer 的 shared row schemas 及其随之失去消费者的 helper，只保留 interfaces、request/event/error schemas 与直接消费的 IO schemas。D-19/Pack B 共用一个 database identity validator 与 marker assertion 实现；source-policy reviewed digest 也只有一个 portable constant，slots 为 frozen ordered values。gate meta tests 70/70、backend disposable PostgreSQL identity/guard tests 10/10 且 skip=0，shared full 330/330。OpenAPI 中 22 个 T-132 persisted integer fields 全部显式为 `int32` 并带上界，其中两个 seed fields 也带 signed lower bound；drift test 和重建后的 API index 固定该人口。
- 210000 migration 的 read-only preflight 复用 Pack A authority census，具名统计 PI inbox/outbox 与 EF inbox/outbox。named-local apply 前为 0/0/0/0；零行只是必要条件而非 apply 授权。未来 pending target 任一表非零均以 `EVENT_STORAGE_HARDENING_NONEMPTY_EVENT_TABLES` 阻塞并要求独立 transform/replacement 授权，partial structural state 直接失败。
- Backend full suite 在 r19/r16 之后为 2,083 tests：2,034 passed、0 failed、49 conditional database/provider-canary skipped、0 todo，duration `396225.938458ms`。这些条件性 skip 不构成 Pack A/Pack B 数据库验收；数据库验收仍只认强制 disposable PostgreSQL 6/6 与 7/7 且 skip=0。
- Pack A authority 使用忽略被删除纯 placeholder 的 semantic digest profile v2；migration apply 前后均为 208 rows / `sha256:494cdf5a02e2379a66a12bc82411e8237f39e949a2f992f3e12a0e220f613d74`。legacy 仍为 257 rows / `sha256:f9e7a6875c9a3597ecb485abc978920e461a3af33f071c676665439dde17211d`。
- strict publisher 对 exact evidence keysets、全部 zero-census/redaction keys 和 provenance fail closed 后，durable `05-app-composition-smoke.json` 的 SHA-256 为 `9f4e0e5f81d4f127ac1cd5c956c639a8f89a9406d0af873e5d06d2d039f10e4e`；durable `06-final-gate-summary.json` 的 SHA-256 为 `bd702c6aca7104248839da1bb224e711d9eee64ddf6a98e4824f713b03e8eaad`，publisher producer SHA-256 为 `982119a31989bbad1da51ae96892241bab90add1ee084031596f550350838c38`。两份 durable 文件仍绑定 named-local r18 source digests `37d262fa11fc45015d5e24320e963459034df77f2857074e3fea9476891de1b9` 与 `53e269275d5b082d2b1f82ad6b2ed47a324380470479de3b0598be6a62a16a0c`。已验证的 PostgreSQL 17 recovery point 位于 `/Volumes/DataDisk/Project/.backups/My-Researcher/packb-quality-remediation-20260714-r1/my_researcher_dev.pre-cleanup.dump`，大小 `8399040887` bytes，SHA-256 `0692d19e6e4ec2ea54389e229eae443b1c5f360e286a8203f9b4b979a4b00ecf`。
- 该 deep-cleanup checkpoint 当时三个 local flags 均为 `false`；2026-07-15 正式 Pack A landing 将 named-local cutover 提交为 `true`，Pack B 产品 E1-E5 随后在一次性 simulation 窗口完成。当前 admission/simulation 均恢复为 `false`；仍未执行非本地 DB apply、真实 provider/fetch、科学结果/证据或 D-18 closure。

## Pack B quality-remediation checkpoint — 2026-07-14

- 最终 disposable run `packb-quality-remediation-final-20260714-r7` 为 `passed`、`blockers=[]`，PB01-PB16 全绿；shared targeted 6/6、backend targeted 63/63、强制 real-Prisma relational 5/5 且 0 skipped。
- 原 migration `20260713210000_add_experiment_foundation_pack_b_provider_control_v2` 保持 immutable；cleanup migration `20260714160000_harden_experiment_foundation_pack_b_v2` 修正 15 个同域 immutable FK 的 `ON UPDATE RESTRICT`、移除 `collectionSequence` 与 3 个冗余索引，并增加 Cycle-wide active-real-Attempt fence 索引。最终 schema census 为 15 FK、35 CHECK、38 index，跨域 FK=0。
- provider external-job identity、exact output key/hash、scheduler backoff/heartbeat、cancel/reconcile race、HTTP response/error schema、PB14 Cycle-wide read fence、repository invariant ownership 和脚本 typecheck 均已收紧；死 API、不可达状态、重复校验和无效 schema/index authority 已删除。
- named-local cleanup migration 在全量 PostgreSQL 17 recovery point 后单独 apply；60/60 migrations up to date。最终只读 run `packb-quality-remediation-local-20260714-r5` 与 app-smoke v5 均 passed，238 张 application tables before/after 零变化、六张 Pack B 表仍为 0 行、Pack A/legacy digests 不变。
- 未执行产品 E1-E5、真实 provider/fetch、科学结果/验证/证据、D-18 Cycle closure、非本地 DB apply、产品入口/traffic cutover；source default 仍为 `false`。持久闭包证据见 `artifacts/implementation/05-pack-b-quality-remediation-closure.md`。

## Pack B named local-development landing — 2026-07-14

- 仅对 `127.0.0.1:5432/postgres?schema=my_researcher_dev` 应用 migration `20260713210000_add_experiment_foundation_pack_b_provider_control_v2`；59/59 migrations up to date，source/database checksum 均为 `c0c49f0f7a268f09619f6c693a1f41955ce6cb1b36b656dd9c8e1d51abe0f70e`。
- apply 前生成 7.8G PostgreSQL 17 custom-format recovery point，SHA-256 `0b167c08ad461e98fd25f5592bbc31dfb01b67e2f1fa49bb02d753fa95588987`；PostgreSQL 17 `pg_restore --list` passed，archive reports 2,062 TOC entries。
- 在该历史 landing checkpoint，local override 曾通过 `env-localctl` 编译为 `EXPERIMENT_FOUNDATION_V2_WORKFLOW_SIMULATION_ENABLED=true`；`.env.local` 保持 `0600`，repository contract default 仍为 `false`，未修改 dev/staging/prod/cloud。深度清理收口后当前 local override 已恢复为 `false`。
- 关闭态 simulation POST 返回 `EF_V2_WORKFLOW_SIMULATION_DISABLED`；启用态越过 capability guard 后因缺少 exact Pack A head acknowledgement 返回 `EXECUTION_HEAD_ACK_REQUIRED`。两种状态下 legacy mutation 均返回 `LEGACY_RECORD_NOT_ELIGIBLE`。
- 最终 gate `packb-local-closure-20260714-r1` passed：Pack A 34/34、Pack B 6/6、combined 40/40 tables exact；Pack A 208-row digest 与 legacy 257-row digest 完全不变；六张 Pack B 表均为 0 行；provider/fetch/scientific effects 为 0。
- 这在当时关闭了 named-local schema apply 与 capability activation 探针，不宣称产品 E1-E5 lifecycle。当前 capability 已关闭；未伪造 disposable fixture 为产品 scope，也未触发真实 provider、科学证据、Cycle closure、UI/search 或 traffic cutover。持久证据见 `artifacts/implementation/03-pack-b-local-landing-closure.md`。

## Pack B implementation outcome — 2026-07-13

- 已落地 exactly six EF-owned provider-control families、独立 shared/route/controller/service/repository/Prisma/worker/scheduler lane、default-off simulation capability 和 deterministic no-network fake transport。
- E1-E5 已覆盖 Attempt 创建、lease-version-fenced command claim/outcome、collection preparation/completion、accepted-response loss、stale lease、exact replay 和事件/输出形状回滚。
- cancel 在 pending submit 前保持零 transport；submit 已租用时先持久化 async cancel intent，并在 submit 收敛后优先于 sync；已租用 reconcile 在 E4 前观察到 durable cancel intent 时必须让出且不得创建 Collection。
- 最终 real-PostgreSQL gate `packb-20260713-final4` 为 `passed`、`blockers=[]`，PB01-PB16 全绿；强制 relational suite 4/4 passed、0 skipped。
- 最终精确写入为 2 ProviderPayload、2 ExecutionAttempt、12 AttemptEvent、8 ProviderCommand、2 CollectionAttempt、2 diagnostic-only ProvisionalOutput；231 张非 Pack B application tables 前后零变化。
- real provider request、`CreateJob`、fetch、legacy write、scientific write 均为 0；Run/RunCell 保持 `scientific_execution_status=not_started`，`evidence_eligibility=false`。
- 在该次技术闭包时未向 existing environment apply Pack B migration、未打开 simulation/product capability，也未切换 traffic/writer 或执行真实云/科学证据流程；后续命名 local-development apply/enable 单独记录在上节。持久技术证据见 `artifacts/implementation/02-pack-b-technical-closure.md`。

## Pack B implementation start — 2026-07-13

- Pack B 被严格映射到既有 Phase 3/M3，不新增 D-23，也不把后续 Phase 6 real read-only cloud preflight 提前合并。
- 冻结六类 additive EF provider-control families、五个 EF-local crash boundaries、default-off simulation capability、event-derived workflow status 与 PB01-PB16 acceptance。
- Pack A 三事件/四事务、Run manifest/head acknowledgement、PI repositories 和 legacy/scientific writers均保持不变。
- 开始实施时的 Git HEAD 为 `f6680225d5134d4f67bd59f4c5dbecb4eb0c476d`，reviewed source-population digest 为 `sha256:d89e4afcc39bb2734ebe82432d5a49c1af978c8c06ac9d051c8caeaafebc62b1`。

## Pack A implementation outcome — 2026-07-13

- 独立 PI/EF v2 contracts、server canonical hash、default-off capability、34 个 additive v2 Prisma models 与单条 migration 已落地。
- 五类 typed asset identity/draft/revision、exact readiness/dependency manifest、PI admission、EF materialization/Run freeze、PI head CAS、EF final inbox acknowledgement 已形成四事务/三事件 durable spine。
- 最终 fixture 精确收敛为 1 WorkOrder revision、2 cells、1 VersionLock、1 RunRecipe、2 TrainingTaskSpecs、1 Run/manifest、1 branch head 和 1 EF acknowledgement；Attempt/provider/result/validation/evidence/closure/UI/search/legacy 写入为 0。
- real-PostgreSQL 并发 replay、rollback、sequence conflict、cell parity、legacy digest 与 excluded-write census 已通过；197 个非 v2 application tables 前后 digest 完全一致。
- 最终 source-policy run `packa-d19-source-policy-20260713-r2` 为 `passed`、`blockers=[]`；两份 exact Dataset/DataPolicy 绑定的 canonical attestation digest 为 `sha256:48dd3546bcf314c478f80a5bc6ba5bcc0ecc57b848ac0d83fd7d5c9b8ac3bb6e`。
- 该 PASS 只关闭 control-plane source binding：未下载/重哈希完整 Wikipedia 语料，未证明 extraction 或 NQ↔2026 Wikipedia scientific alignment，也未执行 provider、科学证据、existing-environment DB apply 或产品 cutover。
- 产品入口配置仍默认 `false`；未向任何现有环境 apply migration，未切换流量或 writer，未调用真实 provider/cloud。
- 持久交接证据见 `artifacts/implementation/00-pack-a-technical-closure.md` 与 `artifacts/implementation/01-pack-a-source-policy-closure.md`；`.ai/.tmp` 机器摘要属于会在最终 deep cleanup 删除的 ephemeral evidence，不作为持久 handoff 依赖。canonical closure 文档和 checked-in `artifacts/` 才是 durable SSOT。

## Pack A local-development landing — 2026-07-13

- 目标被锁定为 loopback PostgreSQL `127.0.0.1:5432/postgres?schema=my_researcher_dev`；未接触 dev/staging/prod/cloud target。
- PostgreSQL 17 custom-format recovery point 在 apply 前完成并通过 `pg_restore --list`；dump SHA-256 为 `021be380ee58580d33905135838500faa86b2200b81ba09407447d1c2771c4e0`。
- 唯一 pending migration `20260713180000_add_experiment_foundation_d19_v2_spine` 通过 `prisma migrate deploy` 应用；58/58 migrations up to date、34/34 v2 tables、PI↔EF cross-domain FK=0。
- migration apply 前后五张 legacy 表使用同一 deterministic digest query 完全一致；随后单独通过现有 EF cancel path 将无 PI lineage 的 mocked capability job 从 `running` 终态化为 `cancelled`。raw submitted HarnessRun 已由 exact trusted terminal monitor/evidence 解释为 resolved，历史快照保持不改。
- reviewed source-policy fixture 首次创建 23 typed identities/revisions、48 lifecycle events、23 readiness attestations；第二次导入全量 exact-reuse，未写 PI/provider/legacy。
- 新增 default-false `PAPER_IMPLEMENTATION_EXPERIMENT_V2_CUTOVER_COMMITTED`；在该历史 Pack A landing checkpoint，本地仅通过 gitignored override 同时设置 cutover/admission `true`。16 个重叠 legacy mutations 在 `onRequest` fail closed，diagnostic GET 和 committed relay drain 保留；深度清理收口后当前两个 local flags 均为 `false`。
- 审查强化后的最终只读 gate `packa-local-landing-20260713-post-review-r3` 为 `passed`、`blockers=[]`；gate 现在只接受 `127.0.0.1:5432/postgres?schema=my_researcher_dev`，并强制 reviewed cluster/database/schema fingerprint、数据库/schema/read-only identity 进入 PASS 判定，同时核对 23 个 exact freeze receipts、48 个 lifecycle events、projection source event 与 draft state。真实 app smoke 仍证明 legacy mutation=409、diagnostic read=200、v2 request 越过 capability 后仅因缺少 active persisted PI scope 返回 `BRANCH_SCOPE_CONFLICT`，且 gate 复跑证明零 partial write。
- Post-review source-backed run `packa-d19-post-review-hardening-20260713-r4` 再次通过 A01-A04/B01-B10，并在 disposable real PostgreSQL 上执行两路并发 fixture import；一次真实 READ COMMITTED 竞态先被复现，修复后的有界重读使并发 import 与后续 replay 精确收敛为 23/23/48/23，持续语义漂移仍 fail closed。
- 本地 scope 缺口不是 Pack A authority-spine 失败：D-19 的 pre-bound scope 是明确 prerequisite，产品 bootstrap 不在 Pack A。完整四事务/三事件/唯一 acknowledgement 仍由 source-backed disposable PostgreSQL gate `packa-d19-source-policy-20260713-r2` 提供验收。

## Goal
保留现有 ExperimentFoundation 领域分层，把 ExperimentFoundation 夯实为 PaperImplementation 的云实验控制面与可信实验事实边界：由 PI-owned 项目/ValidationCycle/WorkOrder branch/revision 发起论文实验意图，由 EF 完成冻结输入、provider 控制、科学验证规则和证据资格边界。PI 与 EF 共享一套精确的 Cycle→branch→revision→Run→ExecutionAttempt 范围身份；PI 维护项目级检索投影并使用结构化范围过滤加语义排序理解工作流，EF 只提供确定性执行事实。首版只验证桌面控制流、精确云 payload、真实只读 preflight 与同 payload 模拟，不执行训练，也不让模拟输出进入科研证据链。

## Product interaction target — confirmed 2026-07-11
| Direction | PaperImplementation responsibility | ExperimentFoundation responsibility | Trust rule |
|---|---|---|---|
| PI → EF: plan and start | 拥有论文目标、项目、ValidationCycle、ResearchWorkOrder、准入前 exact cell plan、优先级和人工意图 | 解析已准入 WorkOrder，逐项验证 exact cells 并物化 Recipe/TaskSpec/provider binding | PI 不直接声明 EF hash/readiness/result；EF 不得在准入后选择、增删或替换 scientific cells |
| PI → EF: control | 请求 submit/cancel/retry/reconcile | 校验并执行 run/attempt 状态转换，维护执行真相 | PI WorkOrder 状态不复制为第二份 EF run 真相 |
| EF → PI: progress | 展示论文流程状态、阻塞与待决策事项 | 发布可重放的生命周期/阻塞投影 | 状态同步可重试；双方不双写同一 canonical state |
| EF → PI: evidence | 通过唯一 Gateway 接收合规 EvidenceCandidate；自动准备结果解释提案，并只在 ValidationCycle closure 赋权威科研结论、派生 selected exit 和驱动 claim/dossier | 对 exact batch Run 执行 typed protocol rules，提供不可变 result/validation/EvidenceCandidate；不判断论文问题的 positive/negative/inconclusive | PI 不得绕过 EF validation 或提高 EF 证据等级；EF、模型、REU、ResultInterpretationPacket 均不得写 Cycle 科研结论 |
| EF standalone → PI | 为独立探索结果选择目标 WorkOrder | 保留原 run lineage，重新验证身份、readiness、validation 和 project scope | 未绑定/未重验的 exploratory run 不能成为论文可信证据 |

ExperimentFoundation 继续拥有独立的资产库与探索工作台；PaperImplementation 是所有“为某篇论文服务”的实验主入口和消费入口。两者是协作 bounded contexts，不是父子模块，也不共享可任意修改的状态。

D-10 further fixes the shared iteration/search boundary: WorkOrder branch is PI's stable logical identity, WorkOrder revision is PI's immutable admitted plan, and EF owns the TaskSpec/Run/ExecutionAttempt/result suffix while carrying the exact PI project/Cycle/branch/revision/hash scope unchanged. PI owns a project-scoped, rebuildable retrieval projection; global services provide technical indexing only. D-13b fixes each per-branch head as the latest frozen execution-lineage Run: EF publishes `RunManifestFrozen`, PI advances by immutable branch revision sequence and emits `BranchHeadAdvanced`, and EF waits for that exact durable acknowledgement before first Attempt/dispatch. Failed/cancelled latest Runs remain head; time, completion, metric and semantic score never select or roll back a head.

D-11 makes revise/fork classification mechanical rather than semantic: before first admission PI may CAS-update the same draft; an exact TaskSpec technical retry creates an EF ExecutionAttempt; exact-plan changes with an identical frozen branch semantic-frame hash create a new WorkOrder revision and require admission; any change to `branch_intent`, `expected_effect`, `difference_from_parent` or parent/fork relation creates a new branch; changing the ValidationCycle question or decision exits creates a new Cycle. D-13a explicitly refines D-11's provisional point-to-Run row: one paper-bound WorkOrder revision may freeze at most one immutable Run containing 1..N required cells, and each technical retry is an ExecutionAttempt scoped to one exact cell/TaskSpec. New revisions/branches affect future Runs only and never rebind an in-flight or completed Run.

D-12 confirms two layers with automation-first interaction minimization. AuthorityGate is the durable domain decision that authorizes a transition; CoordinatorStop is a derived automation pause and never creates a second human-decision authority. T-124's four stop points are coordinator-local, not a product-global cap. When a Stop is caused by an unresolved Gate, the owning screen presents one user action, writes/reuses only the required domain decision records and automatically resumes the coordinator. Deterministic blockers, in-bound Runs/Attempts/retries and lifecycle reconciliation never create confirmation prompts. Manual promotion stays off the normal PI path, external-side-effect authorization appears only for a real write/scope expansion, and ValidationCycle closure remains one batch action.

The first-release interaction target is now concrete. Its fixed golden scenario contains one bound ImplementationProject, one ValidationCycle, one WorkOrder branch, one exact admitted WorkOrder revision, one mode-neutral immutable batch Run and 1..N required cells with cell-scoped simulation Attempts; the scenario performs no cloud write/training, manual promotion, strong-claim acceptance or dossier export. From selecting the validation goal through no-evidence ValidationCycle closure, the target is exactly 1 InitiationAction, 2 AuthorityActions (WorkOrder admission and Cycle closure), 0 RecoveryActions and 0 PlumbingActions. Preparation, payload materialization, offline validation, read-only preflight, simulated lifecycle, safe retry/reconcile and PI projection between admission and closure are one uninterrupted automatic span.

D-14 keeps control-plane and scientific state orthogonal. Simulation/real provenance belongs to ExecutionAttempt, never to a second Run. A terminal simulation Attempt changes only its own lifecycle and a rebuildable `workflow_simulation_status`; every Run/cell remains `scientific_execution_status=not_started` until an eligible real-provider Attempt publishes the required complete scientific result. PI may close the fixed Cycle with `closure_kind=control_flow_validated_no_paper_evidence`, `scientific_disposition=null`, exact head-Run/terminal-Attempt refs and `evidence_eligibility=false`; that closure kind never mutates EF scientific state or creates ExperimentResult, validation or evidence.

D-15 removes the remaining post-admission scientific choice. PI may use ranges, grids and seed counts while authoring a draft, but before the one WorkOrder admission the PI draft flow deterministically compiles, normalizes and persists a non-empty ordered `exact_cell_plan[1..N]`. Each cell fixes a server-derived key, seed/repeat index, exact parameter bindings and required-result contract; `cell_plan_hash` is covered by `approved_plan_hash`. EF receives that immutable authority and may only validate and add Recipe/TaskSpec/provider/result bindings one-to-one. TaskSpec refs/hashes therefore remain EF-owned post-admission outputs rather than circular admission inputs.

D-16 separates scientific evidence from execution accounting. A RunEvidenceUnit can be created only through the single PI Evidence Trust Gateway from a complete, protocol-compliant, validation-passed EF EvidenceCandidate. Complete validated results later assigned positive, negative or inconclusive by the PI Cycle closure share the same completed execution/evidence path; those labels are not REU state. Failed, cancelled and incomplete execution creates no RunEvidenceUnit; the existing ValidationCycle closure action atomically freezes one embedded immutable current-effective-scope snapshot/hash. PaperExperimentSidecar only rebuilds/displays that authority. Dossier readiness consumes explicit closed-Cycle snapshot refs/hashes rather than scanning project-wide failed-like REUs. No `FailureEvidenceUnit`, second gateway, standalone Sidecar ledger or additional human action is introduced.

D-17 makes both halves of that boundary executable. EF v2 EvaluationProtocol has one canonical ordered typed `required_rules` authority; descriptive/free-shape policy text cannot drive validation. A code-local closed capability map validates exact rule type/version at readiness and final validation, and any unsupported required rule returns `UNSUPPORTED_RULE` before Run freeze/dispatch with no best-effort, LLM interpretation, human waiver or EvidenceCandidate. ScientificValidation is batch-scoped: one immutable report binds the exact Run/manifest, ordered required-cell result refs/hashes, exact protocol revision/hash, validator-profile hash and ordered rule results, and only `passed` may atomically mint EvidenceCandidate through the sole writer.

PI owns the contextual scientific conclusion. Exact terminal EF facts automatically make a Cycle closure candidate when the D-18 current-effective scope is stable and no real-provider Attempt anywhere under the Cycle remains active. If eligible REU exists, Result Analysis produces one exact-hash-bound proposal; the proposal is support material only. The existing ValidationCycle closure AuthorityAction is the sole writer of authoritative `positive | negative | inconclusive | null`, closure kind, accepted proposal ref/hash and immutable accounting snapshot/hash. The server derives the selected exit from admission-frozen positive/negative/inconclusive exit definitions. A no-evidence or control-only closure has null scientific disposition and no scientific selected exit. Only `ValidationCycleClosed` may trigger ResultInterpretationPacket materialization; Packet, ClaimCandidate, Dossier, motive/retrieval projections and next-step draft generation consume that exact closed Cycle. EF never consumes the disposition or rewrites an old Run. D-17 adds no conclusion aggregate or user action.

D-18 removes the remaining closure-scope dual track. ValidationCycle closure freezes the current effective decision scope at one CAS-fenced closure watermark, not the complete historical Run inventory. The scope contains every branch with an admitted revision at the watermark; for each branch the snapshot records the exact current admitted revision/hash/sequence and matching effective head Run/manifest, cells, all Attempts, execution/eligibility state and eligible REU refs. A branch whose current admitted revision has no matching frozen/acknowledged head remains visible in the closure candidate with `effective_head_run_ref=null` and stable `BRANCH_HEAD_NOT_FROZEN`, but closure cannot commit until every branch has its exact effective head. Superseded/non-head Runs remain immutable structured-query/audit history and are excluded from the snapshot. An old v2 result may participate in comparison only through an explicit immutable `comparison_input_ref/hash` on the current admitted revision and full server re-resolution; no historical scan, semantic match or legacy trust upgrade can add the result. Any active real-provider Attempt anywhere in the Cycle, including on a non-head Run, blocks closure. Closure atomically CAS-freezes the Cycle version, branch-set watermark and per-branch admitted/head sequences; after closure no new admission, Run freeze, head advance or Attempt is allowed under that Cycle, and result-driven follow-up starts a new ValidationCycle.

D-19 freezes the first implementation acceptance slice as one admission-to-head-acknowledgement spine, not an isolated EF refactor and not the full control-plane release. The slice starts from an already bound PaperProject/ValidationCycle fixture and typed v2 assets persisted through the real Phase 1 identity/readiness path; product PaperProject bootstrap, candidate ingestion and promotion are prerequisites/outside the slice rather than alternate shortcuts. PI admits one WorkOrder revision with a canonical 1..N exact-cell plan; EF exact-materializes VersionLock/RunRecipe/TrainingTaskSpecs, freezes the revision's only batch Run/manifest and emits `RunManifestFrozen`; PI sequence-fenced CAS-advances the branch head and emits `BranchHeadAdvanced`; EF durably consumes that exact acknowledgement. The acceptance fixture uses two required cells to disprove single-job shortcuts and runs through shared contracts, typed HTTP/service/repository/Prisma plus inbox/outbox replay. The endpoint is the durable acknowledgement with zero ExecutionAttempt, provider call, ExperimentResult, validation, EvidenceCandidate, REU, Cycle closure, desktop/search work or legacy-row migration. The v2 path remains capability-gated and performs no dual write. D-18 remains unchanged: if any real-provider Attempt exists anywhere in the Cycle in later phases, including on a non-head Run, `CYCLE_ACTIVE_REAL_ATTEMPT` blocks closure.

D-20 fixes that spine to four domain-owned authoritative commits even when PI and EF share one physical Postgres: PI admission/current-revision CAS plus `WorkOrderRevisionAdmitted` outbox; EF inbox plus exact materialization, sole Run/manifest and `RunManifestFrozen` outbox; PI inbox plus exact-scope/sequence head CAS and `BranchHeadAdvanced` outbox; EF inbox plus the exact durable acknowledgement. Every inbox receipt is committed with the consumer's domain outcome and any emitted outbox in one local Unit of Work. No transaction callback may write both domains, and no shared mutable authority table, cross-domain repository, distributed lock or 2PC exists. Exact event replay returns the stored outcome; the same event/idempotency key with a different canonical payload hash or the same branch sequence with a different Run/manifest is a terminal conflict; lower sequence is durably ignored without head rollback; a temporarily invisible prerequisite retries with zero domain/outbox write. The processed EF inbox receipt for `BranchHeadAdvanced` is the acknowledgement and later dispatch prerequisite—there is no fourth integration event, `dispatch_eligible` mirror or second acknowledgement aggregate. Relay lease/publish/delivery bookkeeping may use infrastructure transactions but never changes domain authority.

D-21 freezes physical storage and cutover on independent additive, domain-owned v2 table families rather than extending the mutable singular `PaperImplementationResearchWorkOrder`/HarnessRun or generic overwrite-capable `ExperimentFoundationRecord`. PI v2 owns branch, immutable revision/exact cells, admission/head and PI inbox/outbox storage; EF v2 owns typed revision/readiness, VersionLock/RunRecipe/TrainingTaskSpec binding, immutable Run/manifest/cells and EF inbox/outbox storage. Cross-domain identity remains exact versioned refs/hashes/sequences/events with no cross-domain ORM relation, cascade or shared write table. Legacy rows remain byte-for-byte unchanged and are available only through existing-field diagnostics/admin reads: no backfill, annotation, trust upgrade, runtime union view, dual write, dual read or fallback. The v2 admission capability defaults off and rejects new product commands with zero write rather than invoking legacy. D-19 acceptance may enable that explicit scope; after acceptance, new paper-bound experiment intake switches to the v2 entrance and overlapping legacy product writers close at the same cutover. Disabling intake blocks only new admissions—already committed outbox/inbox work must drain to the EF acknowledgement—then existing v2 rows/events remain immutable and auditable. Rollback never deletes v2 state or reopens a legacy writer for the same logical object.

D-22 freezes the first migration to the smallest vertical pack that can close Phase 1 and D-19—no future-complete PI/EF schema. Relational columns/rows own identity, immutable revision sequence, current/head CAS, ordered cell keys, same-domain bindings and event/idempotency state. Frozen scientific values live only in named schema-versioned typed snapshots whose canonical JSON and hash are server-produced; no generic `kind/payload`, EAV or caller hash authority is reintroduced. PI and EF may use FKs inside their own domain, while cross-domain project/Cycle/branch/revision/cell/Run references remain external exact ids/hashes/sequences/events with zero cross-domain FK/ORM cascade. The pack includes PI WorkOrder branch/revision/cells/admission/head/inbox/outbox; EF named typed asset draft/revision and lifecycle/readiness support required by the locked D-19 fixture, VersionLock/RunRecipe/TrainingTaskSpec, one immutable Run plus ordered Run cells, and EF inbox/outbox. Run manifest identity is the hash of the ordered immutable Run-cell rows rather than a second mutable manifest payload. Capability stays a default-off admission configuration guard, not a persisted eligibility or dispatch state. The pack excludes candidate/import/promotion/bootstrap, ExecutionAttempt/provider/ExternalTrainingJob/CollectionAttempt, result/validation/evidence, Cycle closure/interpretation, UI/read model/search and legacy mapping/cutover tables. D-22 freezes logical families and invariant placement, not final Prisma names or DDL; those require the DB-SSOT schema/migration review and a separate implementation authorization.

The approved readiness closure freezes the exact D-19 asset allowlist to five top-level typed families: Dataset, DataPolicy, MetricDefinition, Benchmark and a new EvaluationProtocol v2. The D-19 fixture has two Dataset revisions, two dataset-specific DataPolicy revisions, seventeen MetricDefinition revisions, one Benchmark revision and one EvaluationProtocol v2 revision. BaselineImplementationVersion, MethodRecipeComponent, DatasetMirror and provider/platform objects remain outside Pack A. `PAPER_IMPLEMENTATION_EXPERIMENT_V2_ADMISSION_ENABLED` is the dedicated default-off PI admission configuration key. The complete Prisma-family/invariant/error/test/population lock is `07-implementation-readiness-review.md`.

## Why a new task
- `T-043` 是 V1 概念与子任务总图，不是当前产品化执行包。
- `T-106` 聚焦 real-interaction hardening 和尚未落地的 provider canary。
- `T-131` 只完成第一条真实 promotion，且消费验证仍待真实 RunRecipe。
- `T-124` 拥有 PaperImplementation 产品化，不应吸收 ExperimentFoundation 的 canonical identity、execution 或 validation 职责。
- T-132 统一承接 2026-07-10 设计审查发现，并用一个任务包分阶段推进。

## Current assessment
- Worth preserving:
  - reusable assets / evaluation protocol / Recipe / TaskSpec / execution / evidence 分层；
  - ExperimentFoundation 不拥有 claim/dossier 的 bounded-context 边界；
  - platform-neutral recipe 和 adapter 边界；
  - ref + hash 的跨模块追溯方向。
- Must be hardened:
  - frozen records can currently be overwritten and hashes are not canonical-content authority;
  - readiness is not bound to the exact target/dependency versions;
  - current `accept_partial` branch can turn incomplete outputs into a separate evidence path and must be removed;
  - submit/collect control state is not crash-safe，且 LocalScript/fake provider 当前没有与正式科研证据路径硬隔离；
  - trusted evidence has bypass/dual-path risk;
  - preparation services, broker, sidecar writer and project-scoped workflow are incomplete;
  - current PI WorkOrder contracts/storage do not express stable branch identity, immutable WorkOrder revisions or explicit per-branch head Runs, so PI/EF cannot yet enforce the same iteration scope;
  - current WorkOrder/EF preparation contracts do not express a PI-owned exact admitted cell plan, so ranges or later materialization could become an undeclared second scientific selection point;
  - current PI contracts and S3 dossier code mix failed/cancelled execution with negative/inconclusive scientific disposition inside RunEvidenceUnit and project-wide REU accounting; D-16 fixes one target contract but requires an atomic shared migration before the evidence seam can close;
  - current EvaluationProtocol policy blocks are opaque JSON, validation checks only limited per-job metric/artifact/hash presence, and generated validation/evidence records remain writable through generic paths; D-17 replaces those paths with exact-batch typed rules and one ScientificValidation writer;
  - current PI completion accepts caller-authored `cycle_assessment`/`decision_exit`, Result Analysis produces four free-standing scenarios and can directly materialize ResultInterpretationPacket, while downstream consumers do not require one authoritative closed-Cycle disposition; D-17 makes the existing Cycle closure the sole conclusion authority and records the current paths as atomic PI migration debt;
  - PI/EF have no project-scoped hybrid retrieval projection; current vector retrieval is Literature-domain-specific and must not become the experiment truth source;
  - existing UI/full-flow checks do not yet prove a real researcher interaction chain.

## Inherited work and boundaries
- Parent concept: `T-043 experiment-foundation-v1`.
- Evidence/harness inputs: T-090, T-103, T-106.
- First real canonical input: `T-131 experiment-foundation-first-promotion-closure`.
- Coordinated downstream: `T-124 paper-implementation-productization-hardening`.
- T-132 MUST NOT silently change the status, scope or archive state of those packages.

## Non-goals
- Build a training platform, scheduler, generic LLMOps or automatic HPO system.
- Move Literature or PaperImplementation ownership into ExperimentFoundation.
- Generate claims or make final dossier decisions.
- Let PaperImplementation write EF canonical/result/validation state or upgrade EF evidence qualification.
- Treat an unattached standalone EF run as paper-trusted evidence.
- Call Aliyun `CreateJob`, allocate cloud compute or run actual cloud training in the first release gate.
- Treat LocalScript or fake-provider output as a real experiment, ResultValidationReport, EvidenceCandidate, RunEvidenceUnit or dossier source.
- Treat a terminal simulation Attempt as a scientifically terminal Run, create a separate SimulationRun, or use PI Cycle closure to upgrade EF scientific state.
- Admit only ranges or a generator and let EF/model/defaults select scientific cells afterward; first-release paper-bound admission requires explicit exact cells.
- Add a `CellPlanManifest` aggregate, generator registry/DSL, post-admission sampling/default seed, optional cells or adaptive HPO to support D-15; the exact plan stays an embedded WorkOrder revision value collection.
- Add `FailureEvidenceUnit`, a second evidence gateway, a project-wide failed-REU scan or an independently editable Sidecar failure ledger; the existing Cycle closure record's embedded immutable snapshot/hash is the sole execution-accounting authority.
- Encode negative/inconclusive scientific disposition as failed/cancelled execution status or infer dossier scope from all project REUs.
- Add a generic scientific-rule DSL/plugin registry, interpret free-form protocol text with an LLM, silently skip unsupported rules or let a human waive `UNSUPPORTED_RULE`.
- Add a `ScientificConclusion` aggregate, let EF/REU/ResultInterpretationPacket/model/caller assign authoritative disposition, or add a second confirmation beyond the existing Cycle-closure action.
- Treat EF protocol `passed` as a positive paper conclusion, failed/cancelled/incomplete execution as a negative conclusion, or let an open interpretation proposal drive claims, dossier readiness or another experiment.
- Build a general local training/container/sandbox platform in the first release.
- Build a global research-knowledge authority or make ExperimentFoundation own PI research intent/search semantics; shared vector/index infrastructure remains domain-neutral and disposable.
- Generate EF/model-authored experiment summaries for retrieval, or let semantic similarity decide branch head, execution, readiness, evidence qualification or workflow transitions.
- Describe read-only cloud preflight as real cloud training validation.
- Apply DB migrations or submit external jobs without their explicit approval gates.
- Keep raw JSON and manual IDs/hashes as the primary product workflow.

## Acceptance criteria
- [ ] Frozen EF objects use stable logical identities plus immutable revisions and server canonical semantic hashes; frozen changes always create new revisions.
- [ ] Draft updates require CAS/expected hash and cannot receive readiness; execution rejects logical-only refs.
- [ ] Readiness is an immutable attestation bound to the exact target revision/hash and a deterministically ordered recursive dependency revision/hash manifest.
- [ ] Evolving operational state uses append-only events and an explicit projection rather than mutating frozen semantic records.
- [ ] Post-readiness mutation, forged hash, stale ref and concurrent update fail closed.
- [ ] Scientific validation enforces a declared supported protocol subset and treats unsupported rules explicitly.
- [ ] EvaluationProtocol v2 has one canonical ordered typed required-rule authority covered by the exact revision/hash; free-shape policy/constraint text is non-executable and creates no fallback semantics.
- [ ] The first supported slice is limited to typed metric/artifact contracts plus non-configurable exact-cell/seed/repeat/params/result-contract/lineage/real-provider envelope checks; any declared unsupported comparison/statistical/threshold/fairness/derived rule returns `UNSUPPORTED_RULE` before Run freeze/dispatch.
- [ ] One batch-scoped validation report binds exact Run/manifest, ordered required-cell results, exact protocol revision/hash, validator-profile hash and ordered rule outcomes; validation/evidence generic writers and caller-authored trust status are closed.
- [ ] `accepted_partial` and partial-evidence qualification do not exist in the first-release product/API path.
- [ ] Failed, cancelled or incomplete runs retain logs/metrics/artifacts as ExecutionAttempt diagnostics but cannot create EvidenceCandidate.
- [ ] EvidenceCandidate requires a complete protocol-compliant result with `validation_status=passed`; results later assigned positive, negative or inconclusive by PI remain evidence-eligible through that one completed-execution path.
- [ ] Before WorkOrder admission, PI automatically compiles and persists a canonical non-empty ordered `exact_cell_plan[1..N]`; each cell fixes server-derived key, seed/repeat index, exact parameter bindings and required-result contract.
- [ ] WorkOrder admission records one `approved_plan_hash` that binds `cell_plan_hash` plus the established authoritative WorkOrder boundary; ranges/grid/seed-count cannot authorize an unlisted cell, and optional authoring-provenance storage/hash treatment remains a Phase 0 detail.
- [ ] EF validates the admitted cell plan one-to-one, adds EF-owned Recipe/TaskSpec/provider/result bindings and freezes the Run manifest; any extra, missing or drifted scientific cell fails before Run/head/Attempt creation.
- [ ] D-19 is the first implementation acceptance slice: a pre-bound Cycle and real v2-ready asset fixture traverse admitted WorkOrder revision → exact materialization → one batch Run/manifest → `RunManifestFrozen` → PI head CAS/`BranchHeadAdvanced` → EF durable acknowledgement.
- [ ] The D-19 acceptance fixture uses exactly two required cells while the contract remains 1..N; both cells preserve admitted key/seed/repeat/params/result contract and acquire only EF-owned TaskSpec bindings.
- [ ] D-19 executes through shared contracts, typed HTTP/service/repository/Prisma and durable inbox/outbox replay; the same input converges, stale/lower sequence cannot roll back head and same-sequence conflict fails closed.
- [ ] D-19 finishes with one admitted revision, one Run, one PI head and one EF acknowledgement, but zero ExecutionAttempt/provider call/result/validation/evidence/Cycle closure/UI/search/legacy migration writes.
- [ ] D-20 preserves exactly four domain-owned authoritative commits on the successful spine; each Unit of Work writes only PI or EF tables, and same-database deployment cannot collapse the boundary into a cross-domain transaction, shared write repository/table, distributed lock or 2PC.
- [ ] `WorkOrderRevisionAdmitted`, `RunManifestFrozen` and `BranchHeadAdvanced` use a versioned envelope with producer, event/idempotency identity, correlation/causation, canonical payload hash and exact project/Cycle/branch/revision/sequence/hash scope; consumers never resolve `latest` or trust relay delivery markers.
- [ ] Inbox receipt, domain mutation and emitted outbox are atomic per consumer. Commit-before-publish and publish-before-delivery-marker crashes converge by replay; exact duplicates do not duplicate domain/outbox rows; payload/sequence conflicts fail closed and retryable dependency invisibility writes no partial authority.
- [ ] EF's processed exact-scope `BranchHeadAdvanced` inbox receipt is the only durable acknowledgement and future dispatch prerequisite; no fifth domain commit, acknowledgement event/aggregate, Run flag or `dispatch_eligible` mirror is introduced.
- [ ] D-21 creates separate PI-owned and EF-owned additive typed v2 table families; legacy singular WorkOrder/HarnessRun/generic EF records receive no new v2 columns, refs, hashes, annotations, backfill or product write authority.
- [ ] Capability-off rejects new v2 admission with zero v2/legacy write and no fallback. D-19 acceptance scope can enable only the explicit v2 entrance; product cutover later routes new paper-bound experiment intake only to v2 and closes overlapping legacy writers in the same change.
- [ ] PI and EF repositories cannot write or relate across domain table ownership. Cross-domain persistence is exact external identity/hash/sequence/event scope only; no shared table, ORM cascade, cross-domain FK, runtime union view or compatibility alias is accepted.
- [ ] Disabling v2 intake stops new admission but drains every already committed D-20 saga to durable EF acknowledgement. Rollback preserves immutable v2 rows/events and diagnostics, blocks new intake and never rewrites or falls back to legacy.
- [ ] Legacy before/after digests remain identical; existing-field diagnostics/admin reads still work, while every legacy create/update/promote/attach/execute/product command fails closed or stays outside the new v2 product route.
- [ ] D-22 schema census contains only Phase 1 trust-substrate and D-19 spine logical families. Attempt/provider/result/validation/evidence/closure/interpretation/UI/search/legacy-mapping tables and fields are absent.
- [ ] Identity, revision/ordinal/sequence, current/head CAS, same-domain binding and inbox/outbox idempotency are relationally constrained; no authority-critical value is hidden only in JSON.
- [ ] PI semantic frame and cell params/result contract, named EF asset content, readiness qualification, VersionLock, RunRecipe, TrainingTaskSpec and event payloads are schema-versioned typed canonical JSON with server hashes; generic `kind/payload`, EAV and caller-authored hashes are rejected.
- [ ] PI↔EF cross-domain FK/ORM relations/cascades are zero. Same-domain FKs are allowed where one concrete owner table exists; polymorphic exact typed refs still require service-side resolution and hash verification.
- [ ] The Run row plus ordered immutable Run-cell rows are the single manifest authority; `run_manifest_hash` recomputes from those rows and no second manifest JSON/table can drift.
- [ ] Capability is a default-off configuration/route guard for new admission only, with no capability table, persisted eligibility field, acknowledgement boolean or `dispatch_eligible` mirror.
- [ ] Human interaction is limited to four fixed gate categories: WorkOrder admission, manual-promotion decision, external side effect/scope expansion, and ValidationCycle closure.
- [ ] `manual_promote` only decides catalog admission; the action cannot waive deterministic eligibility blockers, grant executable readiness or increase evidence trust.
- [ ] The manual-promotion record remains minimal: candidate revision/hash, `promote | reject`, actor, rationale, decision time and resulting canonical revision/hash when promoted; no waiver/scope/expiry DSL exists.
- [ ] Promotion accepts an exact Candidate revision/hash but no caller-authored canonical ref; one server-owned idempotent transaction creates or reuses the exact canonical revision, records `created | reused`, writes the decision, terminates the Candidate revision and inserts the outbox event.
- [ ] One Candidate revision has one terminal promotion decision; rejection creates no canonical revision, and reconsideration after material change requires a new Candidate revision.
- [ ] Readiness, evidence qualification and external side effects are excluded from the promotion transaction and retain their own gates.
- [ ] Deterministic trust/policy errors block directly and cannot be converted into an approval prompt.
- [ ] ValidationCycle closure freezes the D-18 current effective decision scope at one CAS watermark and requires one explicit batch action; every admitted branch must have a matching effective head Run/cells/Attempts, `BRANCH_HEAD_NOT_FROZEN` blocks instead of substituting history, superseded/non-head Runs are excluded, and any real-provider Attempt anywhere in the Cycle blocks.
- [ ] D-16 reuses that one Cycle-closure AuthorityAction to atomically freeze the embedded execution-accounting snapshot/hash; N Runs/Attempts, dossier snapshot consumption and Sidecar rebuild add no AuthorityAction, RecoveryAction or PlumbingAction.
- [ ] No `DecisionAuthorityManifest`, generic human-policy DSL or general DecisionWorkItem engine is introduced.
- [ ] Provider-control attempts and same-payload simulation are durable, idempotent and recoverable after backend failure without creating scientific result/evidence records.
- [ ] Literature candidate import, canonical promotion, RunRecipe generation and TaskSpec materialization have typed business commands.
- [ ] PaperImplementation receives EF evidence through one trust gateway; WorkOrder and Sidecar projections have complete reverse lineage.
- [ ] EF alone qualifies complete validation-passed EvidenceCandidate; one PI-owned Evidence Trust Gateway is the only RunEvidenceUnit writer for live, monitor, recovery and standalone-attachment flows.
- [ ] Gateway input is identity-only and server-resolved; caller-declared run/validation status, result hashes, evidence arrays or trusted flags cannot influence admission.
- [ ] Failed, cancelled and incomplete effective-head execution creates no RunEvidenceUnit. The existing ValidationCycle closure action atomically freezes one embedded immutable D-18 scope snapshot/hash over admitted branch/current-revision/effective-head Run/cell/Attempt refs, execution states, eligibility codes and eligible REU refs; Sidecar only rebuilds/displays that authority.
- [ ] Complete protocol-valid negative/inconclusive results may create RunEvidenceUnit through the same gateway as positive results, but their scientific disposition is never encoded as execution failed/cancelled/incomplete.
- [ ] RunEvidenceUnit contains trusted evidence identity/lineage only; REU does not own `positive | negative | inconclusive`, and EF validation `passed` cannot select a ValidationCycle outcome.
- [ ] Exact terminal Run/Attempt/evidence events idempotently prepare Cycle interpretation; Result Analysis outputs one exact-hash-bound proposal but cannot write `cycle_assessment`, selected exit or an authoritative ResultInterpretationPacket independently.
- [ ] The existing ValidationCycle closure action is the sole scientific-disposition writer. It atomically freezes closure kind, nullable `positive | negative | inconclusive`, exact accepted Result Analysis proposal ref/hash and the D-18 accounting snapshot/hash; Packet materializes afterward and is outside the hash, while no-evidence/control-only closure has null disposition/selected exit.
- [ ] The selected exit is server-derived from the frozen positive/negative/inconclusive exit definition; caller-authored `decision_exit` is rejected, and downstream next-step drafts/Claim/Dossier/retrieval/motive consumers require the exact closed Cycle.
- [ ] Dossier readiness accepts only explicitly referenced closed-Cycle snapshots with matching project/Cycle scope and hash; open, tampered, incomplete or wrong-project snapshots fail closed, and no project-wide failed-like REU scan or Sidecar fallback exists.
- [ ] Gateway atomically writes PI RunEvidenceUnit, TraceManifest and outbox; PaperExperimentSidecar is a rebuildable projection and never a trust source.
- [ ] LocalScript and fake-provider identities are dev/test-only and are rejected by ExperimentResult/ResultValidationReport/EvidenceCandidate/RunEvidenceUnit product writers.
- [ ] A PI-owned ResearchWorkOrder can delegate experiment preparation/execution/control to EF, and EF lifecycle state returns to the same PI project without dual-write authority.
- [ ] PI and EF share one versioned scope contract for ImplementationProject → ValidationCycle → WorkOrder branch/logical identity → exact WorkOrder revision/hash/`cell_plan_hash` → unique Run/manifest hash → required cell key/TaskSpec ref+hash → ExecutionAttempt; EF never resolves a paper-bound command from `latest`, a logical id alone or range/generator authority.
- [ ] PI owns ValidationCycle, WorkOrder branch/revision and the admitted semantic frame; EF owns TaskSpec/Run/ExecutionAttempt/result facts and carries PI scope refs without interpreting or rewriting their research meaning.
- [ ] PI maintains a project-scoped, rebuildable experiment retrieval projection from PI and EF outbox facts; global infrastructure provides only reusable embedding/vector/index mechanics and never becomes a third domain truth.
- [ ] Retrieval indexes one deterministic semantic document per ValidationCycle and per WorkOrder branch head from PI-authored planning fields; historical v2 revisions/runs remain available through exact structured lineage queries rather than per-run semantic summaries.
- [ ] WorkOrder branch semantics minimally include `branch_intent`, `expected_effect`, `difference_from_parent` and structured parent/fork refs; admitted values are canonical planning input included in `approved_plan_hash`, not post-hoc EF/model summaries.
- [ ] PI exposes explicit `revise` and `fork` operations: `revise` requires an unchanged frozen branch semantic-frame hash and creates a new admitted WorkOrder revision; changing any branch semantic field/relation requires `fork`. No model, EF rule or semantic-distance threshold chooses between them.
- [ ] Draft edit/pre-admission cell compilation, cell-scoped EF ExecutionAttempt, one immutable batch EF Run, PI WorkOrder revision, PI WorkOrder branch and ValidationCycle creation follow the D-11/D-13a/D-15 deterministic classification matrix; every rejected cross-layer mutation returns a stable next action.
- [ ] A paper-bound WorkOrder revision has at most one Run while preparation is incomplete and exactly one after the Run manifest freezes; duplicate creation is idempotent only for the same canonical manifest/hash and otherwise conflicts.
- [ ] The Run manifest is an immutable, canonically ordered list of exactly the admitted 1..N required cells. Each Run cell preserves the admitted key/seed/repeat/exact parameters/required-result contract and adds EF-owned TaskSpec ref/hash; cells are value objects rather than RunSet/RunGroup/domain branches.
- [ ] Technical/provider retries add ExecutionAttempts under the same exact Run cell. Re-execution for scientific sampling, a changed seed/repeat/parameter/result contract, any cell addition/removal, or a WorkOrder-input change that would resolve a different TaskSpec requires a new WorkOrder revision and admission; TaskSpec refs remain EF-owned post-admission bindings and completed metrics never authorize a retry for selection.
- [ ] Run-level validation and EvidenceCandidate qualification require every required cell to have one complete scientific result. Missing/failed/cancelled cells keep the Run diagnostic/ineligible; optional cells, dynamic append, dynamic HPO and partial acceptance do not exist in the first release.
- [ ] A new WorkOrder revision or branch never mutates/rebinds an existing or in-flight Run. `current_admitted_revision_id` and `head_run_id` remain separate explicit projections and head advancement follows D-13b.
- [ ] Each WorkOrder revision carries one PI-issued immutable branch-local revision sequence used only for fencing. The sequence is not a timestamp, scientific field, ranking signal or new policy authority.
- [ ] EF atomically persists the frozen Run/manifest and `RunManifestFrozen` outbox event. PI alone validates exact scope, advances `head_run_id` by sequence-fenced CAS and persists `BranchHeadAdvanced`; neither domain writes the other's canonical state and no cross-domain authority transaction is allowed even under one database.
- [ ] Same-sequence same-Run/manifest replay is idempotent; lower-sequence events are historical and cannot roll back head or dispatch; same-sequence conflicting Run/manifest fails closed. An event ahead of visible PI admission waits/retries rather than dispatching.
- [ ] EF cannot create the first ExecutionAttempt or dispatch until the exact `BranchHeadAdvanced` acknowledgement is durable. A stale frozen but never-dispatched Run remains lineage-only; an already executing old Run is not auto-cancelled when head advances.
- [ ] `head_run_id` means latest frozen execution lineage, not latest successful/running/best/evidence-admitted Run. Failed or cancelled latest Runs remain head and never cause automatic rollback.
- [ ] Retrieval always applies project/permission and lineage filters before semantic ranking, then resolves exact PI/EF revision/hash records; stale/unavailable semantic indexing falls back to structured queries and cannot affect execution or trust decisions.
- [ ] AuthorityGate and CoordinatorStop are separate: Gate is the only durable authorization authority; Stop is a derived pause with an owning gate/action ref and cannot create a second confirmation record.
- [ ] Product UX minimizes interventions: one owning-screen action resolves coincident Stop/Gate requirements and auto-resumes; no confirmation occurs per Run, ExecutionAttempt, safe retry, sync, collect or reconcile inside the admitted boundary.
- [ ] WorkOrder admission approves the already compiled exact cell plan once; draft compilation/preview adds no user action, post-admission validation/materialization adds no confirmation, manual promotion is off the normal PI path, external-side-effect authorization appears only for actual writes/scope expansion, and Cycle closure remains one action.
- [ ] User-visible actions are classified as InitiationAction, AuthorityAction, RecoveryAction or PlumbingAction; a single interaction is counted once by its owning transition and CoordinatorStop acknowledgement is never a fifth action class.
- [ ] The fixed T-132 first-release golden scenario proves exactly 1 InitiationAction + 2 AuthorityActions + 0 RecoveryActions + 0 PlumbingActions, regardless of the number of required cells/cell-scoped Attempts inside its one batch Run.
- [ ] Every golden scenario records theoretical-minimum versus actual user-visible actions; actual AuthorityActions equal the count of exact revision admissions, Cycle closures, required strong-claim acceptances, dossier exports, real external-effect/scope authorizations and explicit manual promotions. Extra actions require a named Gate/Stop/blocker reason.
- [ ] PI bootstrap requires completed PaperProjectIntake plus non-null matching `paper_project_intake_ref` and `target_paper_project_ref`; an unbound bridge fails closed and creates no ImplementationProject or intake snapshot.
- [ ] Primary product navigation enters PI from a bound PaperProject rather than manual bridge ID/hash input; existing null-bound PI records are handled only by D-08 legacy policy.
- [ ] Existing non-v2/null-bound/simulation-era rows remain unchanged in the database and are mechanically rejected with `LEGACY_RECORD_NOT_ELIGIBLE` by all v2 writers, selectors, execution, evidence and PI paths.
- [ ] Legacy reads are limited to existing fields through diagnostics/admin compatibility access; no summary, reason narrative, recommendation, archive UI, revalidation, comparability or cross-module legacy model is introduced.
- [ ] Standalone EF exploration remains possible, but paper consumption requires explicit WorkOrder attachment and full trust-gateway revalidation.
- [ ] A project-scoped desktop flow completes the control-plane scenario without manual internal IDs, hashes or raw JSON.
- [ ] Real Postgres, HTTP/service/repository, DOM/Electron, fault-injection and usage-fit gates are machine-verifiable.
- [ ] The first release materializes the exact Aliyun `CreateJob` payload, validates the payload offline, performs only allowlisted read-only cloud checks, and proves zero cloud writes.
- [ ] The cloud preflight credential lacks `paidlc:CreateJob`; an attempted write is blocked before transport and cannot be authorized by RAM.
- [ ] The exact materialized payload completes fake-provider submit/sync/cancel/collect/reconcile and failure/replay tests.
- [ ] The control-plane simulator may emit `workflow_simulation_passed` only; the release DB/API evidence proves zero simulator-derived scientific evidence.
- [ ] Terminal simulation Attempts leave every required cell and the batch Run scientifically `not_started`, never set scientific completed/failed/cancelled, and rebuild `workflow_simulation_status` without a second Run or scientific-state writer.
- [ ] The no-evidence Cycle closure references the head Run and terminal simulation Attempts, records `scientific_execution_status=not_started` and `evidence_eligibility=false`, and leaves EF Run/cell/Attempt scientific facts unchanged.
- [ ] Release evidence uses `cloud_preflight_passed` and explicitly lists scheduling, image pull, data mount, GPU execution, cloud logs/results and real cleanup as unverified.
- [ ] T-131's promoted v1 RAGPerf protocol remains immutable catalog history and is not execution-ready; original-source content is imported as a new typed v2 protocol and only that v2 identity is consumed by RunRecipe/TaskSpec/provider payload in the control-plane trace without claiming a real run.
- [ ] OpenAPI, API index, context registry, task docs and governance views match the delivered runtime.

## Definition of productized
The first release productizes the experiment control plane, not experiment execution: a researcher can author constraints, automatically preview/compile the exact scientific cell plan, approve the WorkOrder once, materialize exact provider payloads, inspect/recover Attempt-level simulated lifecycle state and complete real read-only cloud preflight without manual refs/hashes/JSON. Real persistence and DOM/Electron interaction must prove that control flow. `workflow_simulation_passed`, `cloud_preflight_passed` and `control_flow_validated_no_paper_evidence` never imply a terminal scientific Run, real scheduling, training, results, scientific validation, EvidenceCandidate or RunEvidenceUnit; those production claims remain deferred until a separately authorized real-provider execution gate.

## 2026-07-22 QR-1 checkpoint

Pack C quality-remediation QR-1 closes review finding R1: all three Pack C events now traverse the strict shared event union, domain outbox decoders, explicit relay routes, and durable consumers; `buildApp` composes the scientific-validation and Evidence Trust Gateway services; the scientific-validation capability is registered default-off in the env SSOT; and the pure readiness GET exposes the server-derived closure input hash. QR-2/QR-3 authority and ordering work remains out of scope. Detailed evidence is in `artifacts/pack-c-preplanning-20260718/11-qr1-wiring-report.md`.
