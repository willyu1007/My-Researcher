# Roadmap — paper-implementation-productization-hardening (T-124)

## 里程碑

| 里程碑 | 内容 | 出口判据 |
|---|---|---|
| M0 对齐 | Phase 0：JD 联合决策登记、T114_* 更名映射、manifest 字段冻结 | 决策签核，03 留痕 |
| M1 参数零死角 | Phase 1：SlotParameterManifest + 四向对账 + 裸参数负例 + overrides | runtime-stress 新 step 绿 |
| M2 上下文不卡死 | Phase 2：context profile registry 化 + 压缩执行闭环 + 长上下文金丝雀 | 双分支用例绿 + 金丝雀 passed + T-127 回归确认 |
| M3 产品可 run | Phase 3：Run Coordinator 异步自动推进（两条 lane + 单步 board）+ selection policy + 故障注入三件套 | 一次 advance 自动推完 lane A + 三件套绿 + ownership scan 绿 |
| M4 debate 按需 | Phase 4：复杂度档位 + DebatePolicy 注册 + debate kernel 抽取 + 试点升档 | 档位必检绿 + kernel 迁移对照绿 + 升档金丝雀 passed |
| M5 决策可记忆 | Phase 5：三记忆 families 写入/消费 + 负例 | 记忆必检绿 |
| M6 清债与 usage-fit | Phase 6：更名迁移 + 矩阵一致性脚本 + cost 接入 + golden rubric 首轮 | 更名后全门绿 + rubric 留档 |

## 排序原则
- M1 先行：manifest 是 M2/M4 的校验载体（context profile id、debate policy id 都挂 manifest 对账）。
- M2 与 M3 可并行：M2 是共享面（受 T-127 协调节奏影响），M3 是纯本域。
- M5 在 M4 后：复杂度信号消费记忆中的历史失败计数。
- M6 收尾：更名涉及全部脚本，放最后避免反复改 run id 前缀；usage-fit 需要 coordinator（M3）就绪。

## Cross-task PI↔EF iteration seam — T-132 D-10 through D-22
- Formal product Pack A update (2026-07-15): active PaperProject `P313` reached an admitted ValidationCycle through normal PI routes, admitted the exact two-cell WorkOrder revision, drained T1-T4 to Run/head and one EF acknowledgement, then closed admission/simulation while retaining committed named-local cutover. Final verifier `formal-pi-scope-packa-product-20260715-verify-r5` passed with zero Pack B/scientific/legacy writes. The next seam gate is separately authorized named-local Pack B E1-E5.
- PI owns ValidationCycle, WorkOrder branch/logical identity, immutable admitted WorkOrder revision, embedded exact scientific cells/`cell_plan_hash`, `approved_plan_hash`, current-revision/head projections and project-scoped retrieval. EF owns experiment TaskSpec/Run/ExecutionAttempt/result facts and binds/materializes the exact PI scope one-to-one.
- PI declares `revise | fork`; unchanged frozen branch semantic-frame hash/relation permits revision plus re-admission, while any branch semantic/relation change requires a new branch. Existing experiment Runs never rebind.
- D-13a fixes one revision→one mode-neutral required-cell batch Run; D-13b fixes EF `RunManifestFrozen` → PI sequence-fenced head/`BranchHeadAdvanced` → EF exact inbox receipt/acknowledgement before first Attempt; D-14 keeps terminal simulation at Attempt/rebuildable-control-projection level; D-15 requires PI exact cells/hash before admission; D-16 reserves REU for complete protocol-compliant validation-passed EvidenceCandidate and assigns failed/cancelled/incomplete execution accounting to the existing Cycle closure record's embedded immutable snapshot/hash.
- T-132 D-18 closure scope is one current-effective branch-head watermark: every admitted branch contributes its current admitted revision and durable head Run with complete head-Run Attempts/accounting; non-head history is excluded unless exact `comparison_input_ref` lineage is declared, and comparison never expands execution scope. A missing head, any Cycle-wide active real Attempt or Cycle/branch/revision/head CAS drift blocks closure and rebuilds without a partial write.
- D-17 makes `protocol-compliant` executable and closes the conclusion chain: EF validates one exact batch Run through canonical typed required rules and is the sole EvidenceCandidate writer; PI Result Analysis produces an exact-hash-bound proposal only; the existing ValidationCycle closure is the sole scientific-disposition authority and server-derives the selected exit; interpretation, claim, dossier, retrieval and next-step consumers require the exact closed Cycle.
- D-19 fixes the first joint implementation acceptance slice as an admission-to-head-acknowledgement spine. A pre-bound PaperProject/ValidationCycle and real readiness-passed typed v2 assets feed one admitted immutable WorkOrder revision; the acceptance fixture has exactly two required cells; EF materializes one-to-one, freezes one batch Run/manifest and emits `RunManifestFrozen`; PI sequence-fenced CAS-advances head and emits `BranchHeadAdvanced`; EF records the exact inbox receipt as the durable acknowledgement. The slice then stops with zero Attempt/provider/result/validation/evidence/closure/UI/search/legacy-migration write. It does not implement bootstrap and cannot replace D10's final golden scenario through the real bootstrap route.
- D-20 freezes four domain-owned authoritative Unit-of-Work commits: PI admission plus `WorkOrderRevisionAdmitted` outbox; EF event inbox plus exact materialization/Run/manifest/`RunManifestFrozen` outbox; PI `RunManifestFrozen` inbox plus head CAS/`BranchHeadAdvanced` outbox; EF `BranchHeadAdvanced` inbox receipt, which itself is the exact durable acknowledgement. Inbox/local authority/outbox commit together. Same-Postgres deployment does not permit cross-domain transactions, shared mutable authority tables, shared write repositories or 2PC. Exact replay, payload conflict, stale/lower sequence, same-sequence Run/manifest conflict and prerequisite retry converge deterministically; relay delivery bookkeeping is not authority and no second acknowledgement record or `dispatch_eligible` mirror exists.
- D-19 is capability-gated and closes only that first spine; it is not a retroactive change to completed coordinator slices and cannot claim the complete PI↔EF seam done. D-16/D-17/D-18 remain a later atomic writer/status/closure/reader cutover. Dossier consumes explicit closed-Cycle current-effective snapshot refs/hashes; Sidecar is display-only, and any Cycle-wide active real Attempt—including on a non-head Run—still blocks closure. The T-124 reference flow remains `1/4/0/0`; validation, proposal preparation, watermark/snapshot freeze and consumption add no action. Current S3 trusted failed/cancelled REU, mixed status, project-wide scan, caller-authored Cycle assessment/exit and open Result Analysis/packet authority are superseded historical implementation; no dual-read/full-history fallback may survive. The canonical PI adoption is in `02-architecture.md`.
- D-21 freezes independent domain-owned additive PI/EF v2 canonical table families. PI owns branch/revision/admission/head/inbox/outbox persistence; EF owns typed identity/readiness/materialization/Run-manifest/cell-binding/inbox/outbox persistence. Existing singular ResearchWorkOrder/WorkOrderHarnessRun and generic EF rows remain unchanged and existing-field diagnostics/admin read-only. No backfill, dual write, fallback, legacy trust upgrade or runtime union view is allowed.
- D-21 capability routing is default-off only for new PI v2 admission. Capability-off requests fail closed with zero PI/EF/legacy write, while an already committed admission saga drains to the durable EF acknowledgement. After D-19 acceptance, new paper-bound admission switches once to v2 and overlapping legacy writers close at the same boundary. Rollback stops new intake and preserves v2 audit/replay/drain state without restoring a legacy writer.
- D-22 freezes the first migration as the minimal Phase 1 identity/readiness plus D-19 durable-ack logical pack. Same-domain identity/unique/CAS/cell/event invariants are relational; cross-domain scope is exact external ref/hash/sequence/event without ORM/FK/cascade; explicitly named schema-versioned typed canonical JSON carries server-hashed frozen snapshots and event-specific payloads without generic kind/payload or EAV. Capability remains an admission config guard with no eligibility/dispatch mirror. Bootstrap/promotion, Attempt/provider/result/validation/evidence/closure/UI/search, legacy migration and global cutover remain outside the pack.
- T-132 Pack A technical implementation, source-backed A01-A04/B01-B10 disposable-PostgreSQL checks, named local-development DB apply/single-writer cutover and formal product admission-to-ack are complete. Pack B same-payload simulation technical implementation passed PB01-PB16 on disposable PostgreSQL; its reviewed migration is also landed on the named local-development target. That target now has the exact formal Pack A Run/head acknowledgement, but named-local E1-E5 has not run and all six Pack B tables remain empty. PI event-derived projection, D-16/D-17/D-18, the full bootstrap-to-dossier golden scenario, every non-local rollout and scientific work remain later slices. No D-23 was introduced.

## 回滚
Coordinator 里程碑可独立回滚；压缩分支与档位升档的特性开关可退回 T-114 coordinator 行为（全 blocked / 固定拓扑）。该规则不适用于 PI↔EF v2 storage/cutover：D-21 rollback 只停止新 v2 admission并保留/排空已提交 v2 saga，绝不 fallback 或恢复 singular/generic legacy writer。
