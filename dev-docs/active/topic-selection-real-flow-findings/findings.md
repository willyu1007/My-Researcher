# Real-flow findings

This register preserves the distinct observations from the 2026-09-01 real topic-selection rehearsal. It records issue identity and disposition only; task state, acceptance, design, and verification remain authoritative in their dedicated bundle documents.

| ID | Observation | Initial severity | Proposed disposition |
|---|---|---|---|
| FIND-001 | The human evidence-landscape view exposes mostly roles and IDs instead of the evidence statements, mechanism, falsifiers, and claim ceiling needed for a decision. | high | In scope: improve the canonical human projection. |
| FIND-002 | The human view reports no open risks while the checkpoint packet includes a material conflict set and disconfirming evidence. | high | In scope after confirming the intended conflict/risk semantics. |
| FIND-003 | The public EvidenceMap request schema does not describe material fields and enums accepted and validated by the runtime route. | medium | In scope after confirming the intended public surface. |
| FIND-004 | A dev-backend exit caused one checkpoint decision request to lose its response; read-before-replay recovered safely through the decision key. | medium, unconfirmed | Reproduce and classify before assigning product work. |
| FIND-005 | Backup/restore readiness could not be demonstrated during preflight. | medium | Adjacent operational gap; propose a separate owner if still material. |
| FIND-006 | The current SciFact workload exposes aggregate fixed-k output but no per-query signal required by the new topic. | medium | Adjacent experiment-asset gap; do not treat as a topic-selection defect. |
| FIND-007 | The five-paper EvidenceMap missed several 2026 papers that directly cover calibrated retrieval-budget allocation, adaptive-k, retrieval-utility prediction, marginal correctness, and harm-aware budget frontiers. | high | In scope: diagnose coverage/freshness ownership and surface unresolved near-duplicate risk before human advancement. |
| FIND-008 | Embedding persistence serialized one pgvector `UPDATE` per chunk inside Prisma's default 5-second interactive transaction. The replacement-topic run reproduced the failure at 205 chunks: three smaller papers completed, while `LIT-2297` failed after the transaction expired at 5.010 seconds and left the four-paper job partial. | high | Closed by T-149: one validated bulk pgvector update replaced the serial transaction; the original job recovered all 205 vectors with `reused_existing: true`, indexed successfully, and the topic pool reached 14/14 active without a new provider request. |
| FIND-009 | The persisted literature-acquisition downloader limit is 1 KiB with a 5-second timeout and one redirect, so the default fulltext job plan accepts normal arXiv PDFs but the resulting download is effectively guaranteed to fail. | high | In scope: distinguish deliberate test settings from runtime defaults and expose an actionable preflight blocker when effective limits cannot admit the planned assets. |
| FIND-010 | EvidenceMap reference admission compares the full functional-ref shape, while the SearchRun coverage projection returns unscoped `literature_record` and `fulltext_paragraph` refs; adding the logically correct `title_card_id` caused a generic “outside SearchRun” rejection until the refs were replayed in the exact unscoped shape. | medium | In scope: normalize equivalent refs or make the required exact reference shape explicit, and identify the rejected field/ref in the error. |
| FIND-011 | Recording an exact strict-human `reject` decision closes the checkpoint but leaves the rejected TitleCard's management status as `active`, so the title-card list can present both the rejected topic and its replacement as active. | medium, unconfirmed | In scope: confirm whether checkpoint rejection should park the TitleCard or whether the workbench must derive and display a terminal research disposition separately. |
| FIND-012 | Literature-dependent topic-selection steps can be completed from caller-supplied SearchRun/EvidenceMap material without proving that a retriever was invoked, and there is no topic-level persisted ledger from which the end-to-end retrieval frequency and convergence contribution can be recovered. The four-call replacement batch also proved that a lost response from the non-persisting retrieval boundary cannot be recovered without replay. | high | Reproduced and operator-instrumented. In scope: choose the smallest product correction between mandatory retriever provenance and a persisted topic-level retrieval ledger, including durable response/telemetry recovery. Keep retriever operations, provider calls, and zero-call evidence materialization distinct. |
| FIND-013 | The evidence-landscape checkpoint gate checks claim-bearing EvidenceUnit roles but does not consume the latest required SearchPlan coverage-assessment verdicts. Both targeted child rows remained `missing`, yet rematerializing adjacent support/context units produced `eligible_for_human_review`, no required actions, and an allowed `advance`. | high | In scope: make required coverage outcomes part of checkpoint eligibility and human presentation. A required `missing` result must remain visible as an explicit accepted risk or research obligation rather than being implicitly satisfied by adjacent evidence. |
| FIND-014 | After v1b bundle publication and later N1 intake admission, research status still derives `topic-selection.research.publish-v1b-input` solely from the decided gap-selection checkpoint while simultaneously requiring `question_contract`; it does not report whether the bundle was published or consumed into v1b. | medium | In scope for classification: make the projection consumption-aware or label this value as the checkpoint-authorized transition rather than the current executable frontier. The run coordinator, not research status, currently owns the actual v1b frontier. |
| FIND-015 | The generic OpenAPI v1b invocation request permits `run_mode` for every node, but deterministic-only N1 rejects a non-null value at runtime. A schema-valid request therefore produced a durable blocked attempt before the corrected request succeeded. | medium | In scope for contract alignment: add node-specific conditional restrictions or document the runtime-field policy so deterministic invocations do not create avoidable blocked audit records. |
| FIND-016 | N6 declares frozen snapshot kind `research_slice_selection_decision`, while the admitted N5 authority and N5-to-N6 handoff identify the same object as `slice_selection_decision`. Directly consuming the exact persisted refs produced durable blocker `FROZEN_INPUT_SOURCE_REF_KIND_MISMATCH`; adding a same-ID alias ref allowed N6 to proceed. | medium | In scope for contract alignment: make N5 emit the N6-required ref kind, change the N6 snapshot-kind contract to the persisted kind, or canonicalize the two names before the generic source-ref guard. Do not require callers to synthesize an undocumented alias. |
| FIND-017 | The human `research_question` stage view omits the actual main question and reports `开放风险：暂无`, while the active contract is `answerable_with_risk`, N7 emitted two human-review triggers plus three risk notes, and the canonical checkpoint packet carries known gaps, open dependencies, confounds, and falsification conditions. | high | In scope: make the human question-checkpoint projection present the question and derive risk/dependency language faithfully from the canonical packet and N7 contract rather than treating an empty objection list as no risk. |
| FIND-018 | N6 has an implemented two-Explorer/one-Critic/one-Arbiter divergent Debate, but the normal initial path is single-agent and Debate is reached only after gate failure plus triage escalation. This allowed a consequential recommendation carrying human-review triggers and risk notes to reach N7 without routine adversarial convergence. | high | User-confirmed product direction: make a bounded Debate a regular N6 component, reuse the existing single deterministic gate, and keep deeper escalation conditional so routine review does not become unbounded machinery. |
| FIND-019 | The operator repeatedly requested permission to restart the same loopback development backend during one already-authorized research flow, adding ceremony without changing environment or effect scope. | workflow friction | User-confirmed operating rule for this rehearsal: continuing authorization covers safe starts/restarts of the same local backend; ask again only for environment changes, provider/cost work, destructive/control effects, or a new Human-authority decision. Classify separately from a product defect unless implementation proves the runtime lifecycle itself owns the friction. |
| FIND-020 | N9 correctly records `refine_question` as a terminal non-advance disposition and stores the active TopicQuestionContract as `loopback_target_ref`, but emits no handoff, route target, supported coordinator recovery path, or independent contract-refinement writer. After the researcher accepted an exact refinement contract, the run still cannot materialize it through the product boundary. | high | In scope: add one explicit, replay-safe non-advance recovery route owned by the disposition contract. Do not turn N9 into a second human/model judgment gate; route the recorded deterministic decision to the proper refinement boundary. |
| FIND-021 | After N8 recorded a 66-point `needs_refinement` assessment, 25 material risk findings, and N9 recorded terminal `refine_question`, research status still reports `topic-selection.research.assess-topic-value` and the human research-question view still says `开放风险：暂无` with promotion as the next review. | high | In scope with FIND-014/FIND-017: make v1b consumption, value assessment, disposition, and material risks part of the current research-status and human-view projection. The coordinator remains execution authority, but the human projection must not contradict it. |

## Retriever convergence observations

This table records the smallest current evidence needed to judge whether retrieval drove convergence. It is not a raw request log or a second workflow authority. Future rows must use product response telemetry when available and leave unknown values explicit.

| ID | Lineage stage / trigger | Query intent and mode | Retriever calls | Provider calls | Evidence delta | Decision or convergence effect | Assessment |
|---|---|---|---:|---:|---|---|---|
| RET-001 | Rejected lineage, literature-readiness QA before the refreshed evidence checkpoint | One explicit topic-scoped semantic/hybrid retrieval QA; exact query text and response telemetry were not retained in the task record. | 1 observed | unknown | Returned direct neighbors without degraded mode, but the resulting five-paper landscape still missed directly overlapping 2026 work. | Confirmed basic retrieval readiness; did not establish novelty coverage. | Insufficient: one retrieval pass was treated as readiness evidence, not a bounded convergence loop. |
| RET-002 | Replacement lineage from fresh TopicSeed through the 14-paper active pool | No selection retriever call; literature discovery, curation, embedding, indexing, and activation are corpus preparation rather than topic convergence retrieval. | 0 | 0 | Four recent/direct-neighbor papers became active; no retriever-ranked evidence packet exists yet for the replacement TitleCard. | Removed corpus-readiness blockers only. | Correctly classified as pre-convergence; the next literature-dependent step must start a recorded retrieval loop. |
| RET-003 | Replacement lineage resource-pool snapshot and SearchPlan | Frozen 14 active papers, then planned four required topic-scoped semantic/hybrid intents: support mechanism, nearest-work challenge, baseline falsifiers, and evaluation context. | 0 | 0 | Snapshot and four `not_run` coverage rows were created; no hit set or evidence delta exists yet. | Made the intended convergence loop and stop condition explicit without falsely claiming retrieval. | Ready but unexecuted: the next boundary is four distinct query embeddings, pending exact provider authorization. |
| RET-004 | Replacement lineage, execution of the four-intent convergence batch | Four topic-scoped semantic/hybrid queries for mechanism support, nearest-work challenge, falsifying baselines, and evaluation context; `top_k=8`, three evidence passages per literature record. | 4 | 3 confirmed; 4 possible maximum | Three retained responses produced 24 hits and 12 unique records. Challenge and baseline coverage became satisfied; support and context remained partial. The first response and its telemetry were not retained. | Narrowed the defensible contribution to low-label recalibration and safe fallback for a frozen router after explicit retriever/index replacement; identified direct-transfer evidence and a replacement-specific benchmark as the remaining gaps. | Productive convergence, but trace completeness failed at the non-persisting response boundary. Confirmed provider telemetry: 63 tokens and `$0.00000819`. |
| RET-005 | Replacement lineage, exact source/locator authority normalization for EvidenceMap admission | Re-materialized the retained evidence using exact SearchRun literature-source and locator refs; no query or corpus search was performed. | 0 | 0 | Ten exact authority bindings made the retained evidence admissible; no new semantic evidence was added. | No change to the topic judgment; enabled the current nine-unit EvidenceMap and exposed the ref-shape normalization gap again. | Correctly separated evidence materialization from retrieval so frequency is not inflated. |
| RET-006 | Replacement lineage, strict-human evidence-landscape disposition | Reviewed the retriever-backed EvidenceMap and its remaining support/context gaps; no new query was executed. | 0 | 0 | No evidence delta; the reviewed snapshot remained unchanged. | The researcher explicitly chose `loopback` to `evidence_or_search`, preserving the two partial coverage intents and material conflict as the next route. | A genuine convergence decision built on the preceding retrieval batch; correctly recorded as zero additional retrieval. |
| RET-007 | Replacement lineage, provider-free recheck planning | Materialized a child SearchPlan with only direct frozen-router transfer support and retriever-replacement benchmark context intents; reused the frozen parent snapshot. | 0 | 0 | Two new required coverage rows are `unassessed`, with zero bindings and no SearchRun. | Converted the human loopback into a bounded two-query stop rule while preserving satisfied challenge/baseline coverage. | Planning and materialization are correctly distinct from retrieval; the next boundary is exactly two provider-backed query embeddings under fresh authorization. |
| RET-008 | Replacement lineage, bounded targeted recheck | Ran the direct frozen-router transfer and retriever-replacement benchmark intents once each against the frozen topic scope; semantic/hybrid mode, `top_k=8`, three passages per record. | 2 | 2 | Sixteen hits collapsed to 10 unique records. Both rows remain `missing`: adjacent utility-routing, calibration, retriever-training, and routing-benchmark work was found, but no direct frozen-router replacement study or replacement-specific benchmark. | Strengthened the narrow novelty boundary while making benchmark construction a first-class feasibility requirement. | Productive negative convergence. Complete telemetry is durable: 49 embedding tokens, `$0.00000637`, no degraded mode, retry, timeout, or rate-limit event. The stop rule forbids automatic broadening or replay. |
| RET-009 | Replacement lineage, exact authority rematerialization | Preserved the nine retained units' exact literature, source, and locator refs in a child-plan SearchRun; no query or corpus search was performed. | 0 | 0 | Added no semantic evidence and did not change either `missing` verdict; enabled a fresh EvidenceMap and checkpoint from the completed recheck lineage. | No independent decision effect; made the already observed evidence and negative recheck recoverable at the next checkpoint. | Correctly kept API authority repair out of retriever frequency. SearchRun `search_run_e0b26e0c-f6b6-4c6e-8142-2e64a59005c9` records the zero-call boundary explicitly. |
| RET-010 | Replacement lineage, human advance and gap-candidate materialization | Reviewed the new recheck-bound EvidenceMap, recorded the user's exact `advance`, and created method-gap and data-gap candidates. | 0 | 0 | No literature delta. The evidence checkpoint closed and the gap-selection arena now contains two distinct machine-viable candidates. | Accepted benchmark construction as a core obligation and moved convergence from evidence search to choosing the primary research gap. | Correctly advanced from retriever-backed evidence without pretending that human choice or candidate generation was another retrieval event. |
| RET-011 | Replacement lineage, method-gap selection preparation | The researcher selected the method-gap candidate; the system ran deterministic readiness and built its validation support packet without any literature query. | 0 | 0 | No literature delta. The selected candidate is `ready_for_validation` with zero blockers or warnings; the packet retains the direct-evidence gap, benchmark obligation, counter-evidence, and claim ceiling. | Narrowed the candidate arena to the intended primary need while preserving the data-gap candidate as a viable alternative and required asset. | Correctly stopped before Human-authority adjudication: candidate selection is not treated as implicit `validate`. |
| RET-012 | Replacement lineage, exact method-gap validation and v1b publication | Recorded the researcher's exact `validate`, confirmed the frozen two-candidate gap review, and published the resulting ValidatedNeed to a v1b input bundle; no literature lookup was needed. | 0 | 0 | No literature delta. The method candidate closed as `resulted_in_validated_need`; the data candidate remains a viable alternative and required benchmark asset, while the residual challenge/conflict refs remain attached to the handoff. | Closed the strict-human gap-selection checkpoint and moved the mainline to the v1b `question_contract` boundary without changing the evidence judgment. | Correct convergence: a human decision consumed the retriever-backed packet, while authority writes and deterministic publication did not inflate retrieval frequency. |
| RET-013 | v1a-to-v1b intake handoff | Recovered the existing bundle idempotently, validated its canonical bundle/source hashes, and admitted deterministic N1; no literature query was performed. | 0 | 0 | No literature delta. Intake `v1b_intake_snapshot_9bd249fc-d08d-48a8-8d8f-619062311685` carries the current EvidenceMap, search, human-decision, and three residual-risk refs. | Established the v1b owner and moved the frontier to the N2 research-constraint profile without changing the scientific claim. | Correct zero-retrieval handoff. The coordinator stopped at Human authority; one earlier schema-valid runtime-field attempt remained blocked and created no intake. |
| RET-014 | v1b N2 constraint acceptance and N3 readiness | Recorded the researcher's exact acceptance of the complete replacement-only constraint profile, then ran deterministic readiness against the frozen intake/profile lineage; no literature query was performed. | 0 | 0 | No literature delta. Profile `research_constraint_profile_61ddf106-8c81-45c0-ab7c-db848f12f128` fixes the claim ceiling, matched budgets, required paired benchmark, baselines, fallback, and non-goals; readiness `v1b_intake_readiness_2626d502-ca13-4838-8a9e-721daddc2211` has zero blockers and warnings. | Closed N2 Human authority and advanced the coordinator to N4 research-slice option generation without creating an option set or choosing a slice. | Correct evidence consumption: an exact human constraint decision and deterministic gate consumed the existing retriever-backed record without inflating retrieval frequency. One malformed caller request was rejected by schema validation before persistence and then corrected under the same stable attempt ID. |
| RET-015 | v1b N4 research-slice option generation | Read the frozen N3 handoff and nine inherited EvidenceUnits, then submitted one contract-shaped Codex-assisted three-option draft through the N4 deterministic gate; no corpus query was performed. | 0 | 0 | No literature delta. Option set `research_slice_option_set_5d8f12a4-2ca5-4556-807a-32cc24cae724` preserves all evidence roles, five exact non-goals, matched budgets, the bounded benchmark obligation, and three distinct risk/budget profiles. | Produced one balanced recommendation plus one ambitious high-risk and one conservative low-risk candidate, then advanced only to N5 Human authority without selecting a slice. | Correct zero-retrieval synthesis. The three warnings accurately carry unresolved comparison choices, one high-risk option, and mandatory human review; none is a blocker. |
| RET-016 | v1b N5 exact research-slice selection | Re-read the option set and three options, then recorded the researcher's exact confirmation of `low_label_recalibration_with_abstention` through the Human-authority boundary; no corpus query was performed. | 0 | 0 | No literature delta. Decision `slice_selection_decision_8f5480bb-7c1d-4de8-9eec-2d4b06e770a4` materialized ResearchSlice `research_slice_b079bbfa-9197-407a-9447-2decbcf6ab67` and preserved the two non-selected options as candidates for explicit loopback. | Closed N5, carried the bounded slice contract to N6, and stopped before generating topic-question candidates. | Correct zero-retrieval authority step. The admitted warnings preserve human-review context and make the non-mutating alternative disposition explicit; neither is a blocker. |
| RET-017 | v1b N6 bounded topic-question generation | Read the selected option, exact N5 handoff, evidence-role refs, and empty candidate-set owner; submitted one three-candidate Codex draft, then corrected only the frozen snapshot-ref kind under a new attempt after the first pre-authority block. | 0 | 0 | No literature delta. Candidate set `topic_question_candidate_set_b6faa9c2-7d32-41ba-bf86-84a896622d53` maps all inherited evidence roles, retains the benchmark gap, and contains three admissible answerable-with-risk questions under the same budgets. | Recommended the joint calibration-and-harm-control question, preserved calibration-transfer and selective-abstention alternatives, and advanced only to N7 mechanical materialization. | Correct zero-retrieval synthesis. The first attempt exposed FIND-016 and created no candidate authority; the corrected attempt admitted with human-review and risk-note warnings, not blockers. |
| RET-018 | v1b N7 mechanical contract materialization | Re-read the frozen N6 candidate set and handoff, applied the registered recommendation-first candidate priority, materialized the contract, and inspected the strict-human packet and human stage view; no corpus query was performed. | 0 | 0 | No literature delta. The recommended question became the active TopicQuestionContract while both alternatives remained candidates; all inherited evidence, benchmark obligations, baselines, falsifiers, and claim ceiling remain linked. | Advanced the machine frontier to N8 but opened a pending strict-human question checkpoint, so N8 remains unauthorized and unexecuted. | Correct zero-retrieval materialization and stop. The canonical packet is decision-rich, while the human projection exposed FIND-017. |
| RET-019 | v1b strict-human question-contract disposition | Re-read the pending checkpoint packet, recorded the researcher's exact `advance`, and recovered the N8 frontier; no literature query or model provider was invoked. | 0 | 0 | No literature delta. Decision `research_checkpoint_decision_66155600-f58d-4c4e-8369-86f56c41dc15` preserved the answerable-with-risk contract, benchmark obligation, and unresolved metric ordering rather than silently choosing operator-recommended metrics. | Closed the question checkpoint and authorized N8 value assessment without changing the scientific evidence. | Correct zero-retrieval Human-authority transition; metric selection remained explicitly unresolved. |
| RET-020 | v1b N8 value assessment, bounded Debate, and N9 disposition | Submitted one Codex-assisted value draft, followed the registered T1 loopback through N7 debate admission, ran the four-role Assessor/Critic/Repair/Synthesizer sequence, and executed deterministic N9; no corpus query or external model provider was invoked. | 0 | 0 | No literature delta. Debate preserved the same evidence ceiling but converted a borderline `advance_to_package` draft into a 66-point `needs_refinement` assessment with explicit metric, coverage, benchmark-validity, and baseline obligations. | N9 recorded terminal `refine_question` and correctly prevented N10 packaging. The missing recovery route and stale projections are FIND-020/FIND-021. | Productive evidence reuse: repeated literature lookup was unnecessary because the disagreement concerned interpretation and experiment-contract completeness, not missing literature. Retriever frequency correctly remained zero. |
| RET-021 | v1b Human acceptance of the refinement contract | Recorded the researcher's exact acceptance of the complete proposed refinement contract and inspected supported product routes; no corpus query, provider, or workflow write was performed. | 0 | 0 | No literature delta. The primary/secondary metrics, quality margin, coverage floor, and benchmark-validity checks are now fixed as Human-authority content in the rehearsal record. | Resolved the semantic choice but could not create a new TopicQuestionContract because terminal N9 has no executable upstream route. | Correct zero-retrieval stop: the missing information was a human experiment-design decision, not a literature gap. Refusing an ad hoc write preserves authority and makes FIND-020 observable. |

## Controls that worked

| ID | Observed control |
|---|---|
| PASS-001 | Rejecting the low-mineability topic and replacing it produced fresh TitleCard and downstream lineage while reusing only eligible literature assets. |
| PASS-002 | The evidence-landscape checkpoint remained strict-human and prevented automatic NeedCandidate creation. |
| PASS-003 | EvidenceMap creation preserved native paragraph-level locators, support/challenge/baseline/context roles, and material conflict structure. |
| PASS-004 | A response-loss retry was safely recovered by reading the owner projection and exactly replaying the checkpoint decision key. |
| PASS-005 | The complete literature path—dedup/import, fulltext dry-run and acquisition, citation/abstract/fulltext processing, curated key content, chunking, embedding, indexing, activation, and scoped retrieval QA—produced a 10/10 active topic pool. |
| PASS-006 | The refreshed snapshot, SearchPlan, SearchRun, and seven-unit EvidenceMap generated a new pending strict-human evidence-landscape checkpoint; no NeedCandidate or authorized automatic transition was created. |
| PASS-007 | An exact human `reject` decision preserved the old lineage, and replacement created a fresh TitleCard and TopicSeed with zero inherited evidence, NeedCandidates, or authorized transition. |
| PASS-008 | Four bounded retrieval intents produced an observed SearchRun, a nine-unit current EvidenceMap, and a new pending strict-human checkpoint without creating a NeedCandidate or authorized automatic transition. |
| PASS-009 | The exact human `loopback` decision closed the replacement checkpoint idempotently, preserved explicit evidence/search refs, and still created no automatic transition or provider work. |
| PASS-010 | The loopback materialized a parent-linked two-intent SearchPlan with no SearchRun, no provider work, and a one-pass stop condition that prevents repeated unbounded retrieval. |
| PASS-011 | The two authorized recheck responses and provider telemetry were persisted in a raw SearchRun artifact before interpretation, so the convergence record remains recoverable without an unauthorized replay. |
| PASS-012 | Exact `advance` was recorded only against the new pending evidence checkpoint, preserved the superseded loopback decision, accepted benchmark construction explicitly, and stopped again at a strict-human gap-selection checkpoint with two distinct viable candidates. |
| PASS-013 | Selecting the method gap triggered only deterministic readiness and support-packet preparation; the product did not create an adjudication, ValidatedNeed, or downstream transition without a separate exact human validation decision. |
| PASS-014 | Exact `validate` produced one adjudication, one HumanConfirmedDecision, one ValidatedNeed, and one v1b input bundle; it closed the frozen gap-selection checkpoint, preserved the benchmark alternative and residual risks, used zero retriever/provider calls, and stopped before the next v1b research-shaping boundary. |
| PASS-015 | Existing bundle recovery was idempotent; corrected deterministic N1 admitted exactly one intake snapshot, preserved full v1a lineage and residual risks, and the coordinator halted before N2 rather than fabricating a researcher's constraint profile. |
| PASS-016 | Exact acceptance created one N2 constraint profile, deterministic N3 admitted one readiness assessment with no blocker or warning, and the coordinator halted at N4 with zero option sets rather than fabricating a research-slice choice. |
| PASS-017 | One Codex-assisted N4 attempt generated exactly three evidence-bound selectable options, preserved the accepted claim ceiling and quantitative budgets, emitted a recoverable N4-to-N5 handoff, and halted before Human-authority selection. |
| PASS-018 | Exact human confirmation created one N5 decision, one selected ResearchSlice, and one N5-to-N6 handoff; the option set and chosen option are `selected`, both alternatives remain recoverable candidates, and N6 has zero attempts. |
| PASS-019 | The corrected N6 attempt created one ready-for-selection candidate set with three admissible candidates, one recommendation, zero blocked candidate contexts, and a recoverable N6-to-N7 handoff; N7 remains unexecuted. |
| PASS-020 | One deterministic N7 attempt admitted the recommended candidate, preserved both alternatives, materialized one active TopicQuestionContract and recoverable N7-to-N8 handoff, opened a pending strict-human question checkpoint, and left N8 at zero attempts. |
| PASS-021 | Exact human `advance` closed the frozen question checkpoint once, preserved its answerability risks and unresolved metric ordering, and exposed N8 without adding retrieval or provider work. |
| PASS-022 | N8's borderline gate triggered the registered N8→N7 loopback; N7 admitted compact Debate support; all four Codex-assisted Debate roles were runtime-verified; the repaired N8 assessment was admitted with warnings; and deterministic N9 prevented package creation by recording `refine_question`. |
| PASS-023 | The researcher's exact refinement contract was preserved without silent value changes, no retriever/provider work was miscounted, and no unsupported write was used to impersonate a new TopicQuestionContract while N9 recovery remains unavailable. |
