# 14 D10 终验收工单（规划稿 2026-07-18，待用户确认后执行）

## 定位
- D10（用户签核 2026-07-10）：T-124 完成定义 = **3 条 golden scenario 全链自动推进至 dossier ready + 五项验收**。素材面已于 G5 达成（gs-001/002/003 三链正式版全达 dossier ready）。本工单 = 五项验收的**正式核验**：验证性切片，零新功能，产出一份可签核的终验收报告。
- **次序建议（对原计划的修订，待用户裁定）**：原排序 golden×3 → D4/D6 → D10；建议改为 **D10 现在执行、D4（memory families）/D6（命名）延后为验收后硬化**。理由：①D10 五项验收不引用 D4/D6 的任何能力；②三场景证据现在最新鲜（产品态=验收态，中间不插入行为改动）；③D4 是特性开发，插在验收前会使验收对象漂移、golden 证据需重跑。

## 五项验收核验方案

### A1 血缘无断链
- 证据基础：gs-001 run 014 血缘 LIVE **20/20**、gs-002 run 002 passed、gs-003 run 002 17/20（3 项失败已定性：claim 强度诚实降档×1 + skeptic blocked 下游确定形状×2——非断链）。
- 核验动作：①血缘断言器自审（20 项检查逐项与设计血缘图对照，确认覆盖 dossier→claim→packet→REU→WO→probe→route 全部环节且无恒真项）；②gs-003 的 3 项失败在验收报告中正式裁定为"合规差异"并给出裁定依据；③抽 1 条链人工逐环核对 ref/hash（机器断言的独立复核）。

### A2 四点停驻集（自动化优先原则的对偶验证）
- 命题双向：**四点必停** + **四点之外不停**（人不做流程内闸门）。
- 必停证据：skeptic 非 proceed→waiting_review（runs 006-014 多次）、强 claim 确认（三场景，含内容绑定）、dossier export（三场景终点）、预算门（TIER_BUDGET_INSUFFICIENT→loop_budget_review L5 + budget_exhausted 队列语义 L5/集成）。
- 不停证据：三场景正式版 run 的全部非四点节点自动推进记录（从 run 步序机器提取"零人工介入节点清单"）；curation revise/motive 确认类停驻逐一裁定归属（waiting_review 家族=第 1 点 skeptic 非 proceed 的同族语义停驻，验收报告明确其归类）。

### A3 治理门对抗不可穿透
- 证据基础：L5 对抗必检全集（77+ 用例：伪造 disposition/blocked 冒充/echo 漂移/identity 漂移/hollow request/后门直改 payload 仍 400/预算投毒/围栏 hash 漂移）+ N7 双侧实证 + 确认单次燃烧/内容绑定。
- 核验动作：①对抗面清单化——把审查以来全部攻击类别（N1-N9、复审各轮 CONFIRMED 攻击路径）编目，逐条映射到钉死它的测试/gate（缺口=验收缺陷）；②**新鲜对抗探针一轮**（红队 agent 对 running app 实施 8-10 条穿透尝试：伪造 REU support、绕确认物化强 claim、豁免列表洗白、export 前直改 dossier 状态等——全部应被 4xx/语义拒绝）；③全量 stress+near-prod 终跑作为验收时点快照。

### A4 成本重付率有数
- 证据基础：S4 遥测 sink + 三场景正式版基线（$3.00/8.8%、$3.11/14.9%、$2.33/9.3%）+ run 007 全档位对照基线 + 重付口径双层定义（run-local/项目级）。
- 核验动作：三场景遥测汇总表入验收报告（总成本/调用数/重付率/per-slot top/档位分布）；重付率口径的可复算性抽验（从原始 telemetry 记录手工复算一个 run 的重付率与端点一致）。

### A5 rubric 四维达标
- 证据基础：三场景正式版 rubric-scored.md（AI 代评审留档；gs-001 历史五评轨迹 4.7→4.9 不劣化）。
- 核验动作：①三场景终版评分汇总（各维≥4.5 为达标线——沿用历史评审的隐含线，验收报告显式化该阈值供用户确认）；②**人工复核可覆盖声明**：AI 代评审为最终评分的前提是用户抽验认可——验收报告附"建议抽验点"（每场景 1-2 个最值得人眼看的节点产出）。

## 执行形态
1. **E1 证据汇编 agent**：产出 `06-d10-acceptance-report.md`（五项验收矩阵，每项=命题/证据指针/机器可复验命令/裁定建议），素材=本包 03/04 全部 run id 与 run 目录。
2. **E2 红队探针 agent**：A3 的新鲜对抗轮（8-10 条穿透尝试，live app in-memory，全部预期被拒；任何穿透=验收阻断项立即修复）。
3. **E3 终跑快照**：全量 stress + prisma smoke + near-prod gate + 三场景 mocked 冒烟（验收时点的可复现状态锚）。
4. **E4 用户签核**：报告呈交（含 A5 抽验点与 gs-003 三项血缘差异裁定、A2 停驻归类裁定两处需用户点头的判断），用户核可后 T-124 标记完成、包归档；GAP-N1/N2、light 地板探针、D4/D6 移交后续任务（建议新开轻量 tracker，如 T-124 尾巴模式）。

## 预计规模
E1/E2 并行（各 1 agent）+ E3 门（~30 分钟）+ E4 用户时间。无迁移、无产品代码改动（除非 E2 穿透触发修复）。
