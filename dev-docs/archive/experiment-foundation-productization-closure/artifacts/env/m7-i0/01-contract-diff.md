# M7-I0 environment contract diff

The change is additive and backward-compatible. Both keys are optional booleans with a `false` default. Existing environments therefore retain zero real-provider intake and zero real-provider command draining until a later explicit rollout authorization.
