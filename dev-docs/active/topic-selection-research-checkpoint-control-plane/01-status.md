# Status

## Goal
Make topic selection a product-governed research process in which current evidence, gap, question, and promotion checkpoints—and not client behavior—control academic-quality review, human participation, loopbacks, and downstream eligibility.

## Progress
- State: in-progress
- Current phase: Phase 6 — fresh dual-track product-path acceptance
- Next step: Obtain explicit authorization for deterministic current-lineage N11 `topic-selection.v1b.publish-v1c-input-bundle.v1`. N10 has created a ready draft TopicPackage and its unpublished v1c input bundle; stop after publication and do not infer v1c preparation or promotion authority.
- Blocker: Run `workflow_run_t147_a9_pos_v1b_current_gap_v1` is intentionally stopped before N11; no technical blocker exists. Deterministic N10 attempt `node_attempt_t147_a9_pos_v1b_current_gap_n10_v1` is `admitted_with_warnings`, and N11 has zero attempts. The title-card research-status projection may still expose old pending promotion checkpoint `research_checkpoint_35406533-fac3-4cdb-b6d9-29296ca66c63`, but that checkpoint belongs to the stale package lineage and must not be retried. No current published v1c handoff, current HumanPromotionDecision, bridge, PromotionDecision, PaperProject, or provider execution exists on the repaired lineage.

## Latest checkpoint

- The approved `pnpm backend:dev:prisma` process is healthy at `http://127.0.0.1:3000`, runs from this repository's `apps/backend`, and passes database-backed identity reads. The historical `ai-rag-finetuning-2022-2026` literature scope remains readable and intent-aligned even though its profile is absent from `/topics/settings`: the scoped overview reports 410 literatures, all 410 in-scope, active, and high-confidence at the preflight read. No profile repair or corpus mutation was performed.
- The repository exposes no bounded backup/restore command to verify. Phase 6 therefore remains append-only in the local development database, with clearly labeled owner records, no destructive action, no external corpus pull, and no provider/cost execution.
- Fresh negative and positive TitleCards own source-grounded v1a records through EvidenceMap and EvidenceStrengthAssessment. Both search-plan coverage matrices are 4/4 satisfied, every core unit is backed by an indexed fulltext paragraph, and both evidence-landscape packets reported `eligible_for_human_review` with no policy issues or open objections. The user authorized exact `advance` decisions against both unchanged snapshots; the product recorded decision owners `research_checkpoint_decision_26f6989c-67d1-4ed6-8f02-a01ed95939c9` (negative) and `research_checkpoint_decision_4f660942-b4fc-4b54-90df-c9d44af4e55b` (positive). Both research-status projections now require `gap_selection` and advertise `topic-selection.research.generate-need-candidates` as the next transition. Exact recovery refs are in `artifacts/phase6-a9-owner-map.md`.
- The provider-free candidate-arena checkpoint is decisive. The negative lineage owns two machine-viable fixed-depth candidates, but both resolve to semantic group `db876292...`; current checkpoint `research_checkpoint_c5f4af13-ab65-4998-ab38-a681702dccaf` reports `loopback_required`, only `GENUINELY_DISTINCT_ALTERNATIVE_REQUIRED`, and no `advance`. The positive lineage owns one uncertainty-conditioned allocation candidate and one adaptive listwise ranking/pruning candidate in two distinct semantic groups; current checkpoint `research_checkpoint_36c4ad76-6a0c-4dcd-bcf9-ab326f893bd5` reports `eligible_for_human_review`, no issues, no required actions, no objections, and advertises all four human actions. Execution stopped before the strict-human gap decision. No provider or corpus mutation ran.
- The user then confirmed the recommended positive candidate disposition. Canonical machine preparation created readiness owner `need_readiness_17887e0e-9dd6-442e-b73f-a79a658395d9` with `ready_for_validation`, zero blockers/warnings/actions, two support, one challenge, zero abstract-only support, and zero strong unresolved challenges. Support owner `validation_packet_3fb847c6-4cda-4ffe-99ab-01c3d7236210` is `ready`, carries five explicit human checks, zero residual risks/gap codes/conflicts, and records the same selected/alternative recommendation. The gap checkpoint and hashes remained unchanged. Because the support packet was created after the user's confirmation, no adjudication or HumanConfirmedDecision was written; execution stopped for exact review of the current packet.
- The user subsequently confirmed that exact current packet and all required gap-review semantics. The pre-write API-contract check found that the registered `POST /topic-selection/v1a/adjudications/{adjudicationResultId}/human-confirmations` route was absent from OpenAPI, while the adjudication request incorrectly advertised an unused `human_actor`. The contract now documents the path, exact HumanConfirmationInput/gap-review structures, `422`, and a response that requires ValidatedNeed; one focused drift test locks the route/schema seam. `pnpm typecheck`, YAML parsing, the full v1a route integration file, and all related contract-drift tests pass. No human authority was written while the public contract was incomplete.
- The exact confirmed v1a authority is now complete. Adjudication `need_adjudication_7f392c9e-830b-4d5d-b0db-9a1adbbed677` reserved and HumanConfirmNeed materialized `validated_need_b79c2b97-4cfc-4533-9b1f-f93cad469e70` under `human_decision_24db76e9-b8ff-44bf-a972-c07556ebd892`. The selected candidate is closed and `human_confirmed`; the gap checkpoint is decided against the unchanged `699cc0...f5a5` candidate-pool hash, with no risks, gaps, actions, or objections. Bundle `v1b_input_bundle_dff55096-06f1-4a0d-84f8-b00e091367c5` published version `t147-a9-pos-v1b-input-v1`.
- v1b run `workflow_run_t147_a9_pos_v1b_v1` recovered the exact bundle and admitted N1 intake snapshot `v1b_intake_snapshot_05a8b8e9-0973-48d9-8b87-9df1609722e0`. The first N1 attempt was truthfully blocked by `INVALID_NODE_RUNTIME_SPEC` because deterministic N1 rejects `run_mode`; owner and trace reads proved no intake write, and retry `node_attempt_t147_a9_pos_v1b_n1_v2` removed that field and admitted with no blockers/warnings. This established the strict-human N2 frontier used by the next checkpoint.
- The researcher explicitly confirmed the presented N2 constraint profile. A second pre-write contract check found that both registered strict-human v1b routes—N2 constraint-profile acceptance and N5 slice selection—were absent from OpenAPI. Both operations, exact human-or-hybrid request schemas, and the `X-Coordinator-Attempt-Nonce` double-submit guard are now documented; focused red/green drift tests, the N2/N5 route integrations, all related contract-drift tests, 198-path YAML parsing, full typecheck, and DB-context alignment pass. N2 remained unwritten until that contract was complete.
- The exact N2 profile is now durable as `research_constraint_profile_9a415e01-c998-47c7-b248-7e8cd6d7a924` (authority hash `a1c1fe88...cd88`) under human attempt `node_attempt_v1b_n2_human_8deb4e1f-b226-4f24-96e9-2b402120b0da`; N3 `v1b_intake_readiness_fa81a8f7-d39a-4964-8226-2c3bb85d1335` returned `ready_for_slice` with no blockers. Fresh N4 inspection then found that caller drafts on the coordinator route are `fixture_replay` and product-rejected, while the existing `runtime_verified` Codex runtime had no HTTP entry. Commit `efd46c9e` adds the narrow product-only N4 bridge, derives planning facts from frozen owners, pins `codex_assisted`/`product`, and rejects provider or mocked execution. Its red/green HTTP test, 103 harness tests, N4 runtime tests, N2/N5 route tests, 199-path OpenAPI parse, and full `pnpm typecheck` pass.
- N4 product attempt `node_attempt_t147_a9_pos_v1b_n4_v1` recorded a non-provider runtime audit and admitted three mechanism-distinct options as `research_slice_option_set_ac409c36-8626-44e6-bf63-d95f97be981a`. The recommended open-loop tri-level controller, lower-cost binary selective-retrieval trigger, and higher-risk closed-loop sequential controller preserve the accepted claim ceiling and matched-budget/model-strength constraints. N4 is `admitted_with_warnings` only because disagreements, one high-risk option, and required human review are explicitly carried to N5; there are no blockers. Owner/handoff reads prove the current frontier is strict-human N5.
- The researcher selected option A, `uncertainty_conditioned_tri_level_allocation`, through the documented strict-human N5 route. Attempt `node_attempt_v1b_n5_human_90ae1b5a-047f-494d-a1eb-ee52e8099e8d` materialized `research_slice_b588bfa5-839a-48f1-afe5-1834820a9896`; N5 is `admitted_with_warnings` only because the two non-selected mechanisms remain available for explicit loopback.
- Fresh N6 inspection exposed the same product-bridge gap previously repaired for N4: the generic coordinator marks caller drafts `fixture_replay`, while the runtime-verified Codex path had no HTTP entry. Commit `a9d6772b` extends the narrow product-only Codex bridge to initial N6, pins `codex_assisted`/`product`, rejects provider/mock execution, and adds exact OpenAPI schemas. Its focused route and drift tests, six N6 runtime tests, all 103 harness tests, 199-path YAML parse, operator lint, and full `pnpm typecheck` pass.
- Live N6 attempt `node_attempt_t147_a9_pos_v1b_n6_v1` admitted three bounded question candidates as `topic_question_candidate_set_3d33bd98-1a34-46bc-aa3e-135b97596698`. Runtime audit `artifact_ref_dacffb2e-aa18-4cd4-955d-a37b3fc9420d` records `non_provider=true`, `run_mode=product`, and `executor_kind=codex_assisted`; there are no blockers or fixture replay. Deterministic N7 attempt `node_attempt_t147_a9_pos_v1b_n7_v1` then selected the recommended matched-budget tri-level frontier, materialized `topic_question_contract_9a1aaa47-6931-4eb6-83e1-08be9e8d6d56`, and preregistered current pending question checkpoint `research_checkpoint_df82d263-8a1d-4399-893f-f7b8ea4ed12e`. The packet is `eligible_for_human_review`, advertises all four human actions, and has no policy issues, required actions, boundary violations, or open objections. Execution stopped before N8.
- The researcher chose `loopback` to investigate whether this is a retrieval method or only retrieval-budget routing. Decision `research_checkpoint_decision_10429bd2-699f-4fdb-bb40-2d784e055351` is bound to the exact `c3503610...7cd4` snapshot and targets `research_slice`; N8 still has zero attempts. The current local EvidenceMap already cites Adaptive-RAG, RAGRouter-Bench, AdaRankLLM, SeaKR, and DTR, which place the idea squarely in adaptive/selective retrieval. Independent primary-source review then found June 2026 preprint [Know Before You Fetch](https://arxiv.org/abs/2606.29959), whose calibrated confidence routing to graded retrieval budgets is near-isomorphic to the current primary design, plus [When Should Active RAG Retrieve?](https://arxiv.org/abs/2607.24010), which requires realized-budget, harm, and trigger-cost separation. These external findings are advisory only until a product-native evidence refresh imports and maps them.
- The researcher authorized the exact bounded acquisition recovery. The old job was already terminal failed, so no cancellation occurred. Downloader `max_byte_size` was temporarily raised from `1024` to `31,457,280`; dry-run `980e55f9-e77f-45b5-91ce-f54fe9725476` selected exactly the two explicit arXiv PDFs with zero resolver/Unpaywall calls; job `072160bd-8aca-4988-ac0f-081c701e22eb` succeeded 2/2; and the setting was restored to exactly `1024`. GROBID-backed fulltext processing then made both papers `FULLTEXT_PREPROCESSED`; curated dossiers `0c948144-88f4-4afa-b116-c0d908ff16d5` and `cbd1562b-6c28-4440-95e9-0fa532b995e1` made both `KEY_CONTENT_READY` with explicit telemetry of zero LLM calls and retries.
- The product-native novelty refresh is now current. Snapshot `literature_snapshot_8a6aa7a3-5417-4aa2-8f65-e7991e455aa2`, SearchPlan `search_plan_d3f7c65b-6107-4cf2-9c15-f612f17d659d`, and manual-import SearchRun `search_run_0cd28666-6604-465e-9fe8-4b6d5141f95b` bind seven papers and fulltext paragraphs across 4/4 satisfied coverage rows. EvidenceMap `evidence_map_d55feebd-b26c-476b-ad59-cfcf508119ba` contains 11 current paragraph-level units (3 support / 2 challenge / 4 baseline / 2 context), one material nearest-work conflict, and a `mixed` recheck assessment `evidence_strength_1b3e6fef-b632-4725-9f48-8892b2ab8db5`. It superseded the prior evidence checkpoint and created pending strict-human checkpoint `research_checkpoint_19a6227d-3849-4d0e-ab6f-9e89e072b457`; the packet is `eligible_for_human_review` with all four actions and no required-action refs.
- The researcher advanced that exact refreshed evidence packet against unchanged snapshot `c749aa6b...7261`. Decision `research_checkpoint_decision_6e7a3a63-9158-45f9-aa47-9d6f10bfac49` and human authority `human_decision_ee1d85e3-9709-4ca2-9d84-2b0c91bcef55` preserve five explicit limitations: graded confidence and binary marginal utility are prior art; retriever/source/model changes may reorder results; claims are confined to signed `0→1` and `1→5` effects in one fixed local pipeline; and trigger, retrieval, passage-token, realized-budget, benefit/harm, and latency accounting stay separate.
- Directly retrying N4 on completed run `workflow_run_t147_a9_pos_v1b_v1` first failed closed on an incomplete non-goal boundary and then produced a valid option set, but N5 correctly returned `409 VERSION_CONFLICT` because the coordinator frontier remained N8. Neither rejected request created a new N5 owner. A fresh provider-free recovery run was therefore required instead of rewinding completed run history.
- Recovery run `workflow_run_t147_a9_pos_v1b_signed_depthwise_v2` reused the immutable v1a→v1b bundle and completed N1-N7 only. Exact human N2 constraints fixed `k=0/1/5`, signed `0→1`/`1→5` targets, the non-depth pipeline, nearest-work baselines, and separate cost/harm metrics. Non-provider Codex-assisted N4 admitted three distinct slices; the researcher selected option A, creating ResearchSlice `research_slice_849816f6-a5b6-4646-ad81-71d1773b8643`. Non-provider N6 admitted three bounded question candidates, and deterministic N7 materialized TopicQuestionContract `topic_question_contract_81234493-e21d-4af6-991c-f40b7295b872`.
- Question checkpoint `research_checkpoint_2be0d1f1-f5c4-4627-94a8-ca9fe91897ad` superseded the old looped-back question checkpoint and froze snapshot `8ceaa149...760d`. Its claim asks whether transition-specific signed utility produces a better harm-aware quality-efficiency frontier than calibrated tri-level confidence, binary budgeted marginal utility, and query-complexity routing in at least one prespecified generator-strength stratum. The fallback is bounded help/saturation/harm subgroup analysis. Three hard falsifiers cover baseline domination, non-identical pipelines or collapsed cost/harm accounting, and no held-out information beyond confidence plus binary utility. It is now decided `advance` as recorded below.
- The researcher authorized exact `advance` against that unchanged question snapshot. Decision `research_checkpoint_decision_f33257d5-5c29-40c2-82d1-701dd222356b` and human authority `human_decision_3c0f9483-aaac-40bd-9c39-8d3b437f978a` made `topic-selection.research.assess-topic-value` the next authorized transition. The subsequent explicit N8 authorization exposed one product-path gap before execution: the generic coordinator correctly labels caller drafts as `fixture_replay`, but the runtime-verified Codex-assisted N8 path had no HTTP bridge. Commit `390cc843` minimally extends the existing N4/N6 bridge to N8, pins `codex_assisted`/`product`/non-provider execution, invokes the question guard before runtime-artifact generation, and rejects provider or mocked execution. Its focused red/green route test, contract-drift tests, full v1b route integration, Prisma HTTP smoke, 199-path OpenAPI parse, operator lint, `git diff --check`, and full `pnpm typecheck` pass.
- The authorized backend restart loaded that bridge, and live attempt `node_attempt_t147_a9_pos_v1b_signed_depthwise_n8_v1` materialized TopicValueAssessment `topic_value_assessment_0381abe1-f060-4dde-b397-0c71433eaa0c` with total score `75`, confidence `0.72`, readiness `ready`, and recommendation `advance_to_package`. All six semantic hard gates pass. The assessment deliberately caps originality at `68` and reviewer-risk at `64`, preserves direct confidence/binary-utility/query-complexity baselines, separates harm and realized costs, confines claims to one fixed local pipeline and prespecified generator strata, and preserves a null-result subgroup fallback. N8 is `admitted_with_warnings`, with zero blockers and only `N8_VALUE_RISK_NOTES_CARRIED_FORWARD` plus `N8_CRITIC_REVIEW_TRIGGERED`; no debate loopback fired. Runtime audit `artifact_ref_5268cc5d-0cc4-4875-9457-745eb37072f7` proves `run_mode=product`, `non_provider=true`, `executor_kind=codex_assisted`, and no fixture replay. Handoff `artifact_ref_aa6a3fed-8cfc-430a-907b-e38184485e4c` targets deterministic N9, which remains at zero attempts.
- The researcher then authorized exactly deterministic N9. Attempt `node_attempt_t147_a9_pos_v1b_signed_depthwise_n9_v1` resolved the unchanged N8 assessment and memo, materialized active ValueDispositionDecision `value_disposition_decision_601c14ea-7ce8-469f-9812-bf46b3b56e45`, and admitted `advance_to_package` with zero blockers. Its only warning, `N9_VALUE_RISK_NOTES_CARRIED_FORWARD`, preserves the known nearest-work, label-sparsity, subgroup-masking, and claim-ceiling risks rather than clearing them. The N9 trace has null semantic and runtime-admission hashes, and its two artifacts contain no provider or fixture-replay provenance. Handoff `artifact_ref_830134d5-01db-4dc5-8b7d-0f52c2b08b19` targets N10 draft-package creation; N10 remains at zero attempts.
- The researcher then authorized exactly deterministic N10 draft-package creation. Attempt `node_attempt_t147_a9_pos_v1b_signed_depthwise_n10_v1` consumed the unchanged N9 disposition and materialized TopicPackage `topic_package_9ea9b9bc-1bfe-4794-b89a-95a3b88095f0` with authority hash `2c7960ee...b56d` and readiness `ready_for_promotion_review`. The package owns five unique evidence units, the fixed local SciFact `k=0/1/5` evaluator plan, explicit baselines/falsifiers, zero blockers, and the single warning `N10_PACKAGE_RISKS_CARRIED_FORWARD`; no risk was silently accepted. N10 also created trace-boundary check `package_trace_boundary_check_7a7975e2-5ff1-4071-95ac-349bec87f4cc`, readiness assessment `package_readiness_assessment_75b9dcf0-da56-4676-b26d-bb1f7295099a`, and v1c input bundle `v1b_to_v1c_input_bundle_73ccc347-de79-4fef-93de-188a979dafa9`. Its two attempt artifacts contain no provider or fixture-replay provenance, and the deterministic trace has null semantic and runtime-admission hashes. Handoff `artifact_ref_829176ab-52b8-4784-a824-49923892f033` targets N11 bundle publication; N11 remains at zero attempts.
- The researcher then authorized exactly deterministic N11 publication. Attempt `node_attempt_t147_a9_pos_v1b_signed_depthwise_n11_v1` published the unchanged bundle `v1b_to_v1c_input_bundle_73ccc347-de79-4fef-93de-188a979dafa9` with authority hash `c43ce236...c7c2`, zero blockers, and only `N11_PACKAGE_RISKS_CARRIED_FORWARD`. Handoff `artifact_ref_128836ff-2c17-4908-a62a-45aad407ddee` is a `V1cInputBundle` targeting `v1c.entry` and binds the exact package and bundle hashes. The deterministic trace has null semantic/runtime-admission hashes, and its two artifacts contain no provider or fixture-replay provenance. The recovery run now reports `run_complete=true`, N11 attempt count one, and no next v1b node; no v1c promotion owner or PaperProject side effect was created.
- The researcher then authorized v1c preparation through the strict-human promotion stop. PromotionInputSnapshot `promotion_input_snapshot_082798ba-6979-4ac5-84fb-f72a3e2c1669` closed `ready_for_gate` with all four currentness/lineage checks passing. Deterministic support `promotion_decision_support_36e612f9-dff6-44e9-bac2-86a86f759a5b` and dossier `promotion_dossier_b77800a1-9ae9-48e3-8d74-f4c21c5fab67` succeeded with `llm_draft_payload=null`; no provider ran. ArgumentReadinessMiniCheck `argument_readiness_mini_check_10155c2e-d72b-42c5-a160-335937579ad7` passed all six checks, and PromotionGateCheck `promotion_gate_check_e24c1cd7-5205-4f9c-8b27-5e5100d996f1` reports `ready_for_human_decision`, `promote_allowed=true`, and zero blockers, warnings, required actions, or loopback hints. The gate arrays are clear while the package's explicit claim ceiling, nearest-work, label-sparsity, subgroup, cost/harm, and baseline risks remain visible for the human review. Read-after-write title-card lists contain one gate check, zero promotion decisions, and zero PaperProject bridges.
- The researcher confirmed `promote_with_conditions` against exact snapshot `1cde2ea9...9605`, with three owned conditions for direct strong-baseline/nearest-work verification, fixed-pipeline and separate cost/harm accounting, and a bounded local claim plus fallback. After an authorized backend restart, the exact Gate and zero-decision state were reverified. The HumanPromotionDecision service accepted the condition shape and materialized pending promotion checkpoint `research_checkpoint_35406533-fac3-4cdb-b6d9-29296ca66c63`, but the central guard returned `422 CHECKPOINT_CHAIN_INCOMPLETE`: refreshed evidence checkpoint `research_checkpoint_19a6227d-3849-4d0e-ab6f-9e89e072b457` advances, the current gap checkpoint is `CHECKPOINT_UPSTREAM_STALE`, and current question checkpoint `research_checkpoint_2be0d1f1-f5c4-4627-94a8-ca9fe91897ad` otherwise advances. The promotion packet preserves all three proposed condition actions but advertises only `hold`, `loopback`, and `reject`. Recovery reads prove zero promotion decisions and zero bridges, so no human authority or downstream effect was fabricated.
- The authorized current-evidence gap rebuild and exact strict-human decision are complete. NeedCandidate A `need_candidate_ccdb06f3-aebc-49a3-8371-b3400204fd9e` predicts open-loop signed `0→1` and `1→5` utility; genuinely distinct candidate C `need_candidate_132054e7-48a7-416f-809d-dd71b32ed3bc` uses bounded post-`k=1` feedback for a sequential stop-or-continue decision. Both bind all 11 current EvidenceUnits and the material nearest-work conflict from EvidenceMap `evidence_map_d55feebd-b26c-476b-ad59-cfcf508119ba`, are current/non-speculative/machine-viable, and occupy different semantic groups. Binary signed retrieve-or-skip remains a required direct-neighbor baseline rather than a manufactured gap candidate. Against unchanged snapshot `fceeff15...c748` and packet `248a284b...e2284`, adjudication `need_adjudication_fb7a51a5-f433-42ac-b999-9eb05f385c3a` recorded `validate`; human authority `human_decision_42eda73c-ad6f-4d28-81a9-3aa126e3228f` accepted all seven checks, selected A, and retained C as a viable alternative across all five substantive distinctness axes. HumanConfirmNeed materialized ValidatedNeed `validated_need_a58766a3-d482-4dc3-9b0b-cdeac9e3d095`; A is closed/`human_confirmed`, and gap checkpoint `research_checkpoint_de57273c-4b2e-4820-9f98-898920a6fe63` is decided `advance` with no required actions. The accepted/residual risks remain retriever/evidence-source conditionality `evidence_unit_0bcd64e5-a92e-4038-a9ff-ec1d65741c99`, generator strength `evidence_unit_8e2554bd-3ffe-461e-bb54-801e1b3ef4bf`, and nearest-work novelty pressure `evidence_conflict_53be99f0-8601-43cb-a3f6-58ecb42c0e2c`. The researcher then authorized the fresh question-lineage start: v1b input bundle `v1b_input_bundle_fc913271-74bb-4359-b19b-0d4216a609bc` published version `t147-a9-pos-signed-depthwise-current-gap-v1b-input-v1`, and deterministic N1 attempt `node_attempt_t147_a9_pos_v1b_current_gap_n1_v1` admitted intake snapshot `v1b_intake_snapshot_8641862d-753a-4cf3-8ac9-4d5f90ec89f3` with zero blockers/warnings. Run `workflow_run_t147_a9_pos_v1b_current_gap_v1` now points to N2. Execution stopped before recording its Human-authority constraint profile; no provider execution or downstream ResearchSlice/question/package/v1c/promotion/bridge/PaperProject owner was created.
- The researcher accepted the exact current-lineage N2 constraint profile. Human attempt `node_attempt_v1b_n2_human_df29404f-774e-4d8f-b0c4-0fa56af89483` materialized `research_constraint_profile_2a24de6f-5188-497b-943d-99d17833165b` with authority hash `bedeaacf...de3`, preserving fixed `k=0/1/5`, signed `0→1`/`1→5`, fixed non-depth pipeline, direct-neighbor baselines, separate harm/cost accounting, local one-workstation scope, and no paid-provider execution. Deterministic N3 attempt `node_attempt_t147_a9_pos_v1b_current_gap_n3_v1` materialized readiness `v1b_intake_readiness_dc04d11e-e429-43e0-8efb-0ac24bea83bf`, admitted with zero blockers/warnings. Non-provider Codex-assisted N4 attempt `node_attempt_t147_a9_pos_v1b_current_gap_n4_v1` then admitted current option set `research_slice_option_set_45f3d8bc-9a9e-4142-b905-f5d9c2691b2a`: recommended open-loop signed-depthwise A, binary direct-neighbor fallback B, and feedback-conditioned stopping C. Runtime audit `artifact_ref_76fcf02f-7bba-4b3c-a1c6-e9a2419c5b` records `non_provider=true`, `run_mode=product`, and `executor_kind=codex_assisted`.
- The researcher selected exact option A through strict-human N5. Attempt `node_attempt_v1b_n5_human_82c017e2-98de-4d4a-b522-248517b8711e` materialized ResearchSlice `research_slice_7c821aa3-cda7-4f4c-ba17-57a743648acf` and selection decision `slice_selection_decision_48723b18-4301-496e-9440-89a86b0c5dba`; its warnings preserve the human-review context and both deferred alternatives. The first N6 request failed closed with `FROZEN_INPUT_SOURCE_REF_KIND_MISMATCH` because its frozen source refs omitted the compatibility alias `research_slice_selection_decision`; it created no candidate-set authority. Corrected non-provider Codex-assisted attempt `node_attempt_t147_a9_pos_v1b_current_gap_n6_v2` added that alias and admitted candidate set `topic_question_candidate_set_b5213573-43a5-4887-9daa-e79d8816115a`. Audit `artifact_ref_d625dc20-f781-420f-9fb7-3045e1bd6f36` proves `non_provider=true`, `run_mode=product`, `executor_kind=codex_assisted`, and no model option or provider invocation.
- Deterministic N7 attempt `node_attempt_t147_a9_pos_v1b_current_gap_n7_v1` selected the recommended signed-depthwise harm-aware frontier, materialized TopicQuestionContract `topic_question_contract_fd208913-dde6-466b-a311-808d66c4ad02`, TopicQuestion `topic_question_5a8eec61-6b79-4a76-8c41-242a2c0b95df`, and answerability plan `topic_question_answerability_plan_962c96ce-4490-4643-9737-1cb8cbe2c690`. Pending question checkpoint `research_checkpoint_5a8b3dc7-cb77-4b46-81c6-342044b8ecb9` supersedes the stale prior question checkpoint, is `eligible_for_human_review`, exposes `advance`/`hold`/`loopback`/`reject`, and has zero policy issues, boundary violations, dependencies, required actions, or open objections. Its three hard falsifiers cover strong-baseline domination, invalid non-identical pipeline or collapsed cost/harm accounting, and no held-out information beyond confidence plus binary utility. Execution stopped before N8; N8 remains at zero attempts.
- The researcher accepted that exact current question and explicitly confirmed review of mechanism identifiability, proxy operationalization, confounds, falsification conditions, claim ceiling, and open objections. Snapshot-bound decision `research_checkpoint_decision_d76ab015-5af7-4e2b-8603-2fbeb4e98c2a` and Human authority `human_decision_9999a402-bd52-4305-89fe-c42523bfe0f1` record `advance` under stable key `t147-a9-pos-v1b-current-gap-question-advance-46fe13b8-v1`; all six review booleans are true, the fixed local claim ceiling and separate harm/cost accounting remain in review notes, and required-action/loopback refs remain empty. The checkpoint is now `decided`, its snapshot and packet hashes are unchanged, and the repaired run exposes N8 with zero attempts. No N8 or provider execution occurred.
- The researcher authorized the current-lineage non-provider N8 value assessment. First attempt `node_attempt_t147_a9_pos_v1b_current_gap_n8_v1` failed closed on `N8_UNKNOWN_VALUE_TRACE_REF` because its source IDs were current but the contract and slice `version_id` values still named the prior lineage; it created no TopicValueAssessment authority or N8 handoff. Corrected attempt `node_attempt_t147_a9_pos_v1b_current_gap_n8_v2` used the current versions and admitted TopicValueAssessment `topic_value_assessment_bb3d2b7a-ecb0-4e7d-b5f1-926ce92fa20b` with total score `75`, confidence `0.72`, readiness `ready`, and recommendation `advance_to_package`. All six hard gates pass. The `15`-point risk penalty preserves nearest-work pressure (`8`), sparse/noisy negative-utility labels (`4`), and fixed-local-benchmark scope (`3`); originality remains `68`, reviewer-risk `64`, and the critic must check baseline absorption and subgroup masking. Runtime audit `artifact_ref_bb5e89d4-4c73-48fb-ac40-1e62816fbbab` proves `run_mode=product`, `non_provider=true`, `executor_kind=codex_assisted`, model option null, and no provider invocation. Handoff `artifact_ref_6771faf2-1155-4d89-86ad-d0a7dfa2f4fa` targets deterministic N9, which remains at zero attempts.
- The researcher then authorized exactly deterministic current-lineage N9. Attempt `node_attempt_t147_a9_pos_v1b_current_gap_n9_v1` resolved the unchanged N8 assessment and reasoning memo, materialized active ValueDispositionDecision `value_disposition_decision_ff17f7e2-015a-4f8c-b192-bb5b1ea64874`, and admitted `advance_to_package` with zero blockers. Its only warning, `N9_VALUE_RISK_NOTES_CARRIED_FORWARD`, preserves the known nearest-work, negative-label, subgroup-masking, and fixed-local-pipeline risks rather than accepting or clearing them. Semantic and runtime-admission hashes are null because N9 is deterministic; trace `trace_snapshot_0e7577ce-1256-4f1e-816a-0005a855d460` records disposition `advance_to_package` and route target N10. Handoff `artifact_ref_95f91459-26b6-4b27-9bce-66d677fa1267` targets draft-package creation; N10 remains at zero attempts.
- The researcher then authorized exactly deterministic current-lineage N10 draft-package creation. Attempt `node_attempt_t147_a9_pos_v1b_current_gap_n10_v1` materialized TopicPackage `topic_package_37bba4de-16fc-484c-a9b3-0d41f777a55c` (`v1`) with readiness `ready_for_promotion_review`, five unique selected evidence units, the fixed local SciFact `k=0/1/5` evaluator plan, direct calibrated-confidence/binary-utility/query-complexity baselines, explicit falsifiers, and zero blockers. Warning `N10_PACKAGE_RISKS_CARRIED_FORWARD` keeps the N8/N9 risks in the package rather than accepting them. N10 also created boundary check `package_trace_boundary_check_8ba6bb8f-2608-4ebf-901c-46b8fefcb393`, readiness assessment `package_readiness_assessment_5b517822-9ad8-45a4-8093-a71dac30b243`, and unpublished v1c input bundle `v1b_to_v1c_input_bundle_0ae63fe7-64c6-4486-8fe2-22d6d00d806a`. Its deterministic semantic/runtime-admission hashes are null. Handoff `artifact_ref_3c59b879-6f5c-4208-ae96-301f1e85f73c` targets N11; N11 remains at zero attempts.
- The user authorized a fresh local-DB, no-paid-provider product-path acceptance through topic handoff. The historical SciFact top-k 10-versus-5 question is now an explicit negative control; the positive lineage uses a mechanism-level uncertainty-conditioned adaptive-retrieval question with genuinely distinct alternatives. Fresh labeled owner records remain durable audit evidence, and execution stops for every Human-authority decision.
- A post-completion implementation audit repaired six gaps before archive: every downstream checkpoint now carries and validates the exact current predecessor-checkpoint ref; question advancement requires an explicit `objections_reviewed: true`; concurrent input-snapshot creation converges on the unique winner; concurrent distinct objection resolutions yield one winner and one stable `409`; the matrix negative self-test removes a real parsed cell instead of relying on brittle string replacement; and v1a workflow fixtures now satisfy the production support/context/challenge/direct-neighbor evidence contract.
- The supported Node range is executable again on Node 26: backend/shared TypeScript entrypoints use `tsx` via `--import` instead of the incompatible `ts-node` ESM loader. Developer commands, operator scripts, canaries, the full-suite runner, and runtime-stress children use the same loader contract.
- Decisive post-audit verification is green: `pnpm typecheck`; root `pnpm test` on Node 26 (backend 2,776 tests: 2,707 passed, 69 conditional skips, 0 failed); three repeated real-Postgres checkpoint integration runs plus the full-suite run; workflow-matrix and injected-drift checks; LLM config, N8 dry-run, and slot-manifest operator checks; DB-context alignment; and disposable-shadow migration drift with an empty diff.
- Phase 5 closes promotion and intake. HumanPromotionDecision now materializes an exact hash-versioned promotion checkpoint, rejects an incomplete native evidence→gap→question chain, and requires every advancement-relevant warning or independent critic finding to map to an accepted risk, evidence-backed disposition, or owned condition/action.
- Promotion and bridge services require checkpoint control as a constructor dependency; omission is a type error rather than a silent runtime bypass. Bridge creation and every intake attempt re-run the complete current checkpoint chain before replay or any PaperProject write, so later objections, stale hashes, or open actions invalidate an unconsumed bridge.
- Non-promote human decisions remain durable and decided but non-advancing, with product-owned required actions. The bounded-debate N3 semantic layer and critic resolution map survive into the promotion dossier.
- Legacy title-card semantic writers for need, question, value, package, decision, and direct promotion now return only `409 GATE_CONSTRAINT_FAILED` with the canonical research-status recovery path. Title-card/evidence intake and historical reads remain available.
- A real HTTP product chain, real-Postgres full-chain persistence, incomplete-chain and post-bridge objection negatives, and the original top-k parameter-only academic objection regression pass. Full v1c route, focused checkpoint, legacy cutover, v1b compatibility, shared contract, type, OpenAPI (198 documented paths), workflow-matrix, DB-context, migration-drift, and idempotent-backfill checks are green.
- Phase 4 closed the research-design path. N7 now derives a pending question-contract checkpoint from the exact selected candidate, contract, question frame, answerability plan, evidence/boundary/assumption/falsification rows, and upstream refs before publishing its handoff.
- Question advancement requires identifiable mechanism/comparison/claim, operational outcome plus metrics and success criteria, explicit confounds or alternative explanations, challenge evidence, resources/baselines/ablations/setting, active source-bound pre-value falsification, claim ceiling/prohibited claims, no boundary violation, and an answerable verdict. Aggregate scores or generic risk prose cannot substitute for a missing component.
- N8 independently invokes the central guard before draft resolution or provider work and accepts only a current strict-human `advance` decision bound to the exact TopicQuestionContract snapshot. A partial N7 authority-write failure is recoverable: retry supersedes the orphan preregistration and binds the current contract without weakening the guard.
- Blocking and critical objections now freeze their source checkpoint and required loopback. They block that checkpoint and its downstream while permitting the named upstream correction path; only strict-human, current-snapshot, evidence-backed resolution against a substantively revised expected authority can close them. Rewording the question while retaining the challenged ResearchSlice remains blocked.
- Phase 3 closed the evidence-to-gap path with native checkpoint assembly. Evidence review now requires current source authority, inspectable non-abstract core claims, complete required direct-neighbor coverage, complete required disconfirming coverage, and no blocking evidence conflict before `advance` is advertised.
- Candidate arenas now preserve admitted candidates and rejected framings, fail closed on a lone viable candidate or wording/parameter-only duplicates, and expose actionable candidate-generation loopbacks. Counts and semantic-group hashes remain tripwires; the strict-human gap review must name a genuinely distinct viable alternative and the substantive axes that differ.
- Both direct and batch candidate writers materialize the same current arena. Readiness/adjudication changes refresh meaningful candidate facts, stale pool hashes fail, and HumanConfirmNeed advances the gap checkpoint only through its existing human-confirmed decision authority. Exact confirmation replay is recoverable; drifted replay conflicts.
- v1b bundle publication now requires the current advancing gap checkpoint. API and native-harness route tests traverse the same evidence decision and competitive gap review rather than a test-only quality mode.
- Phase 2 closed with product-owned v1 checkpoint, missing-stage decision, objection, and objection-resolution authorities; canonical packet/history/status APIs; snapshot-bound strict-human decisions; deterministic supersession; and a central transition guard.
- Both NeedCandidate persistence paths now call the same evidence-checkpoint guard before any write. Pending, stale, non-advancing, required-action, and blocking-objection states fail closed.
- Migration `20260825120000_add_topic_selection_research_checkpoints` replayed without Prisma drift and is applied to the local development schema. DB context and OpenAPI are aligned.
- The versioned backfill initially produced 2,249 pending `backfilled` anchors across 1,080 title cards. Final replay after later local fixtures returned the same 2,295 anchors across 1,100 title cards twice: 665 evidence, 660 gap, 532 question, and 438 promotion.
- Focused contracts, service, route, bypass, and real-Postgres concurrency tests pass; one of two simultaneous distinct human decisions wins atomically and exact replay returns the winner.

## Done when
- [x] A1: Product-owned checkpoint state, packets, decisions, currentness, supersession, and transition guards cover evidence, gap, question, and promotion review through documented HTTP APIs.
- [x] A2: Evidence and gap advancement requires product-validated source quality, nearest-work/disconfirming coverage, and genuinely distinct candidate competition under current policy.
- [x] A3: TopicQuestion value assessment cannot begin without a current human question confirmation that includes mechanism identifiability, proxy/confound, falsification, claim-ceiling, and objection review.
- [x] A4: Durable blocking human objections invalidate affected downstream authority and cannot be cleared by rewording, client memory, or non-human action.
- [x] A5: Promotion and PaperProject intake fail closed on unresolved objections, incomplete required actions, stale decisions, or unmapped advancement-relevant risks.
- [x] A6: An API-first research-status projection lets any client recover completed work, alternatives, unresolved issues, current checkpoint, allowed decisions, and next authorized transition without reconstructing internal records.
- [x] A7: Production contains no rehearsal/reduced-quality runtime path and no Codex- or GUI-specific semantic authority; test scenarios exercise the same guarded product contracts.
- [x] A8: Versioned cutover, migration, replay/idempotency, OpenAPI/context alignment, and a full-chain negative regression based on the top-k academic-objection case are decisively verified.
- [ ] A9: A fresh dual-track local API acceptance proves the parameter-only negative boundary, strict-human checkpoint operation, post-bridge objection invalidation, substantive-repair lineage refresh, and exactly-once topic handoff without paid-provider execution.
