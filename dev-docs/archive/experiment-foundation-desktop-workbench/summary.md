# experiment-foundation-desktop-workbench

## Outcome

- Add the desktop `实验基座` workbench below `文献管理`, exposing asset registry, readiness, recipe, job/result, evaluation fact, and sidecar workflows without owning experiment semantics in the UI.

The task was complete under the previous repository contract and is archived by location under the current contract.

## Project placement

- Task: `T-078`
- Feature: `F-001` — Research Lifecycle Governance Core
- Milestone: `M-001` — LLM Research Lifecycle v1
- Last old-contract update: `2026-08-24`

### Historical Requirement provenance

- `R-012` — Experiment foundation reusable assets baseline

The retired Requirement objects are provenance only; they were not promoted into the current Milestone/Feature graph.

## Durable verification

- `实验基座` appears below `文献管理`.
- No legacy CSS paths are recreated.
- Disabled/blocked states reflect backend readiness/job selection.

## Recovery and migration note

The complete pre-conversion bundle, including detailed decisions, verification, tools, and historical artifacts, is recoverable from Git at `5cf904fb:dev-docs/archive/experiment-foundation-desktop-workbench/`. During T-145 the archive was compressed only after live fixtures and maintained contracts had moved to supported owners. Historical paths and commands in that snapshot are evidence, not current operational interfaces.
