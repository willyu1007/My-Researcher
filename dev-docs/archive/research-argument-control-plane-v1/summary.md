# research-argument-control-plane-v1

## Outcome

- Historical goal, retained for context:
- 为仓库建立一条新的 pre-writing research argument control plane 主线，协调 `title-card` 上游证据链与 `paper-project` 下游写作容器之间的 research convergence。
- 完整覆盖 research-argument 这一需求切片，并补齐投稿前风险报告与写作交接输出。
- 通过 umbrella task 固定子任务拆分、执行顺序、跨任务边界、验证口径与 handoff 方式。

The task was complete under the previous repository contract and is archived by location under the current contract.

## Project placement

- Task: `T-023`
- Feature: `F-001` — Research Lifecycle Governance Core
- Milestone: `M-001` — LLM Research Lifecycle v1
- Last old-contract update: `2026-06-01`

### Historical Requirement provenance

- `R-011` — Research argument legacy transition baseline

The retired Requirement objects are provenance only; they were not promoted into the current Milestone/Feature graph.

## Durable verification

- `T-024` and `T-025` are complete and archived.
- `T-026` / `T-027` / `T-028` were planned-only stubs with pending verification.
- Planned-only stubs were archived as unimplemented placeholders and consolidated into the `T-023` backlog.
- A new `research-argument` implementation plan should not be created; future work belongs under `PaperImplementation` migration/replacement/decommission scope.

## Recovery and migration note

The complete pre-conversion bundle, including detailed decisions, verification, tools, and historical artifacts, is recoverable from Git at `5cf904fb:dev-docs/archive/research-argument-control-plane-v1/`. During T-145 the archive was compressed only after live fixtures and maintained contracts had moved to supported owners. Historical paths and commands in that snapshot are evidence, not current operational interfaces.
