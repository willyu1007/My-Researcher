# Topic Selection Research Checkpoint Control Plane — Verification

## Evidence

| Claim / reference | Check / procedure | Latest result | Evidence / limitation |
|---|---|---|---|
| Existing product has a hard human need confirmation and hard human promotion decision. | Inspect maintained workflow matrix and current routes. | observed | `topic-selection-workflow-matrix.md` marks v1a human-confirm-need and v1c record-human-promotion-decision as human review; current POST routes exist. |
| Existing product lacks a hard evidence checkpoint. | Inspect EvidenceMap row, services, and API routes for a required human decision before NeedCandidate generation. | observed | EvidenceMap has deterministic materialization/readiness but `human_review_required=no`; no evidence-landscape decision authority exists. |
| Existing product lacks a hard final question checkpoint. | Inspect N6/N7 rows and v1b human routes. | observed | Slice selection accepts human input, but N7 materialization is mechanical by product decision and no current TopicQuestion confirmation authority blocks N8. |
| Production has a NeedCandidate authority bypass. | Compare both NeedCandidate writers and aggregate current local records. | observed | The orchestrator batch writer persists directly; 644 of 661 local NeedCandidates have null input-snapshot, gate-result, and transition-attempt ids. Both writers require the same future checkpoint guard. |
| Candidate competition is usually absent rather than merely too small. | Group local NeedCandidates by EvidenceMap and question candidates by candidate set. | observed | 633 evidence maps have one NeedCandidate; 568 of 620 question candidate sets have one candidate. Counts are diagnostic; the proposed blocker is the absence of a genuinely distinct viable alternative. |
| Legacy production routes bypass the v1a/v1b/v1c chain. | Trace registered `/title-cards/*` routes and `TitleCardManagementService.promoteTitleCardToPaperProject`. | observed | The production app registers direct need/question/value/package/promotion writes, and the legacy promotion service creates a PaperProject after only legacy hard-gate/package checks. |
| Current bridge intake does not prove condition closure. | Aggregate bridge decisions, conditions, accepted risks, early-check obligations, and intake refs. | observed | 31 of 464 active bridges carry conditions; 2 conditioned bridges completed intake, while 29 conditioned/accepted-risk bridges and 1 plain bridge remain without intake. |
| Existing-record cutover affects a material population. | Count current local v1a/v1b/v1c and legacy authorities without reading content. | observed | 648 EvidenceMaps, 633 ValidatedNeeds, 601 QuestionContracts, 899 v1b-to-v1c bundles, 464 promotion decisions, 464 bridges, and 434 bridge-created PaperProjects already exist. Blanket invalidation or grandfathering would both be unsafe. |
| Current product path can be operationally complete while academically shallow. | Review the 2026-08-25 real local API rehearsal artifacts. | observed | Four EvidenceUnits, three abstract-only; one NeedCandidate; top-k 5/10 remained the research object; N8 score 74 included originality 60 and reviewer-risk 58; multiple `pass_with_risk` findings advanced without conditions. This is diagnostic evidence, not a benchmark corpus. |
| Explicit academic objection was not preserved as a blocking product constraint. | Compare user objection with subsequent slice/question/package/promotion records. | observed | The objection that top-k 5/10 was insufficiently academic did not invalidate the downstream authority chain; narrative enrichment advanced to an active bridge. |
| Archived work provides reusable control-plane and human-review foundations rather than a duplicate active outcome. | Query governance and read T-042/T-088/T-089/T-115/T-123/T-127/T-128 summaries. | observed | All are archived and establish prior design/runtime/product baselines; T-129 is active but limited to externally gated calibration/provider debate. |
| A1/A6 Phase 2 foundation: Checkpoint and research-status APIs are product-owned and recoverable. | Contract/schema tests, route integration, projection fixtures, direct NeedCandidate-writer bypass negatives, and full history/packet recovery. | passed | Seven canonical HTTP endpoints expose history/detail/packet/decision/objection/resolution/status; full pending/decided/superseded history remains readable, and both NeedCandidate persistence paths fail before writes on a pending evidence checkpoint. Remaining stage-specific adapters and downstream guards are owned by Phases 3-5. |
| A2 Phase 3: Evidence/gap quality fails closed on shallow or non-competitive material. | Negative cases for abstract-heavy core evidence, incomplete required nearest-work coverage, incomplete required disconfirming coverage, lone viable candidate, wording-duplicate candidates, stale pool hash, stale candidate version, and wrong stage authority; positive direct and native-harness chains. | passed | Product policy removes `advance` and emits required actions for shallow evidence/arenas. HumanConfirmNeed requires every frozen candidate disposition plus a machine-eligible viable alternative with explicit substantive distinctness axes; its existing human decision advances the gap checkpoint, and v1b publication rechecks it. Focused service/route/schema suites pass; the real Prisma adapter persists and replays an existing human authority. |
| A3/A4 Phase 4: Question confirmation and objections govern currentness. | Snapshot-drift, explicit objection-review contract, objection lifecycle and concurrent resolution, unauthorized resolution, semantic no-op rewording, valid revision, N7 partial-write recovery, and N8-entry tests. | passed | The question packet fails closed per missing mechanism/comparison/claim, outcome/metric/success criterion, confound, challenge evidence, evaluation component, active source-bound pre-value falsification, claim ceiling, boundary, answerability condition, or `objections_reviewed: true`. N8 blocks before draft/provider work until an exact strict-human advance. A blocking top-k academic objection survives question rewording, permits its required upstream repair, and closes only after a changed ResearchSlice plus current evidence citation. The full v1a workflow harness passes 111/111. |
| A5 Phase 5: Promotion and intake reconcile risks, actions, objections, and the complete checkpoint chain. | Human-decision, promotion-packet, bridge, intake, real HTTP, and real-Postgres tests; incomplete-chain, stale predecessor lineage, unmapped warning/critic, stale target, open action, and post-bridge objection negatives. | passed | The complete native chain creates and consumes one bridge. Each stage after evidence binds the exact current predecessor checkpoint; rematerializing upstream authority makes the downstream chain stale. Missing checkpoints prevent bridge persistence; an objection recorded after bridge creation blocks intake before the PaperProject gateway; non-promote decisions remain decided but non-advancing. Checkpoint control is a required constructor dependency in both authority services. |
| A7: No reduced-quality production mode or client semantic authority exists. | Search runtime source for rehearsal/reduced-quality modes; verify required service injection and direct legacy-writer rejection. | passed | No production runtime mode or client-specific authority was found. Isolated tests use explicit fixtures; product app wiring and constructor types require the same checkpoint service. All legacy semantic POST/PATCH/direct-promotion routes return conflict-only recovery responses. |
| A8: Cutover, replay, contracts, and the top-k academic regression are complete. | Disposable shadow-DB drift check, DB-context check, backfill twice, OpenAPI parse/contract tests, workflow-matrix consistency, typechecks, full-chain deterministic regression, v1b compatibility, and v1c HTTP routes. | passed | Prisma reports no migration drift. Two final backfill runs both returned 2,295 anchors across 1,100 title cards (665 evidence, 660 gap, 532 question, 438 promotion). OpenAPI has 198 paths and parses; v1a HumanConfirmNeed plus v1b N2/N5 human operations, exact request/response schemas, and the human-submission nonce are documented and protected by focused drift tests. Maintained matrix matches code. The parameter-only `top-k 5/10` objection remains blocking through bridge intake instead of disappearing under narrative enrichment. |
| Post-completion implementation audit is repaired and reproducible on the declared Node range. | Review lineage/currentness, human-decision completeness, Prisma unique races, matrix negative-control sensitivity, production-shaped workflow fixtures, and every TypeScript launcher; run typecheck, root tests, focused repeats, operator self-tests, DB context, and disposable-shadow drift. | passed | Node 26 root tests are green; backend reports 2,776 tests with 2,707 passed, 69 conditional skips, and 0 failures. The real-Postgres checkpoint integration passed three consecutive focused runs and the full suite. `pnpm typecheck`, matrix consistency plus injected drift, LLM-config checks, N8 synthetic dry-run, slot-manifest freshness, DB context, and an empty Prisma drift diff all pass. Live provider/calibration scenarios remain explicitly conditional and outside T-147. |
| Phase 6 live use exposes a legibility and authorization-cadence gap above the correct control plane. | Review the end-to-end operator conversation, user intervention points, durable stage outputs, and vocabulary presented at each stop. | passed | Phase 8B exposes a deterministic seven-stage manifest, generic artifact resolver, concise Chinese Markdown, and a substantially richer LLM working set over the same canonical current owners and hashes. Reprojection after supersession changes manifest/view hashes and keeps the old checkpoint only in history. The continuation envelope then admits only local reads, meaning-preserving deterministic writes, bounded non-provider jobs, verification, exact retry, and the selected backend lifecycle; nine research/authority/cost/control/environment/scope/recovery effects stop exactly, a pending checkpoint stops at the human boundary, drift forces refresh, replay is stable, and evaluation creates no authority. Shared schema 6/6, service 11/11, route 1/1, OpenAPI drift 1/1, OpenAPI parse, and root typecheck pass. |
| Phase 6 live use exposes excessive path dependence and insufficient early divergence. | Compare the initial framing, nearest-work loopback, repaired question, N8 value dimensions, residual risks, and gate semantics. | observed; design pending | The loopback correctly replaced near-isomorphic graded-confidence routing with signed adjacent-depth utility, but the process continued optimizing inside a narrow selected scope. Current N8 is `75` with originality `68`, reviewer-risk `64`, and material nearest-work, label, subgroup, and fixed-local-pipeline limits. The ready gate proves a complete defensible package, not that this is the best available topic. A cheap abandon/reframe/expand route and cross-scope candidate comparison are not yet first-class. |
| Repository debate capability was not exercised by the current live lineage. | Compare the workflow matrix and debate runtime contracts with current N4/N6/N8 audits and current PromotionDecisionSupport. | observed; design pending | Dedicated v1a NeedCandidate, conditional v1b N6/N8, and v1c promotion-support debate mechanisms preserve role artifacts and transcript hashes. The current lineage instead records single `codex_assisted` N4/N6/N8 audits; N8 did not fire its debate loopback; current promotion support is deterministic with `llm_draft_payload=null`. Existing node-local mechanisms are not a unified, researcher-visible debate session or agent-management surface. |
| Current prompts and consumers structurally prefer continuation over an honest no-topic result. | Trace v1a/v1b/v1c prompt instructions through output schemas, loopback choices, gate/admission behavior, model routes, and tool exposure. | contract layer implemented; prompt calibration pending | The pre-HumanConfirmNeed v1a seam plus v1b N4/N6 now carry the additive two-layer `portfolio_disposition`. Evidence-grounded no-topic, expansion, and reframe results are admitted without manufactured option/candidate authority; selected portfolios require complete grounded `selected`/`parked`/`dropped` classification. N4/N5 and N6/N7 keep parked/dropped alternatives out of downstream selection while preserving their evidence and reopening metadata. Invalid evidence, confidence, coverage, drop, or reopening contracts fail closed; legacy output remains compatible and exact replay is covered. Shared schema passes 32/32, full v1b harness 117/117, coordinator 61/61, and root `pnpm typecheck` passes. Prompt incentives, N8 comparative advancement burden, shadow behavior, and provider calibration remain unfinished. |
| Literature processing and retrieval capability were not systematically connected to the live topic-selection reasoning loop. | Trace literature pipeline/retrieval services, TitleCard snapshot construction, SearchRun recording, the live novelty refresh, N4 planning input, and downstream source propagation. | observed; design pending | Fulltext/key-content/chunk/embed/index processing and pgvector hybrid retrieval exist. Topic-selection snapshots come from EvidenceBaskets, while SearchRun records supplied provenance/refs rather than invoking retrieval. The successful refresh was a manual-import seven-work run with 11 paragraph units after one Codex-subagent investigation; later stages primarily carried EvidenceMap/evidence-role refs, hashes, and compact summaries with no retrieval tools. Product-native role-specific RAG and claim-bearing ref resolution remain missing. |
| The proposed divergence/retrieval mechanism can reuse current product foundations without a pipeline rewrite. | Map required arena responsibilities to literature retrieval, SearchPlan/SearchRun, EvidenceMap, debate/runtime artifacts, snapshots/checkpoints, objections/rechecks, and Codex-assisted execution. | observed; design pending | Existing owners cover evidence processing, retrieval, provenance, role outputs, runtime audit, currentness, and human authority. The smallest candidate is one lightweight snapshot-bound ResearchArena plus orchestration and disposition/schema changes, first before HumanConfirmNeed. Superseded by the Phase 7 audit row below: storage form now has an evidence-backed recommendation; behavioral calibration and implementation ownership remain Phase 8-10 work. Branching was settled on 2026-08-28 as one active path with delta-gated parked return and no fork. No implementation is claimed. |
| Phase 7 owner/currentness audit: existing owners cover everything except the arena session root. | Four read-only subsystem audits (evidence chain, debate/runtime artifacts, dispositions/risk carry, projections) plus a live ref-to-excerpt resolution proof against the real lineages, cross-reviewed read-only by Codex gpt-5.6-sol with seven corrections incorporated. | observed; confirmed by the researcher 2026-08-28 | `artifacts/phase7-owner-map.md` (2026-08-28): live proof that EvidenceUnit locators resolve to durable paragraph text through an existing product route while similarity search cannot substitute for keyed resolution; manifest/human/LLM views are pure read-time projections needing zero new writable authority; the risk-carry seam is N8/N9/N10 prose-not-refs on the deterministic v1c path, repaired by a neutral machine-owned risk-finding record because AcceptedRisk is a human authority; four cooperating service-path layers make a second active lineage impossible; session identity, pre-exposure independence, minority-report linkage, loop-delta admission, and arena supersession have no faithful home, so one minimal persisted `ResearchArenaSession` coordination root is recommended, with the role-execution layer's shape (child table vs typed artifact columns) a Phase 8 detail. |
| A10 planned: arena roles reason over actual claim-bearing evidence. | Resolve every role citation from persisted ref through query provenance to inspectable excerpt; replay independent pre-exposure outputs from the exact arena snapshot. | planned | Phase 7 defines the EvidencePacket contract; Phase 8 must reject UUID-only, summary-only, evidence-free, or correlated-role inputs. |
| A11 planned: the workflow can stop, park, reframe, expand, or select without manufacturing a viable topic. | Run dominance-pair, perturbation-variant, zero-candidate, semantic-duplicate, one-delta retry, and no-delta retry cases before HumanConfirmNeed. | planned | Shadow mode precedes authority changes. Exactly one active path plus delta-gated parked alternatives; fork is out of scope for this task. |
| A12 planned: material dissent cannot disappear on the way to promotion. | Seed high-severity topic-killer/minority findings at the arena/N8 boundary and trace stable refs through package, v1c support, gate, and human packet. | planned | Every item must end in repair, named accepted risk, loopback, park, or drop. A zero-warning gate with unresolved carried dissent fails. |
| A13 planned: the arena improves decisions at bounded cost and human interaction remains stage-level. | Compare current baseline with shadow runs across recorded dominance pairs, evidence-perturbation counterfactuals, and the current ambiguous lineage; measure dominance-pair consistency, perturbation sensitivity, override/reopen accumulation bucketed per drop-reason code, evidence delta, cost/latency, work avoided, artifact legibility, and authorization pauses. | planned | Activation requires researcher-reviewed thresholds over evidence-backed drops, dominance-pair order, and override convergence; absolute worthwhile labels are not required because no reliable source for them exists. Justified `drop`/`park` counts as success; no provider execution is needed for the first slice. |
| A9: Fresh dual-track local API acceptance proves real checkpoint governance. | Read-only preflight, parameter-only negative lineage, mechanism-level positive lineage, strict-human checkpoint decisions, post-bridge blocking objection, wording-only resolution rejection, substantive ResearchSlice/evidence repair, topic handoff, and exact replay. | in progress; current lineage frozen | The negative arena remains non-advancing. Exact human advancement of refreshed evidence checkpoint `research_checkpoint_19a6227d-3849-4d0e-ab6f-9e89e072b457` preserved the nearest-work limits. Completed v1b history refused to rewind, while provider-free recovery run `workflow_run_t147_a9_pos_v1b_signed_depthwise_v2` reached a deterministic ready package and v1c gate. The attempted conditional promotion then proved the deeper central guard by returning `422 CHECKPOINT_CHAIN_INCOMPLETE` with zero promotion decisions or bridges. Authorized repair created two current-evidence, machine-viable, genuinely distinct NeedCandidates; exact gap advancement materialized current ValidatedNeed `validated_need_a58766a3-d482-4dc3-9b0b-cdeac9e3d095`, bundle `v1b_input_bundle_fc913271-74bb-4359-b19b-0d4216a609bc`, and run `workflow_run_t147_a9_pos_v1b_current_gap_v1`. N2-N7 are admitted on that repaired lineage, corrected non-provider N8 admitted a ready score-75 assessment, N9 admitted active `advance_to_package`, and N10 materialized ready TopicPackage `topic_package_37bba4de-16fc-484c-a9b3-0d41f777a55c`. Deterministic N11 published current bundle `v1b_to_v1c_input_bundle_0ae63fe7-64c6-4486-8fe2-22d6d00d806a`; current PromotionInputSnapshot `promotion_input_snapshot_fe9e16eb-ee23-4d56-b6f4-bf5135b2d066` passed all four checks. Authorized deterministic support and dossier bind that exact snapshot, and current gate `promotion_gate_check_39bfb045-47d2-4e67-b6b4-f9a125aa88be` is `ready_for_human_decision` with `promote_allowed=true`. ArgumentReadiness passes all six checks, but its zero-warning result conflicts with material earlier narrative risks and helped motivate A12. The researcher therefore froze this coherent-but-moderate lineage as an ambiguous calibration fixture with zero promotion decisions and zero bridges. A future topic selected by the improved process must complete the post-bridge objection cycle and exactly-once handoff for A9. |

## Outstanding verification

- Phase 7 is closed: the audit (`artifacts/phase7-owner-map.md`) is researcher-confirmed and the settled contracts live in `02-architecture.md`; the kickoff gate is ready and implementation is authorized through Phase 8D shadow validation. Phase 8 is split into independently decisive checks: 8A lands the two-layer disposition contract with red/green schema/gate/loopback tests; 8B proves manifest-led LLM and human stage views plus effect-boundary scenarios; 8C migrates the arena-session root and `TopicSelectionResearchArenaRoleExecution` child table with drift-clean Prisma replay and rejects peer-contaminated/evidence-free admission; 8D runs the shadow scout-killer slice against the DP-1/DP-2/PV-1/AF-1 fixtures with deterministic recovery and no live-authority writes. Preserve canonical database authority, keep the frozen current gate as an ambiguous calibration fixture, and keep provider calibration/activation with T-129.
- Implement only the early pre-HumanConfirmNeed scout-killer slice in shadow mode first, after the disposition-contract precondition (`none_viable`/`park`/`drop` as legal successful terminal outputs across N4/N6 schemas, gates, loopbacks, and terminal semantics) lands. Prove independent role-specific retrieval, model-visible excerpts, zero-to-many successful dispositions, one-delta retry admission, human-readable derived output, and deterministic recovery before allowing an arena result to govern a checkpoint.
- Calibrate with dominance pairs, evidence-perturbation counterfactuals, and accumulated human overrides — no absolute worthwhile labels — and obtain researcher acceptance of drop-justification, dominance-consistency, override-convergence, evidence-delta, cost/latency, and work-avoided results before activating the arena or generalizing to question/N8 stages.
- Preserve current gate `promotion_gate_check_39bfb045-47d2-4e67-b6b4-f9a125aa88be` and snapshot hash `64ede7615c3c31ebaa7a5b9dfd558d2f7cbccd7700bd024b2e0b86b428cea36c` unchanged as the ambiguous calibration fixture; do not record a HumanPromotionDecision or create a bridge for it under the confirmed route. Complete the remaining A9 post-bridge objection, wording-only rejection, substantive repair, and exactly-once handoff proof later with a process-selected advancing topic. Preserve the negative arena as the non-advancing control and the non-selected slice/question candidates as explicit alternatives. Research-status still surfaces old pending promotion checkpoint `research_checkpoint_35406533-fac3-4cdb-b6d9-29296ca66c63`, stale gate `promotion_gate_check_e24c1cd7-5205-4f9c-8b27-5e5100d996f1`, and a stale pre-repair next transition; none owns continuation. The configured `raw_files` root remains transient even though normalized text is persistent; do not alter it without separate exact authorization. Provider calibration remains owned by T-129 and requires separate authorization.
