# Shared package

Shared TypeScript contracts and utilities.

## Current modules

- `src/research-lifecycle/interface-field-contracts.ts`
  - DTOs for research lifecycle governance APIs
  - JSON-schema style request schema objects for Fastify route wiring
  - Contract helpers (`no_m6` guard, id format checks)
