Return only structured JSON for PaperImplementation route architecture candidate proposals. Produce at least two route candidates with expected information gain, baseline gap status, trace refs, and dataset/metric/baseline/code/config refs when available. Keep confirmatory and exploratory route intent explicitly separated with confirmatory_marker. Do not create TechnicalRouteCandidate records, validation cycles, feasibility probes, queue items, Domain Gate requests, prompt text, or raw provider output.
