# Topic Selection Research Checkpoint Control Plane — Roadmap

## Scope and constraints

### In scope
- Add product-native research checkpoint control across evidence review, gap confirmation, research-question confirmation, and promotion.
- Make user objections, academic-quality obligations, currentness, and allowed loopbacks enforceable by backend transition guards.
- Expose checkpoint packets and current research state through canonical local HTTP APIs for Codex and future clients.
- Upgrade existing v1a/v1b/v1c gates without creating a parallel topic-selection pipeline.
- Add evidence-grounded divergence before convergence at research-semantic decisions, beginning with the gap/need portfolio before HumanConfirmNeed.
- Resolve persisted evidence refs into bounded, claim-bearing model-visible packets and connect role-specific retrieval to existing literature/SearchRun authority.
- Preserve candidate alternatives, fatal or minority objections, explicit stop/park/reframe dispositions, and loop deltas through downstream handoffs.
- Produce concise human stage artifacts and authorize routine local execution by effect boundary rather than by internal node.
- Validate the new behavior in shadow mode against recorded dominance pairs, evidence-perturbation variants, and the current ambiguous lineage before production adoption.

### Out of scope
- GUI surfaces, Codex-specific decision logic, writing-center work, experiment execution, provider activation, calibration release, and any production rehearsal mode.
- A generic multi-agent platform, unrestricted model-controlled browsing, debate at mechanical packaging/publication nodes, live multi-provider activation, or parallel downstream execution of every candidate.

### Constraints and dependencies
- Existing domain authorities remain authoritative; checkpoint records coordinate review and transition eligibility only.
- Human choices are explicit, exact, snapshot-bound, and append-only; upstream change supersedes affected review decisions.
- Current workflow trace, replay, hash, and repository boundaries remain valid through a versioned cutover.
- T-129 retains ownership of externally gated prompt calibration and provider-debate activation.

## Decision alignment

| Decision question | Options / tradeoffs | Current direction | Status | Owner / required confirmation | Closure evidence | Consequences |
|---|---|---|---|---|---|---|
| Runtime modes | Research-only production / rehearsal plus research | Research-only production; fixtures and canaries remain test scenarios | decided | User | Latest confirmed instruction | No reduced-quality business path or `rehearsal` runtime semantic is introduced. |
| Semantic authority | Product control plane / Codex runbook / GUI workflow | Product control plane | decided | User | Latest confirmed instruction | Clients render packets and submit exact decisions but cannot define or bypass gates. |
| Human participation structure | Four major checkpoint phases / ad hoc human prompts | Evidence, gap, question, and promotion checkpoints, with existing slice selection retained inside the research-design flow | decided | User | Confirmed enhancement direction | Product must expose pending review and block unauthorized transitions. |
| Checkpoint ownership model | Generic duplicate content authority / lightweight control state referencing existing authorities | Lightweight checkpoint state and packet projection; reuse existing decisions and add only missing evidence/question decisions | decided | User | Confirmed task opening | Avoids a second content authority while giving all clients one control interface. |
| Existing-record cutover | Grandfather all / block all / derive pending checkpoints from current authority and require review before new advance | Keep all records readable; backfill pending anchors; require the earliest unsatisfied current checkpoint before any new advance; preserve completed intake only as pre-checkpoint provenance | decided | User | Phase 1 planning checkpoint confirmed 2026-08-25 | Avoids retroactive deletion and blanket grandfathering while making every future transition fail closed. |
| Academic policy strictness | Fixed numeric thresholds / role-and-quality rules with bounded policy triggers | Semantic role/quality blockers are authoritative; counts, similarity, recency, confidence, and scores are configurable escalation tripwires only | decided | User | Phase 1 planning checkpoint confirmed 2026-08-25 | Direct-neighbor/disconfirming coverage, distinct alternatives, question identifiability, objections, and obligation closure become hard semantics. |
| Minimal persistence | Per-stage duplicate content tables / generic event engine / lightweight coordination with existing authorities | Checkpoint + missing-stage decision + objection + objection-resolution authorities; reuse existing snapshots, stage decisions, recheck propagation, and projections | decided | User | Phase 1 planning checkpoint confirmed 2026-08-25 | Adds explicit currentness and human control without duplicating evidence/question/package content. |
| Legacy semantic routes | Preserve as compatibility writers / retire all TitleCard APIs / retain intake and reads but cut over semantic writes | Keep title-card/evidence-basket intake and reads; reject legacy need/question/value/package/promotion writes after enforcement | decided | User | Phase 1 planning checkpoint confirmed 2026-08-25 | Closes the direct PaperProject promotion bypass while preserving useful compatibility reads. |
| Positive-lineage novelty repair | Keep calibrated closed-book confidence as the primary graded-budget mechanism / use binary marginal utility / predict signed adjacent-depth utility under fixed retrieval components | Predict signed `0→1` and `1→5` answer-utility changes while fixing reader, retriever, corpus/index, candidate ranking, prompt, and decoding; retain calibrated tri-level confidence and binary budgeted utility as nearest-work comparators rather than novelty claims | decided | User | Researcher advanced the refreshed evidence packet and selected the signed-depthwise option on 2026-08-26 | Evaluation must test incremental value over both nearest-work mechanisms and report benefit, harm, realized retrieval calls, passage tokens, trigger cost, and measured latency separately. |
| Research-decision rhythm | Converge immediately / debate every node / diverge then converge only at semantic choices | Evidence/search scope, gap portfolio, question design, and comparative value first form independent alternatives with role-specific evidence, then challenge and converge; mechanical nodes remain deterministic | decided | User | Research-process review on 2026-08-27 | Debate becomes a bounded research-quality mechanism rather than workflow ceremony. |
| Model-visible evidence | Persisted refs and upstream summaries / whole-corpus context / bounded claim-bearing packets | Resolve refs into inspectable excerpts with locator, query intent, role, freshness, and support/challenge relation; load only task-relevant slices | decided | User | Literature/RAG audit on 2026-08-27 | UUID lineage remains traceability evidence but no longer masquerades as content the role actually read. |
| Arena result and branching | Force one winner / unrestricted multi-branch / zero-to-many portfolio with one active path | Permit `select`, `park`, `drop`, `reframe`, or `expand`; exactly one active path plus snapshot-bound parked alternatives with delta-gated serial return; fork is out of scope and an arena fork recommendation is recorded as `park` plus a surfaced recommendation | decided | User | Divergence discussion on 2026-08-27; fork retired in the product-quality discussion on 2026-08-28 | “No worthwhile topic” is a successful result; no branch-identity, child-TitleCard, or sibling-supersession machinery is built. |
| Disposition contract precedence | Fix prompts only / fix contracts with the first arena slice / fix contracts first as a precondition | Land a two-layer disposition contract before or with the first arena slice: set-level outcomes (`selected`/`none_viable`/`evidence_expansion_required`/`reframe_required`) routed by the checkpoint with stop as a first-class route, and per-candidate dispositions (`selected`/`parked`/`dropped`) — legal successful terminal outputs across N4/N6 output schemas, gates, loopbacks, and terminal semantics; contract work independent of any new role | decided | User | Product-quality discussion on 2026-08-28; two-layer refinement adopted from external review the same day | Without a legal no-topic exit any critic role is forced to repair fatal findings into an advancing answer, and without the layer split a bare `drop` could terminate the wrong level of research object. |
| First-slice arena roles | Full four-role arena / topic-killer only / scout-killer pair | Pair an opportunity scout (bounded out-of-basket retrieval) with a prior-art/topic-killer (repair prohibited); the arbiter is deterministic or human in the first slice; the empirical skeptic and multi-provider diversity join only after the pair proves value | decided | User | Product-quality discussion on 2026-08-28 | The excavate-then-negate order is preserved — the existing generation path plus the scout excavate, the killer negates — while correlated-role noise and cost stay bounded. |
| Arena persistence owner | Reuse existing owners only / overload checkpoint kinds / one minimal new session root | Add one `TopicSelectionResearchArenaSession` coordination root (snapshot binding, one-current key, supersession, enumerated termination reason, loop-delta refs) plus a dedicated `TopicSelectionResearchArenaRoleExecution` child table for role-execution integrity (exposure-set hash, evidence partition, unique runtime identity, composite role-slot/instance uniqueness; paper-implementation runtime-artifact precedent); content authority unchanged and `support_only` preserved | decided | User | Phase 7 owner audit `artifacts/phase7-owner-map.md`; researcher confirmed the boundary change on 2026-08-28; child-table shape settled from external review the same day | The arena can prove session identity, independence, minority-report linkage, loop delta, and supersession without becoming a second research authority. |
| Stage artifacts and authorization delivery | Couple to arena integration / deliver early as an independent slice | Human stage Markdown and the effect-based authorization envelope may land early and independently of arena validation because they are read-only projections and operating protocol over canonical owners; the LLM working plane is preferred as manifest-first read-time projection, with a persisted corpus admitted only if projection cost is proven prohibitive | decided | User | Product-quality discussion on 2026-08-28 | Researcher legibility and cadence improve without waiting for the arena, and no second content authority is created. |
| Objection and risk survival | Re-summarize at each stage / immutable carry ledger with explicit disposition | Every unresolved material objection or risk remains machine-visible through package and promotion until repaired, accepted, looped back, parked, or dropped | decided | User | Live N8-to-promotion audit on 2026-08-27 | A green final gate cannot silently erase a material earlier critic finding. |
| Adoption strategy | Replace the live path immediately / shadow first and calibrate | Close the bounded shadow proof, repair stable dissent carry independently, complete product-persisted arena execution identity, then expose the support-only arena inside HumanConfirmNeed as advisory evidence that records explicit human accept/override/defer labels; activate checkpoint authority only in Phase 10 after drop-justification, dominance-consistency, override-convergence, and cost evidence is acceptable | decided | User | Integrated recommendation on 2026-08-27; calibration method revised on 2026-08-28; Phase 9 reordering approved on 2026-08-29 | Phase 9 may improve traceability and collect live advisory evidence without allowing the arena to control production state; production activation remains a separate recoverable policy decision. |
| Phase 10 activation evidence rule | One aggregate score / arbitrary minimum case count / semantic coverage plus hard-fail invariants | Reuse the existing OfflineEvaluation dataset/case/run/metric owners and require a bounded product-v2 evidence matrix: two dominance pairs, one causally relevant and one irrelevant evidence perturbation, one successful non-advance, one advancing case, and strict-human accept/override/non-advance coverage. Every role execution must be product-invocation verified and evidence-resolvable; every drop must have a coded reason, evidence, and reopening condition; any confirmed false drop/false continue, unexplained override, replay drift, authority leak, peer exposure, missing loop delta, provider call, or extra human stop blocks activation. The report may return only `insufficient_evidence`, `remain_advisory`, or `eligible_for_activation`; it never changes policy. At least one real decision improvement or measured downstream work avoided is required, otherwise the Arena is redundant. | decided | User | Researcher approved the Phase 10 planning checkpoint on 2026-08-31 | Avoids false precision from the current 2-pair/1-perturbation fixture and gives the system an explicit, cheap way to recommend non-adoption. |
| Phase 9C review ownership and defer semantics | Add a review table / overload HumanConfirmNeed prose / bind a typed label artifact to the existing checkpoint and human decision owners | Reuse `TopicSelectionArtifactRef` for one typed, idempotent `TopicSelectionResearchArenaAdvisoryReview@v1` label bound to the exact gap-checkpoint InputSnapshot and advisory hash. An advancing HumanConfirmNeed must reference an `accept` or `override` label when current advice was exposed; `defer` records the human response but leaves the checkpoint pending and creates no ValidatedNeed. No new table or arena authority is introduced. | decided | User | Researcher approved the Phase 9C planning checkpoint on 2026-08-30 | Labels remain recoverable from checkpoint-snapshot artifacts and, on advance, from the existing HumanConfirmedDecision artifact refs. A changed label cannot silently replay an earlier confirmation, while a defer remains a successful non-advancing stage response. |
| Phase 9C-3 recovery and stage-action boundary | Rerun the Arena / add a composite decision authority / recover projections and labels from existing owners while treating two exact writes as one authorized operation group | Add one deterministic post-synthesis gap-projection recovery action that cannot invoke agents; expose checksum-validated review history from `TopicSelectionArtifactRef` by checkpoint InputSnapshot and derive reopen signals only in the read model; keep the review POST and HumanConfirmNeed POST as separate existing writes executed under one researcher decision with exact partial replay. No-advice compatibility means the Arena review ref is optional, not that the strict current gap-selection review can be bypassed. | decided | User | Phase 9C-3 planning confirmation and separate Phase 9C-3A implementation authorization on 2026-08-31 | Recovery remains local, idempotent, and support-only; no event table, composite human authority, new continuation effect class, or legacy confirmation bypass is introduced. |
| Current medium-value lineage | Promote to finish the acceptance / delete it / freeze it as an ambiguous calibration fixture | Freeze the current gate without a HumanPromotionDecision and reuse the lineage as an ambiguous shadow-evaluation case; complete A9 later with a topic the improved process recommends advancing | decided | User | User approved the integrated checkpoint and requested its full commit/push on 2026-08-28 | Prevents process-completion pressure from becoming evidence that the topic deserves promotion. |

### Assumptions

| Assumption | Risk if wrong | Validation |
|---|---|---|
| Existing control-plane transition, snapshot, recheck, and human-decision primitives can be extended without a second workflow engine. | A checkpoint layer could duplicate authority or require a larger redesign. | Phase 1 inventory of contracts, repositories, transition guards, and archived decisions. |
| Existing human-confirm-need and promotion decisions can satisfy gap and promotion checkpoints through adapters. | New generic decisions could conflict with existing authority. | Contract mapping and integration tests before persistence design is settled. |
| Research-status can be a read projection over current authority plus checkpoint state. | Reconstructing state may be ambiguous or too expensive. | Prototype deterministic projection over representative v1a/v1b/v1c records. |

## Task relationships

| Task | Relationship from this task | Owned boundary / exchanged contract | Coordination condition |
|---|---|---|---|
| T-042 | derived-from | Evidence-first, falsification, value, package, and promotion principles | Preserve the accepted decision-chain intent while correcting current product gaps. |
| T-088 | follows | WorkflowHarness, AgentOrchestrator, trace, and profile execution foundation | Reuse runtime contracts; do not create a second workflow engine. |
| T-089 | follows | Current workflow matrix and node-policy semantic ownership | Update the maintained matrix and policy contracts when checkpoint semantics change. |
| T-115 | follows | Existing v1b human-delegated review inputs and slice/question surfaces | Reuse compatible human authority and remove bypass paths rather than duplicating them. |
| T-128 | follows | Product-run reachability, recovery, prompt packet, and sign-off closure | Treat prior readiness as operational baseline, not proof of academic checkpoint sufficiency. |
| T-129 | sibling | External-corpus prompt calibration and provider-debate activation | Coordinate only if this task changes provider-gated debate contracts; otherwise remain independent. |

## Implementation plan

### Phase 1 — Settle checkpoint and cutover contracts
- Outcome: A current-state inventory and confirmed product contract define checkpoint authority, decision reuse, academic policy boundaries, and existing-record cutover without duplicate semantics.
- Approach: Trace the maintained v1a/v1b/v1c nodes, routes, repositories, control-plane transitions, human decisions, rechecks, and downstream intake guards; turn the top-k rehearsal findings into explicit negative cases.
- Planned changes:
  1. Map current checkpoint ingredients and every transition that must become guarded.
  2. Define the minimal checkpoint state/packet/decision interfaces and versioning boundary.
  3. Classify academic rules as hard semantic blockers, configurable triggers, or advisory packet content.
  4. Present a concrete cutover recommendation for current packages, decisions, bridges, and in-flight runs.
- Affected boundaries / entry points: Topic-selection shared contracts, workflow matrix, v1a/v1b/v1c service boundaries, promotion/bridge intake handoff, and OpenAPI design.
- Dependencies: Confirmed task opening; user decisions on the Phase 1 cutover and academic-policy recommendations before implementation kickoff.
- Exit criteria: No competing authority remains in the design; cutover, strictness, persistence, and legacy-route choices are confirmed; the first implementation slice is executable.
- Verification: Contract review against current code, representative API records, archived task outcomes, and negative-case definitions.
- Recovery: Keep kickoff pending and make no application-code change if inventory invalidates the proposed control model.

### Phase 2 — Establish checkpoint control and research-status APIs
- Outcome: Product-owned current checkpoints, packets, decisions, supersession, and transition guards are available through canonical APIs and can be recovered by any client.
- Approach: Add the smallest persistence and service layer needed to coordinate review while referencing existing domain authorities; expose a deterministic research-status projection.
- Planned changes:
  1. Implement checkpoint currentness, packet identity, allowed actions, decision binding, and supersession.
  2. Guard downstream transitions against missing, stale, non-advancing, or incomplete checkpoint decisions.
  3. Add checkpoint list/detail/decision and title-card research-status HTTP contracts.
  4. Preserve exact human authority and replay/idempotency semantics.
- Affected boundaries / entry points: Shared contracts, Prisma/repositories when confirmed, backend services/controllers/routes, OpenAPI, and process context.
- Dependencies: Phase 1 design and cutover decision.
- Exit criteria: A client cannot bypass a pending checkpoint, and the full pending/decided/superseded state is recoverable without client-side reconstruction.
- Verification: Schema, repository, route, currentness, replay, concurrency, stale-hash, and transition-negative tests.
- Recovery: Disable new transition eligibility behind the versioned cutover while keeping existing records readable; remove no previous authority until compatibility is verified.

### Phase 3 — Enforce evidence and gap quality
- Outcome: Need discovery starts only after current evidence review, and gap validation reflects candidate competition, direct prior-art pressure, and disconfirming evidence rather than a lone plausible framing.
- Approach: Add the missing evidence-landscape human decision, adapt HumanConfirmNeed to the gap checkpoint, and strengthen existing evidence/candidate gates.
- Planned changes:
  1. Assemble evidence-landscape packets from search, map, strength, source-quality, coverage, and nearest-work material.
  2. Require direct-neighbor and disconfirming coverage under product policy before advance.
  3. Enforce genuinely distinct candidate comparison and preserve rejected alternatives with reasons.
  4. Bind gap confirmation to the reviewed candidate-pool snapshot.
- Affected boundaries / entry points: v1a EvidenceMap/strength/search handoffs, NeedCandidate generation/adjudication/human confirmation, work queue, and checkpoint projection.
- Dependencies: Phase 2 control plane.
- Exit criteria: Shallow or single-candidate evidence cases fail closed with actionable loopbacks, while a qualified candidate pool advances through explicit human confirmation.
- Verification: Abstract-heavy evidence, missing-neighbor, missing-disconfirming, duplicate-candidate, stale-pool, and positive full-chain cases.
- Recovery: Return to evidence/search or candidate generation without mutating accepted upstream evidence authority.

### Phase 4 — Enforce research-design confirmation and durable objections
- Outcome: The selected slice and question cannot reach value assessment until a researcher confirms an identifiable, falsifiable, bounded design and all blocking human objections are resolved through evidence-backed loopback.
- Approach: Preserve the existing slice decision, add a topic-question confirmation authority, make objections durable product records, and connect them to currentness and loopback policy.
- Planned changes:
  1. Persist and project human objections with target, severity, status, resolution, and required loopback.
  2. Treat rejection of the research object or academic sufficiency as a stale/loopback condition, not a wording-repair request.
  3. Gate question confirmation on mechanism/proxy/confound/falsification/claim-ceiling completeness.
  4. Block N8 value assessment until the current question checkpoint advances.
- Affected boundaries / entry points: v1b constraint profile, slice selection, N6/N7 question contracts, answerability/falsification material, recheck memory, and N8 entry.
- Dependencies: Phase 3 evidence/gap authority and Phase 2 checkpoint control.
- Exit criteria: The top-k academic-objection regression loops back to slice/evidence; rephrasing alone cannot advance; a genuinely revised current design can be confirmed.
- Verification: Objection lifecycle, stale propagation, unauthorized resolution, semantic no-op revision, valid revision, and current question confirmation tests.
- Recovery: Supersede affected downstream review state and preserve the previous slice/question/package as traceable history.

### Phase 5 — Harden promotion, intake, and end-to-end acceptance
- Outcome: Promotion and PaperProject intake represent an informed, current research decision with reconciled risk/action obligations and a complete human-readable product packet.
- Approach: Strengthen existing N8/v1c gates and promotion dossier without replacing HumanPromotionDecision or PaperProjectBridge authority; close with a real-DB product-path regression.
- Planned changes:
  1. Require advancement-relevant `pass_with_risk` findings to map to accepted risks or required actions.
  2. Carry unresolved objections and independent critic findings into promotion support and gate disposition.
  3. Guard bridge creation/intake on the complete current checkpoint chain.
  4. Align OpenAPI, workflow matrix, context docs, compatibility behavior, and operational status projection.
- Affected boundaries / entry points: v1b N8/N9/N10/N11, v1c N1-N5, PaperProject bridge/intake, scenario runners, and maintained API/process documentation.
- Dependencies: Phases 2-4 and any confirmed migration.
- Exit criteria: All acceptance references A1-A8 are verified; no legacy or client-specific bypass remains; task completion contract can be reviewed.
- Verification: Full-chain positive and negative API scenarios, top-k objection regression, real-DB migration/replay, concurrent decision/idempotency checks, typecheck, OpenAPI drift tests, and relevant backend suites.
- Recovery: Keep bridge/intake fail-closed, retain prior authority read access, and roll back the cutover version rather than weakening checkpoint policy.

### Phase 6 — Fresh dual-track product-path acceptance
- Outcome: One fresh negative lineage and one fresh positive/recovery lineage prove that the checkpoint control plane governs real API operation rather than only test fixtures, while preserving the live interaction and research-quality gaps as the baseline for the next route.
- Approach: Use the current local development database and retrieval-ready corpus without paid-provider execution. Treat the historical SciFact top-k 10-versus-5 framing as a negative control. For the positive recovery lineage, predict signed adjacent-depth answer utility for `0→1` and `1→5` under fixed retrieval components and treat calibrated closed-book confidence plus binary marginal-utility routing as nearest-work comparators. Persist fresh, clearly labeled owner records as audit evidence.
- Planned changes:
  1. Run a read-only runtime, API-identity, topic-profile, scoped-corpus, evidence-readiness, and backup-readiness preflight before creating records.
  2. Drive a parameter-only negative TitleCard through qualified evidence to the competitive-gap boundary and prove that wording or top-k variants cannot advance.
  3. Drive a mechanism-level positive TitleCard through evidence, gap, question, promotion, and bridge checkpoints, stopping for every strict-human authority decision.
  4. Stop at the exact current promotion gate, audit whether the topic is comparatively worth advancing, and preserve interaction, artifact, retrieval, debate, risk-carry, and authorization findings without inferring a HumanPromotionDecision.
  5. Freeze the current coherent-but-moderate lineage as the confirmed ambiguous calibration fixture; move the post-bridge objection and exactly-once handoff proof to Phase 10 using a topic the improved process recommends advancing.
- Affected boundaries / entry points: Canonical local HTTP APIs for topic-selection research status/checkpoints, v1a/v1b/v1c workflow actions, PaperProject bridge and `/paper-implementation/topic-handoffs`; maintained operator documentation and T-147 verification evidence only if runtime behavior remains correct.
- Dependencies: Local backend and database availability; one unambiguous recoverable topic literature scope (normally an active topic profile); current retrieval-ready, claim-bearing role-balanced literature; user decisions at every Human-authority stop. Provider/cost execution remains disabled.
- Exit criteria: The negative lineage remains non-advancing; the repaired positive lineage reaches a current promotion gate; its moderate comparative value and process gaps are durably recorded; and the researcher freezes it as the ambiguous calibration fixture. Full post-bridge objection and handoff closure remains A9 work in Phase 10 rather than a reason to force this topic forward.
- Verification: Read `research-status`, checkpoint history/packets, blockers, allowed actions, and current promotion owner IDs after every mutation. Preserve idempotency keys and verified `409`/`422` fail-closed outcomes. No HumanPromotionDecision, bridge, or topic handoff is created solely to close the phase.
- Recovery: Stop at the last persisted owner projection, retain IDs and idempotency keys, and resume only through the returned allowed action. Do not delete audit records, query Prisma directly, start a server without approval, or substitute provider output for missing product authority.

### Phase 7 — Settle the evidence-bearing arena contract
- Outcome: One minimal contract joins evidence resolution, independent role work, candidate portfolios, durable dissent, disposition, loop delta, human artifacts, and currentness without creating a second research authority.
- Approach: Treat `ResearchArena` as a logical snapshot-bound coordination boundary first. Audit existing SearchPlan/SearchRun, EvidenceMap, runtime artifacts, debate artifacts, checkpoints, objections, rechecks, and human decisions before choosing whether any new persistence owner is required.
- Planned changes:
  1. Define the bounded EvidencePacket projection and prove that every cited source ref resolves to the exact excerpt a role can inspect.
  2. Define arena input, role/evidence partition, candidate genealogy, objection/minority report, disposition, termination, delta, replay, and supersession contracts.
  3. Define one-active-plus-parked behavior with delta-gated serial return, and prove by thought experiment only that parked candidates cannot collide under supersession; no fork, child-TitleCard, or branch-identity design.
  4. Define concise human stage Markdown and a manifest-first LLM working-plane projection over canonical owners; prefer read-time projection over a persisted corpus, and allow this sub-slice plus the authorization envelope to land early, independent of arena validation.
  5. Define the effect-based authorization envelope and the evaluation fixtures — dominance pairs, perturbation variants, and override accounting — plus budgets and activation thresholds.
- Affected boundaries / entry points: Literature retrieval and SearchRun orchestration, topic-selection runtime artifacts, v1a candidate arena/checkpoint packets, process context, and task verification fixtures. No provider route is activated.
- Dependencies: Confirmation of the integrated plan and disposition of the current medium-value lineage; existing retrieval-ready corpus and canonical checkpoint owners.
- Exit criteria: The first shadow slice can be implemented without deciding product semantics inside a prompt, duplicating content authority, or reopening branch and risk-survival questions.
- Verification: Contract review against current owners and three representative lineages; ref-to-excerpt resolution proof; supersession/branch thought experiments; authorization-effect table review.
- Recovery: Keep kickoff pending and perform only read-only discovery if owner mapping, branch currentness, or evidence resolution cannot be made unambiguous.

### Phase 8A — Legalize honest portfolio outcomes
- Outcome: N4/N6 can return zero viable candidates without being treated as generation failure, while set-level routing and per-candidate disposition remain unambiguous and evidence-backed.
- Approach: Change contracts before prompts or agents. Introduce the two-layer disposition in shared/output schemas, then make gates, loopbacks, and terminal semantics recognize `none_viable`, `evidence_expansion_required`, and `reframe_required` as successful research-management outcomes rather than malformed advancing answers.
- Planned changes:
  1. Add set-level outcomes (`selected`/`none_viable`/`evidence_expansion_required`/`reframe_required`) and per-candidate dispositions (`selected`/`parked`/`dropped`) to the N4/N6 boundaries without permitting a bare candidate `drop` to terminate the portfolio.
  2. Require evidence, rejection rationale, confidence, and reopening conditions for a zero-viable portfolio; require every `dropped` candidate to carry one enumerated evidence-backed drop-reason code.
  3. Route stop, expansion, and reframe through deterministic checkpoint/workflow semantics; do not manufacture a candidate to preserve downstream progress.
  4. Align focused contract, gate, loopback, replay, and compatibility tests before changing prompt incentives.
- Affected boundaries / entry points: the pre-HumanConfirmNeed v1a NeedCandidate portfolio plus v1b N4/N6 shared contracts and output schemas, deterministic gates, loopback routing, terminal status, fixtures, and maintained API/process documentation.
- Dependencies: Phase 7 disposition contract. No arena persistence or provider execution is required.
- Exit criteria: A well-formed `none_viable` result completes the generation decision successfully and chooses an explicit stop/expand/reframe route; an ambiguous layer, evidence-free drop, or missing reopening contract fails admission.
- Verification: Red/green schema and route tests for selected, successful no-topic, expansion, reframe, park, and every invalid drop/replay case; relevant typecheck and focused suites.
- Recovery: Revert the additive contract path as one unit; the existing advancing-candidate path remains current until Phase 9 activation.

### Phase 8B — Deliver derived research views and effect authorization
- Outcome: The researcher can read one concise Markdown result per semantic stage, while LLM consumers obtain a substantially richer manifest-led working plane and routine local work proceeds to the next human decision under one effect-bounded instruction.
- Approach: Build read-time projections over canonical owners and a protocol-level authorization envelope; neither surface becomes a writable research authority or depends on arena activation.
- Planned changes:
  1. Expose a manifest/current-pointer projection with an explicit current-selection rule and resolvable artifact references.
  2. Generate task-scoped LLM working-plane slices and concise human stage Markdown from the same canonical refs, hashes, alternatives, objections, risks, and supersession state.
  3. Encode and document the effect boundary that permits routine local reads, deterministic writes, bounded non-provider jobs, recoverable retries, and the already selected backend lifecycle without node-by-node confirmation.
  4. Verify regeneration, staleness, and human legibility without persisting a competing corpus.
- Affected boundaries / entry points: Research-status/artifact read APIs, stage projection services, operator/process documentation, and client-facing action envelopes.
- Dependencies: Phase 7 projection and authorization contracts; independent of Phase 8A and arena migration where the underlying owner already exists.
- Exit criteria: Every current stage has a regenerable human view and manifest-led LLM view, stale refs cannot appear current, and routine execution reaches the next semantic decision without an extra authority write.
- Verification: Projection snapshots, current-selection and artifact-resolution negatives, stage-view review, and effect-boundary scenarios.
- Recovery: Remove or disable derived routes; canonical owners and checkpoint behavior remain unchanged.

### Phase 8C — Establish evidence and arena integrity
- Outcome: Product-resolved EvidencePackets and one minimal arena-session root can prove what each role independently saw and produced, without becoming a second evidence, candidate, or decision authority.
- Approach: Add the settled `TopicSelectionResearchArenaSession` root and `TopicSelectionResearchArenaRoleExecution` child table, then connect role-specific retrieval and read-time excerpt resolution to their admission invariants before any shadow synthesis runs.
- Planned changes:
  1. Resolve row/parser locators into bounded claim-bearing excerpts with query intent, role, freshness, and support/challenge relation; validate quote integrity against resolved text.
  2. Persist session snapshot binding, one-current identity, supersession, enumerated termination reason, and loop-delta refs.
  3. Persist role execution with exposure-set hash, evidence partition, runtime identity, role-slot/instance uniqueness, and durable pre-exposure output.
  4. Reject UUID-only, summary-only, evidence-free, peer-contaminated, stale, or semantically duplicate first-pass participation.
- Affected boundaries / entry points: Literature keyed resolution and retrieval, SearchRun provenance, runtime artifacts, Prisma/repository contracts, checkpoint packet projection, and DB context.
- Dependencies: Phase 7 owner audit and settled child-table shape. Uses the Phase 8A disposition vocabulary but does not activate it as checkpoint authority.
- Exit criteria: Every admitted role execution replays from its exact snapshot and actual evidence exposure; invalid independence/evidence cases fail before synthesis.
- Verification: Focused resolver/admission tests, migration replay and drift check, real-repository uniqueness/currentness cases, and ref-to-excerpt replay.
- Recovery: Disable arena writes and retain diagnostic sessions; remove no canonical literature or checkpoint authority.

### Phase 8D — Prove the scout–killer arena in shadow mode
- Outcome: Before HumanConfirmNeed, an independent opportunity scout and prior-art/topic-killer produce a traceable zero-to-many recommendation from distinct product-retrieved evidence without changing live authority; the empirical skeptic remains deferred until the pair proves value.
- Approach: Reuse Codex-assisted/runtime mechanisms over the Phase 8C integrity boundary. Persist both pre-exposure outputs before one bounded deterministic or human synthesis, and evaluate the recommendation only as a shadow projection.
- Affected boundaries / entry points: Candidate generation/adjudication support, literature retrieval, arena/runtime artifacts, SearchRun coverage, derived stage communication, and shadow-evaluation tooling.
- Dependencies: Phases 8A and 8C; Phase 8B for the maintained human/LLM views. Provider/multi-provider work remains with T-129.
- Exit criteria: Every role replays from its actual evidence; a no-topic result is successful; correlated shared-summary role play and evidence-free criticism fail admission; no shadow result writes live research authority.
- Verification: Shadow replay on the current ambiguous lineage, recorded dominance pairs, and evidence-perturbation variants; inspect evidence independence, terminal disposition, retry delta, human legibility, runtime cost, and deterministic recovery.
- Recovery: Disable the shadow projection without changing checkpoint or research authority; retain artifacts as diagnostic evidence.

#### Phase 8D-1 — Prepare role-specific product evidence
- Run bounded role/query-specific product retrieval, persist SearchRun plus hit-level chunk provenance, and resolve only current reviewed EvidenceUnits into an arena-snapshot-bound EvidencePacket artifact.
- Treat an out-of-Map retrieval hit as useful divergence but not yet claim-bearing evidence: return `requires_evidence_materialization` with the unresolved literature refs and do not manufacture EvidenceUnits or bypass EvidenceMap review.
- Exit when the strict advisory API can produce ready, no-hit, and evidence-materialization outcomes without checkpoint, risk-acceptance, human-decision, or promotion writes.

#### Phase 8D-2 — Persist independent role outputs and synthesize once
- Run opportunity-scout and prior-art/topic-killer first passes over distinct role inputs, persist both pre-exposure outputs, then record proposals, objections, concessions, unresolved minority reports, semantic groups, and evidence alignment.
- Produce one bounded synthesis using the Phase 8A set-level and per-candidate disposition vocabulary, including evidence-backed reason codes for every drop recommendation.
- Exit when peer-contaminated first passes and evidence-free criticism fail admission, while a no-topic result is a successful advisory outcome.
- Implementation checkpoint (2026-08-29): the non-provider `POST /topic-selection/research/arena/shadow/run` boundary starts both source invocations before any role output is persisted or admitted, exposes only each role's exact EvidencePacket, validates complete snapshot-bound candidate coverage and packet-local citations, prohibits killer-side candidate repair/proposal, persists both output artifacts and first-pass execution records, and then creates one deterministic support-only synthesis. Selection requires independent agreement; conflict parks and requests an evidence delta; two coded drops yield successful `none_viable`; scout proposals yield `reframe_required`. Phase 8D-3 remains responsible for frozen-lineage replay, perturbation sensitivity, one-delta recovery, maintained stage views, cost/legibility evidence, and zero-authority closure.

#### Phase 8D-3 — Replay, perturb, and close the shadow proof
- Replay the frozen ambiguous lineage, recorded dominance pairs, and evidence-perturbation variants; admit at most one retry and only after a typed evidence/candidate/constraint/human-objective delta.
- Render the result through the Phase 8B human and LLM views, retain a replayable technical trace, and prove deterministic recovery and zero live-authority writes.
- Exit when the researcher can judge evidence independence, terminal disposition, retry value, legibility, cost, and work avoided before any Phase 9 activation.
- Implementation checkpoint (2026-08-29): the arena now rejects an initial delta without a predecessor, a no-delta retry, a claimed delta on the unchanged InputSnapshot, a delta ref absent from the changed snapshot, and any third attempt. Shadow responses include deterministic non-provider/retrieval/excerpt/duration accounting with provider calls fixed to zero. The read-only proof evaluator replays both output hashes and exposure identities, evaluates exact AF-1/DP-1/DP-2/PV-1 fixtures, produces separate human Markdown and LLM working-set projections, estimates avoided stages transparently, and always recommends `remain_shadow_only`; it has no writer dependency. Executable fixture evidence and maintained views are in `artifacts/phase8d3-shadow-proof.md` and `artifacts/phase8d3-shadow-proof-llm.json`. Phase 8D-3 remains at the researcher-review boundary rather than auto-activating Phase 9.
- Actual-replay checkpoint (2026-08-29): the product now opens and recovers an exact snapshot-bound arena session through HTTP, explicitly distinguishes provider-free local-snapshot retrieval from provider hybrid retrieval, records actual provider-call counts, and admits only zero-provider local preparations to the Phase 8D runner. The first real positive-lineage replay stopped before role invocation: local retrieval returned out-of-Map/materialization-required evidence, and an audit of the exact current map found all 11 reviewed EvidenceUnits inadmissible (six `QUALITY_NOT_ACTIVE`, five quote-integrity failures). The durable views are `artifacts/phase8d3-actual-replay-stop.md` and `artifacts/phase8d3-actual-replay-stop-llm.json`; canonical diagnostic artifact `artifact_ref_42424f21-0019-403d-ba85-27a54d6f717c` proves zero provider calls, zero role invocations, and zero authority writes. Exit is not met: the next route-changing action is an explicitly authorized evidence repair plus changed-snapshot retry, not Phase 9 activation or a fabricated debate result.
- Evidence-repair retry checkpoint (2026-08-29): explicit researcher authorization rebuilt all 11 units from canonical locators, activated the two missing reviewed-quality records through existing evidence-review authority, and produced exact current map `evidence_map_de5e35b6-48d9-4513-8aff-fd7e5894e1fc`; strict packet resolution is 11/11 exact. Direct keyed evidence readiness is now correctly separated from embedding-index readiness, while retrieval remains index-gated. `POST /topic-selection/research/arena/retry-snapshots` compiled one support-only snapshot containing exactly the new EvidenceMap delta; the single admitted retry `research_arena_def21744-92fb-41cb-96f1-20798e76392a` exposed distinct local packets with no peer hashes and synthesized `evidence_expansion_required`, parking both candidates. Actual replay accounting is two non-provider role invocations, two SearchRuns, ten hits, zero replay provider calls, 1466 evidence characters, and 53 ms. This structurally closes evidence repair and one-delta recovery, but does not close Phase 8D-3: the packets do not settle signed adjacent-depth utility or post-`k=1` feedback value, and both role positions were authored by one Codex main agent rather than cognitively independent subagents. Durable views are `artifacts/phase8d3-evidence-repair-replay.md` and `artifacts/phase8d3-evidence-repair-replay-llm.json`; Phase 9 remains inactive and the new pending evidence checkpoint has no decision.
- Independent-agent calibration checkpoint (2026-08-29): two `fork_turns=none` subagents independently consumed the exact disjoint product packet contents with zero peer exposure, repository context, retrieval, or provider calls. Scout parked both candidates. Killer first mislabeled two surviving candidates as selected, exposing that admission allowed multiple selections and synthesis rejected a valid one-active-path-plus-parked-alternative result. Red-green tests now reject more than one selected candidate per role and admit exactly one consensus selection alongside consensus-parked alternatives; both shipped role prompts advance to v2 and define selection as unique comparative preference rather than survival. A contract-only killer retry, still without scout output, parked both candidates; the derived outcome remains `evidence_expansion_required` with no dissent. The task evidence is `artifacts/phase8d3-independent-agent-calibration.md` and `artifacts/phase8d3-independent-agent-calibration-llm.json`. No third product session was written, preserving the one-retry limit. Phase 8D-3 is ready for researcher closeout review; Phase 9 is not activated.
- Researcher closeout (2026-08-29): the researcher accepted the bounded Phase 8D-3 shadow proof and closed Phase 8D-3. This acceptance confirms that the researcher can judge evidence independence, terminal disposition, retry value, legibility, cost, and work avoided from the maintained evidence; it is not production activation. Both current candidates remain `parked`, pending evidence checkpoint `research_checkpoint_7925bb9a-9782-43ec-a5ec-86cda8f2573f` remains undecided, and the frozen promotion gate remains unchanged. Product-persisted execution-agent identity plus provider/override calibration remain open task-level activation evidence under A10/A13. The reordered Phase 9 plan is approved but remains inactive pending explicit implementation authorization.

### Phase 9 — Integrate stable dissent and an advisory arena without activating authority
- Outcome: Material dissent cannot disappear, arena executions are product-replayable, and HumanConfirmNeed can review and label the current support-only arena result without letting it decide or block production state.
- Approach: Land three independently reversible slices. Repair the known deterministic risk-carry defect first, then complete arena audit identity and candidate-state persistence, then add advisory human binding and override capture. Reuse the Phase 8B stage views and continuation envelope; do not rebuild them.
- Dependencies: Closed Phase 8 shadow proof, existing checkpoint/human-decision owners, the agent-orchestrator audit artifact, and the current N8→N11→v1c ref pipeline. Prompt changes land only with the consuming schemas and admission checks.
- Recovery: Each slice can be disabled independently. Removing advisory projection restores the current HumanConfirmNeed packet; removing risk-finding production leaves existing prose readable but must fail closed rather than fabricate a cleared risk. A shadow artifact is never reinterpreted as a human decision.

#### Phase 9A — Make material dissent a stable machine-owned input
- Mint each material N8 risk, critic trigger, and admitted arena minority finding as an immutable snapshot-bound `TopicSelectionRiskFinding@v1` payload under the existing artifact owner, with a nullable deterministic stable key added only for exact replay. Do not mint `TopicSelectionAcceptedRisk`; it remains strict-human authority.
- Carry exact finding refs through N9, N10, N11, deterministic and model-assisted v1c support, gate checks, the promotion packet, stage views, and research status. Define materiality independently of the existing blocking-objection severity set.
- Require every advancement-relevant finding to end in an evidence-backed repair, named accepted risk, owned action, loopback, park, or drop disposition. A green gate with an unmapped material finding fails closed.
- Exit when the frozen ambiguous fixture deterministically surfaces its existing material risks instead of producing a zero-warning gate, exact replay returns the same finding identities, and no machine path creates a human AcceptedRisk.
- Verification: Red-green N8→promotion lineage tests for deterministic and model-assisted paths, replay/concurrency identity, stale-source invalidation, unmapped/mapped disposition cases, human packet and stage-view regeneration, migration/context checks, and relevant typechecks.
- Implementation checkpoint (2026-08-29): complete. Material N8 risks, capped value dimensions, critic triggers, pass-with-risk gates, and admitted arena material/minority findings now mint immutable `TopicSelectionRiskFinding@v1` artifact refs. One nullable unique `stableKey` on the existing artifact owner supplies exact replay; no risk table or machine `AcceptedRisk` writer was added. N8 finding refs survive N9, N10, N11, TopicPackage, v1c input, deterministic/model-assisted support, gate, promotion checkpoint, stage views, and research status; arena finding refs remain attached to the support-only response/transcript until Phase 9C projects arena advice into human review. Stale source snapshots fail closed, the frozen ambiguous risk shape (`originality=68`, `reviewer_risk=64`, nearest-work and fixed-pipeline limits) no longer becomes a zero-warning path, and unmapped promotion findings cannot advance. A pre-existing active human `AcceptedRisk` maps only when its canonical `source_ref` names the exact finding; an owned condition/action maps by the existing exact ref contract. Exact replay, in-memory and real-Postgres concurrency, migration drift/context, schema, focused regression, and repository typechecks pass. Phase 9B/9C remain unimplemented and unauthorized.

#### Phase 9B — Complete arena audit identity and candidate persistence
- Preserve each role invocation's existing agent-orchestrator audit artifact ref and execution-provenance hash on `TopicSelectionResearchArenaRoleExecution`; bind the transcript to those identities without adding a generic agent-management platform.
- Add the minimal candidate fields required by the settled contract: arena/session binding, semantic group, portfolio disposition, parking/drop rationale, reopening conditions, and the selected candidate against which an alternative is parked. Keep one active research path; no fork or child TitleCard is introduced.
- Keep every session and synthesis `support_only=true`. Do not add a checkpoint, HumanConfirmNeed, AcceptedRisk, promotion, or bridge writer to the arena service.
- Exit when a product replay can prove which isolated execution produced each role output, candidate parking survives recovery and supersession, and task-only subagent calibration cannot be mistaken for a product role execution.
- Verification: Schema/repository migration tests, exact and conflicting replay, exposure/audit identity, parked-return delta and no-fork tests, OpenAPI/context alignment, and focused typechecks.
- Implementation checkpoint (2026-08-30): complete. Newly admitted role executions use `TopicSelectionResearchArenaRoleExecution@v2` and bind the exact succeeded product invocation-audit artifact ref/hash plus execution-provenance hash; the runtime identity also binds pass kind and the complete retrieval-provenance hash. Admission checks the snapshot, workflow/node/attempt, `multi_agent_debate` acceptance mode, and output hash before persistence. Exact source fields are compared after every repository create/replay result, so a conflicting concurrent record fails as structured `409 VERSION_CONFLICT`; records minted under the earlier v2 runtime-hash profile remain replayable only when those source fields are identical. Transcript v2 names those exact execution identities, and synthesis rejects missing, duplicated, legacy-only, or forged first passes plus termination/disposition drift; repository synthesis conflicts are likewise normalized to structured `409 VERSION_CONFLICT`. Existing v1 rows remain readable only as `legacy_unverified` with null audit identity and cannot satisfy v2 synthesis, so the historical single-main-agent replay and task-only `fork_turns=none` calibration cannot be relabeled as verified product execution. `NeedCandidate` now owns one required stable semantic-group hash and one required typed `currentArenaAdvisory` slot containing the session/synthesis identity, support-only disposition, rationale, drop reason, reopening conditions, and selected-against candidate; the shared JSON Schema now matches the TypeScript/OpenAPI contract for both required fields. Session synthesis and all candidate projections commit atomically; the arena never changes formal decision, lifecycle, review, or freshness state and exposes no checkpoint, HumanConfirmNeed, AcceptedRisk, promotion, bridge, or fork writer. Shared contracts pass 437/437, the focused arena-service repair set passes 6/6, real PostgreSQL arena replay passes 1/1, and the full backend suite against the fully migrated local database reports 2,853 tests: 2,783 passed, 70 conditional skips, and 0 failures. All 80 migrations are current; DB context, OpenAPI parse, backend/root typechecks, and diff checks pass. The full-suite audit also repaired a legacy v1c compatibility defect: Prisma reads now preserve an absent optional `risk_finding_refs` field instead of injecting a hash-visible `undefined` key. Phase 9C remains a separate, unauthorized review boundary.

#### Phase 9C — Bind the support-only arena into human review and collect overrides
- Review finding (2026-08-30): Phase 9B durably projects `currentArenaAdvisory` onto candidates, but the current gap-checkpoint packet builder ignores it; the shadow runner does not rematerialize the gap checkpoint after synthesis; HumanConfirmNeed has no typed arena-label binding and its replay comparison cannot distinguish changed label content; `defer` has no successful non-advancing persistence path; and the generic research-gap Markdown renderer is too weak to explain no-topic, park, drop, dissent, and reopening conditions. Phase 9C must close these seams without turning the arena into authority.

##### Phase 9C-1 — Project only current advice into the existing gap snapshot and stage views
- Extend the gap-checkpoint packet with one nullable `arena_advisory` bundle containing the exact current synthesized session, synthesis ref/hash, support-only outcome, candidate dispositions, risk-finding refs, and reopening conditions. Include the bundle in the checkpoint target hash and source refs; do not change allowed actions or required actions because advice is present or absent.
- Admit the bundle only when every projected candidate points to the same current, synthesized, support-only session and the synthesis artifact checksum, title card, and candidate refs agree. A missing arena remains normal. A stale, superseded, mixed, or incomplete projection is omitted with an explicit issue code and cannot be submitted for review, but it does not block the Phase 9 human path.
- Invoke one idempotent gap-checkpoint projector after successful synthesis and again during HumanConfirmNeed preflight. Projection retry is a deterministic local write and must never rerun scout/killer agents or require another human authorization.
- Add an explicit research-gap renderer for human Markdown and the LLM working set. It must translate the advisory into plain-language recommendation, disagreements, candidate outcomes, evidence-linked findings, and reopening conditions; the generic keyword renderer is not sufficient.
- Implementation checkpoint (2026-08-30): complete. The gap packet now carries one hash-visible nullable `TopicSelectionResearchGapArenaAdvisory@v1` bundle only when every candidate projection agrees with the exact current synthesized support-only gap Arena, its checksum-valid transcript, title card, candidate refs, risk-finding refs, and termination outcome. Missing advice remains normal; mixed, stale, incomplete, unverifiable, or invalid advice is omitted with an explicit issue code and never changes the existing allowed actions or required actions. Arena session/InputSnapshot/synthesis/risk refs enter checkpoint lineage. The shadow runner invokes the deterministic projector only after successful synthesis, while existing HumanConfirmNeed preflight rematerializes from current persisted candidates without rerunning agents. The research-gap human view now leads with a plain-language recommendation, candidate disposition/rationale, reopening conditions, dissent, and checksum-valid risk summaries; the LLM working set preserves the full advisory and resolved risk artifacts. Review repairs additionally reject cross-title risk refs and changed candidate versions before projection. No schema migration, provider call, checkpoint decision, HumanConfirmedDecision, ValidatedNeed, AcceptedRisk, promotion decision, bridge, fork, or PaperProject authority was added. Focused service/route checks pass 35/35, shared contracts pass 437/437, real-Postgres Arena/checkpoint checks pass 2/2, the full backend suite reports 2,855 tests with 2,785 passed, 70 conditional skips, and 0 failures, and root typecheck plus DB-context checks pass.

##### Phase 9C-2 — Persist one exact human label without adding a new authority table
- Add a typed `TopicSelectionResearchArenaAdvisoryReview@v1` artifact with a caller idempotency key, strict-human actor, gap-checkpoint id/InputSnapshot, confirmed candidate-pool hash, advisory snapshot hash, response (`accept`, `override`, or `defer`), rationale, bounded reason codes, and the normalized human gap-selection review hash plus selected candidate when an advance is proposed. Store it under the existing artifact owner and bind it to the checkpoint InputSnapshot so Phase 10 can recover every label from checkpoint history.
- When current advice was exposed, an advancing HumanConfirmNeed must carry a dedicated review-artifact ref whose current checkpoint, InputSnapshot, title card, advisory hash, accountable human, selected candidate, and normalized candidate-review hash match exactly. Its ref is added to the existing HumanConfirmedDecision artifact refs and confirmation trace so exact replay includes the label; a generic caller artifact ref cannot satisfy this binding. `defer` writes only the review artifact, leaves the checkpoint pending, and creates no HumanConfirmedDecision, ValidatedNeed, v1b input, or downstream authority.
- Admission derives and verifies accept/override classification by comparing the human candidate review with the exposed arena dispositions instead of trusting a caller label or asking the researcher to duplicate classification. Agreement with a selected/parked/dropped mapping may be `accept`; any advancing choice against no-topic/reframe/expand advice, selection of a parked/dropped candidate, or conflicting non-selected disposition is `override` and requires a reason code. Stale, cross-title, checksum-invalid, actor-mismatched, or changed-content replay is rejected before any human-decision or ValidatedNeed write. The artifact records the human response, never an arena decision.
- Implementation checkpoint (2026-08-30): complete. The new strict-human review endpoint stores one immutable `TopicSelectionResearchArenaAdvisoryReview@v1` payload under the existing `TopicSelectionArtifactRef` owner and binds its stable replay identity to the current gap checkpoint, exact InputSnapshot and target hash, checksum-valid advisory hash, accountable human, caller idempotency key, normalized gap-selection review hash, and selected candidate. The server derives `accept`, `override`, or `defer` plus bounded reason codes from the exposed candidate dispositions; callers cannot self-classify an agreement, hide a no-topic override, or substitute a generic artifact ref. Exact concurrent requests converge on one artifact, while stale or superseded checkpoints, cross-title refs, checksum drift, actor mismatch, changed selection, and changed-content replay fail before HumanConfirmedDecision or ValidatedNeed writes. An advancing HumanConfirmNeed must carry the dedicated exact review ref whenever current advice was exposed, and the ref survives in the existing human-decision artifact refs and trace. `defer` persists only the label and leaves the checkpoint pending. Review repairs additionally make the binding fail closed when an injected guard lacks the review validator, exclude server timestamps from stable payload checksums so concurrent exact writes remain identical, reject hybrid actors that merely reuse a human id, and verify the dedicated stable-key provenance, human creator, and derived review identity rather than accepting a lookalike generic artifact. No table, migration, provider call, Arena authority, AcceptedRisk, promotion, bridge, fork, or PaperProject writer was added. Focused checkpoint/need/API checks pass 39/39, shared contracts pass 438/438, real-Postgres concurrent replay passes 1/1, the full backend suite reports 2,861 tests with 2,791 passed, 70 conditional skips, and 0 failures, OpenAPI parses with 211 paths, and root typecheck plus DB-context checks pass.

##### Phase 9C-3 — Close recovery, compatibility, and stage-level interaction
- Planning refinement (2026-08-31): Phase 9C-1 already closed advice projection and legibility, while Phase 9C-2 already closed exact label admission, strict-human binding, defer persistence, stale/cross-title/checksum/actor rejection, and conflicting/concurrent replay. Phase 9C-3 therefore adds only the missing recovery and operating surfaces below; completed semantics remain regression obligations rather than duplicate implementation work.

###### Phase 9C-3A — Recover a failed gap projection without rerunning the Arena
- Extract the current inline projection callback into one named `TopicSelectionResearchGapProjectionService`. Its recovery operation accepts only an Arena session id and requires `findSessionById` plus `findCurrentSession(title, gap_portfolio)` to identify the same support-only session with status `synthesized`; the current key remains present after synthesis and is cleared only when a replacement session supersedes it. The operation reloads the bound InputSnapshot and checksum-valid transcript, obtains the exact candidate refs from the persisted advisory synthesis, verifies title card and candidate versions plus their exact session/transcript advisory bindings, then invokes the existing gap-checkpoint materializer. It must not call the agent invoker, retrieval, role recording, synthesis, or provider paths.
- Reuse that owner after successful synthesis and during HumanConfirmNeed preflight, and expose `POST /topic-selection/research/arena/sessions/:arenaSessionId/gap-projection/recover` with no caller-supplied candidate pool and the existing checkpoint record as its response. A projection failure after durable synthesis may leave the advisory unprojected, but exact recovery must converge on the same current gap checkpoint and a second recovery must be a no-op replay.
- Exit when an injected post-synthesis projection failure is repaired through the recovery action with unchanged role-execution, invocation-audit, transcript, risk-finding, and candidate-advisory identities and zero additional agent/provider calls.
- Verification: Failure injection immediately after synthesis; missing, open, superseded, wrong-kind, cross-title, InputSnapshot, transcript-checksum, candidate-version, and advisory-binding negatives; exact and concurrent recovery; stage-view refresh after recovery; agent-invocation count fixed at the original two; OpenAPI/route/schema alignment; real-Postgres persistence; focused typechecks/tests.
- Implementation checkpoint (2026-08-31): complete. One `TopicSelectionResearchGapProjectionService` now owns both ordinary gap projection and exact-session recovery, replacing the application-level inline callback while preserving the existing HumanConfirmNeed policy-version binding. The shadow runner and HumanConfirmNeed preflight reuse this owner; the new session-id-only recovery POST derives candidate refs exclusively from the checksum-valid persisted synthesis and rejects missing, open, superseded, wrong-kind, cross-title, snapshot-drifted, transcript-drifted, candidate-version-drifted, duplicate, or advisory-drifted inputs. Projection also fails closed when candidates do not share one exact EvidenceMap and workspace. Recovery materializes only the existing gap checkpoint, then verifies that the resulting packet contains exact current Arena advice with no projection issues. Exact, repeated, and eight-way concurrent replay converge; the post-synthesis failure fixture proves the Arena session, transcript, and candidate-advisory identities remain unchanged and both human and LLM stage views recover the advice. The dependency graph contains no agent invoker, retrieval, role-recording, synthesis, risk recorder, or provider path. Focused checks pass 27/27, real PostgreSQL concurrent recovery passes 1/1, the full backend suite reports 2,868 tests with 2,798 passed, 70 conditional skips, and 0 failures, OpenAPI parses with 212 paths, and root typecheck, DB-context, and diff checks pass. No schema migration or new authority owner was added; Phase 9C-3B/3C remain separately unauthorized.

###### Phase 9C-3B — Recover exact review history and derive reopen signals without new persistence
- Add `GET /topic-selection/checkpoints/:checkpointId/arena-advisory-reviews` with response contract `TopicSelectionResearchArenaAdvisoryReviewHistory@v1`. The deterministic response names checkpoint currentness (`current` or `superseded`), valid labels ordered by `created_at` then artifact-ref id, projection issues, exact HumanConfirmedDecision bindings, derived reopen signals, and a `history_hash`. Labels remain immutable; no synthetic “current label” is introduced.
- Reuse the existing `TopicSelectionArtifactRef` InputSnapshot index and add only a minimal title-card HumanConfirmedDecision reader. Extract a currentness-free review decoder from the existing advancing guard: read recovery revalidates dedicated stable-key provenance, derived review id, strict-human creator, artifact checksum/schema, title card, checkpoint/InputSnapshot/candidate-pool binding, historical advisory hash, normalized human-review hash, selected candidate, response, and recomputed reason-code classification. The HumanConfirmNeed guard continues to impose currentness separately.
- Ignore unrelated generic artifacts. An artifact that claims the review schema without dedicated provenance surfaces a typed lookalike issue; a dedicated review candidate with checksum, title, InputSnapshot, checkpoint, advisory, human-review, selection, or classification drift is excluded from valid history and surfaces a typed projection issue. One corrupt candidate must not hide other valid labels, and invalid candidates never emit signals.
- A `defer` label remains readable without a HumanConfirmedDecision. For every valid label, report `proposed` until a valid existing HumanConfirmedDecision for the same title card references the exact review artifact; report `confirmed` with that decision ref only after the binding is proven. Only confirmed signals may be described as already reopened or advanced.
- Derive signals only in the read model. `SELECTED_PARKED_CANDIDATE` or `SELECTED_DROPPED_CANDIDATE` yields `candidate_reopened`; `ADVANCE_AGAINST_NONE_VIABLE`, `ADVANCE_BEFORE_EVIDENCE_EXPANSION`, or `ADVANCE_WITHOUT_REFRAME` yields `advanced_against_stop`. `NON_SELECTED_DISPOSITION_CHANGED` alone, defer, and an accepted stop yield no signal. Every signal carries the review ref, selected candidate, reason codes, status, and optional HumanConfirmedDecision ref; no event, disposition, checkpoint, or authority state is written.
- Project all valid current and superseded histories, bindings, signals, and issues into the research-gap LLM working set. Human Markdown gives a concise current conclusion, defer/accept/override history, proposed-versus-confirmed wording, and superseded-history count without leading with internal ids. Add review candidate refs and projection issue codes to the research-gap manifest entry so label-only changes invalidate `manifest_hash` and downstream `view_hash` while the checkpoint `snapshot_hash` stays unchanged.
- Exit when labels survive restart and checkpoint supersession, deterministic multi-label order and hashes hold, partial label-only advancement is reported as proposed, exact HumanConfirmedDecision binding is reported as confirmed, corrupt/lookalike records are isolated with issues, manifest/view invalidation works, and both maintained views remain intelligible.
- Verification: Deferred and advancing labels; proposed/confirmed binding; current/superseded and multiple-label history; corrupt checksum, lookalike, wrong title/InputSnapshot/checkpoint/advisory/classification/human-review hashes; unrelated generic artifacts; valid-plus-invalid isolation; manifest hash change with stable checkpoint snapshot hash; human/LLM rendering; route/OpenAPI/shared-schema drift; real-Postgres persistence and reads; full relevant typechecks/tests. No migration, provider, authority writer, or continuation-envelope change is permitted.
- Implementation checkpoint (2026-08-31): complete. `GET /topic-selection/checkpoints/:checkpointId/arena-advisory-reviews` now returns deterministic `TopicSelectionResearchArenaAdvisoryReviewHistory@v1` over the existing InputSnapshot-indexed artifact owner. One currentness-free decoder revalidates dedicated stable-key and derived review identity, strict-human provenance, schema/checksum, historical checkpoint/title/InputSnapshot/candidate-pool/advisory binding, normalized human review and hash, selected candidate, and server-recomputed response/reason codes; the existing HumanConfirmNeed binding still requires the current checkpoint separately. Unrelated artifacts remain invisible, while lookalike, checksum, provenance, binding, and classification failures are excluded and surfaced as typed issues without hiding valid labels. Existing title-card HumanConfirmedDecisions are read without a migration: an exact valid review ref changes a label and its signals from `proposed` to `confirmed`; defer, accepted stop, and non-selected-disposition change alone emit no reopen signal. Current and superseded histories, bindings, issues, and signals enter the LLM working set; concise human Markdown distinguishes pending proposals from confirmed advancement without leading with internal ids. Review artifacts, bound decision refs, and issue codes invalidate the research-gap manifest and stage-view hashes while checkpoint snapshot identity stays fixed. Review repairs additionally made confirmation binding itself invalidate the manifest, classified derived review-id drift as provenance failure, and required the bound ValidatedNeed target to name the same title card. No event table, schema migration, provider call, authority write, candidate mutation, continuation-envelope change, or Phase 9C-3C operation group was added. Focused checkpoint/route checks pass 27/27, shared contracts pass 439/439, real-Postgres restart recovery passes 1/1, the full backend suite reports 2,874 tests with 2,804 passed, 70 conditional skips, and 0 failures, OpenAPI parses with 212 paths, and root typecheck plus DB-context checks pass. Phase 9C-3C remains separately unauthorized.

###### Phase 9C-3C — Treat one researcher decision as one replayable operation group
- Keep the continuation-envelope schema and effect classes unchanged. At the research-gap human boundary, one exact researcher decision authorizes one documented client operation group: record the advisory review label, then—only for an advancing accept/override with a selected candidate—call the existing HumanConfirmNeed using the returned review ref. `defer` and agreement with a no-topic/reframe/expand stop write only the label. The product HTTP contracts and guards own the semantics; no desktop-GUI composition is added in this slice.
- Bind every new HumanConfirmNeed attempt to one immutable, server-hashed `TopicSelectionHumanConfirmNeedIntent@v1` under the existing `TopicSelectionArtifactRef` owner. The intent binds the reserved adjudication/ValidatedNeed lineage and the complete normalized `HumanConfirmationInput`, treating the Arena review ref as the one server-returned slot. An advancing Arena label carries that same typed intent and hash before HumanConfirmNeed runs; a changed payload after a label-only partial result therefore conflicts instead of becoming a new implicit human decision. Existing pre-9C-3C review history remains readable and retains its previous advancing compatibility, while every newly recorded advancing label requires the intent binding.
- Do not add a composite HumanDecision endpoint, cross-service transaction, decision-event table, or second decision authority. HumanConfirmNeed derives a deterministic HumanConfirmedDecision identity from the bound intent and create-or-recovers exact concurrent writes; its stable intent artifact makes full confirmation replay content-bound rather than comparing only actor/rationale/artifact refs. If the label succeeds and HumanConfirmNeed fails, the client resumes with the same label idempotency key and confirmation payload. Exact or concurrent replay must create at most one review artifact, intent artifact, HumanConfirmedDecision, ValidatedNeed, and advancing checkpoint adaptation. The client refreshes the continuation envelope after the operation group rather than asking for another authorization between its two writes.
- Preserve no-advice compatibility precisely: the dedicated Arena review ref is absent and HumanConfirmNeed follows the existing strict-human, current-snapshot gap-selection review path. The legacy actor/rationale shortcut does not bypass candidate-pool review. All Phase 9C-1/2 stale, cross-title, changed-label, no-topic override, and defer behaviors remain regressions.
- Exit when a production-shaped HTTP test proves one human decision can complete the replayable label-plus-confirmation group without a second stop, a label-only partial failure resumes cleanly, defer/accepted-stop remains non-advancing, the no-advice path remains guarded and compatible, and the Arena still has no checkpoint, AcceptedRisk, promotion, bridge, or fork authority.
- Verification: Continuation-envelope before/after group; advised accept/override, no-topic override, defer, accepted stop, no-advice, label-only partial failure, exact/changed/concurrent replay, complete-intent and legacy-history compatibility, authority-count assertions, OpenAPI/process-document alignment, focused real-Postgres checks, and full relevant typechecks/tests.
- Implementation checkpoint (2026-08-31): complete. `TopicSelectionHumanConfirmNeedIntent@v1` now binds the reserved adjudication/ValidatedNeed lineage and complete normalized confirmation input before every new advancing label, with the dedicated Arena ref held as the sole null server-returned slot. HumanConfirmNeed stores the same stable intent ref, derives HumanConfirmedDecision identity from its hash, and create-or-recovers exact primary-key and ValidatedNeed uniqueness races; exact or concurrent replay therefore converges on one label, intent, decision, ValidatedNeed, and checkpoint adaptation. Changed content conflicts after a label-only partial result, while pre-9C-3C labels preserve their prior readable/bindable behavior. Defer and accepted stop remain label-only, and a no-advice caller must provide the same complete current gap review without an Arena ref; the legacy actor/rationale shortcut cannot bypass the guard. OpenAPI and the maintained continuation-envelope process document now specify the two existing endpoints as one researcher-authorized client operation group with no intermediate envelope reevaluation. Focused HTTP behavior and contract drift pass, shared contracts pass 439/439, the full backend suite passes 2,805/2,875 with 70 conditional skips and zero failures, real-Postgres eight-way concurrency passes 1/1, OpenAPI parses with 212 paths, and root typecheck plus DB-context checks pass. No migration, composite endpoint, cross-service transaction, new decision table, provider path, effect-class change, or desktop-GUI composition was added. Phase 10 remains separately unauthorized.

### Phase 10 — Calibrate, adopt, and generalize only where valuable

#### Phase 10A — Make calibration evidence product-readable without creating a new authority
- Outcome: One deterministic, API-readable Arena calibration report distinguishes missing evidence from observed failure and from activation eligibility; current evidence correctly reports `insufficient_evidence`.
- Approach: Extend the existing OfflineEvaluation dataset/case/run/metric contract with a `research_arena` stage and Arena-specific case/metric vocabularies. Reuse its current Prisma tables and repository rather than adding a calibration table. Cases freeze exact refs and expected relations; evaluation reloads canonical InputSnapshots, EvidencePackets, v2 role executions, transcripts, advisory-review history, HumanConfirmedDecision bindings, and accounting instead of trusting caller-authored observed output.
- Planned changes:
  1. Add dominance-pair, causal-perturbation, irrelevant-perturbation, ambiguous/non-advance, and advancing case types plus evidence-grounding, execution-independence, replay, human-label, cost/latency, and work-avoided metrics.
  2. Emit a checksum-stable `TopicSelectionResearchArenaCalibrationReport@v1` with human Markdown, a larger LLM working set, exact case/result refs, coverage gaps, hard blockers, and one recommendation: `insufficient_evidence`, `remain_advisory`, or `eligible_for_activation`.
  3. Expose strict create/read/evaluate routes under the research-arena API; evaluation is support-only, cannot run roles/providers, and cannot mutate checkpoints, policy, human decisions, candidates, promotion, bridges, or PaperProjects.
  4. Treat task-only v1/legacy role simulations and the Phase 8 `fork_turns=none` calibration as historical evidence only; they may seed expected relations but cannot satisfy product-v2 execution coverage.
- Dependencies: Closed Phase 9C-3C; existing OfflineEvaluation owners and Phase 8 frozen relations. No provider, prompt, external corpus, or T-129 change.
- Exit criteria: The current corpus yields an exact, replayable `insufficient_evidence` report naming the missing product-v2/label coverage; changed or forged source identity fails closed; exact/concurrent evaluation converges without a migration or policy change.
- Verification: Shared schemas, existing-owner compatibility, report determinism, product-v2 provenance and evidence re-resolution negatives, HTTP/OpenAPI, real-Postgres replay, full relevant tests/typechecks, and zero-authority assertions.
- Recovery: Mark an incomplete evaluation run failed or cancelled through the existing evaluation lifecycle, retain its immutable evidence, and ignore the report. Production remains advisory.
- Implementation checkpoint (2026-08-31): complete. The existing OfflineEvaluation dataset/case/run/result/metric/replay owners now admit the closed `research_arena` vocabulary, while one Arena-specific service alone computes the six calibration metrics from canonical InputSnapshots, EvidencePackets, role executions, transcripts, advisory-review history, HumanConfirmedDecision bindings, and explicit accounting. Five strict HTTP operations create datasets/cases/runs, evaluate a run, and read its report; none can execute a role/provider or activate policy. Legacy v1 executions remain usable only as historical relations and never satisfy product-v2 coverage. Product-v2 admission re-resolves exact evidence partitions, retrieval provenance, non-provider invocation audit, output hash, and execution identity; source drift fails before result writes, an ungrounded drop is a hard blocker, malformed persisted observations fail closed on report read, and missing accounting or human labels remain visible gaps rather than false success. The real current corpus persisted dataset `offline_eval_dataset_db8f792d-9c20-4df5-9b68-53719ca77f02`, case `offline_eval_case_fa12cf40-a3a5-4c9c-bb46-1118e051dcad`, and run `offline_eval_run_d64bd0c4-8074-4474-bf41-d9c6ef51bfe1`; exact POST/GET replay agrees on trace hash `143a4639c8b8c0ba964d109226a5f3b9715ba3620e72ad9a54e5afc26cf8847f` and recommendation `insufficient_evidence`, with 12 named coverage gaps, zero product-v2 members, zero human labels, and zero hard blockers. The maintained human and LLM evidence views are `artifacts/phase10a-current-corpus-calibration.md` and `artifacts/phase10a-current-corpus-calibration-llm.json`. Shared contracts pass 443/443; the full backend suite passes 2,814/2,885 with 71 conditional skips and zero failures; real-Postgres eight-way evaluation convergence passes 1/1; root typecheck and DB-context checks pass; OpenAPI parses with 217 paths and 522 schemas. No migration, role/provider execution, checkpoint/candidate/human/promotion/bridge/PaperProject write, or policy activation was added. Phase 10B remains separately unauthorized.

#### Phase 10B — Collect one bounded product-v2 decision-quality matrix
- Outcome: A small fixed matrix supplies the missing product-native evidence without turning calibration into an endless research project.
- Review refinement (2026-08-31): the six named cases are not six Arena executions. With unique members, two dominance pairs and two perturbation pairs plus two singleton cases require at most ten product-v2 Arena sessions and twenty role invocations. The Phase 10A case endpoint currently freezes members only after sessions exist, derives perturbation relations only from final outcomes, and can report eligibility without proving that every member has one exact human label. Its evaluator also looks for authorization-pause and work-avoided fields that the product transcript contract never writes. Existing strict-human admission requires the member's gap checkpoint to remain current and pending. Phase 10B therefore starts with a pre-registration and derived-measurement contract slice and uses two execution tranches; it does not run the six cases under the Phase 10A contract as written.
- Approach: Reuse recorded lineages and current reviewed literature for exactly six unique-member slots: two dominance pairs, one relevant and one irrelevant perturbation, one successful non-advance, and one advancing case. Cap the whole batch at ten Arena sessions, twenty fixed-role local non-provider invocations, and one typed-delta retry per eligible lineage. No Arena session may count in more than one slot. Freeze selection rules, exact source identities, expected relations, allowed deltas, measurement windows, budgets, and stop conditions before role output is observed. Label every member while its strict-human gap checkpoint is current; batch those labels into at most two researcher review points per tranche rather than requesting authorization for routine execution steps. Stop immediately on a hard blocker and stop after the first tranche when it demonstrates neither a decision difference nor a measured downstream stage avoided.

##### Phase 10B-1 — Pre-register the matrix and make post-hoc case selection impossible
- Extend the existing `research_arena` OfflineEvaluation dataset payload to a backward-readable `TopicSelectionResearchArenaCalibrationDataset@v2` protocol manifest; do not add a table, migration, second manifest owner, or role-execution route. The dataset freezes six slot keys, case types, tranche membership, ordered eligible lineage/source pools, deterministic selection rules, expected relation classes, measurement windows, stage-level work-avoided definitions, and the ten-session/twenty-invocation/two-review-points-per-tranche ceilings before the first tranche runs. Each existing OfflineEvaluation case row then becomes the immutable pre-run member registration for one slot: it stores exact planned session keys, InputSnapshot/candidate/evidence refs, label-slot identity, allowed single-delta refs and causal-or-irrelevant classification, and dominance axes/rationale before any bound role output exists.
- Keep existing v1 datasets/cases readable as historical `insufficient_evidence` inputs. A v2 case is admitted only before execution and only when its member recipes match one unused dataset slot and the frozen ordered selection rule. Evaluation resolves actual sessions through the registered stable keys and exact canonical identities; source drift, post-run registration, cross-slot member reuse, a missing or extra delta, or a result-derived relation declaration fails before evaluation writes.
- Strengthen the report gate so every completed member—not merely the aggregate label counts—must have the one designated exact strict-human label and complete accounting. Keep runtime call/retrieval/evidence-size/wall-time values sourced from the immutable transcript; derive one semantic authorization pause from the exact advisory-review/advancement operation group and derive work avoided from an accepted non-advance plus absence of the predeclared canonical downstream stages. Do not ask a caller or transcript to invent either value. Define decision difference and override categories in the frozen protocol instead of interpreting them after results; inability to derive an exact value remains `insufficient_evidence`.
- Exit when focused contract/service/HTTP/real-Postgres tests prove pre-result registration, exact one-variable relation validation, unique-member accounting, complete member labels, backward v1 readability, deterministic replay, and zero role/provider/research-authority/policy writes. This slice changes calibration admission only and performs no Arena execution.

##### Phase 10B-2 — Run and judge the first three-case tranche
- Curate—not merely query by row count—the ordered eligible lineage/source pools for all six slots and freeze the dataset protocol before any first-tranche role output. Register the exact five unique member recipes for dominance pair 1, the relevant perturbation, and the successful non-advance control; open or prepare only the needed deterministic session keys/InputSnapshots, and precompute the permitted retry snapshot while the first attempt is still open.
- Execute at most five product-v2 sessions and ten role invocations under the approved Phase 10B operation boundary, with zero provider calls and no per-run authorization stop. Record the first-wave labels together while each checkpoint is current; for the relevant perturbation, record the control label before opening/running its one typed-delta retry, then present the variant in the second and final tranche review point.
- Evaluate the three registered cases immediately. Any provenance, independence, evidence-resolution, relation, drop-justification, replay, label, authority, provider, or accounting hard blocker ends the batch. If there is no observed decision difference and `work_avoided_stage_count` remains zero under the frozen measurement window, retain the partial report and stop for demonstrated redundancy.

##### Phase 10B-3 — Conditionally complete the second three-case tranche
- Proceed only when 10B-2 has no hard blocker and has produced non-redundant decision or work-avoided evidence. Register the exact remaining member recipes for dominance pair 2, the irrelevant perturbation, and one genuinely new process-selected advancing case from the already frozen ordered pools before their role outputs; the frozen moderate gate remains an ambiguous fixture and cannot fill the positive slot or be promoted for sample completion.
- Execute at most five additional product-v2 sessions and ten additional role invocations, with the same current-checkpoint label ordering and at most two grouped researcher review points. The irrelevant perturbation must preserve the decision under exactly one predeclared irrelevant evidence delta; the advancing singleton must arise from the predeclared process-selected lineage rather than result shopping.
- Produce the final or decisively stopped Phase 10B report with exact denominators, all member labels, override categories, semantic-group/direct-neighbor refs, evidence deltas, retrieval/model-visible evidence accounting, wall time, authorization pauses, and actual downstream stages not entered. A justified stop, park, reframe, or expansion counts as success; explained repair, human-objective difference, possible false drop, and possible false continue remain separate instead of being averaged away.
- Dependencies: Phase 10A report contract and researcher approval of Phase 10B on 2026-08-31. That approval covers 10B-1 implementation and routine bounded local execution in both tranches without per-run stops; the researcher still judges actual outputs at the grouped strict-human label points. T-129 remains pending and no provider execution is allowed.
- Exit criteria: The six-slot matrix is complete or has stopped early with a decisive hard failure/redundancy finding; every completed member is product-v2, evidence-resolvable, independently exposed, replayable, exactly human-labeled, uniquely counted, and cost-accounted.
- Verification: Protocol-manifest freeze, pre-run identity and slot admission, role/exposure/audit identity, citation resolution, exact relevant/irrelevant evidence-delta behavior, dominance rationale, one-delta limit, label-before-supersession ordering, label-to-HumanConfirmNeed binding where advancement occurs, no-provider accounting, grouped interaction ceilings, and retained stopped/parked controls.
- Recovery: Stop the batch and retain its immutable partial report and registered unused slots; do not fill or redefine a slot merely to improve a rate. A new batch requires an explicit evidence or policy delta and separate review.

#### Phase 10C — Make the researcher-owned adoption decision
- Outcome: The evidence produces an explicit `activate`, `remain_advisory`, or `retire` decision; the evaluator cannot choose or execute it.
- Approach: Apply the predeclared semantic coverage and hard-fail rule. Dominance and both perturbation directions must have no known miss; every drop is evidence-backed and reopenable; every human override is explained; no confirmed false drop/false continue, provenance/replay failure, authority leak, provider call, or extra authorization pause is tolerated. At least one real decision improvement or measured downstream stage avoided is required—otherwise recommend `remain_advisory` or `retire` even when the mechanism is technically correct.
- Planned changes:
  1. Present human and LLM reports together with exact source refs, denominators, missing coverage, limitations, and the single best recommendation.
  2. Record the researcher's exact decision and rationale as the Phase 10 planning/policy checkpoint, not as an Arena or checkpoint decision.
  3. If evidence is insufficient, permit at most one separately reviewed delta-bearing collection batch; do not indefinitely defer activation through repeated unchanged calibration.
- Dependencies: Phase 10B report accepted as complete or decisively stopped.
- Exit criteria: One adoption disposition is confirmed and its next route is unambiguous. `remain_advisory` does not silently authorize future activation; `retire` requires a separate removal checkpoint; `activate` authorizes only the early gap arena policy cutover.
- Verification: Report/source-hash agreement, no post-result threshold changes, explicit limitation accounting, and task/governance synchronization.
- Recovery: Before a policy cutover, changing the disposition only requires a new researcher-confirmed planning checkpoint grounded in new evidence.

#### Phase 10D — Conditionally cut over only the early gap policy
- Outcome: If and only if Phase 10C chooses `activate`, new gap advancement requires current, reviewable Arena advice while strict-human HumanConfirmNeed remains the sole research authority.
- Approach: Introduce one server-owned gap-policy v2 selected by product code, never by the client. Already decided history stays readable. New advancement under the active policy requires a current v2 gap checkpoint whose complete candidate pool carries one checksum-valid product-v2 Arena synthesis and exact advisory label; missing/stale/invalid advice fails before HumanConfirmedDecision or ValidatedNeed. Arena `support_only=true` remains invariant and a human may accept, override, defer, or stop.
- Planned changes:
  1. Make the active gap policy and its rollback version explicit in one backend policy owner; caller `policy_version_id` cannot opt out or opt in.
  2. Rematerialize an undecided legacy gap checkpoint under v2 only after current advice exists; never rewrite decided checkpoints or delete history.
  3. Preserve the one-decision Phase 9C-3C operation group, exact replay, no extra authorization pause, and current stage views.
  4. Do not add question-design, N8, provider, or multi-provider activation. Any later arena placement becomes a separately planned outcome after the early slice proves production value.
- Dependencies: Explicit Phase 10C `activate` decision and separate implementation authorization. If the decision is `remain_advisory`, this phase is skipped; if `retire`, replace it with a separately reviewed compatibility-preserving removal slice.
- Exit criteria: Missing advice cannot advance under v2; exact accepted/override advice can; stop/defer stays non-advancing; rollback returns future advancement to advisory mode without corrupting history or existing decisions.
- Verification: Policy-owner and caller-bypass negatives, currentness/supersession, full operation-group replay/concurrency, old decided-history reads, rollback test, OpenAPI/process docs, real Postgres, and full suite.
- Recovery: Revert the server-owned active policy to the advisory version. Preserve all Arena, review, human-decision, and checkpoint history; do not downgrade or rewrite records.

#### Phase 10E — Prove a fresh outcome and close or explicitly narrow the task
- Outcome: The chosen adoption disposition is demonstrated end to end, and A9-A13 close without promoting the frozen moderate topic or manufacturing a success.
- Approach: For activation, run one genuinely process-selected advancing topic plus one successful stopped/parked control through canonical local APIs. Complete the positive branch through post-bridge blocking objection, wording-only rejection, substantive ResearchSlice/evidence repair, and exactly-once topic handoff. For remain-advisory or retire, record the bounded negative conclusion and reconcile the task outcome/acceptance rather than pretending production divergence activated.
- Dependencies: Phase 10C disposition and, when activated, Phase 10D cutover. Provider and prompt calibration remain T-129.
- Exit criteria: Fresh positive and successful-stop evidence prove the selected policy behavior, risk/dissent survival, stage-level human control, and recoverable handoff; or the task explicitly narrows/retires the Arena with no competing live path. Later-stage arena generalization is either rejected or opened as a separate tracked outcome.
- Verification: Fresh local dual-track acceptance, exact replay, post-bridge objection invalidation, substantive repair, handoff exactly once, stop/non-advance control, no paid provider, no legacy bypass, final OpenAPI/context/task/hub alignment, and researcher closeout.
- Recovery: Stop at the last canonical owner, preserve all evidence, and leave the task in progress rather than forcing a positive closeout.

## Kickoff gate

- Status: ready
- Authorized boundary: through Phase 10B
- [x] Decisions: the current medium-value gate is frozen as an ambiguous calibration fixture instead of being promoted for process completion.
- [x] Design: EvidencePacket resolution, arena owner boundary, and risk-carry projection are settled by the Phase 7 audit (`artifacts/phase7-owner-map.md`) and the researcher-confirmed `TopicSelectionResearchArenaSession` coordination root; branching is settled — fork is out of scope, and parked-alternative return is serial and delta-gated.
- [x] Route: contract alignment and shadow validation precede stable dissent carry, product-auditable advisory integration, live override collection, and only then measured production adoption.
- [x] Verification: dominance-pair, perturbation, and ambiguous-lineage fixtures plus drop-justification, override-convergence, evidence-delta, cost, risk-survival, replay, and full-chain obligations are identified in `verification.md`.

## Risks and recovery

| Risk | Detection | Mitigation | Recovery / rollback |
|---|---|---|---|
| Checkpoint state becomes a second content authority. | Packet or checkpoint stores independent versions of evidence, question, or promotion facts. | Store refs, hashes, decisions, and transition state; keep domain content in existing authorities. | Remove duplicate fields and rebuild the projection from domain records. |
| New gates make current records permanently unusable. | Existing current packages/bridges cannot be classified or reviewed under the cutover. | Inventory record classes and require an explicit versioned migration decision. | Keep records readable and block only new advance until a current review is created. |
| Academic policy becomes arbitrary count-based bureaucracy. | Advancement depends on raw paper/candidate counts despite weak evidence roles. | Make semantic role/quality rules authoritative and counts configurable tripwires only. | Reclassify the rule and preserve the negative evidence that exposed it. |
| Clients retain hidden bypasses. | Direct node routes can advance without a current checkpoint decision. | Centralize transition guard enforcement and cover every entry point with negative tests. | Fail closed at the control plane and remove the bypass before cutover. |
| Human participation becomes ceremonial again. | Packets omit alternatives, eliminated paths, unresolved objections, or allowed loopbacks. | Define packet completeness in product contracts and acceptance tests. | Block the checkpoint and regenerate the packet from current authority. |
| Acquired source assets depend on a transient configured raw-file root. | `storage_roots.raw_files` resolves under the macOS temporary directory even though normalized processing roots are persistent. | Preserve paragraph-level normalized text and key-content owners; do not overwrite the storage setting without a separate exact control authorization. | Reacquire the two explicit OA assets through a new bounded dry-run if the raw files disappear; never broaden the corpus or downloader policy implicitly. |
| Arena roles receive correlated summaries instead of independent evidence. | Different roles repeat the same framing, citations, and conclusions despite distinct names. | Persist pre-exposure outputs and product-owned role-specific retrieval scopes; reject evidence-free or semantic-duplicate participation. | Return the arena to shadow mode and revise role/evidence partitioning. |
| Evidence refs are traceable but not model-visible. | A role cites UUIDs or inherited summaries without resolvable claim-bearing excerpts. | Require EvidencePacket resolution with locator and support/challenge relation before admission. | Fail the arena input and repair retrieval/index ownership rather than hallucinating content. |
| Material dissent disappears before promotion. | N8 critic risks exist while the final gate contains no warning, risk ref, action, or accepted disposition. | Carry stable objection/minority-report refs through every handoff and gate on explicit disposition. | Invalidate the affected downstream packet and rematerialize it from current dissent authority. |
| Branch currentness collides. | Two valid siblings for one TitleCard/stage supersede each other or both appear canonical. | Fork is out of scope: exactly one active path plus snapshot-bound parked alternatives with delta-gated serial return; a fork recommendation is recorded as `park`. | Collapse back to one active path without deleting parked evidence. |
| Calibration rewards continuation. | Ready-gate rate improves while dominated topics keep advancing or accumulated overrides show good topics being killed. | Treat justified stop as success and measure dominance-pair consistency, perturbation sensitivity, override convergence, evidence delta, and work avoided. | Keep the arena shadow-only or disable the underperforming stage. |

## Phase closeout

- Review: At each phase, review authority boundaries, human decision semantics, bypass analysis, and whether new rules improve informed control rather than only adding records.
- Record update: Keep status, architecture, verification evidence, workflow matrix, OpenAPI, and project mapping aligned with confirmed product reality.
- Checkpoint: Land only verified, recoverable phase units with the task trailer after scoped sync and governance lint.
- Post-completion audit: Re-run lineage, concurrency, contract-completeness, matrix-drift, fixture-realism, and supported-Node verification before archive readiness; any discovered gap remains owned by T-147 until repaired and decisively reverified.
