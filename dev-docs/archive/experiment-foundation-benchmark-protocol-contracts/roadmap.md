# Roadmap

## Purpose
- Make comparison assets lockable and compatible with research-argument readiness without creating semantic duplication.

## Milestones
1. Benchmark, protocol, metric, baseline ownership freeze.
2. Protocol version/hash and baseline implementation version.
3. Readiness ladders and schema tests.
4. Downstream review with recipe and result validation tasks.

## Handoff
- Close before `RunRecipe` locks or result validation protocols are finalized.
