# Verification

## Phase 4 entry checkpoint — 2026-09-08

- **Implemented:** FIND-032 / TSRF-22 replaces opaque research-stage labels in 21 desktop files and maintained operator guidance. Navigation/action identities, API paths and authority behavior are unchanged. Desktop renderer/main typecheck passes; five Vite SSR surfaces render the new terms; an AST audit preserves literal navigation/action values and finds no opaque version label in Chinese UI copy. Review of this bounded copy delta is Clear. No new tests or layout machinery were added. The initial plain-Node SSR probe could not supply Vite's `import.meta.env`; the final Vite middleware SSR run (dependency discovery disabled) passed cleanly.
- **Historical recovery:** [selected HTTP observations](artifacts/phase4-local-reread-2026-09-08.json) preserve exact source IDs and response hashes. All three original rehearsal titles recover; the first two show research rejection independently of management `active`, and the third remains independent. Current refined question text/metrics and the 25-risk current assessment are visible. Two checkpoint packets and the promotion bundle reread identically after clean restart; all five frozen packet/support/dossier/bundle responses remain identical after database synchronization and new-lineage creation. This does not establish the original process-exit cause (FIND-004).
- **Environment repair (FIND-033):** original evidence-map GET reproduced 500 twice. A temporary service-boundary error-code probe identified Prisma `P2022`, missing `TopicSelectionEvidenceMap.predecessorEvidenceMapId`; Prisma migration status confirmed exactly two pending migrations. Reviewed and applied `20260903090000_add_topic_selection_evidence_convergence_contracts` and `20260907100843_align_arena_execution_state_constraints` using existing `pnpm db:dev:migrate` to local `postgres` / `my_researcher_dev` at `127.0.0.1:5432`. All **82 migrations are current**; original GET now returns 200 with two historical maps, and nine units are readable. No new migration or data reset. The ephemeral probe and debug journal were removed; a clean backend performed the verification.
- **Downloader preflight (FIND-009):** a request-only four-byte limit over `LIT-2297` yields `DOWNLOAD_PDF_SIGNATURE_LIMIT`, zero planned resolver/download calls and `network_verified=false`. Persisted 100 MiB / 60 s / five-redirect settings and job history were not changed.
- **Fresh evidence boundary:** [operation receipt and frozen packet](artifacts/phase4-fresh-evidence-2026-09-08.json) recovers TitleCard `title_card_e22967ea-bb22-41b6-8306-86f5acb714e2`, SearchRun `search_run_9ed44667-90f6-4d9a-bd69-58128bc36f6b`, EvidenceMap `evidence_map_10dd5a3a-553f-47fb-ba8b-0d0524623b4f` and checkpoint `research_checkpoint_300dc6d5-e741-40ba-b85b-1e547796f8fc`. Nine unchanged source statements from eight records (seven paragraph / two section locators) pass real HTTP admission with scoped EvidenceMap refs over unscoped SearchRun refs. Support/challenge/baseline/context, material conflict, both missing coverage rows and unresolved literature limitations are visible. Policy is `human_acceptance_required`, exactly one checkpoint exists and its Human decision is null. Continuation evaluation returns `stop_for_human`; no prior Human decision is copied.
- **Accounting:** RET-033/034 record 0 retriever operations, 0 provider calls and 0 new literature evidence delta. SearchRun is `manual_import`; raw provenance retains source owner IDs and both historical query intents without counting imported units as retrieval hits. Historical response/telemetry gaps remain explicitly unknown. No provider, fulltext download, backup/restore, build or new Human decision ran.
- **Human checkpoint:** [readable review](artifacts/phase4-human-review-2026-09-08.md) and [exact proposed payload](artifacts/phase4-human-decision-proposal-2026-09-08.json) are drafts, not authority. Shared request-schema validation and exact missing-row ref matching pass. The proposed review flags become true only if the researcher confirms that review. Do not submit on a generic implementation continuation or infer a new decision from historical acceptance.
- **Disposition (TSRF-05):** FIND-004 remains unproven with a recurrence capture criterion; FIND-005 proposes an operations-owned bounded backup/restore proof; FIND-006 proposes an experiment-owner paired benchmark slice. No task package or adjacent implementation was created. FIND-012 is reconciled to T-148 accounting and T-150 durable-kernel ownership; the generic retrieval endpoint is not claimed fixed.
- **Checkpoint checks:** scoped T-148 sync/apply/lint, global governance sync dry-run/lint and `git diff --check` pass. Session-only HTTP capture files and the debug journal are removed. The local verification backend uses `buildApp({ backgroundWorkEnabled: false })`; no unrelated job drain is enabled. Its session-owned process was stopped after verification, restoring the initial backend lifecycle state.
- **Remaining:** exact fresh Human decision, then fresh research-gap/question/value/refinement and regular N6/promotion Debate plus condition-group acceptance. Historical deterministic promotion has 25 risks and no LLM draft; its preserved outcome cannot prove FIND-027/028. TSRF-04 and TSRF-07 remain unchecked until the remaining verification is complete. T-148 is not done. T-151's concurrent `ac05970b` changes only its task records and is excluded.

## Conditional-promotion support checkpoint — 2026-09-08

- FIND-028 / TSRF-16 red/green: a canonical 25-finding/five-group final output originally lost its condition candidates during admission; incomplete candidate coverage was also accepted. Typed candidates now survive N2 support, dossier, N3 handoff and exact replay. Shared/runtime schemas and one deterministic validator require complete material/accepted/memory/recheck coverage, identical group/action refs, unique group/action identities and nonblank early checks. Model-assigned owners, missing groups, stale/invented refs, overlap and malformed actions fail before support publication.
- The real in-memory HTTP N1→N2→N3→N4 path uses the native checkpoint chain: removing one of five Human conditions returns 422 `UNMAPPED_PASS_WITH_RISK_FINDING` with no current PromotionDecision/commitment bundle. Supplying all 25 exact refs with Human-assigned owners and edited check timing succeeds and replays exactly. N4 never reads advisory candidates as authority. Deterministic memory/recheck proposals cannot waive `recheck_required`. New N3 checks reject incomplete legacy groups; historical completed gate replay remains available. Fake-Prisma JSON round-trip preserves candidates across service reconstruction; no schema migration is needed.
- Focused backend command: `DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0 pnpm exec node --import tsx --test src/services/topic-selection-v1c-*.unit.test.ts src/routes/topic-selection-v1c-routes.integration.test.ts` from `apps/backend`: **188 passed / 0 failed / 1 explicitly skipped** (real Prisma HTTP smoke). Shared suite: **455/455**. `pnpm llm:config:check`: **5/5**. Root `pnpm typecheck`: passed (shared/backend/desktop, Prisma generation and DB-context constraints). Workflow matrix checker and its two consistency/drift tests passed. OpenAPI YAML parses; shared/OpenAPI candidate schemas agree on 10 valid/invalid checks, including owner rejection and empty/blank checks.
- Main and independent code review: **Clear**, no remaining Must-fix / Should-fix. Independent coordinator/gate checks passed **44/44**, and the FIND-028 native-checkpoint HTTP case passed **1/1**. Bounded prompt version **3** and optional single-agent prompt version **2**, OpenAPI and maintained operator/matrix/scenario guidance describe the same advisory contract. Provider/model routing and role budgets are unchanged. Scoped task sync/apply/lint, global sync dry-run/lint and `git diff --check` passed before this checkpoint. The final documentation increment also received an independent Clear review.
- Limits: no build, live provider, actual product DB access/migration, new Human research decision or full backend suite. No external PostgreSQL concurrency or interrupted execution recovery is claimed. T-148 remains **in-progress** for Phase 4 real-flow acceptance, stage language and adjacent-finding disposition. Parallel T-151 source/test changes were committed separately by their owner and are excluded from this checkpoint.

## Material-risk Debate verification — 2026-09-08

- FIND-027 / TSRF-15 red/green reproduced ordinary deterministic/single-agent support bypass, omitted material-finding coverage in four-role admission, and duplicate support/artifacts on completed replay. Accepted risks or canonical material RiskFinding refs now require Debate before support generation or compatibility gate creation; refs are recovered from handoff, snapshot and source-bundle fields, including legacy artifacts. Risk-free input retains its deterministic fast path; free-text package risk prose does not classify material findings.
- The existing four-role runtime receives material refs in context, hashes and allowed refs. Final semantic admission rejects dropped findings while retaining accepted-risk, recheck and critic-resolution checks. At the FIND-027 checkpoint, prompt version 2 documented this risk-coverage contract; the current version 3 adds the condition candidates described above. The fixed HTTP caller records product/codex_assisted provenance and four existing role budgets with zero provider calls; it does not infer external authoring cost or retrieval.
- Dossier policy and execution records bind frozen input, admission identity/hash and four role artifacts. Concurrent calls sharing a gate service and reconstructed coordinator replay reuse the original support without added artifacts; changed attempt input conflicts. Incomplete role sets fail before runtime writes. N3 rejects a new material-risk gate from legacy support lacking Debate evidence, and rejects mismatched snapshots. Completed historical gate replay remains intact; the original N3 blockers and exact N4 Human authority still apply.
- Focused backend v1c service/acceptance/admission/runtime/coordinator/gate, matrix and contract-drift suite: **160 passed / 0 failed / 0 skipped**. Full v1c HTTP integration: **14 passed / 0 failed / 1 explicitly skipped** (Prisma smoke requires DATABASE_URL). `pnpm llm:config:check`: **5/5**. Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation and DB-context constraints. Matrix checker and drift-injection tests passed. OpenAPI YAML parses; the new request compiles with Ajv, accepts its four role objects and rejects seven malformed variants.
- Parent and independent review: **Clear**, no remaining Must-fix or Should-fix. Independent focused review checks passed **49/49** and material-risk HTTP **1/1**. Scoped task sync/apply/lint and `git diff --check` passed; global sync dry-run/lint passed before the implementation commit. The post-commit global check observed concurrent T-151 drift (kickoff ready with 2/4 items checked, and registry planned versus in-progress); T-148 scoped lint still passed. Those foreign records/generated views are excluded and remain with their owner. The final import/type cleanup also passed coordinator/gate **28/28** and backend TypeScript.
- Limits: no build, live provider, actual product DB access/migration, new Human research decision or full backend suite. Actual Prisma concurrency, cross-process execution claims and interrupted-provider recovery were not exercised or added. FIND-028 grouped conditions are covered by the current checkpoint above; Phase 4 real-flow acceptance remains and T-148 stays in-progress. Parallel T-151 changes are excluded.

## Current regular N6 bounded Debate checkpoint — 2026-09-07

- FIND-018 / TSRF-10 red/green: normal coordinator N6 previously requested/accepted a lone draft. It now requires `n6_divergent` with `initial_from_n5`, fixed 2 Explorer / 1 Critic / 1 Arbiter outputs, and the existing candidate gate. Missing review and wrong generation mode stop before runtime/authority writes. Fresh N5 re-selection excludes stale failure projections; existing escalation, regeneration, N8 feedback and refinement recovery remain covered.
- Codex product HTTP now takes `{ request, role_outputs }`. One real in-memory HTTP flow produces the candidate set through the original deterministic gate, then repeats the exact request with identical authority/handoff refs and no added artifacts. A changed response under the same attempt conflicts. The direct product gate also rejects a lone runtime-verified draft. Independent review reproduced an omitted-request-run_mode bypass; checking the artifact's effective mode closes explicit product, absent and null request variants with no candidate/handoff authority.
- Runtime receipts bind canonical frozen input, execution settings, all four role descriptors/audits, transcript/admission and the bridge draft. Concurrent same-input calls sharing a control-plane service return one result. Reconstructing the runtime and replaying a completed attempt adds no role/bridge artifacts; changed role output fails with `VERSION_CONFLICT`. Missing initial roles fail before writes. Provider execution remains dormant and zero-write at entry; existing configured model routes, prompts and budgets are unchanged.
- Focused backend runtime/coordinator/harness/admission/matrix/OpenAPI-drift suite: **227 passed / 0 failed / 0 skipped**. HTTP integration: **17 passed / 0 failed / 1 explicitly skipped** (Prisma smoke requires DATABASE_URL). Shared suite: **455/455**. `pnpm llm:config:check`: **5/5**. Root `pnpm typecheck` passed (shared/backend/desktop, Prisma generation and DB-context constraints); final backend TypeScript passed after fixture updates. The final wrong-generation-mode assertion also passed in the focused coordinator test.
- The workflow matrix checker and its drift-injection tests passed. OpenAPI YAML parses and the N6 role-output schema compiles with Ajv; valid 2/1/1 shape passes and wrong slot/missing/extra instances reject. Main and independent code review: **Clear**, no remaining Must-fix / Should-fix. `git diff --check` passed.
- No live provider, product database access/migration, new research decision, build or full backend suite ran. Four non-provider role audits plus the existing bridge audit are not counted as retrieval/provider calls. Completed persistence replay is verified; cross-process concurrency and interrupted role-execution recovery are not claimed. Historical harness replay and low-level acceptance/test fixtures remain supported. Phase 4 real-flow acceptance is outstanding; promotion policy is covered by the current FIND-027 checkpoint above.

## Current downloader-preflight checkpoint — 2026-09-07

- FIND-009 red/green: a real settings/acquisition service fixture first demonstrated that dry-run omitted policy origins; the next fixture showed a known 1025-byte asset remained planned under a persisted 1024-byte ceiling. Dry-run now reports repository defaults, decoded configured values, per-field origins, settings timestamp and requested/effective byte limits. Partial/invalid persisted values fall back or clamp consistently with the runtime reader. The 1 KiB/5-second/one-redirect policy remains visible without guessing that an unknown remote asset must fail.
- Local admission: expected_byte_size is an optional positive safe integer bound to an explicit URL. Known sizes above the effective ceiling block with DOWNLOAD_SIZE_LIMIT_EXCEEDED; exact-limit and unknown-size assets stay planned. A ceiling or known size below the required five-byte PDF signature blocks with DOWNLOAD_PDF_SIGNATURE_LIMIT; the boundary and signature-disabled cases remain planned. HTTP rejects zero, negative, fractional and unsafe sizes before service execution and preserves valid size constraints. No network probe is added; network_verified remains false.
- Blocked-only job creation produces zero resolver/download estimates, zero fetch calls, no source attempts and a non-retryable BLOCKED item. Correcting settings makes a fresh plan admissible while the historical job estimate remains unchanged. Existing budget, rights/source priority, retry, pause/resume/cancel, crash recovery, arXiv/Unpaywall download and redirect/localhost protections pass.
- Focused service/HTTP suite: **53 passed / 0 failed / 0 skipped** across literature-fulltext-acquisition-service.unit.test.ts, literature-service.unit.test.ts and research-lifecycle-routes.integration.test.ts (`DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`). After extending the signature check to known lengths, the final acquisition service suite passed **14/14** again. Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation and DB-context constraints.
- OpenAPI YAML parses; its request and policy schemas compile with Ajv and pass valid/invalid size, nullable policy metadata and unverified-network boundary checks. Parent and independent review of the final implementation: **Clear**, no remaining Must-fix or Should-fix. `git diff --check` passed.
- No live provider, product database access/migration, product settings change, new research decision, build or full backend suite ran. Historical jobs without downloader_policy remain readable. Phase 2 local repairs are focused-verified; later bounded real-flow acceptance remains outstanding.

## Current SearchRun/EvidenceMap reference checkpoint — 2026-09-07

- FIND-010 red/green: the service fixture reproduced the generic outside-SearchRun rejection when unscoped admitted literature/paragraph refs were supplied with the correct title scope. The existing admission set now treats absent/null/current-title scope as equivalent without modifying any supplied or persisted ref. Bidirectional fixtures preserve the resulting EvidenceUnit refs and the complete original SearchRun record, including hashes.
- Identity and diagnostics: type, ID, version and explicit foreign-title drift are rejected for unit literature/source refs and admitted locator refs, including locator.literature_ref. Errors retain 409 GATE_CONSTRAINT_FAILED and return details.field with the unit/ref index and details.rejected_ref. A separate fixture proves that foreign-scoped SearchRun inputs/bindings cannot be admitted by dropping or changing their scope, and another run's bindings under the same SearchPlan cannot supply authority. Rejections create no EvidenceMap.
- Focused checks: the six-file EvidenceMap, SearchRun, native workflow harness, convergence pilot, v1a HTTP integration and contract-parity suite passed **166/166** (`DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`); the subsequently added current-run/foreign-scope fixture passed separately, for **167 distinct checks, 0 failed, 0 skipped**. The HTTP flow stores unscoped SearchRun refs, consumes their same-title forms, and checks the serialized error details. The existing fulltext, native frozen-draft, strict-Human, replay and material-successor flows pass.
- Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation and DB-context constraints. OpenAPI YAML parses and documents the local equivalence and error contract. Parent and independent static review: **Clear**, with no remaining Must-fix or Should-fix. `git diff --check` passed.
- No live provider, product database access/migration, new research decision, build, or full backend suite ran. Frozen extraction-draft identity and T-150 convergence authority are unchanged. FIND-009 is covered by the current downloader-preflight checkpoint above; later bounded live rehearsal remains outstanding and T-148 stays in progress.

## Current N5→N6 reference-composition checkpoint — 2026-09-07

- FIND-016 red/green: the real N5→N6 harness fixture first reproduced `FROZEN_INPUT_SOURCE_REF_KIND_MISMATCH` when the synthetic alias was removed. N6 now consumes N5’s persisted `slice_selection_decision` ref while retaining the stable `research_slice_selection_decision` frozen snapshot label. The coordinator’s `retype_authority_as_snapshot` option and alias construction are removed. Unit and HTTP helpers now carry only original N5 refs and handoff-required refs.
- Parent falsification also demonstrated that a type-only exception could admit a different selection ID in source_refs. The existing N6 lineage gate now checks the complete persisted selection ref, accepting the legacy kind only for identity comparison. Focused cases reject payload ID/title/version drift, selection/handoff hash drift, a missing selection source, and canonical/legacy source-list ID/title/version drift without candidate or handoff writes. Other nodes keep their own snapshot-kind requirements.
- Legacy alias requests still admit and exactly replay with identical hashes and authority refs. Removing the alias on the same attempt is a replay conflict; semantic-artifact drift remains rejected. No frozen input, hash, historical record, Human decision or persisted N5 authority is normalized or rewritten. The canonical candidate-generation fixture and coordinator predecessor-ref preservation assertion pass.
- Final focused suite: **219 passed / 0 failed / 1 explicitly skipped** across the v1b harness, coordinator, native HTTP integration, N6 draft runtime, N6 divergent-Debate runtime, and workflow-matrix consistency test files (`DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`). The skip is the environment-gated Prisma HTTP smoke. Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation and DB-context constraints. `git diff --check` passed.
- OpenAPI and the maintained workflow matrix distinguish the stable snapshot label from the persisted source-ref kind and explain legacy compatibility. YAML parsing and the adjacent matrix checker pass. Parent and independent review after source-identity hardening: **Clear**, no unresolved Must-fix or Should-fix.
- No live provider, product database access/migration, new research decision, build, or full backend suite ran. FIND-010 is covered by the current SearchRun/EvidenceMap checkpoint above; FIND-009 is covered by the current downloader-preflight checkpoint above; bounded live rehearsal remains separate and T-148 stays in progress.

## Current coordinator public-contract checkpoint — 2026-09-07

- FIND-022: OpenAPI now documents the existing state/advance paths, optional-body/no-frontier behavior, request budgets and scopes, node inputs, exact Human refinement schema, state/attempt/recovery projections, and advance report/halt vocabulary. It distinguishes checkpoint grants from executable frontier and specifies N9 `retry_node_id=topic-selection.v1b.materialize-topic-question-contract.v1` with `node_inputs[N7].refinement_payload`; later substantive checkpoint recovery consumes only the persisted Human payload and bounded delta Debate.
- FIND-015 red/green: the new shared-schema fixture first reproduced N1 accepting non-null `run_mode`. A policy-derived conditional now covers deterministic-only N1/N9/N10/N11. The real HTTP flow verifies `run_mode` and `profile_id` rejection through both direct invocation and bootstrap, with run-state still 404 after each failure, followed by successful corrected submission of the same N1 attempt with null fields. Existing N1→N11, Human checkpoint, N8 loopback, product Codex-assisted, and idempotent re-advance flows stay green; direct harness admission and historical replay are unchanged.
- Focused verification: **238 passed / 0 failed / 1 explicitly skipped** across the shared harness schema, v1b HTTP integration, v1b contract-drift, coordinator unit, and harness unit files (`DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`). The skip is the environment-gated Prisma HTTP smoke. The final contract-drift check compares registered request fields/required inputs/bounds, deterministic policy nodes, response fields, and every coordinator halt reason; its final run passed **4/4**.
- Inline YAML/Ajv verification compiled invocation/advance/state/report schemas, accepted both documented advance examples, rejected malformed budgets and a non-Human refinement actor, and checked absent/null versus non-null runtime fields on all four deterministic nodes both directly and nested in bootstrap. An actual no-frontier HTTP report and a populated response-shape fixture validated. No temporary script, fixture or artifact remains.
- Root `pnpm typecheck` passed (shared/backend/desktop TypeScript, Prisma generation and DB-context constraints). `git diff --check` passed. Parent and independent review: **Clear**, with no unresolved Must-fix or Should-fix. One Should-fix in budget prose was corrected to distinguish ordinary frontiers from bootstrap and checkpoint refinement-delta recovery.
- No live provider, product database access/migration, new research decision, build, or full backend suite ran. FIND-009 is covered by the current downloader-preflight checkpoint above; the later bounded real-flow rehearsal remains unfinished; FIND-010 is covered by the current SearchRun/EvidenceMap checkpoint above; FIND-016 is covered by the current reference-composition checkpoint above.

## Current EvidenceMap public-contract checkpoint — 2026-09-07

- FIND-003 / TSRF-03: the registered `POST /topic-selection/v1a/evidence-maps` and its service input confirm that units, source/locator metadata, review status, links, clusters, patterns, conflicts, digest, version, workspace, creator, and policy fields are public inputs. OpenAPI now describes the request and six child schemas, including exact enums, required fields, locator extra-property rejection, and optional/nullable values. It distinguishes route-recognized `llm_inference` from the existing 409 source-authority rejection and retains admitted SearchRun/fulltext provenance requirements.
- Red/green: the new contract check first failed on nine undocumented top-level fields. It now compares registered route properties, required inputs, extra-property policy, and enums to all seven public schemas and checks their request references. The expanded HTTP flow verifies persisted unit metadata, structural refs, conflict issue codes, and digest; invalid review/attribution enums return 400 and inference returns 409, with no EvidenceMap created by those failures.
- The HTTP fixture also reproduced `page_number: null` becoming `0` through the former number-first `anyOf`. The existing v1a-route and shared EvidenceMap nullable-number helpers now use a number/null type union. The same fixture verifies null page/cluster/unit confidence, numeric offsets/confidence, and real zero pattern confidence without rewriting historical data or changing source authority.
- Focused backend suite: **20 passed / 0 failed / 0 skipped** across v1a contract parity, full v1a HTTP integration, and EvidenceMap service files, using `DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`. Shared evidence-map schema tests: **4 passed / 0 failed / 0 skipped**. Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation, and DB-context constraints. `git diff --check` passed.
- A separate inline check parsed the maintained YAML and compiled its request schema with Ajv: a complete paragraph-locator payload passed, while 12 malformed enum, locator, number, empty-unit, blank-key, and missing-source variants failed. This check left no script or artifact and does not add a maintained general OpenAPI validator.
- Parent and independent review: **Clear**, no remaining Must-fix or Should-fix. No live provider, product database access/migration, new research decision, build, or full backend suite ran. Reference equivalence (FIND-010) is covered by the current SearchRun/EvidenceMap checkpoint above; later live rehearsal remains unfinished; FIND-022/015 are covered by the current coordinator checkpoint above.

## Current rejection-disposition checkpoint — 2026-09-07

- FIND-011 owner inspection confirms that checkpoint decisions do not own TitleCard management transitions. The original red fixture returned no rejection projection; the repair derives `research_rejection` from an exact Human `reject` on the connected current checkpoint chain, with checkpoint/decision refs, rationale, and decision time. No new persisted authority or management-status mutation is introduced.
- The HTTP fixture records a rejection, then verifies the same projection in list/detail/research status, active/pending-promotion exclusion, retained management `active` and historical package, exact decision replay, and an independent replacement card with no inherited checkpoint/rejection. Service fixtures cover evidence and question rejection, stopped continuation, all seven stage views, unchanged historical decisions, clearing on supersession, disconnected downstream rejection, and nonterminal `hold`/`loopback` controls.
- Parent review found that other stage recommendations could still suggest progression after rejection; shared working-set assembly now supplies terminal guidance across every stage while retaining its evidence and history. Desktop overview and Seed card were checked with inline React server rendering for rejected and resumed projections, management-state separation, rationale, and the absence of a create-plan prompt after rejection. Sidebar labeling was inspected and typechecked; no graphical desktop session was run.
- Final focused suite: **242 passed / 0 failed / 0 skipped**, covering checkpoint service, title management service/routes, production app assembly, HTTP contracts, native workflow harness, and run coordinator with `DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`. Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation, and DB-context constraints. `git diff --check` passed.
- Parent and independent review: **Clear**, no remaining Must-fix or Should-fix. Phase 1 implementation is complete; the next implementation boundary is Phase 2 public EvidenceMap contract alignment.
- Limits: no live provider, product database access/migration, new Human research decision, build, or full backend suite ran for this slice. Full rejection/replacement lineage and graphical/live re-read remain part of Phase 4 / TSRF-04; no completion is inferred for that acceptance reference.

## Current literature-risk visibility checkpoint — 2026-09-07

- TSRF-06 / FIND-007: current EvidenceMap status and satisfied coverage rows do not establish recent-literature coverage. The new focused fixture reproduced an otherwise eligible evidence packet with no recorded literature limitations. New packets now freeze `RECENT_LITERATURE_COVERAGE_UNVERIFIED` and `NEAR_DUPLICATE_RISK_UNRESOLVED`, with the exact literature snapshot, SearchPlan, and SearchRun refs. Both Human and LLM views retain these limitations even when every row is satisfied.
- The existing Human decision remains bound to the exact snapshot and still requires nearest-work review. Stale-snapshot and unchecked-review attempts fail; a Human-authored search limitation persists, stays visible after advancement, and replays without another decision. Reads do not change the frozen packet. This is the unresolved-risk branch of TSRF-06, not proof that recent work is covered or that all overlap has been excluded.
- A second red/green fixture reproduced accidental rematerialization of an older checkpoint after the additive packet field changed its hash. The compatibility lookup now matches the complete original packet identity before creating authority. An already superseded legacy checkpoint replays to its original ID, retains its packet/decision, and leaves the successor current. Legacy Human views identify unrecorded historical verification without adding retrospective acceptance.
- Checkpoint unit file: **36 passed / 0 failed / 0 skipped**. Combined checkpoint, linked-round, convergence-pilot, workflow-harness, checkpoint HTTP, and contract-drift files: **160 passed / 0 failed / 0 skipped**, with `DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`. Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation, and DB-context constraints. `git diff --check` passed.
- Parent and independent review: **Clear**, no remaining Must-fix or Should-fix across the final code, replay compatibility, tests, and task records.
- Limits: no new coverage-assessment authority, automatic retrieval, risk-acceptance store, or weakening of required-missing/blocking-conflict gates. No live provider, product database access/migration, new Human research decision, build, or full backend suite ran for this slice. Bounded live verification remains in Phase 4; broader historical check results below belong to their respective checkpoints.

## Current warning reconciliation checkpoint — 2026-09-07

- FIND-023's original execution trace was re-read in `research - 真实选题测试-1` (thread `01a05aa0-3a63-7200-9e13-5fff52cb692f`, turn `01a06042-8452-7bf2-9715-ade40703f486`). The stale obligations were two `human_review_trigger` entries: “Choose the primary calibration and harmful-routing metrics before N8 value assessment.” and “Confirm whether task-quality improvement is required or only non-worsening.” The three independent risk notes concerned benchmark scope, insufficient calibration drift, and collapse to a tuned baseline. The final fixture preserves that distinction; replacing risk notes alone cannot close this finding.
- The existing Human refinement contract now accepts optional exact trigger resolutions, each bound to supplied update fields and a rationale. The focused N9-to-N7 scenario fixes the metric/quality choices, resolves those two triggers, preserves all three independent risks plus the new contract quality note and an unrelated pending trigger, and verifies contract-note ID/version provenance. Unknown/duplicate triggers, unsupplied fields, and blank rationales fail before authority writes. Shared schema tests reject invalid field names, empty/duplicate field lists, and empty rationale; coordinator and HTTP fixtures cover forwarding and ingress validation.
- Red/green evidence covers reviewed checkpoint reopening that previously reintroduced the two resolved prompts, and a second partial refinement that previously reverted metrics to the initial candidate. The final scenario verifies omitted-field inheritance, cumulative resolution retention, and reopening both metric-bound triggers after a third refinement changes metrics without reconfirmation. Admission, gate readmission, reviewed reuse, and the Human question view retain the same independent risks. Exact old/refined replay and frozen packet contents remain unchanged.
- Final focused verification passed across the workflow harness, checkpoint service, run coordinator, refinement-delta classifier, harness parser, and native v1b HTTP integration, with `DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0`; the database smoke remains explicitly gated. Shared harness-contract schema tests: **33 passed / 0 failed / 0 skipped**. Root `pnpm typecheck` passed, including shared/backend/desktop TypeScript, Prisma generation, and DB-context constraints. `git diff --check` and scoped T-148 sync/lint passed.
- Parent and independent review: **Clear**, no remaining Must-fix or Should-fix after repairing later-refinement baseline loss and stale resolution suppression. Legacy reviewed reuse keeps candidate-default matching for decisions without the new exact-refinement marker; that compatibility branch was inspected but has no new dedicated multi-refinement fixture.
- Limits: suppressing a pending trigger requires explicit Human confirmation in the new optional payload; metric wording alone cannot authorize it. Existing historical records are not auto-resolved or backfilled. A bounded live refinement with that confirmation remains in Phase 4. No live provider, product database migration, new Human research decision, build, or full backend suite ran for this repair; earlier broader-suite evidence is recorded below.

## Current projection checkpoint — 2026-09-07

- TSRF-09 / FIND-017: Human question projection includes the actual main question, design and claim boundaries, answerability risks, dependencies, and gaps. Native N7, legacy exact-contract reads, and top-level backfilled claim boundaries are covered; every view leaves the frozen packet unchanged.
- TSRF-12 / FIND-021: status and stage manifest share current-question assessment selection. Tests cover a newer pending assessment, its later `refine_question` disposition, current risk summaries, old-contract exclusion, and consistent overview/question/value guidance without changing checkpoint grants or the run coordinator.
- Focused verification: checkpoint projection plus native workflow harness **153/153**; checkpoint contract drift, run coordinator, and convergence pilot **66/66**. The five top-level backfill claim assertions failed before the compatibility fix and passed after it.
- Independent review: **Clear**, no remaining Must-fix or Should-fix. The one backfilled-claim compatibility finding was repaired and re-reviewed.
- Root `pnpm typecheck` passed: shared/backend/desktop TypeScript, Prisma generation, and DB-context constraints.
- Final full backend suite: **2,889 passed, 0 failed, 81 explicitly skipped**, using `DATABASE_URL= BACKEND_TEST_PRESERVE_REAL_ENV=0 pnpm --filter @paper-engineering-assistant/backend test`. The native HTTP flow separately passed **17 tests with 1 database-gated skip**.
- `git diff --check`, scoped T-148 sync/lint and global governance lint passed. No verification outcome is inferred from a skip.
- Full-suite environment finding: the first mainline run loaded `.env.local` and automatically enabled the legacy T-054 Prisma HTTP smoke, which failed; it also observed the new backfill test before the compatibility fix. This mixed run is not acceptance evidence. Final deterministic verification explicitly clears `DATABASE_URL`; no conclusion is drawn for gated external checks.
- Limitation: this was a deterministic projection repair; FIND-023 is covered by the subsequent warning reconciliation checkpoint above, while the live rehearsal remains outstanding. No live provider, planned disposable-database canary, new Human research decision, or build ran for that projection checkpoint.

## Evidence

| Claim / reference | Check / procedure | Latest result | Evidence / limitation |
|---|---|---|---|
| Baseline worktree and governance are healthy | `git status --short`; governance lint before task opening | passed | Worktree was clean and `ctl-project-governance.mjs lint` passed on 2026-09-01. |
| No duplicate tracked outcome exists | Governance queries for topic selection, evidence landscape, stage view, rehearsal findings, backup readiness, and EvidenceMap OpenAPI | passed | All six queries returned no matching task. Search terms may not capture semantically unrelated tasks. |
| PASS-001: rejection and replacement preserve fresh lineage | Compare rejected and replacement TitleCard, seed, snapshot, SearchPlan, SearchRun, EvidenceMap, and checkpoint IDs | observed | Replacement used a new TitleCard and new downstream records; reusable literature records were the only shared assets. Focused automated coverage remains outstanding. |
| PASS-002: strict-human authority blocks automatic progression | Read research status after the refreshed EvidenceMap creation and before the user's decision | observed | `research_checkpoint_4fa82ea5-d030-4b4e-affc-36a53da4b83c` remained pending with no next authorized transition and zero NeedCandidates until the user supplied the exact `reject` action. |
| PASS-003: paragraph-level provenance is enforced and preserved | Create and re-read EvidenceMap through the product API | observed | Seven current EvidenceUnits use native `paragraph` locators and source references in `evidence_map_2388bad8-5ced-4dce-8b3c-ba9bd7367fda`; roles are 1 support, 4 challenge, 1 baseline, and 1 context. |
| FIND-001 / TSRF-01: human evidence view is decision-ready | Materialize a current evidence checkpoint containing working claim, mechanism, support/challenge/baseline/context statements, falsifier, claim ceiling, and all Human actions; read the public Human stage view | passed; focused implementation verified | The view renders substantive role-labeled evidence and decision boundaries from the frozen packet instead of emitting record enums and IDs as its primary content. |
| FIND-002 / TSRF-02: risk presentation agrees with packet conflict state | Materialize one material conflict and one required missing coverage assessment, then read the pending and accepted Human views | passed; focused implementation verified | The pending view renders the exact missing row, assessment issue code, material conflict, and all actions; after `advance`, the exact accepted row refs and Human rationale remain visible. The 43-test checkpoint/route suite and backend typecheck passed on 2026-09-03. |
| FIND-003 / TSRF-03: public EvidenceMap request matches the runtime surface | Compare seven public schemas to registered ingress; validate expanded HTTP materialization and rejection paths | passed; focused implementation verified | Documented fields, required inputs, extra-property policy and enums match the registered request/children. Valid source, review, locator, structure and digest fields persist; bad enums return 400 and llm_inference returns 409 with no EvidenceMap write. Null numeric values remain null and zero remains zero. |
| FIND-004: backend response-loss path needs stability classification | Reproduce local backend termination and inspect owner projection before retry | observed, not classified | One checkpoint decision request lost its response when the dev backend exited. Exact replay after a read confirmed idempotent recovery; root cause and product relevance are unknown. |
| FIND-005 / TSRF-05: durable backup readiness was not demonstrable | Inspect repository-supported backup/restore and bounded recovery checks | observed, adjacent | No confirmed repository backup/restore command or bounded restore/read check was found. This did not block replaceable topic writes but would block an irreplaceable run. |
| FIND-006 / TSRF-05: current SciFact workload cannot test the selected mechanism | Inspect workload README and entrypoint outputs/factors | observed, adjacent | The workload exposes aggregate `micro_recall_ppm` and fixed `retrieval_top_k` factors, not per-query signed marginal utility. This is an experiment-asset capability gap, not currently a topic-selection defect. |
| FIND-007 / TSRF-06: the evidence landscape missed directly overlapping recent work | Preserve the rehearsal finding; verify explicit literature limitations with satisfied rows, exact Human review, and historical replay | T-148 visibility repair passed; coverage remains unverified | The rehearsal identified overlapping work including ArXiv 2606.29959, 2606.29090, 2607.24010, 2601.14546, and 2606.11907. New packets and Human/LLM views now retain unresolved freshness and near-duplicate risk with search provenance. These papers were not re-searched in this repair, and no new coverage verdict is inferred. T-150 retains retrieval/onboarding ownership. |
| PASS-005: refreshed literature is retrieval-ready and scoped | Run the canonical acquisition and processing stages, activate evidence, then execute topic-scoped retrieval QA | observed | The topic has 10/10 active, pipeline-ready records; retrieval returned direct neighbors without degraded mode. `LIT-0711` remains usable with a citation/source freshness warning. |
| PASS-006: the refreshed evidence lineage stops at strict human review | Create a fresh snapshot, SearchPlan, SearchRun, and EvidenceMap, then re-read research status and NeedCandidates | observed | Snapshot `literature_snapshot_95d9087d-dfa7-44be-8761-795f244de505`, SearchRun `search_run_c1a850b0-944e-429d-ba2e-347f235e9143`, and EvidenceMap `evidence_map_2388bad8-5ced-4dce-8b3c-ba9bd7367fda` led to a pending evidence-landscape checkpoint with one material conflict and zero NeedCandidates; it advanced only when the user explicitly chose `reject`. |
| FIND-008: embedding persistence can exceed the Prisma transaction limit | Run `EMBEDDED` for the selected direct-neighbor papers, repair the native-vector write, retry the durable owner once, and verify processing and topic activation projections | reproduced and closed by T-149 | The original partial job `7c5d10a2-0afe-4b23-905c-fca6ca6c3110` recovered to `SUCCEEDED`. `LIT-2297` materialized 205 vectors with `reused_existing: true`, indexed and activated embedding version `b64e2c92-e352-4dff-87ae-586a1e13c09e`, and the topic pool reached 14/14 active. The reused artifact retained its original one-call/$0.00297414 telemetry; recovery issued no new embedding request. |
| FIND-009: effective downloader limits can invalidate an otherwise accepted plan | Exercise default/persisted/request policy composition, known-size and PDF-signature boundaries, zero-call blocked job creation and recovery via a fresh plan | passed; focused implementation verified | Dry-run exposes the 1 KiB/5-second/one-redirect override beside the 100 MiB/60-second/five-redirect defaults. Known impossible assets block locally; unknown network conditions remain unverified. Settings repair admits a fresh plan without changing the old job snapshot or retrying its blocked item. |
| FIND-010: logically equivalent scoped refs are rejected | Exercise both scope representations through the service and real HTTP boundary, reject identity/foreign-scope drift and other-run bindings, then verify original refs remain unchanged | passed; focused implementation verified | Correct current-title scoping is equivalent to absent/null only for admitted refs. Type/ID/version, current-run membership and fulltext gates remain enforced. A rejected ref returns its indexed field and supplied identity before EvidenceMap persistence. The original seven-unit rehearsal remains historical evidence. |
| PASS-007: exact rejection and replacement preserve authority and fresh lineage | Record the user-supplied `reject`, re-read the old research status, then create and read the replacement TitleCard and TopicSeed | observed | Decision `research_checkpoint_decision_ce93b762-1121-463b-ad8a-42574db9db7a` closed the old checkpoint. Replacement `title_card_e80b4a40-544c-41c6-9c12-a80428c6604f` and seed `topic_seed_88e3cd0e-5332-4731-8c69-b8b440df2e92` are fresh and have zero evidence, NeedCandidates, questions, checkpoints, or authorized transitions. |
| FIND-011: rejected research is distinct from TitleCard management status | HTTP reject → list/detail/research-status → replacement; connected question rejection and upstream supersession; desktop rendering | passed; focused implementation verified | The shared read projection identifies exact current Human rejection, adjusts active/pending-promotion counts, and stops continuation while keeping management `active`, historical packages/decisions, and exact replay unchanged. Replacement has no checkpoint or rejection; a new current upstream checkpoint excludes stale downstream rejection. Live re-read remains in Phase 4. |
| FIND-012 / TSRF-07: retriever frequency and convergence contribution are not recoverable from the topic owner | Compare the real operator trace, non-persisting `/literature/retrieve` contract, SearchRun recording path, and Arena role-evidence provenance; execute the replacement four-intent batch | reproduced; operator trace complete | The batch required four retriever operations. Three responses confirm three provider calls, 63 embedding tokens, and `$0.00000819`; the first response was lost, so its provider telemetry is irrecoverable without an unauthorized replay. SearchRun/EvidenceMap remain caller-supplied, while the product has no topic-level persisted ledger that can recover this end-to-end history. |
| RET-003 / TSRF-07: the replacement convergence loop has an auditable execution-before baseline | Add all 14 exact topic-scope records to the replacement TitleCard basket, create a resource-pool snapshot, and create four explicit coverage intents without invoking retrieval | passed | Basket contains 14/14 active records. Snapshot `literature_snapshot_e4df6e3a-78b7-4ac4-bee2-c38c102a5dd2` reports 14 abstract/fulltext/key-content-ready records and 15 sources, including three stale-source warnings. SearchPlan `search_plan_cefa8f5c-4c3b-4225-bf8a-face0849ff90` is `ready` with four coverage rows; retriever/provider counts remain 0/0 and no SearchRun exists. |
| RET-004 / TSRF-07: four bounded retrieval intents materially drive convergence | Execute each planned intent once, persist observed accounting in a primary SearchRun, and compare coverage before and after | passed with trace limitation | SearchRun `search_run_9a2aa3a9-d03e-452f-b528-1df359b5c292` records four calls, 24 retained hits, 12 unique records, two satisfied and two partial coverage intents. The evidence narrowed novelty and supplied baselines/falsifiers; the first response loss prevents complete provider accounting. |
| RET-005 / FIND-010: zero-call authority normalization does not inflate retriever frequency | Create a separate follow-up SearchRun from retained results using exact source/locator refs and no retrieval replay | passed | SearchRun `search_run_73007f30-330a-48a4-bbef-3025b1c7ce05` records zero retriever and provider calls. Its ten bindings enabled EvidenceMap admission without adding semantic evidence and reproduced the exact-ref-shape gap. |
| PASS-008: replacement evidence lineage stops at strict human review | Create the nine-unit EvidenceMap from retained evidence, inspect conflicts/digest, then re-read research status, stage view, and checkpoint packet | observed | EvidenceMap `evidence_map_94343cb5-0418-4d91-8f5d-90b37f63a5ed` is current and machine-checked. Checkpoint `research_checkpoint_e59a2da2-1768-4763-ad9e-6a4eeb70b891` initially remained pending with actions exactly `advance`, `hold`, `loopback`, and `reject`, no next authorized transition, and a digest recommending `loopback_or_hold`. |
| RET-006 / PASS-009: exact loopback preserves human authority and retriever accounting | Read the pending owner and packet, submit the user's exact `loopback` once with a stable decision key, then re-read research status | passed | Decision `research_checkpoint_decision_a9661379-26ef-4da8-913c-68226f3bf747` returned HTTP 201 and closed the checkpoint as `decided`. Its target is `evidence_or_search`; loopback refs identify the partial support/context intents and material conflict. `next_authorized_transition` remains null, and this step used zero retriever and provider calls. |
| RET-007 / PASS-010: loopback planning remains provider-free and bounded | Create one SearchPlan recheck request from the human decision, materialize a revised plan without a follow-up SearchRun, then inspect its coverage matrix | passed | Recheck `search_recheck_b4bea6d6-606f-4416-a1b1-7748a3469526` is `materialized`. Child plan `search_plan_3e2a7a83-6381-406f-b2e7-5ea143f7a259` is `ready`, parent- and snapshot-linked, with two unassessed rows, zero bindings, and no SearchRun. Its strategy records two planned calls, fresh provider authorization, and a one-pass stop condition. |
| RET-008 / TSRF-07: the bounded recheck resolves the loopback without repeated retrieval | Execute each child-plan intent exactly once, persist the SearchRun, and inspect both coverage rows | passed | SearchRun `search_run_bf8282ac-02ff-4fda-b3ee-658dd0857697` records 2 retriever operations, 2 provider calls, 49 tokens, `$0.00000637`, 16 hits, 10 unique records, and 6 duplicates. Both coverage rows are `missing`, so the recorded stop condition is satisfied and no automatic broadening or replay is warranted. |
| PASS-011: complete recheck responses are recoverable before interpretation | Re-read the SearchRun and raw artifact after both non-persisting retrieval responses returned | passed | Artifact `artifact_ref_7aad9a43-fa9d-454b-b22b-07bcb9e3dbde` contains both exact request/response pairs and telemetry. Unlike RET-004, no authorized provider response was lost at the caller boundary. |
| RET-009: exact authority rematerialization remains a zero-call step | Record a child-plan SearchRun containing the retained literature, source, and locator refs, then inspect its provenance and accounting | passed | SearchRun `search_run_e0b26e0c-f6b6-4c6e-8142-2e64a59005c9` records 0 retriever operations, 0 provider calls, 0 new results, and 25 exact authority refs. It enabled EvidenceMap admission without altering either `missing` coverage verdict. |
| FIND-013 / TSRF-08: required missing coverage constrains evidence-checkpoint advancement | Focused service, route, OpenAPI-drift, and shared-schema tests over latest-assessment selection, exact row refs, bare/incomplete/stale acceptance, replay, persistence, transition authorization, and successor-checkpoint lineage | passed; implementation verified | `REQUIRED_COVERAGE_MISSING` is derived from latest current-plan assessments; only an exact snapshot-bound `review_payload.accepted_coverage` on a strict-Human `advance` clears the required action. Backend/shared typechecks and 51 focused tests passed on 2026-09-03. A live Prisma replay remains part of the later bounded real-flow phase rather than this atomic contract check. |
| PASS-012: exact advance preserves lineage and reaches strict-human gap selection | Record `advance` once against the new pending evidence checkpoint, re-read owner state, create two semantically distinct candidates, and inspect the gap-selection packet | passed | Decision `research_checkpoint_decision_ac2b9421-c1ce-4508-966a-296a2816a6c4` closed checkpoint `research_checkpoint_b6358af3-fa20-4ef8-9c41-1f12dc154f45` and opened the authorized candidate path. Gap checkpoint `research_checkpoint_e5e6ba92-ba21-4c05-a7e5-fe36dc0a4393` is pending with two machine-viable semantic groups, no required action refs, and no automatic selection. |
| RET-011 / PASS-013: method-gap selection remains separate from validation authority | Run readiness for the selected candidate, create its validation support packet, then re-read candidate and checkpoint state | passed | Readiness `need_readiness_8ccbdb2d-bd1a-4419-b921-8668f99052e1` is `ready_for_validation` with zero blockers and warnings. Packet `validation_packet_61a661be-154f-4fac-918a-6454a959ec37` is ready and preserves six human checks plus residual challenge/conflict refs. The candidate remains awaiting human adjudication; no ValidatedNeed or automatic transition exists. Retriever/provider counts are 0/0. |
| RET-012 / PASS-014: exact validation closes gap authority without inflating retrieval frequency | Re-read the frozen candidate pool and support packet, submit exact `validate`, complete the same-authority HumanConfirmNeed write, publish the v1b input bundle, and re-read research status | passed | Adjudication `need_adjudication_0b9e5bae-4605-4afb-b657-32fd63081bc6`, HumanConfirmedDecision `human_decision_human_confirm_need_e09d1bfd141a04adaf144a6f8b59e64c4e222c4918f488e780dc40adf191ec8b`, ValidatedNeed `validated_need_5b478f0d-3266-49a1-a850-6fb9c414e606`, and bundle `v1b_input_bundle_ff2e4c3a-e924-4cb1-ab30-82e7c41e8d13` were each created once. The method candidate closed as human-confirmed, gap checkpoint `research_checkpoint_e5e6ba92-ba21-4c05-a7e5-fe36dc0a4393` is decided, the benchmark candidate remains viable, and the next required checkpoint is `question_contract`. Retriever/provider counts are 0/0. |
| RET-013 / PASS-015: deterministic v1b intake preserves authority and stops at N2 | Recover the existing bundle by exact version, compute its canonical bundle/source hashes, invoke N1 through the run coordinator, then re-read intake, handoff artifact, and run state | passed after one corrected request | Workflow run `workflow_run_t148_v1b_ff2e4c3a_retry1` admitted N1 and created intake `v1b_intake_snapshot_9bd249fc-d08d-48a8-8d8f-619062311685`. Trace status is `passed`, the EvidenceMap is `current`, all three residual-risk refs are preserved, and `next_node_id` is N2 Human authority. Retriever/provider counts are 0/0. |
| FIND-014: research status does not expose v1b consumption state | Re-read research status after the N1 intake snapshot was admitted and compare with v1b run state | reproduced and classified | Research status still reports `topic-selection.research.publish-v1b-input`, while the coordinator reports N1 completed and N2 as the actual frontier. The static checkpoint-transition label is not a reliable current-action projection after v1b consumption. |
| FIND-015: generic invocation schema admits runtime fields that deterministic N1 rejects | Shared node-policy schema checks and actual direct/bootstrap HTTP rejection before persistence | passed; focused implementation verified | N1/N9/N10/N11 reject non-null run_mode/profile_id at HTTP ingress. Absent/null fields remain schema-valid; the corrected same N1 attempt advances. Original blocked traces and direct harness admission/replay are unchanged. |
| RET-014 / PASS-016: exact N2 acceptance and deterministic N3 stop at option generation | Record the accepted constraint profile with a stable Human-authority nonce, invoke N3 from the exact N1/N2 authority and handoff hashes, then re-read the run state, N3 handoff, and option-set collection | passed after one pre-write request correction | Profile `research_constraint_profile_61ddf106-8c81-45c0-ab7c-db848f12f128` and readiness `v1b_intake_readiness_2626d502-ca13-4838-8a9e-721daddc2211` were each created once. N3 is `admitted` with zero blockers/warnings; handoff `artifact_ref_1c257917-8d70-4c16-8da6-400d735dbb1c` targets N4, coordinator `next_node_id` is N4, and the option-set list is empty. The first N3 request used a string instead of a functional-ref object and was rejected by schema validation before persistence; the corrected request reused the stable attempt ID. Retriever/provider counts are 0/0. |
| RET-015 / PASS-017: N4 generates a bounded comparative portfolio and stops at N5 | Submit one Codex-assisted N4 draft from the exact N1/N2/N3 refs and hashes, then re-read the option set, three options, handoff, and coordinator frontier | passed with expected warnings | Attempt `node_attempt_t148_v1b_n4_2626d502_v1` admitted option set `research_slice_option_set_5d8f12a4-2ca5-4556-807a-32cc24cae724` with three selectable options and no blocker. The balanced option is recommended; the shift-conditioned option is high risk; the threshold wrapper is low risk. Warnings carry two unresolved comparison questions, one high-risk option, and mandatory human review. Handoff `artifact_ref_f90fd8dc-14c5-4bdf-93b8-fa7a384daa55` targets N5, whose attempt count remains zero. Retriever/provider counts are 0/0. |
| RET-016 / PASS-018: exact N5 confirmation selects one slice and preserves alternatives | Submit the researcher's exact confirmation with a stable Human-authority nonce, then re-read the option set, options, ResearchSlice, handoff, and coordinator frontier | passed with expected warnings | Decision `slice_selection_decision_8f5480bb-7c1d-4de8-9eec-2d4b06e770a4` selected `low_label_recalibration_with_abstention` and created ResearchSlice `research_slice_b079bbfa-9197-407a-9447-2decbcf6ab67`. The set and chosen option are `selected`; the shift-conditioned and threshold-wrapper alternatives remain `candidate` for explicit loopback. N5 is `admitted_with_warnings` with `SELECTION_HUMAN_REVIEW_CONTEXT_CARRIED_FORWARD` and `DEFERRED_CANDIDATE_PRESERVED`, no blocker, and handoff `artifact_ref_501eb6a8-e44a-4b53-b97b-5e7ebed2393d` targets N6, whose attempt count remains zero. Retriever/provider counts are 0/0. |
| FIND-016: N5/N6 selection ref kinds do not compose directly | Consume exact persisted N5 refs through harness, HTTP and coordinator; verify identity drift and legacy replay | passed; focused implementation verified | N6 accepts the canonical selection ref under the stable snapshot label. No caller/coordinator alias is needed. Canonical and legacy source refs bind the exact persisted selection ID/title/version, while payload/handoff hashes remain enforced. Historical frozen requests preserve their replay identities. |
| RET-017 / PASS-019: N6 generates three bounded questions and stops at N7 | Submit the evidence-bound three-candidate Codex draft through the corrected frozen-input contract, then re-read candidate set, candidates, handoff, provenance, and coordinator frontier | passed with expected warnings after one contract correction | Attempt `node_attempt_t148_v1b_n6_b079bbfa_v2` admitted candidate set `topic_question_candidate_set_b6faa9c2-7d32-41ba-bf86-84a896622d53`. All three candidates are admissible and `answerable_with_risk`, `joint_recalibration_harm_control` is recommended, and blocked-candidate context is empty. Warnings are `HUMAN_REVIEW_TRIGGERED` and `CANDIDATE_RISK_NOTE_PRESENT`; handoff `artifact_ref_e2de2127-1064-4966-8987-92d7dfdd3866` targets N7, whose attempt count remains zero. Both N6 attempts were non-provider; retriever/provider counts are 0/0. |
| RET-018 / PASS-020: deterministic N7 materializes the recommended contract and stops at Human authority | Invoke N7 once from the exact N6 candidate-set and handoff payload, then re-read candidate states, contract handoff, coordinator frontier, research status, checkpoint packet, and human stage view | passed with expected warnings; projection defect recorded | Attempt `node_attempt_t148_v1b_n7_b6faa9c2_v1` admitted TopicQuestionContract `topic_question_contract_5f0d3032-183c-4ee7-8c13-7284ad45d546` and handoff `artifact_ref_08ef6197-e423-43f5-bb12-d608c767501e`. `joint_recalibration_harm_control` is admitted; both alternatives remain candidates. Checkpoint `research_checkpoint_7954facf-f2e5-44e1-9025-ff275c64a51f` is pending with four exact actions, and N8 has zero attempts. Retriever/provider counts are 0/0. The human view omits the main question and reports no open risk despite the answerable-with-risk contract, five N7 warnings, and packet dependencies/confounds; FIND-017 records the mismatch. |
| FIND-018: normal N6 bounded Debate | Run regular coordinator/product HTTP review through the existing gate; check effective product mode, fixed arity, exact replay, input drift and original recovery boundaries | implemented; focused-verified | Regular N6 requires one existing 2/1/1 review. Product admission binds the exact reviewed draft; canonical completed receipts preserve frozen inputs, role audits and original authority/handoff on replay. Human authority and conditional deeper recovery remain intact; Phase 4 real-flow acceptance is outstanding. |
| FIND-019: repeated loopback-backend authorization interrupts one continuous flow | Compare the operator's repeated restart prompts with the unchanged local API base, process identity, and already-authorized research scope | observed; operating rule confirmed | The researcher supplied standing authorization for safe starts/restarts of the same loopback backend during this rehearsal. Provider/cost, destructive/control, environment changes, and each new Human-authority decision remain separate authorization boundaries. |
| RET-019 / PASS-021: exact question advance preserves unresolved choices without retrieval inflation | Re-read the frozen question checkpoint, submit the researcher's exact `advance` once, then recover checkpoint, research-status, and coordinator owners | passed | Decision `research_checkpoint_decision_66155600-f58d-4c4e-8369-86f56c41dc15` closed checkpoint `research_checkpoint_7954facf-f2e5-44e1-9025-ff275c64a51f`. The recorded review keeps the benchmark obligation, answerability risks, and absent primary-metric choice explicit. N8 became authorized. Retriever/provider counts are 0/0. |
| RET-020 / PASS-022: bounded N8 Debate changes the disposition and N9 prevents premature packaging | Submit a borderline N8 draft, follow the registered N8→N7 feedback route, admit compact Debate, run all four roles, re-enter N8, and invoke deterministic N9 | passed with expected warnings | Initial N8 attempt `node_attempt_t148_v1b_n8_5f0d3032_v1` triggered T1 at 69/0.68 and wrote feedback only. N7 feedback re-entry admitted compact Debate. The four runtime-verified Codex-assisted roles resolved four material critic findings and produced assessment `topic_value_assessment_65d6dddd-5972-4255-a256-0b44cd8f4073` at 66/0.76 with `needs_refinement`. N9 decision `value_disposition_decision_603d5b59-8659-4178-8835-33d4e33c3eb5` recorded `refine_question`; N10 stayed at zero attempts. Retriever/provider counts are 0/0. |
| FIND-020 / TSRF-11: N9 `refine_question` recovery route | Compare the N9 authority/route, strict refinement schema, coordinator frontier, N7 lineage validation, supersession transaction, and resumed projection | fixed, locally verified, and exercised on the real run | N9 emits hash-bound `N9ToN7RefinementHandoff@v1`; the coordinator requires a human `TopicSelectionV1bN9QuestionRefinement@v1`; N7 verifies the current disposition and previous contract before creating a replacement and superseding the old question/contract/checkpoint lineage; the newest N7 admission projects N8 next. Focused harness test passed, coordinator suite passed 64/64, shared schema suite passed 32/32, and the HTTP schema test passed. The historical run successfully consumed the handoff once. |
| RET-021 / FIND-020: exact refinement acceptance has a supported product landing point | Preserve the researcher's complete accepted contract and validate the route rejects non-human, empty, stale, wrong-target, or hash-drifted input before authority write | passed in code and real product authority | The accepted Brier/safety/quality/coverage/benchmark contract fits the strict refinement payload and was applied by deterministic N7 rather than inferred by a model. Non-human harness attribution leaves the previous contract active. No retriever or provider call is required because this is experiment-contract refinement, not literature convergence. |
| FIND-021 / TSRF-12: current value and Human projections agree | Read a newer pending assessment, then its exact refine-question disposition and verified risk artifact across status and overview/question/value views | passed; focused implementation verified | Current-question ownership excludes old-contract risks and old dispositions. The view explains refinement instead of premature promotion; frozen packet identity and existing checkpoint grant semantics remain unchanged. Live rehearsal re-read remains in Phase 4. |
| RET-022 / PASS-024: historical N9 replay materializes the recovery edge exactly once | Start the same loopback backend, recover the historical run and N8 handoff, submit one explicit N9 retry with `max_steps=1`, then re-read N9, N10, and the handoff artifact | passed with expected terminal warning | HTTP 200 created attempt `node_attempt_workflow_run_t148_v1b_ff2e4c3a_retry1_decide_value_disposition_v1_2`, decision `value_disposition_decision_00ffff6d-4e1d-447d-b32a-cc5a12bef2ab`, and handoff `artifact_ref_3eb0ee5b-0866-4eec-a6d6-f3f936f30fad`. Gate status remains `terminal_no_advance`, but route decision is now `loopback`; route signal is `question_refinement_required`, target is N7, the assessment and previous-contract hashes match the frozen N8 lineage, and N10 attempt count remains zero. Retriever/provider counts are 0/0. An initial non-retry advance produced `steps=[]` and no write, so it is not counted as a replay. |
| FIND-022: canonical v1b coordinator routes are absent from OpenAPI | Compare registered routes/request schema and exported response/halt contracts, compile public schemas, exercise coordinator/HTTP flows | passed; focused implementation verified | Both paths now describe the current contracts, bounded budget scopes, explicit N9-to-N7 Human refinement and subsequent checkpoint delta-Debate recovery. Existing routes and strict-Human authority remain the only execution path. |
| RET-023 / PASS-025: accepted refinement replaces the contract and stops at Human authority | Submit the exact accepted payload once through the supported N7 recovery route, then re-read old/new owners, handoff, run state, checkpoint packet, research status, and human view | passed with projection defects recorded | HTTP 200 created attempt `node_attempt_workflow_run_t148_v1b_ff2e4c3a_retry1_materialize_topic_question_contract_v1_3`, contract `topic_question_contract_97c59cf9-5aa2-4826-bd97-56d0a730b0cd` at hash `c946d4630a5af78b5a149b8861ba0bac8e924fc54c49fc8e1a5d30428a2a7b4f`, and handoff `artifact_ref_6f0c086b-c798-4acc-82b7-fde8dbd1f161`. The old question/contract/checkpoint are superseded, the exact accepted metric and benchmark terms are present, run state is N8 next, new checkpoint `research_checkpoint_30a2ec52-2ba4-458e-b1a9-055ab6c216ff` is pending, and N10 remains at zero attempts. Retriever/provider counts are 0/0. |
| FIND-023: replacement N7 retains warnings resolved by refinement | Bind exact Human trigger resolutions to supplied fields; check admission/readmission, reviewed reuse, the Human view, successive partial refinements, and old/refined replay | passed; focused implementation verified | Two confirmed metric/quality triggers disappear while all three independent candidate risks and current contract notes remain. Omitted fields inherit the prior contract/plan; changed supporting fields reopen triggers. Warnings retain owner provenance, historical replay and frozen packets remain unchanged. Live refinement with explicit Human confirmation remains outstanding. |
| RET-024 / PASS-026 / FIND-024: exact loopback records authority but exposes no delta-Debate route | Re-read the current replacement checkpoint/packet, submit one exact strict-human `loopback` with stable decision key and frozen refs, then re-read checkpoint, packet, coordinator, research status, and human stage view | decision passed; recovery route missing | HTTP 201 created decision `research_checkpoint_decision_c7c82712-8b2e-4d91-a123-37de25ea84e2` and HumanConfirmedDecision `human_decision_25297307-0bbb-44dd-b66a-1dc4f1e1606c` against snapshot `d450fe4db256de71d7005ef0e6262a25e067937a088f360f28ed889b33cf65f4`. The checkpoint is decided with target `question_contract`; retriever/provider counts are 0/0 and no new N6/N8/N9/N10 attempt exists. However no route handoff was emitted, run state remains N8 next, research status still says `publish-v1b-input`, and the human stage view still offers another decision. |
| RET-025 / PASS-027 / FIND-024: exact loopback executes one bounded delta Debate and stops at fresh Human authority | Restart the same local backend with the fixed coordinator; re-read the historical run; attempt advance without Debate support; then submit one Codex-assisted Explorer/Critic/Arbiter pass and re-read the contract, N7 trace, role artifacts, admission, checkpoint, and run state | passed; FIND-024 closed for the approved slice | Before support, advance halted read-only with `delta_debate_required` and exposed no N8 frontier. The accepted pass covered all eight changed fields exactly once and produced admission `artifact_ref_e9906df8-6e53-4fe9-ac7d-254687fb2637` with verdict `admit_unchanged`. N7 attempt `node_attempt_workflow_run_t148_v1b_ff2e4c3a_retry1_materialize_topic_question_contract_v1_4` reused contract `topic_question_contract_97c59cf9-5aa2-4826-bd97-56d0a730b0cd` at unchanged hash `c946d4630a5af78b5a149b8861ba0bac8e924fc54c49fc8e1a5d30428a2a7b4f`, emitted reviewed handoff `artifact_ref_5f445a05-e1b6-4b4d-9684-3a95e98e4de2`, and opened fresh pending checkpoint `research_checkpoint_40ddf095-4d28-4844-bdfc-11aaff0ba6ee`. Exactly three role outputs and one admission exist for the pass; N8 stayed at two historical attempts; retriever/provider counts are 0/0. Backend typecheck, 5 LLM-config checks, 2 delta-classifier checks, 5 admission/runtime checks, 64 coordinator checks, the focused N9→N7 harness test, 32 shared harness-contract checks, 17 shared runtime-contract checks, and the focused HTTP request-schema check passed. |
| T-148 refinement-delta slot remains represented in the workflow SSOT | Run the workflow-matrix consistency check and its injected-drift self-test after comparing the registered `n7_n6_refinement_delta_admission` slot with the Invocation Slot Map | passed | The missing support-only slot row was added with its exact node, allowed modes, fallback, and output contract. Both matrix tests pass, while the v1b coordinator and full 119-test harness suite remain green. |
| v1b HTTP coordinator respects the strict-Human question checkpoint | Drive the two coordinator flows through N7, inspect the hidden frontier, record the exact Human `advance`, and then continue to N8; run the complete v1b route file with its Prisma smoke on a migrated disposable database | passed | Both formerly stale expectations now assert `no_frontier` before Human review and `model_input_required` at N8 only afterward. The focused red/green pair passes 2/2; the complete v1b route suite passes 18/18, backend typecheck passes, and the disposable database was removed. No product barrier was weakened. |
| RET-026 / PASS-028: exact post-Debate checkpoint advance releases N8 | Re-read fresh checkpoint `research_checkpoint_40ddf095-4d28-4844-bdfc-11aaff0ba6ee`, record the researcher's exact `advance`, then re-read the workflow frontier | passed | Decision `research_checkpoint_decision_d2e613da-01e0-4a46-b831-1fc8326985fe` and Human decision `human_decision_1727d309-146c-4cde-8eea-ca4f8bccaa91` closed the checkpoint and released only N8. The contract hash remained `c946d4630a5af78b5a149b8861ba0bac8e924fc54c49fc8e1a5d30428a2a7b4f`; retriever/provider counts were 0/0. |
| FIND-025 / RET-027 / PASS-029: refinement audit hashes no longer false-block N8 | Add refinement audit hashes to the real N7 projection shape at the N8 runtime seam, observe the intended pre-fix failure, change the validator to require core keys while validating all additive hash values, rerun the focused test, restart the same local backend, and replay the exact blocked N8 frontier | fixed and decisively verified | The focused test failed before the correction with `N7-to-N8 projection source hash map drifts from frozen N8 lineage` and passed afterward (5/5). Backend typecheck passed. Real attempt 3 reproduced the same blocker with no assessment authority; retry attempt 4 admitted with warnings, created assessment `topic_value_assessment_257ba79c-a281-4c22-b327-96c6bedb98ed` at 73/0.78, recorded 25 stable risk findings, and exposed N9. The assessment remains bounded to two replacements and carries paired-access, statistical-protocol, benchmark-validity, and baseline-collapse risks. Retriever/provider counts were 0/0. |
| RET-028 / PASS-030: deterministic N9 advances only to the package frontier | Invoke the current N9 once through the coordinator with `max_steps=1`, then re-read the run, N9 handoff, and TitleCard package owner | passed with expected warnings | HTTP 200 created attempt `node_attempt_workflow_run_t148_v1b_ff2e4c3a_retry1_decide_value_disposition_v1_3`, decision `value_disposition_decision_22e9b282-5609-42a8-bf93-c6be2360cb69`, and handoff `artifact_ref_7f7c19f9-5d11-495d-bb65-1dca1c0fbd1d`. The gate is `admitted_with_warnings`; `N9_ADVANCE_WITH_VALUE_GATE_RISK` and `N9_VALUE_RISK_NOTES_CARRIED_FORWARD` preserve the current assessment risks. The handoff route signal is `advance_to_package_candidate`, the run exposes N10 next, and topic-package count remains zero. Retriever/provider counts were 0/0. |
| RET-029 / PASS-031: deterministic N10 creates one trace-ready draft package and stops before publication | Invoke the current N10 once through the coordinator with `max_steps=1`, then re-read the run, package owner, N10 handoff, and N11 attempt count | passed with expected warnings | HTTP 200 created attempt `node_attempt_workflow_run_t148_v1b_ff2e4c3a_retry1_create_draft_topic_package_v1_1`, TopicPackage `topic_package_ab6a7f4f-18b1-4aa0-9cb6-addba7c0c5c1`, and handoff `artifact_ref_aef8236e-80eb-42b2-b733-47934e8dc553`. Readiness is `ready_for_promotion_review`; the package carries 25 material risk refs, 20 key risks, eight selected evidence refs, and zero blockers. The transaction prepared bundle `v1b_to_v1c_input_bundle_9d685a2e-76ea-4263-ad6b-a0ba7a0931ce`; N11 attempt count remains zero. Retriever/provider counts were 0/0. |
| FIND-026: N10 readiness misses narrative-quality defects | Compare the persisted package title candidates, non-goals, background, and contribution summary with the deterministic builder and readiness evaluator | reproduced | `title_candidates` contains only the full main question and a `method:`-prefixed full claim; five upstream non-goals become ten semantically duplicate exact-string variants; background and contribution summary include `..`. `buildNarrative` uses exact-string uniqueness, direct question/claim reuse, and unconditional terminal punctuation, while `evaluateTraceBoundary` checks lineage/boundary consistency only. The package still reports `ready_for_promotion_review` and N11 is exposed. |
| FIND-026 / TSRF-14: N10 narrative repair fails closed and replacement packages invalidate stale promotion input | Run red/green service tests through both in-memory and Prisma repositories, backend typecheck, focused N10/coordinator/harness/v1c tests, then bounded real recoveries | passed | Pre-fix tests reproduced all three defects; the first two diagnostic recoveries exposed the duplicate harness builder and contract-internal semantic duplicates without executing N11. Final package `topic_package_2fa1c548-1d86-4145-a137-580bfa9d8c9f` has two title-shaped candidates of 147/158 characters, five canonical non-goals, no repeated punctuation, 25 risk-finding refs, 20 key risks, and zero blockers. All three predecessors report `superseded`. Backend typecheck passes and the final focused N10/coordinator/harness/v1c suite passes 219/219. Retriever/provider counts are 0/0. |
| RET-030 / PASS-032: N11 publishes only the corrected current bundle and closes v1b | Execute one coordinator step from the corrected N10 handoff, then re-read N11 and run state | passed with expected warnings | Attempt `node_attempt_workflow_run_t148_v1b_ff2e4c3a_retry1_publish_v1c_input_bundle_v1_1` admitted bundle `v1b_to_v1c_input_bundle_00456606-7864-4246-ab49-c8de1d83307e` at authority hash `5ada93dddc31ef38025198782e36e192a2edb9afbf71a98bf91afd6941663aa4`. Handoff `artifact_ref_a22166aa-4685-4bba-8279-8c6aa8c7b445` targets `v1c.entry`; the run reports `run_complete: true`, no next node, and no recovery frontier. Warnings carry package risks and material risk findings; retriever/provider counts are 0/0. |
| RET-031 / PASS-033: v1c N1 through N3 prepare a complete Human decision frontier | Create the promotion-input snapshot, deterministic support/dossier, readiness mini-check, and gate; re-read every owner before continuing | passed with carried-forward risk | Snapshot `promotion_input_snapshot_3ce1ec0c-8675-44f4-8f23-0debe82751b9` is `ready_for_gate` at hash `823e14ef285aae7b07996a9f990dc9273da1c8038ef53dd35f3b3864c8437e6a`. Support `promotion_decision_support_6dd962b5-c257-497d-ac08-986e53c0b113` succeeded deterministically with two reviewer questions, 25 advancement-relevant risk findings, and no recheck notes; dossier `promotion_dossier_7493e662-02e5-4958-9ffc-a4fd6ce20b7b` and mini-check `argument_readiness_mini_check_4873f08b-5048-4b50-b581-ec59f95131e8` passed 6/6. Gate `promotion_gate_check_dc661cce-9594-4155-af45-c094905df24b` is `ready_for_human_decision` with promotion allowed and material-risk warning. Retriever/provider counts are 0/0. |
| FIND-027 / TSRF-15: material-risk promotion Debate policy | Risk and risk-free local service/HTTP flows; admission/ref preservation; N3 provenance; completed replay and contract checks | implemented and focused-verified | Ordinary and combined support paths require four-role Debate for accepted/material risk refs. Dossier stores policy/admission and four role artifacts; new N3 cannot consume legacy unsupported risk while existing gate replay stays historical. Canonical OpenAPI, maintained operator guidance and product/non-provider provenance agree. Phase 4 real-flow re-read remains. |
| FIND-028 / TSRF-16: complete conditional-promotion support | 25 findings grouped into five typed candidates through N2/N3, invalid proposals, native-checkpoint HTTP Human editing, omitted mappings and exact replay | implemented and focused-verified | Complete candidates and early checks are retained in existing support/dossier JSON; the Human supplies owners and exact conditions. An omitted group yields 422 with no PromotionDecision/commitment bundle; complete edited conditions succeed. Recheck gates, legacy replay and N4 mapping authority remain intact. Real-flow re-read remains Phase 4 work. |
| RET-032 / PASS-034: exact conditional promotion and bridge preserve every obligation | Record the researcher's exact `promote_with_conditions` with total risk mapping, re-read decision/commitment owners, then create and verify the PaperProjectBridge | passed | Human decision `human_promotion_decision_3978da52-4762-428d-832d-5ae488170147` and confirmed record `human_confirmed_decision_b89dec4a-a39d-4be2-ac0d-8bf8897928e4` admitted current promotion decision `promotion_decision_dae0f3fc-0749-4738-8584-6c55e26272e5`. Commitment profile `promotion_commitment_profile_7941eb3b-ec4b-494b-ae82-0f69c6468641` contains five early-check obligations. Bridge `paper_project_bridge_e9ec7e21-3085-4a96-8f3f-b1aee5486809` is active and retains all conditions/commitments. Retriever/provider counts are 0/0. |
| FIND-029 / T-150: global retrieval and evidence admission are separate boundaries | Inspect the unscoped literature retriever, the real run's explicitly topic-scoped requests, SearchPlan/SearchRun snapshot guards, Arena retrieval, and downstream frozen bundle | reproduced; transferred and rerouted | Unscoped retrieval already considers all active, evidence-ready embedding versions. The downstream question bundle freezes its evidence map and cannot consume a local successor without reopening lineage. T-150 therefore pilots onboarding and successor lineage at evidence-landscape convergence; no retriever rebuild remains here. |
| FIND-030 / T-150: Debate cannot request new evidence | Compare all recorded Debate traces and retriever telemetry with their role contracts | reproduced; transferred | The question-candidate, value-assessment, refinement-delta, and promotion-support paths produced role artifacts over frozen context but no Debate-triggered retrieval; recorded Debate retriever counts are 0. T-150 owns the typed request, corpus identity, evidence-delta, and linked-round capability at the evidence-landscape pilot. |
| FIND-031 / T-150: gate failures lack a reusable resolution composition | Compare required-missing coverage, the original stranded refinement disposition, promotion-risk mapping rejection, and their recovery routes | reproduced; split | This task retains the concrete coverage-gate and minimal Human acceptance contract. T-150 owns one evidence-landscape issue → resolution route → declared delta → fresh checkpoint recheck composition; exact repository-wide edge counts do not justify a broad redesign. |
| FIND-032 / TSRF-22: internal version labels leak into user operation | Review the real-flow instructions and researcher feedback | reproduced; terminology confirmed | The operating trace repeatedly used `v1a`, `v1b`, and `v1c` as primary stage names. The researcher explicitly found them opaque. Future user-facing copy uses research activity names while retaining exact version identifiers only in technical diagnostics. |

## Outstanding verification

- FIND-004 remains unproven; reopen a stability correction only with a captured recurrence and process-exit evidence.
- FIND-005/006 have explicit operations/experiment-owner follow-up proposals; their implementation is outside T-148.
- Re-read FIND-007's repaired literature-risk visibility during bounded live evidence review; deterministic implementation checks are complete, and full-library retrieval/admission remain T-150 work.
- Re-read FIND-009 policy provenance and local blockers during the later bounded real-flow rehearsal; local implementation, HTTP admission and job-history preservation are verified. Unknown remote size, latency and redirects are not preflight proof.
- Re-read FIND-010 optional title-scope compatibility during the later bounded real-flow rehearsal; local reference identity, current-run provenance and HTTP diagnostics are verified.
- Re-read FIND-011's separate terminal research disposition in the later bounded live rejection/replacement flow; owner inspection and deterministic implementation checks are complete.
- Re-read the repaired FIND-017 native/backfilled Human question projections in the later bounded real-flow phase; deterministic implementation checks are complete.
- Re-read FIND-018 during Phase 4 real-flow acceptance; regular N6 routing, product admission, frozen cost/provenance and completed replay are locally verified. Keep the provider path dormant and Human decisions exact.
- Re-read FIND-016’s direct N5-to-N6 composition during the later bounded real-flow rehearsal; local implementation and identity/replay checks are complete.
- Re-read the documented coordinator frontier and exact recovery route in the later bounded rehearsal; FIND-014 checkpoint-grant semantics are classified and FIND-015/022 local contract repairs are verified.
- Continue explicit RET accounting under FIND-012; T-150 owns durable retrieval execution at its bounded kernel. No second ledger or generic endpoint correction is included here.
- Re-read the repaired FIND-021 current-value projection during the later value-refinement replay; deterministic implementation checks are complete.
- Verify FIND-023 during a later bounded live refinement with exact Human trigger confirmations; preserve historical replay and independent risks rather than auto-resolving the old records.
- Re-read FIND-027 in Phase 4 real-flow acceptance: local risk-trigger, four-role support, N3 provenance, replay and cost/contract alignment are verified; provider activation and external DB concurrency are not implied.
- Re-read FIND-028 during Phase 4 real-flow acceptance: complete candidate coverage, Human editing and native N4 unmapped-risk rejection are locally verified; no real Human decision is inferred from test fixtures.
- Keep FIND-029 through FIND-031 retrieval-native implementation out of this task and verify that T-148's concrete coverage gate composes cleanly with T-150's later evidence-landscape pilot without depending on it.
- FIND-032 copy is complete and verified; preserve exact technical API/node identities and historical packets.
