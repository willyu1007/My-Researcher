# Process context

Keep only current process contracts and operational notes in this directory. Historical task
harnesses, release gates, and acceptance runners belong in archived task history, not as live
commands here.

When a maintained process matrix or scenario registry changes, run its adjacent consistency
checker under `apps/backend/scripts/` and the corresponding backend tests.
